# Editor Change Full Guidance

This file is detailed reference material for the `.claude/skills/gameengine-editor/SKILL.md` skill. Load it only when the short `SKILL.md` router says the current task needs detailed API names, detailed validation lanes, retained ids, or exact package/render/editor counters.

# GameEngine Editor

## Boundary Rules

- Keep `editor/core` GUI-independent and buildable in the default preset.
- Put SDL3, Dear ImGui, OS windows, and native UI handles only in optional GUI targets.
- Route persistent editor behavior through `mirakana_editor_core` models before wiring panels.
- Store project-wide editor tool configuration in `ProjectDocument`/`ProjectShaderToolSettings` and edit it through `ProjectSettingsDraft` before using it from `mirakana_editor`.
- Store editor viewport backend preference with `mirakana::editor::EditorRenderBackend` in `ProjectDocument`; resolve availability through `mirakana::editor::choose_editor_render_backend` before using it from GUI code.
- Keep engine runtime modules independent from editor code.
- Do not expose SDL, ImGui, HWND, or graphics API handles through public game APIs.
- Treat Dear ImGui as the optional developer/editor shell, not the production runtime UI foundation.
- Runtime game UI should target first-party `mirakana_ui` contracts; do not expose Dear ImGui, SDL3, Qt, NoesisGUI, Slint, RmlUi, or other UI middleware through public game APIs.
- Text shaping, font rasterization, IME, OS accessibility bridges, image decoding, and concrete renderer adapters belong behind adapters; do not hand-roll those systems inside panels or leak their dependencies into editor/core models.
- For complex editor panels, add GUI-independent state/model contracts in `editor/core` first, then expose durable retained models through `mirakana_editor_ui` before wiring an ImGui adapter.

## Dear ImGui shell (`editor/src/main.cpp`)

The visible `MK_editor` entry point is a large translation unit; keep it consistent with `.clang-tidy` and IDE diagnostics.

- **Varargs ImGui** (`Text`, `TextWrapped`, `SetTooltip`, and other `printf`-style APIs): never pass `std::string_view::data()` as `%s`; views are not guaranteed null-terminated. Prefer `ImGui::TextUnformatted(const char* begin, const char* end)` over an explicit `[begin, begin + size)` span, a tiny file-local helper for repeated `string_view` labels, or `std::string{view}.c_str()` scoped to the call when a C string is required.
- **`ImGui::Combo` item lists**: use `static constexpr std::array<const char*, N>` and pass `labels.data()` with `static_cast<int>(labels.size())` instead of `const char*[]` plus `IM_ARRAYSIZE`.
- **Scratch numeric UI** (transforms, colors, sprite size/tint, etc.): use `std::array<float, N>` and `.data()` for `DragFloat*` / `ColorEdit4` instead of C arrays.
- **Sets / maps**: prefer `contains` over `count(...) > 0` where the container supports it.
- **Disabled widgets**: prefer `ImGui::BeginDisabled(bool)` paired with `ImGui::EndDisabled()` instead of branching on `BeginDisabled()` with no predicate.
- **`path::generic_string()` into `optional<string>`**: use `auto s = path.generic_string()` (non-const) when the value is returned or moved from; `const auto` can block move on some MSVC diagnostics.
- **Validation loop** after substantive shell changes: `cmake --preset dev`, `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-tidy.ps1 -Files editor/src/main.cpp` (add `editor/src/material_preview_gpu_cache.cpp` and/or `editor/src/sdl_viewport_texture.cpp` when those files change), and `cmake --build --preset dev` for `MK_editor` (then `validate.ps1` at the slice gate per **Required Checks** below).

## MK_editor Windows and material-preview host cache

- For `_WIN32`, include `<windows.h>` once (with `WIN32_LEAN_AND_MEAN` / `NOMINMAX` when needed) before any `MK_EDITOR_ENABLE_D3D12` includes so the same TU does not pull `windows.h` twice (`misc-include-cleaner`).
- Read environment variables with `GetEnvironmentVariableA`/`W` into `std::string` rather than `_dupenv_s` + CRT `free` in new or refactored `MK_editor` paths.
- Shell-local GPU caches for material preview (`MaterialPreviewGpuCache` in `editor/include/mirakana/editor/material_preview_gpu_cache.hpp`, SDL display bridge in `editor/include/mirakana/editor/sdl_viewport_texture.hpp`): keep GPU/RHI state `private`; extend behavior through `rebuild` / `ensure` / `render` and dependency structs (`MaterialPreviewGpuRebuildDeps`, `MaterialPreviewGpuRenderDeps`) from the shell—do not widen public mutable fields.
- **Public headers under `editor/include/mirakana/editor/`:** include only what the declaration surface needs; keep optional RHI backend implementation headers (for example `mirakana/rhi/d3d12/d3d12_backend.hpp`) in the `editor/src/*.cpp` that performs interop unless a type from that header appears in the public class interface (`misc-include-cleaner`, clangd “not used directly”).
- **Dependency structs with `std::function` members:** default-construct to an empty callable; omit redundant trailing `= {}` on those members (`readability-redundant-member-init`).
- **clangd fallbacks:** when a new header under `editor/include/` or a new include chain needs roots that MSVC already sees via CMake, update `editor/include/compile_flags.txt` and/or `editor/src/compile_flags.txt` in the same task so IDE diagnostics stay aligned with `AGENTS.md` AI Development Workflow.

## Editor core unit tests (`MK_editor_core_tests`)

- Large GUI-independent editor/core behavior is covered in **`tests/unit/editor_core_tests.cpp`** (CTest name **`MK_editor_core_tests`**). Keep it aligned with **Unit tests** in `docs/cpp-style.md` (complete designated initialization in **struct member declaration order**, including trailing **`std::function`** members such as **`ScenePrefabInstanceRefreshPolicy::load_prefab_for_nested_propagation = {}`** when unused, C++23 **`std::string::contains` / `starts_with`** and the same on **`std::string_view`** where applicable, anonymous namespace layout ending immediately before `int main()`, `class` test doubles with private state and `const` getters when guideline checks fire on counters).
- Narrow validation: `cmake --build --preset dev --target MK_editor_core_tests` and/or `ctest --preset dev --output-on-failure -R MK_editor_core_tests` after changing `editor/core` or that TU.

## Editor core C++ (`editor/core/src/*.cpp`, `editor/core/include/**`)

- Prefer C++23 **`std::string_view::contains`** / **`starts_with`** over `find(...) != std::string_view::npos` (and the same style on `std::string`) for substring/prefix membership; matches **Unit tests** / production guidance in `docs/cpp-style.md`.
- File-local adapters over C function tables or similar non-duplicable state: if the destructor is non-trivial and copy is deleted, also **`= delete` or define** move constructor and move assignment (**Rule of Five**) so clang-tidy special-member diagnostics stay clean; **`= delete` move** is appropriate when relocation would duplicate callback ownership (`std::unique_ptr` to the adapter still works because `unique_ptr` moves the pointer, not the owned `T`).
- **`check-tidy.ps1 -Files`** only accepts **`.cc` / `.cpp` / `.cxx`**; when you change **`editor/core/include/**/*.hpp`**, run tidy on the **implementation TU** that includes that header (for example `editor/core/src/scene_authoring.cpp` for `scene_authoring.hpp`), or use IDE **`clangd`** on the header directly.
- Small closed **`enum class`** in editor-core public headers: prefer an explicit **`std::uint8_t`** (or other fixed-width) underlying type when **`performance-enum-size`** would fire; see **`docs/cpp-style.md`** (ABI note).
- **`bugprone-unchecked-optional-access`**: add explicit **`optional.has_value()`** (or an equivalent guard) before `->` / `*` when helper predicates do not satisfy clang-tidy’s dataflow.
- **Planner / validation call graphs:** when a public planner calls validation that must re-plan the same surface (for example nested prefab propagation simulation), keep the **acyclic** shape by splitting **graph-only planning** from **post-plan simulation** in a file-local helper instead of **`plan` → `validate` → same public `plan`** (which triggers **recursive call chain** / **`misc-no-recursion`** diagnostics); see **`editor/core/src/scene_authoring.cpp`** (`plan_scene_prefab_instance_refresh_without_nested_propagation_simulation` + public `plan_scene_prefab_instance_refresh`).
- After substantive edits outside the huge ImGui TU, run `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-tidy.ps1 -Files editor/core/src/<changed>.cpp` (after `cmake --preset dev`) alongside `cmake --build --preset dev --target MK_editor_core` when only `editor/core` changed; when only a paired header changed, pass the same primary `.cpp` to `-Files`.

## Required Checks

1. Identify whether the change belongs in `editor/core`, `editor`, or an engine module.
2. Add or update tests in **`tests/unit/editor_core_tests.cpp`** before production editor-core behavior (CMake executable and CTest filter: **`MK_editor_core_tests`**). Use the canonical `MK_editor_core_tests` name in new guidance and validation evidence.
3. Update `docs/editor.md` and the engine agent manifest for new editor-facing contracts by editing the relevant `engine/agent/manifest.fragments/*.json` and running `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/compose-agent-manifest.ps1 -Write`; when adding retained `play_in_editor.*` row ids, CMake target strings surfaced to agents, or other literals enforced in `tools/check-ai-integration.ps1`, extend the matching Needles blocks there and keep `.agents/skills/editor-change/SKILL.md` equivalent paragraphs aligned in the same task.
4. During implementation, prefer a focused loop: `cmake --build --preset dev --target MK_editor_core_tests` and `ctest --preset dev --output-on-failure -R MK_editor_core_tests` when `editor/core` or `tests/unit/editor_core_tests.cpp` changes, and run only the static checks matching touched docs/manifest/API files.
5. Run `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/build-gui.ps1` when `editor`, SDL3, Dear ImGui, or GUI CMake files change.
6. Run `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/validate.ps1` once at the slice-closing gate after code, docs, manifest, and static-check updates are settled.

## Asset and Viewport Rules

- Use `mirakana::editor::ContentBrowserState` for filterable asset lists.
- Use `mirakana::editor::AssetPipelineState` for import queue and hot reload UI state.
- Use `mirakana::editor::AssetPipelineState::apply_import_execution_result`, `apply_recook_requests`, and `apply_hot_reload_results` when mapping asset import and hot reload tool execution into editor UI state.
- Use `mirakana::editor::editor_asset_hot_reload_event_kind_label`, `editor_asset_hot_reload_recook_reason_label`, and `editor_asset_hot_reload_apply_result_label` for GUI or AI-facing hot reload diagnostics instead of duplicating strings.
- Use `mirakana::editor::make_imported_asset_records` or `mirakana::editor::add_imported_asset_records` when successful asset imports, including scene imports, must become `AssetRegistry` records and refresh the Content Browser after adding.
- Use `mirakana::editor::make_editor_content_browser_import_panel_model` and `mirakana::editor::make_content_browser_import_panel_ui_model` for read-only Assets panel diagnostics over `ContentBrowserState`, `AssetPipelineState`, import progress, diagnostics, dependency rows, thumbnail requests, material preview rows, and hot-reload summaries. Use `mirakana::editor::make_content_browser_import_open_dialog_request`, `mirakana::editor::make_content_browser_import_open_dialog_model`, and `mirakana::editor::make_content_browser_import_open_dialog_ui_model` for reviewed native Content Browser import source selections under retained `content_browser_import.open_dialog` rows; the visible shell may route `Browse Import Sources` through `mirakana::SdlFileDialogService`, convert accepted in-project first-party `.texture` / `.mesh` / `.material` / `.scene` / `.audio_source` documents and reviewed `.png` / `.gltf` / `.glb` / `.wav` / `.mp3` / `.flac` codec sources to safe project-relative paths, and replace the import plan while keeping `Import Assets` as the explicit execution action. Use `mirakana::editor::make_content_browser_import_external_source_copy_model` and `mirakana::editor::make_content_browser_import_external_source_copy_ui_model` for reviewed external source copy staging under retained `content_browser_import.external_copy` rows; the visible shell may copy through explicit `Copy External Sources` into project-relative `imported_sources/<filename>` targets before rebuilding the import plan. The visible shell may pass `mirakana::ExternalAssetImportAdapters::options()` into explicit import/recook execution for reviewed PNG/glTF/common-audio selections. The models must not execute imports, recooks, hot reload, validation recipes, package scripts, arbitrary shell, shader compilers, arbitrary importer adapters, arbitrary file import, automatic import execution, free-form manifest edits, package streaming, renderer/RHI work, native handles, or dynamic game-module Play-In-Editor.
- Use `mirakana::editor::make_editor_material_preview` for material preview data; it exposes material factors, material uniform byte size, and sampled texture/sampler binding rows without making `editor/core` depend on GUI or RHI backends.
- Use `mirakana::editor::MaterialAuthoringDocument` for source material authoring. It owns first-party `MaterialDefinition` factor edits, fixed texture-slot rows, registry-backed texture validation, dirty/staged state, text-store save/load, deterministic texture dependencies, and `UndoStack` actions without making `editor/core` depend on Dear ImGui, SDL3, renderer, RHI, or runtime game APIs. Prefer registry-aware stage/save calls in editor workflows, and keep material undo stacks scoped to the edited document lifetime.
- Use `mirakana::editor::make_editor_material_gpu_preview_plan` for GUI-independent selected-material GPU preview planning from cooked runtime material/texture payloads; keep actual RHI uploads and display textures in `mirakana_editor`.
- Use `mirakana::editor::make_editor_material_asset_preview_panel_model`, `mirakana::editor::apply_editor_material_gpu_preview_execution_snapshot`, `mirakana::editor::editor_material_gpu_preview_vulkan_visible_refresh_evidence`, `mirakana::editor::editor_material_gpu_preview_metal_visible_refresh_evidence`, and `mirakana::editor::make_material_asset_preview_panel_ui_model` for selected-material Assets panel diagnostics over cooked material metadata, factor rows, texture dependency rows, GPU payload texture readiness, D3D12/Vulkan material-preview shader readiness, and caller-supplied host-owned GPU preview execution snapshots under retained `material_asset_preview.gpu.execution` ids plus `material_asset_preview.gpu.execution.vulkan_visible_refresh` and `material_asset_preview.gpu.execution.metal_visible_refresh` Vulkan- / Metal-scope evidence labels, bundled checklist rows under `material_asset_preview.gpu.execution.parity_checklist` bound to `ge.editor.material_gpu_preview_display_parity_checklist.v1`, and model field `gpu_display_parity_checklist_rows` (`EditorMaterialGpuPreviewDisplayParityChecklistRow`). Keep shader compiler execution, RHI uploads/display textures, package scripts, arbitrary shell, package streaming, native handles, Vulkan display parity, Metal readiness, and broad renderer/editor readiness out of editor core; `mirakana_editor` may map its optional preview cache into the snapshot after host-owned rendering.
- Use `mirakana::editor::make_editor_profiler_panel_model`, `mirakana::editor::make_editor_profiler_trace_export_model`, `mirakana::editor::save_editor_profiler_trace_json`, `mirakana::editor::make_editor_profiler_trace_save_dialog_request`, `mirakana::editor::make_editor_profiler_trace_save_dialog_model`, `mirakana::editor::make_editor_profiler_telemetry_handoff_model`, `mirakana::editor::make_editor_profiler_trace_import_review_model`, `mirakana::editor::import_editor_profiler_trace_json`, `mirakana::editor::make_editor_profiler_trace_open_dialog_request`, `mirakana::editor::make_editor_profiler_trace_open_dialog_model`, and `mirakana::editor::make_profiler_ui_model` for Profiler panel data. Record editor CPU zones/counters through `mirakana::DiagnosticsRecorder` in the shell or host, keep sorting/formatting and retained `profiler.trace_export` / `profiler.trace_file_save` / `profiler.trace_save_dialog` / `profiler.telemetry` / `profiler.trace_import` / `profiler.trace_import.reconstructed_*` / `profiler.trace_file_import` / `profiler.trace_open_dialog` rows in `editor/core`, use `mirakana::export_diagnostics_trace_json` for the visible `Copy Trace JSON` path, write `Save Trace JSON` only through `ITextStore` safe project-relative `.json` paths, route visible `Browse Save Trace JSON` through `mirakana::IFileDialogService` / `mirakana::SdlFileDialogService` before converting accepted in-project save selections back to safe project-relative output paths, surface telemetry handoff readiness from `mirakana::build_diagnostics_ops_plan` without uploading telemetry, review and reconstruct pasted first-party exported Trace Event JSON through `mirakana::import_diagnostics_trace_json` for the visible `Review Trace JSON` path, read `Import Trace JSON` only through `ITextStore` safe project-relative `.json` paths, and route visible `Browse Trace JSON` through `mirakana::IFileDialogService` / `mirakana::SdlFileDialogService` before converting accepted in-project open selections back to safe project-relative paths. Leave broader editor native save/open dialogs outside Profiler, Scene, and Prefab Variant, arbitrary JSON conversion beyond first-party exported Trace Event JSON reconstruction, GPU markers, allocator diagnostics, telemetry SDK/upload execution, crash reports, backend timestamps, native handles, and production flame graphs for explicit observability slices.
- Use `mirakana::editor::make_editor_resource_panel_model` and `mirakana::editor::make_resource_panel_ui_model` for Resources panel diagnostics, reviewed capture request handoff rows, and host-owned capture execution evidence rows. Feed it plain caller-supplied rows from the GUI/host adapter, including `EditorResourceCaptureRequestInput` and `EditorResourceCaptureExecutionInput` rows only for explicit host/operator workflows (optional `host_gates` on execution snapshots mirroring request rows), and keep `IRhiDevice`, renderer objects, SDL3, Dear ImGui, OS handles, PIX APIs, D3D12 debug-layer APIs, ETW, and native RHI handles out of `editor/core`; the panel must not enforce residency, evict resources, stream packages, execute GPU captures, expose native handles, or implement allocator policy. Outside `editor/core`, the Windows `mirakana_editor` shell may run only the reviewed `mirakana::Win32ProcessRunner` paths for `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/launch-pix-host-helper.ps1` with either `-SkipLaunch` or exactly five arguments after `-File` (no `-SkipLaunch`) via `mirakana::run_process_command` / `is_safe_reviewed_pix_host_helper_invocation` / `execute_pix_host_helper_reviewed` after an acknowledged PIX capture request and discovery of the repository script via cwd walk; the launch-enabled path requires an explicit Dear ImGui confirmation modal. This does not broaden arbitrary process execution, unacknowledged `run-validation-recipe` execution, or automatic PIX UI launch. Retained evidence rows use `EditorResourceCaptureExecutionRow` (`phase_code`, bounded artifact/diagnostic strings) and `resources.capture_execution`, including `resources.capture_execution.contract_label` (`ge.editor.resources_capture_execution.v1`), `resources.capture_execution.operator_validated_launch_workflow_contract_label` (`ge.editor.resources_capture_operator_validated_launch_workflow.v1`), and `resources.capture_execution.pix_gpu_capture.host_helper_hint`. See `docs/superpowers/plans/2026-05-11-editor-resource-capture-pix-launch-helper-v1.md`, `docs/superpowers/plans/2026-05-11-editor-resource-capture-pix-host-handoff-evidence-v1.md`, and `docs/superpowers/plans/2026-05-11-editor-resources-capture-operator-validated-launch-workflow-v1.md`.
- Use `mirakana::editor::make_editor_ai_command_panel_model` and `mirakana::editor::make_ai_command_panel_ui_model` for AI Commands panel diagnostics over the existing AI package/preflight/readiness/handoff/evidence/remediation/workflow models. Use `mirakana::editor::make_editor_runtime_scene_package_validation_execution_model`, `mirakana::editor::execute_editor_runtime_scene_package_validation`, and `mirakana::editor::make_editor_runtime_scene_package_validation_execution_ui_model` for reviewed non-mutating `validate-runtime-scene-package` execution from an `EditorPlaytestPackageReviewModel`; the visible AI Commands panel may expose `Validate Runtime Scene Package` through caller-owned filesystem state and transient result evidence, but `editor/core` must not run external processes or mutate manifests for this path. Use `mirakana::editor::make_editor_ai_playtest_evidence_import_model` and `mirakana::editor::make_editor_ai_playtest_evidence_import_ui_model` for externally supplied `GameEngine.EditorAiPlaytestEvidence.v1` paste/review rows under retained `ai_evidence_import` ids, then feed accepted transient rows into the existing evidence summary. Use `mirakana::editor::make_editor_ai_reviewed_validation_execution_plan`, `mirakana::editor::make_editor_ai_reviewed_validation_execution_batch`, `mirakana::editor::make_ai_reviewed_validation_execution_ui_model`, and `mirakana::editor::make_ai_reviewed_validation_execution_batch_ui_model` before visible editor execution of reviewed `run-validation-recipe` rows; host-gated rows require explicit per-row host-gate acknowledgement and reviewed `-HostGateAcknowledgements` arguments before execution, including through retained `ai_commands.execution.batch` / visible `Execute Ready`. Execute only from the optional GUI through a platform process runner, not from `editor/core`. The panel may display reviewed dry-run command text/argv rows, imported external evidence, locally executed evidence, and transient acknowledgement state, but it must not run unacknowledged host-gated recipes, execute arbitrary shell or raw manifest command strings, perform free-form manifest edits, mutate remediation data, load dynamic game modules, embed a runtime host for Play-In-Editor, expose renderer/RHI handles, make Metal ready, or claim broad renderer quality.
- Use `mirakana::editor::make_editor_input_rebinding_profile_panel_model` and `mirakana::editor::make_input_rebinding_profile_panel_ui_model` for Input Rebinding panel diagnostics over `RuntimeInputActionMap` and `RuntimeInputRebindingProfile` rows. Use `mirakana::editor::make_editor_input_rebinding_capture_action_model` and `mirakana::editor::make_input_rebinding_capture_action_ui_model` when the visible editor arms a reviewed digital action capture against first-party `RuntimeInputStateView`; apply captured candidates only to the in-memory profile. Use `mirakana::editor::make_editor_input_rebinding_capture_axis_model` and `mirakana::editor::make_input_rebinding_capture_axis_ui_model` when the visible editor arms reviewed axis capture: gamepad axis capture via `capture_runtime_input_rebinding_axis` and optional keyboard key-pair axis capture through `EditorInputRebindingAxisCaptureDesc::capture_keyboard_key_pair_axes` for two-simultaneous-hold keyboard `key_pair` sources; apply captured axis candidates only to the in-memory profile. Use `mirakana::editor::validate_editor_input_rebinding_profile_store_path`, `mirakana::editor::save_editor_input_rebinding_profile_to_project_store`, and `mirakana::editor::load_editor_input_rebinding_profile_from_project_store` only for explicit operator-initiated `.inputrebinding` Save/Load through `ITextStore` after path review, not from capture paths. The visible editor may show base bindings, profile overrides, save readiness, conflicts, unsupported-claim diagnostics, action capture controls, axis capture controls, and optional `input_rebinding.persistence` path/status/diagnostics rows, but it must not mutate rebinding files through capture alone, execute manifest commands from capture, consume runtime UI focus beyond focused rebinding capture contracts, generate platform input glyphs, assign multiplayer devices, expose SDL3/native input handles, write automatic or cloud/binary saves, or claim broad input middleware.
- Use `mirakana::editor::ShaderCompileState` for shader compile queue and cache-hit UI state.
- Use `mirakana::editor::ShaderCompileState::apply_execution_results` when mapping shader tool execution into editor UI state.
- Use `mirakana::editor::make_viewport_shader_compile_requests` for the standard viewport shader compile plan so D3D12 DXIL and Vulkan SPIR-V artifact requests stay aligned.
- Use `mirakana::editor::make_material_preview_shader_compile_requests` for factor-only and base-color-textured material preview shader artifacts; do not reuse the default viewport shader for material preview rendering.
- Use `mirakana::editor::ShaderToolDiscoveryState` when exposing `mirakana::discover_shader_tools` results and `mirakana::evaluate_shader_toolchain_readiness` diagnostics in editor UI or AI-authored editor workflows.
- GUI shader discovery should merge reviewed project-relative toolchain roots with known installed SDK locations such as `MK_SHADER_TOOLCHAIN_ROOT`, `VULKAN_SDK`/`VK_SDK_PATH`, vcpkg tool roots, and Windows SDK `dxc.exe`, then surface DXC SPIR-V CodeGen readiness separately from DXIL-capable DXC availability.
- Use `mirakana::editor::ViewportShaderArtifactState` when native viewport or material preview availability depends on compiled shader bytecode readiness, including D3D12 DXIL and Vulkan SPIR-V readiness. When multiple shader programs share one readiness surface, query by shader id so viewport and material-preview artifacts cannot be confused.
- Use `mirakana::editor::ProjectShaderToolSettings` as the source for editor shader compiler executable, working directory, artifact root, and cache index paths.
- `ProjectShaderToolSettings::executable` may be a reviewed PATH tool name, project-relative tool path, or absolute installed SDK/tool path; working directory, artifact root, and cache index paths stay project-relative.
- Use `mirakana::editor::ProjectSettingsDraft` for editable project settings so invalid values do not mutate the active project.
- Use `mirakana::editor::make_project_open_dialog_request`, `mirakana::editor::make_project_save_dialog_request`, `mirakana::editor::make_project_open_dialog_model`, `mirakana::editor::make_project_save_dialog_model`, and `mirakana::editor::make_project_file_dialog_ui_model` for native `.geproject` open/save-as review rows under retained `project_file_dialog.open` / `project_file_dialog.save` ids. In the visible `mirakana_editor` shell, route `Open Project...` and `Save Project As...` through `mirakana::IFileDialogService` / `mirakana::SdlFileDialogService`, convert accepted in-store `.geproject` selections to safe store-relative project paths, infer companion workspace/startup-scene bundle paths, and reuse `load_project_bundle` / `save_project_bundle`; keep SDL3/native handles, path conversion, arbitrary project browsing, package scripts, validation recipes, dynamic game-module loading or in-process runtime-host embedding, renderer/RHI uploads, and manifest mutation out of editor core.
- Use `mirakana::editor::make_editor_render_backend_descriptors` and `mirakana::editor::choose_editor_render_backend` for D3D12/Vulkan/Metal/Null viewport backend UI state instead of hardcoding GUI-only backend strings.
- Keep native editor viewport backend availability tied to `ViewportShaderArtifactState` and runtime probes; native backend failures must fall back to `NullRhiDevice` without exposing backend handles to editor core.
- Use `mirakana::editor::SceneNodeTransformDraft`, `mirakana::editor::SceneNodeComponentDraft`, and `mirakana::editor::ViewportTransformEdit` for selected-node scene editing before wiring Inspector or viewport controls, including sprite renderer component edits.
- Use `mirakana::editor::SceneAuthoringDocument` for GUI-independent source scene and prefab authoring. It owns deterministic hierarchy rows, selected-node state, dirty tracking, scene/prefab `ITextStore` save/load helpers, registry-backed mesh/material/sprite reference diagnostics, package candidate rows, package registration draft rows from caller-supplied `runtimePackageFiles` entries, reviewed package registration apply plans, a narrow `ITextStore` updater for `game.agent.json.runtimePackageFiles`, `UndoStack` actions for create/rename/reparent/delete/duplicate/transform/component/prefab-instantiation edits, path-aware prefab instantiate actions, review-only source-link rows through `ScenePrefabInstanceSourceLinkModel`, `make_scene_prefab_instance_source_link_model`, and `make_scene_prefab_instance_source_link_ui_model` over `SceneNode::prefab_source` / `ScenePrefabSourceLink` metadata, and explicit scene prefab instance refresh review through `ScenePrefabInstanceRefreshPlan`, `ScenePrefabInstanceRefreshPolicy`, `plan_scene_prefab_instance_refresh`, `apply_scene_prefab_instance_refresh`, `make_scene_prefab_instance_refresh_action`, retained `scene_prefab_instance_refresh` rows, and multi-root batch review through `ScenePrefabInstanceRefreshBatchPlan`, `ScenePrefabInstanceRefreshBatchTargetInput`, `plan_scene_prefab_instance_refresh_batch`, `apply_scene_prefab_instance_refresh_batch`, `make_scene_prefab_instance_refresh_batch_action`, `make_scene_prefab_instance_refresh_batch_ui_model`, plus retained `scene_prefab_instance_refresh_batch` rows, nested prefab tooling alignment through retained `scene_prefab_instance_refresh.nested_prefab_variant_alignment` / `scene_prefab_instance_refresh_batch.nested_prefab_variant_alignment` contract ids and per-row `nested_variant_alignment.resolution_kind`, plus local-child and stale-source subtree alignment through retained `local_child_variant_alignment` / `stale_source_variant_alignment` (`ge.editor.scene_prefab_local_child_variant_alignment.v1`, `ge.editor.scene_prefab_stale_source_variant_alignment.v1`) and per-row `local_child_variant_alignment.resolution_kind` / `stale_source_variant_alignment.resolution_kind`, via `prefab_variant_conflict_resolution_kind_label`. Plan refresh only for explicitly selected linked prefab roots (single selection or an explicit ordered batch set); fail closed on duplicate batch roots and ancestor/descendant overlap; sequential batch apply may remap node ids through `ScenePrefabInstanceRefreshResult::source_to_result_node_id`. Preserve retained scene node state, add new refreshed source nodes, remove reviewed stale nodes by default, block ambiguous source names and unsupported local children by default, preserve local child subtrees only through explicit `keep_local_children` / `keep_local_child` reviewed rows, keep safe stale source-node subtrees as local author-owned nodes only through explicit `keep_stale_source_nodes_as_local` / `keep_stale_source_node_as_local` reviewed rows, preserve nested linked prefab subtrees only through explicit `keep_nested_prefab_instances` / `keep_nested_prefab_instance` reviewed rows, block unsafe stale subtrees through `unsupported_stale_source_subtree`, block unsupported nested roots through `unsupported_nested_prefab_instance`, while automatic nested prefab refresh and fuzzy merge/rebase propagation UX stay unsupported. Reviewed deterministic chained nested refresh after root is available when the operator enables `ScenePrefabInstanceRefreshPolicy::apply_reviewed_nested_prefab_propagation`, supplies `load_prefab_for_nested_propagation`, and accepts `reviewed_chained_prefab_refresh_after_root` / `apply_reviewed_nested_prefab_propagation` MK_ui policy rows: `validate_nested_prefab_propagation_apply` simulates the preview-order chain to tighten `plan.can_apply`, and `apply_scene_prefab_instance_refresh` runs nested passes with nested propagation disabled on inner plans; batch planning allows propagation apply for disjoint multi-target sets, while duplicate roots and ancestor/descendant overlaps remain fail-closed. Keep these contracts GUI-independent without making `editor/core` depend on Dear ImGui, SDL3, renderer, RHI, broad JSON editing, CMake, package scripts, or runtime game APIs. Use `mirakana::editor::make_scene_open_dialog_request`, `mirakana::editor::make_scene_save_dialog_request`, `mirakana::editor::make_scene_open_dialog_model`, `mirakana::editor::make_scene_save_dialog_model`, and `mirakana::editor::make_scene_file_dialog_ui_model` for native `.scene` open/save review rows under retained `scene_file_dialog.open` / `scene_file_dialog.save` ids, and use `SceneAuthoringDocument::set_scene_path` only after a reviewed save-as path is accepted. Keep scene authoring undo stacks scoped to the edited document lifetime.
- In the visible Scene panel, expose `Keep Local Children`, `Keep Stale Source Nodes`, `Keep Nested Prefab Instances`, nested prefab propagation preview diagnostics, optional `Apply Nested Prefab Propagation` when prefab documents can be loaded for nested paths, and `Refresh Prefab Instance` only for selected linked prefab roots with safe source paths; expose optional batch staging on source-link rows (`Review batch prefab refresh`, `Clear batch selection`); keep mutation behind `Apply Reviewed Prefab Refresh` using `make_scene_prefab_instance_refresh_batch_action` (the shell may route single-root planning through the same batch APIs).
- Use `mirakana::editor::EditorPlaySession` for Play-In-Editor source/simulation scene isolation. It snapshots a `SceneAuthoringDocument` into an isolated simulation scene, reports source-scene edit blocking while active, tracks simulation ticks, and discards the simulation copy on stop. Use `mirakana::editor::IEditorPlaySessionDriver` only for caller-supplied begin/tick/end callbacks against that isolated simulation scene with `EditorPlaySessionTickContext`. Use `mirakana::editor::EditorPlaySessionControlsModel` for visible Run/Viewport Play/Pause/Resume/Stop availability, render the simulation scene while active, and block source-scene authoring plus undo/redo while `source_scene_edits_blocked()` is true; do not load dynamic game modules, embed runtime hosts, execute package scripts or validation recipes, create renderer/RHI uploads from editor core, run hot reload, stream packages, or expose native handles from editor core.
- Use `mirakana::editor::EditorRuntimeHostPlaytestLaunchDesc`, `mirakana::editor::EditorRuntimeHostPlaytestLaunchModel`, `mirakana::editor::make_editor_runtime_host_playtest_launch_model`, and `mirakana::editor::make_editor_runtime_host_playtest_launch_ui_model` for reviewed external runtime-host launch rows under retained `play_in_editor.runtime_host` ids. `editor/core` may validate caller-supplied argv, working directory, host gates, acknowledgement, and unsupported claims into a safe `mirakana::ProcessCommand`, but the visible `mirakana_editor` shell must own explicit host-gate acknowledgement, `mirakana::Win32ProcessRunner` execution, and transient evidence. Do not add dynamic game-module loading, in-process runtime-host embedding, package script execution, arbitrary shell/raw manifest evaluation, free-form manifest edits, renderer/RHI/native handles, hot reload, package streaming, or editor-core process execution.
- Use `mirakana::editor::EditorInProcessRuntimeHostDesc`, `mirakana::editor::EditorInProcessRuntimeHostModel`, `mirakana::editor::EditorInProcessRuntimeHostBeginResult`, `mirakana::editor::make_editor_in_process_runtime_host_model`, `mirakana::editor::begin_editor_in_process_runtime_host_session`, and `mirakana::editor::make_editor_in_process_runtime_host_ui_model` for reviewed linked-driver in-process runtime-host rows under retained `play_in_editor.in_process_runtime_host` ids. `editor/core` may start only the existing isolated `EditorPlaySession` with an already-linked caller-supplied `IEditorPlaySessionDriver`; the visible editor should enable `Begin In-Process Runtime Host` only while a reviewed driver is available. Do not add `DesktopGameRunner` embedding, package script or validation recipe execution, arbitrary shell/raw manifest evaluation, free-form manifest edits, renderer/RHI uploads or handles, hot reload, package streaming, native handles, or editor-core process execution.
- Use `mirakana::editor::EditorGameModuleDriverApi`, `mirakana::editor::EditorGameModuleDriverContractMetadataModel`, `mirakana::editor::EditorGameModuleDriverCtestProbeEvidenceModel`, `mirakana::editor::EditorGameModuleDriverReloadTransactionRecipeEvidenceModel`, `mirakana::editor::EditorGameModuleDriverUnloadModel`, `mirakana::editor::make_editor_game_module_driver_contract_metadata_model`, `mirakana::editor::make_editor_game_module_driver_contract_metadata_ui_model`, `mirakana::editor::make_editor_game_module_driver_load_model`, `mirakana::editor::make_editor_game_module_driver_reload_model`, `mirakana::editor::make_editor_game_module_driver_from_symbol`, `mirakana::editor::make_editor_game_module_driver_load_ui_model`, `mirakana::editor::make_editor_game_module_driver_reload_ui_model`, `mirakana::editor::make_editor_game_module_driver_unload_model`, `mirakana::editor::make_editor_game_module_driver_unload_ui_model`, `mirakana::editor::make_editor_game_module_driver_ctest_probe_evidence_model`, `mirakana::editor::make_editor_game_module_driver_ctest_probe_evidence_ui_model`, `mirakana::editor::make_editor_game_module_driver_reload_transaction_recipe_evidence_model`, `mirakana::editor::make_editor_game_module_driver_reload_transaction_recipe_evidence_ui_model`, `mirakana::editor::editor_game_module_driver_reload_transaction_recipe_evidence_contract_v1`, `mirakana::editor::make_editor_game_module_driver_host_session_snapshot`, and `mirakana::editor::make_editor_game_module_driver_host_session_ui_model` for reviewed dynamic game-module driver load, stopped-state reload, unload, same-engine-build contract metadata rows, CTest probe evidence labels, reload-transaction validation-recipe argv evidence labels, and host session phase plus DLL barrier/policy rows under retained `play_in_editor.game_module_driver` / `play_in_editor.game_module_driver.reload` / `play_in_editor.game_module_driver.contract` / `play_in_editor.game_module_driver.unload` / `play_in_editor.game_module_driver.ctest_probe_evidence` / `play_in_editor.game_module_driver.reload_transaction_recipe_evidence` / `play_in_editor.game_module_driver.session` ids (including `barriers_contract_label`, `barrier.play_dll_surface_mutation.status`, `policy.active_session_hot_reload`, `policy.stopped_state_reload_scope`, `policy.dll_mutation_order_guidance`, `ge.editor.editor_game_module_driver_reload_transaction_recipe_evidence.v1`, `ge.editor.editor_game_module_driver_host_session.v1`, `ge.editor.editor_game_module_driver_host_session_dll_barriers.v1`). `editor/core` reviews only absolute module paths, safe factory symbols, Play-In-Editor session gates, already-loaded policy, loaded-driver state, stopped Play-In-Editor state, `GameEngine.EditorGameModuleDriver.v1`, required callbacks, host-owned module lifetime, unsupported claims, and function-table ABI validity; the visible shell owns `Game Module Driver`, `Load Game Module Driver`, `Reload Game Module Driver`, `Unload Game Module Driver`, `mirakana::load_dynamic_library`, `mirakana::resolve_dynamic_library_symbol`, library lifetime, and enabling load/reload/unload only when the review models allow it, and renders host-session DLL mutation order guidance from `EditorGameModuleDriverHostSessionSnapshot::policy_dll_mutation_order_guidance` next to other session policy lines in the Viewport Game Module Driver section. Use `MK_editor_game_module_driver_probe` / `MK_editor_game_module_driver_load_tests` only as Windows same-engine-build test evidence for the real exported `mirakana_create_editor_game_module_driver_v1` path; the retained `ctest_probe_evidence` rows document targets without running CTest inside the editor UI. The retained `reload_transaction_recipe_evidence` rows document reviewed `run-validation-recipe` argv for `dev-windows-editor-game-module-driver-load-tests` with explicit `windows-msvc-dev-editor-game-module-driver-ctest` acknowledgement without executing the runner inside the editor UI. Keep active-session hot reload, stable third-party ABI, `DesktopGameRunner` embedding, package scripts, validation recipes, renderer/RHI uploads or handles, native handles, package streaming, and broad editor productization unsupported. Engine 1.0 excludes a **vendor-stable third-party C ABI** for editor game module DLLs; see `docs/legal-and-licensing.md` (Editor game module driver and third-party DLL ABI) and `docs/superpowers/plans/2026-05-11-editor-game-module-driver-stable-third-party-abi-1-0-exclusion-v1.md`. Same-engine-build `GameEngine.EditorGameModuleDriver.v1` remains the only reviewed interchange.
- Use `mirakana::editor::PrefabVariantAuthoringDocument` for GUI-independent source prefab variant authoring. It owns deterministic override rows, composed-prefab review, dirty tracking, `ITextStore` save/load helpers, registry-backed mesh/material/sprite diagnostics for component overrides, `PrefabNodeOverride::source_node_name` hints for editor-authored overrides, and `UndoStack` actions for name/transform/component override edits without making `editor/core` depend on Dear ImGui, SDL3, renderer, RHI, JSON parsing, CMake, or runtime game APIs. Use `mirakana::editor::make_prefab_variant_conflict_review_model` and `mirakana::editor::make_prefab_variant_conflict_review_ui_model` for conflict review rows under retained `prefab_variant_conflicts` ids; those models report can-compose state, blocking missing-node/duplicate-override/source-node-mismatch rows, non-blocking redundant/component-family warnings, reviewed cleanup metadata for redundant/later duplicate/missing-node stale override rows, reviewed retarget metadata only when a missing-node row or existing-node `source_node_mismatch` row has a unique safe `source_node_name` match, reviewed `accept_current_node` / `Accept current node N` metadata for existing-node `source_node_mismatch` rows when no safe retarget exists, and a reviewed batch plan under `prefab_variant_conflicts.batch_resolution`. Use `mirakana::deserialize_prefab_variant_definition_for_review` only for editor review-cleanup/retarget loading of stale variants; keep strict `deserialize_prefab_variant_definition` for valid variant IO and strict `mirakana_scene` composition index-based. Use `mirakana::editor::resolve_prefab_variant_conflict` and `mirakana::editor::make_prefab_variant_conflict_resolution_action` only for explicit reviewed cleanup, single-row retarget, or accept-current hint repair through a variant-scoped `UndoStack`; accept-current updates only `PrefabNodeOverride::source_node_name`. Use `mirakana::editor::resolve_prefab_variant_conflicts` and `mirakana::editor::make_prefab_variant_conflict_batch_resolution_action` only when the author explicitly chooses `Apply All Reviewed`; the batch resolver must recompute conflict rows after each reviewed row. Use `mirakana::editor::plan_prefab_variant_base_refresh`, `mirakana::editor::make_prefab_variant_base_refresh_ui_model`, `mirakana::editor::apply_prefab_variant_base_refresh`, and `mirakana::editor::make_prefab_variant_base_refresh_action` only for explicit reviewed embedded-base refresh rows under retained `prefab_variant_base_refresh` ids; preserve or retarget overrides by unique `PrefabNodeOverride::source_node_name`, block missing hints, missing or ambiguous refreshed source nodes, invalid inputs, and duplicate target node/kind keys, and keep this out of automatic nested prefab propagation or fuzzy merge/rebase UX. Use `mirakana::editor::make_prefab_variant_open_dialog_request`, `mirakana::editor::make_prefab_variant_save_dialog_request`, `mirakana::editor::make_prefab_variant_open_dialog_model`, `mirakana::editor::make_prefab_variant_save_dialog_model`, and `mirakana::editor::make_prefab_variant_file_dialog_ui_model` for native `.prefabvariant` open/save review rows under retained `prefab_variant_file_dialog.open` / `prefab_variant_file_dialog.save` ids; keep SDL3/native handles, path conversion, and I/O in the optional shell. Do not apply cleanup/retarget/accept-current/batch automatically and do not treat source-node retarget, source mismatch retarget, accept-current hint repair, batch resolution, or Prefab Variant native dialogs as nested prefab propagation, broad merge/rebase UX, or broader editor native save/open dialogs outside Profiler, Scene, and Prefab Variant. Keep prefab variant undo stacks scoped to the edited document lifetime. In the visible `mirakana_editor` shell, keep the Prefab Variant Authoring section as an adapter over this document: create/load/save `.prefabvariant` files, route `Browse Load Variant` / `Browse Save Variant` through `mirakana::IFileDialogService` / `mirakana::SdlFileDialogService` before converting accepted in-project selections to safe project-relative paths, render rows/diagnostics/conflict review/file dialog rows, submit override edits through `make_prefab_variant_*_override_action`, guard `composed_prefab()` behind conflict review can-compose state, apply only reviewed redundant/duplicate/missing-node cleanup, unique source-node retarget for missing-node or source-node-mismatch rows, accept-current hint repair for existing-node source mismatches, or explicit batch reviewed resolution actions, and instantiate composed prefabs into the active `SceneAuthoringDocument` without adding nested propagation, fuzzy matching, automatic merge/rebase/resolution UX, package scripts, validation recipes, manifest mutation, or renderer/RHI coupling.
- In the visible `mirakana_editor` shell, keep `SceneAuthoringDocument` as the active scene source for Scene, Inspector, Viewport, and Assets panels. Route hierarchy create/rename/delete/duplicate, prefab save/load/instantiate, Inspector transform/component edits, and viewport transform deltas through `make_scene_authoring_*` actions, pass saved prefab or prefab-variant paths into path-aware instantiate actions, render retained `scene_prefab_source_links` rows as diagnostics only, expose `Keep Local Children`, `Keep Stale Source Nodes`, `Refresh Prefab Instance`, and reviewed multi-root batch controls (`Review batch prefab refresh`, `Clear batch selection`) only for eligible linked roots with safe prefab paths, route accepted refreshes through `make_scene_prefab_instance_refresh_batch_action` while keeping `make_scene_prefab_instance_refresh_action` in editor core for direct single-root callers and `tools/check-ai-integration.ps1` shell needles, route `Open Scene...` / `Save Scene As...` and `Browse Open Scene` / `Browse Save Scene As` through `mirakana::IFileDialogService` / `mirakana::SdlFileDialogService` before converting accepted in-project `.scene` selections to safe project-relative paths, and render `validate_scene_authoring_references`, `make_scene_package_candidate_rows`, `make_scene_package_registration_draft_rows`, `make_scene_package_registration_apply_plan`, plus retained `scene_file_dialog.open` / `scene_file_dialog.save` rows as package and file-dialog preparation data. Scene open/save-as must reuse `load_scene_authoring_document` / `save_scene_authoring_document`; save-as must update `SceneAuthoringDocument::set_scene_path`; scene replacement must use the existing path that stops Play-In-Editor and clears document undo state. The editor may expose `Apply Package Registration` for reviewed `add_runtime_file` rows through `apply_scene_package_registration_to_manifest`; do not run CMake, package builds, shader tools, arbitrary manifest mutation, automatic nested prefab propagation, fuzzy matching, automatic merge/rebase/resolution UX, package streaming, renderer/RHI uploads, or native-handle work from the editor. `tools/register-runtime-package-files.ps1` remains the headless/manual lane when reviewed draft rows need manifest `runtimePackageFiles` updates outside the GUI. For Editor Playtest Package Review Loop v1, keep package registration, manifest `runtimeSceneValidationTargets` selection, `validate-runtime-scene-package`, and host-gated desktop smoke as separate reviewed steps.
- Use `mirakana::build_asset_import_plan`, `mirakana::build_asset_recook_plan`, `mirakana::execute_asset_import_plan`, `mirakana::assemble_asset_cooked_package`, `mirakana::write_asset_cooked_package_index`, `mirakana::deserialize_audio_source_document`, `mirakana::choose_file_watch_backend`, `mirakana::PollingFileWatcher`, Windows `mirakana::WindowsFileWatcher` when native watching is selected, `mirakana::scan_asset_files_for_hot_reload`, `mirakana::AssetHotReloadTracker`, `mirakana::AssetHotReloadRecookScheduler`, and `mirakana::AssetHotReloadApplyState` instead of ad hoc editor-only asset state when handling texture/mesh/audio/material/scene import, package assembly, or recook workflows.
- Keep `mirakana::renderer::RhiViewportSurface` as the renderer-owned viewport target, use `mirakana::RhiViewportDisplayFrame` for native display interop, use `mirakana::RhiViewportReadbackFrame` for CPU fallback copies, and keep GUI-local display textures as editor-shell details.
- For the Windows D3D12 editor display path, prefer SDL's `direct3d12` renderer, wrap exported shared viewport textures through editor-private SDL texture code, and fall back to CPU readback whenever SDL is not using D3D12 or the shared texture cannot be opened.

## Do Not

- Add GUI dependencies to the default build.
- Run external import or shader tools through shell strings.
- Save editor formats with nondeterministic ordering.
- Add UI middleware, fonts, or UI assets without `license-audit` and dependency/notice updates.
- Update Codex guidance without updating the equivalent Claude Code surface when behavior changes.
