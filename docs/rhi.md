# Renderer Hardware Interface

## Current Scope

`MK_rhi` is the low-level graphics API boundary. It is intentionally separate from `MK_renderer`:

- `MK_renderer`: high-level renderer-facing contracts.
- `MK_rhi`: backend capability/probe contracts, Renderer/RHI Resource Foundation v1 through `RhiResourceLifetimeRegistry` resource ids, owner labels, debug names, generation-checked handles, deferred-release records, frame-indexed retirement, diagnostics, marker-style lifetime events, NullRHI lifetime wiring, D3D12/Vulkan deferred native teardown, GPU debug marker/timestamp-frequency rows, read-only memory diagnostics, Upload/Staging Foundation v1 through foundation-only `RhiUploadStagingPlan` staging allocation rows, buffer/texture copy rows, submitted `FenceValue` tracking, completed-fence retirement, and `RhiUploadDiagnosticCode::stale_generation` same-id/wrong-generation checks, device, resources, samplers, swapchains, explicit swapchain frame acquire/release, swapchain resize, shaders, descriptor set layouts, descriptor sets, upload-buffer writes, copy/upload/readback commands with explicit texture footprints, CPU buffer readback, transient resource leases, presentation ownership, graphics pipelines with explicit vertex input layouts/attributes plus backend-neutral depth-test/write/compare state, render passes with optional first-party depth attachments, vertex/index buffer binding, direct and indexed draw calls, transitions, explicit texture aliasing barrier intent, submission, fence semantics, and backend-neutral fence lifecycle stats.
- backend modules: future Direct3D 12, Vulkan, and Metal implementations.

The API-independent backend contract includes `BackendCapabilityProfile`, `BackendProbePlan`, and `BackendProbeResult`. These describe host support, shader artifact format (`dxil`, `spirv`, `metallib`, or none), native presentation features, and the deterministic bootstrap steps a backend must complete before the editor can call it available. Vulkan probes are modeled around runtime loading, physical device enumeration, queue family inspection, logical device creation, and shader artifact verification. Metal probes are modeled around runtime loading, default device creation, command queue creation, and Metal library verification.

The current backend is `NullRhiDevice`. It does not create GPU objects. It validates resource, sampler, descriptor, upload-buffer write, copy footprint, CPU buffer readback, transient lease, presentation, explicit swapchain frame acquire/release, swapchain resize, pipeline, vertex input layout/attribute, vertex buffer, index buffer, indexed draw, and RHI Depth Attachment Contract v0 descriptions. Optional depth render-pass attachments must be owned by the device, include `TextureUsage::depth_stencil`, use `Format::depth24_stencil8`, match the color target extent, be in `ResourceState::depth_write`, and provide finite clear-depth values in `[0, 1]`; depth textures may additionally include `TextureUsage::shader_resource` for sampled-depth use after an explicit `depth_write` to `shader_read` transition, but `NullRhiDevice` rejects using a shader-read depth texture as a depth attachment until it transitions back. Bound graphics pipelines must match the active color and depth formats. `NullRhiDevice` rejects unknown handles, incompatible descriptor set bindings, invalid write/copy ranges/usages, invalid readback ranges/usages, invalid vertex/index buffer ranges/usages, invalid sampler descriptors, invalid swapchains, invalid swapchain frame sequencing, released transient resources, and shader stage mismatches, records command list and render pass lifecycle, stores written upload-buffer bytes for deterministic copy/readback tests, counts write/copy/read/draw/indexed-draw/present/resize/frame/transient/sampler intent, tracks fence waits, wait failures, last submitted fence values, and last completed fence values, and signals fences immediately.

Renderer/RHI Resource Foundation v1 is closed for the Engine 1.0 Windows-default renderer/RHI foundation surface: `RhiResourceLifetimeRegistry`, NullRHI lifetime wiring, D3D12/Vulkan deferred native teardown for committed resources and pipeline-class objects, GPU debug markers/timestamp-frequency rows, read-only memory diagnostics, editor Resources diagnostics, and reviewed capture handoff evidence are implemented while keeping native handles private. Frame Graph and Upload/Staging Foundation v1 remain deliberately narrower than production GPU scheduling and upload management. `FrameGraphV1Desc` and `RhiUploadStagingPlan` are standard-library-only upload/staging value/planning contracts; they do not implement production render graph ownership, native async upload package integration, a GPU allocator, residency budgets, package streaming, Metal `IRhiDevice` parity, or 2D/3D playable vertical-slice readiness. Runtime RHI Upload Submission Fence Rows v1 records submitted `mirakana::rhi::FenceValue` rows from runtime texture/mesh/skinned/morph/material-factor uploads, preserves actual scene upload submit order in `RuntimeSceneGpuBindingResult::submitted_upload_fences`, and derives `submitted_upload_fence_count` plus `last_submitted_upload_fence` in `RuntimeSceneGpuUploadExecutionReport`; this is host-owned first-party telemetry, not native async upload execution or staging-ring integration. RHI Upload Stale Generation Diagnostics v1 makes stale upload handles deterministic by returning `stale_generation` when a handle id is known but its generation no longer matches, including upload-ring ownership and byte-offset checks.

Texture Aliasing Barrier Command v1 adds `IRhiCommandList::texture_aliasing_barrier`, `RhiStats::texture_aliasing_barriers`, D3D12 `DeviceContextStats::texture_aliasing_barriers`, and the Frame Graph helper `FrameGraphTextureAliasingBarrier` / `FrameGraphTextureAliasingBarrierRecordResult` / `record_frame_graph_texture_aliasing_barriers`. Null validates/counts without state mutation, Vulkan records a conservative synchronization2 memory dependency, and D3D12 committed texture handles record engine-level intent/stat only; native D3D12 placed-resource aliasing, Vulkan memory alias allocation, wildcard/null alias barriers, and automatic executor insertion remain future work.

`MK_renderer` now includes `RhiViewportSurface`, a backend-neutral renderer-owned offscreen color target. It creates an RHI texture with render-target, shader-resource, copy-source, and optionally shared usage for native display interop, recreates that texture on resize, routes color-state transitions to `render_target`, `copy_source`, and `shader_read` through `execute_frame_graph_rhi_texture_schedule`, records either a clear render pass or an `IRenderer`-submitted draw frame through `RhiViewportSurface::render_frame`, forwards renderer clear colors into `RenderPassColorAttachment::clear_color`, submits it, and waits for completion when requested. `MeshCommand` carries scene mesh/material asset identity plus optional `MeshGpuBinding` and `MaterialGpuBinding`; `RhiFrameRenderer::draw_sprite()` and `RhiFrameRenderer::draw_mesh()` validate and queue ordered primary draw work, and `RhiFrameRenderer::end_frame()` executes a scheduled `primary_color` frame graph RHI pass callback that begins the existing render pass, binds the needed pipelines, records queued sprite/mesh/native-overlay work in order, ends the render pass, and increments backend-neutral framegraph counters while swapchain acquire/present and command-list submission remain renderer-owned. RHI Depth Attachment Contract v0 lets `RhiFrameRendererDesc` carry an optional host-owned depth texture and forward it as a first-party render-pass depth attachment; this does not expose depth ownership through `IRenderer`, runtime-host reports, gameplay APIs, or native backend handles. The Frame Graph v0 model declares ordered resources and passes with deterministic diagnostics for missing producers/consumers, write/write hazards, same-pass read/write hazards, and cycles. Frame Graph v1 foundation-only planning adds `FrameGraphV1Desc`, explicit resource access rows, imported/transient resource policy, deterministic pass ordering, barrier intent rows, explicit texture-barrier recording through caller-owned bindings, `execute_frame_graph_v1_schedule` for caller-owned linear barrier/pass callback dispatch, `execute_frame_graph_rhi_texture_schedule` for ordered caller-owned `IRhiCommandList::transition_texture` recording, `FrameGraphTexturePassTargetAccess` plus `build_frame_graph_texture_pass_target_accesses` writer-access rows, declared `FrameGraphTexturePassTargetState` preparation that must match the pass/resource writer access before pass callbacks, pass callback dispatch, and declared `FrameGraphTextureFinalState` reusable final-state transitions, `plan_frame_graph_transient_texture_aliases` / `FrameGraphTransientTextureAliasPlan` for conservative transient texture lifetime, exact-desc alias grouping, estimated unaliased/aliased bytes, shared `TextureHandle` state handoff so barrier, pass-target, and final-state execution update every `FrameGraphTextureBinding` row that names the same backend-neutral texture handle while rejecting conflicting initial shared-handle states, and Frame Graph Texture Aliasing Barrier Command v1 through explicit `FrameGraphTextureAliasingBarrier`, `FrameGraphTextureAliasingBarrierRecordResult`, and `record_frame_graph_texture_aliasing_barriers` helper rows over public `IRhiCommandList::texture_aliasing_barrier`. This remains foundation-only: aliasing barrier commands can now be recorded explicitly, but renderer-wide execution migration, native transient heap allocation, D3D12 placed-resource alias allocation, automatic executor insertion, wildcard aliasing barriers, package streaming, Metal aliasing-barrier parity, and native heap alias execution remain follow-up work. `RhiPostprocessFrameRenderer` now queues scene mesh commands during `draw_mesh()`, lets `execute_frame_graph_rhi_texture_schedule` prepare declared writer-access-backed `scene_color` and optional `scene_depth` target states, records the scene render pass from the executor `scene_color` callback, then runs scheduled post-chain transitions/callbacks and reusable scene-depth final-state restoration while keeping swapchain acquire/present, postprocess pass bodies, native UI overlay preparation, and native resource ownership renderer-owned. `RhiDirectionalShadowSmokeFrameRenderer` now feeds its scheduled shadow-depth, scene-color, scene-depth inter-pass texture transitions, declared writer-access-backed shadow-color/shadow-depth/scene-color/scene-depth target-state preparation, pass callbacks, and reusable scene-depth/shadow-depth final-state restoration through the same executor while keeping swapchain acquire/present, render-pass bodies, and native UI overlay preparation renderer-owned. Postprocess Depth Input Readback Foundation v0 lets the postprocess renderer opt into a renderer-owned `depth24_stencil8` sampled depth texture. The stable postprocess descriptor policy binds scene color at texture/sampler bindings `0/1` and optional scene depth at texture/sampler bindings `2/3`, transitions depth from `depth_write` to `shader_read` for the postprocess pass through the frame graph executor, restores it to `depth_write` through executor final states for the next frame, presents, and records backend-neutral framegraph/postprocess pass counters from executor results. Native GPU UI Overlay Foundation v1 lets the postprocess and directional-shadow smoke paths queue public `SpriteCommand` rows from `MK_ui_renderer`, upload rectangle vertices through renderer-owned RHI buffers, and draw a final colored overlay after postprocess without exposing RHI/native handles to gameplay. Native UI Textured Sprite Atlas Foundation v1 extends only that overlay path: `SpriteCommand` can carry first-party atlas page and UV metadata, the overlay owns sampled texture/sampler descriptors, and runtime host code binds an already-cooked package texture as a single atlas page for the sample image sprite. Tests cover the viewport-shaped path where `MK_runtime_rhi` uploads a runtime texture, creates a sampler-aware material descriptor binding, and `RhiViewportSurface::render_frame` submits a material-bound draw, plus raw `RhiFrameRenderer` primary-color callback execution, color-only, depth-input, native-overlay, textured UI overlay postprocess paths, executor pass-target access/final-state diagnostics, executor-routed directional shadow smoke paths, device-free transient texture alias planning, shared-`TextureHandle` executor state handoff, and explicit texture aliasing barrier recording. D3D12 and env-gated Vulkan RHI tests prove depth-aware postprocess output through CPU readback; `sample_desktop_runtime_game` now also has selected D3D12 and strict Vulkan package smokes that require `postprocess_depth_input_ready=1`, `ui_overlay_ready=1`, and `ui_texture_overlay_atlas_ready=1`. SSAO, depth of field, fog, temporal history, broader postprocess materials/effect stacks, production render graph ownership, renderer-wide pass migration beyond the completed `RhiFrameRenderer` `primary_color`, postprocess scene, directional-shadow, and viewport color-state executor slices, renderer-wide manual-transition removal beyond the completed final-state, writer-access-backed pass-target-state policy, shadow-color target-state ownership, viewport color-state, and shared-`TextureHandle` state-handoff executor slices, native transient heap allocation and alias execution, automatic aliasing-barrier insertion into frame execution, multi-queue scheduling, source image decoding, production atlas packing, production text/font/accessibility, and Metal postprocess/overlay readiness remain follow-up work. `RhiViewportSurface::prepare_display_frame` prepares the color target for shader-read state through the frame graph executor and returns a backend-neutral `RhiViewportDisplayFrame` without doing a CPU copy. `RhiViewportSurface::readback_color_frame` first prepares copy-source state through the frame graph executor when needed, records a texture-to-buffer copy with 256-byte-aligned rows, reuses its readback buffer while the required size is unchanged, and reads the buffer back through `IRhiDevice::read_buffer` into `RhiViewportReadbackFrame`. The desktop editor uses this as the current viewport render surface, resolves the project viewport backend preference through `mirakana::editor::choose_editor_render_backend`, can create the D3D12 `IRhiDevice` bridge on Windows after `mirakana::editor::ViewportShaderArtifactState` verifies real non-empty VS/PS DXIL artifacts, submits scene packets through `MK_scene_renderer`, and uses a D3D12 shared-texture SDL display path when the SDL renderer is D3D12. If that native bridge is unavailable, it falls back to CPU readback into a GUI-local SDL texture for Dear ImGui display.

Swapchain-backed `RhiFrameRenderer::resize` now forwards the requested extent to `IRhiDevice::resize_swapchain` before updating the renderer extent. `DesktopGameRunner` can therefore keep a game-window renderer synchronized with the public `IWindow` extent without exposing SDL, OS, DXGI, Vulkan, Metal, or backend-native handles to game code. `MK_runtime_host_sdl3_presentation` is the first host/backend adapter on that path: it keeps SDL3 window-property probing in the `.cpp` implementation, privately converts Win32 surface candidates into first-party D3D12/Vulkan surface handles only inside the adapter, accepts host-supplied precompiled D3D12 DXIL or Vulkan SPIR-V vertex/fragment shader bytecode through first-party descriptors, can create a swapchain-backed `RhiFrameRenderer` when a real Win32 SDL surface, runtime support, and valid bytecode are present, can inject host-owned D3D12 or Vulkan scene GPU bindings for cooked scene packages, validates scene renderer request vertex input against referenced cooked mesh payload layouts before treating scene GPU bindings as ready, creates scene graphics pipelines from the exact runtime scene palette material pipeline layout, can create a host-owned `RhiPostprocessFrameRenderer` from target-specific postprocess shader artifacts, can request renderer-owned native UI overlay shaders for final `MK_ui_renderer` box/image-placeholder sprite output, can bind a cooked texture as a renderer-owned UI atlas page for sample image sprites, and exposes only backend-neutral `SdlDesktopPresentationReport` / backend report rows plus `evaluate_sdl_desktop_presentation_quality_gate` for requested/selected backend, fallback status/reason, diagnostic counts, renderer stats, scene GPU status/stats, `postprocess_status`, `postprocess_depth_input_requested`, `postprocess_depth_input_ready`, native UI overlay status/counters, textured UI atlas status/counters, `renderer_quality_status`, `renderer_quality_framegraph_execution_budget_ok`, and `framegraph_passes`. `sample_desktop_runtime_shell` now compiles its procedural-triangle HLSL to build-output DXIL and SPIR-V when suitable DXC tooling is available, loads those packaged artifacts through `MK_runtime_host` `IFileSystem` contracts, and can require shader artifacts or a real D3D12/Vulkan renderer in focused smoke runs. `sample_desktop_runtime_game` now compiles target-specific scene, postprocess, shadow, and UI overlay DXIL/SPIR-V artifacts, can request D3D12 or Vulkan scene GPU bindings plus depth-aware postprocess, native UI overlay, cooked texture/atlas-backed UI image sprites, and `--require-renderer-quality-gates` through first-party descriptors, ships a cooked position+normal+uv triangle plus lit material payload, and proves Lit Material v0 plus package-visible depth-aware Frame Graph/Postprocess v0, fixed 3x3 PCF directional shadow smoke, native GPU runtime UI overlay, sampled UI atlas proof, and Renderer Package Quality Gates v1 with base-color texture/material factors, one deterministic directional Lambert light, `postprocess_status=ready`, `postprocess_depth_input_ready=1`, `renderer_quality_status=ready`, `renderer_quality_framegraph_execution_budget_ok=1`, `ui_overlay_status=ready`, `ui_texture_overlay_status=ready`, `ui_texture_overlay_atlas_ready=1`, and `framegraph_passes=3` in selected D3D12 and strict Vulkan package smokes on a ready Windows host. If any gate is missing, these paths report deterministic `NullRenderer` fallback diagnostics. Metal standalone game-window presentation remains follow-up Apple-host adapter work and is not part of the public game API.

`MK_assets` now owns shader compiler command planning, shell-free shader tool runner validation, compiler output capture contracts, shader include dependency discovery, shader include dependency graph edge generation, and shader artifact manifest serialization. It can describe DXC-based HLSL to DXIL commands for D3D12, DXC-based HLSL to SPIR-V commands for Vulkan with `-spirv` and `-fspv-target-env=vulkan1.3`, and Metal command-line IR/library steps without adding compiler binaries to the repository. The current runner contract accepts executable-plus-argument vectors and captures exit code, diagnostic text, stdout, stderr, and artifact provenance while supporting recording/dry-run behavior. `MK_platform` provides a shell-free Windows process runner and rooted filesystem, and `MK_tools` bridges shader tool commands to platform process execution with artifact output-root policy, reviewed executable override for PATH names, project-relative paths, or absolute installed SDK/tool paths, deterministic toolchain discovery for `dxc`, `spirv-val`, `metal`, and `metallib`, backend-specific shader toolchain readiness diagnostics including DXC SPIR-V CodeGen capability, `spirv-val` artifact validation actions, input-fingerprint provenance, persistent cache index storage, cache-hit reuse, shader hot reload plans from artifact provenance, backend pipeline recreation decisions after successful non-cached compiles, batch shader pipeline cache planning through `build_shader_pipeline_cache_plan`, first-party cache-index repair through `reconcile_shader_pipeline_cache_index`, runtime recook execution through the asset import path, deterministic artifact marker writes for tested tool workflows, and real artifact enforcement when marker writes are disabled. Native D3D12/Vulkan/Metal pipeline-cache blobs remain backend-private and are not exposed through the first-party cache-index metadata.

`MK_assets` also provides backend-neutral material binding metadata and shader descriptor reflection records. `build_material_pipeline_binding_metadata` maps material factors and texture slots into stable first-party set/binding/resource/stage semantics: binding 0 is the material factor uniform, each texture slot emits a sampled texture binding, and each texture slot also emits a stable sampler binding. Renderer or tool layers can translate this metadata into concrete RHI descriptor set layout and pipeline layout descriptions without making `MK_assets` depend on `MK_rhi`.

`MK_rhi_d3d12` is a Windows-only backend scaffold. It includes the Windows SDK Direct3D 12 and DXGI headers, links the official D3D12/DXGI libraries, probes DXGI factory creation, debug layer availability, hardware adapter support, and WARP fallback support, then validates a minimal device, direct command queue, fence bootstrap, first committed upload/readback/default texture resource ownership, and a PIMPL `DeviceContext` that keeps the D3D12 device, queues, fence, committed resources, command allocators, command lists, HWND swapchains, RTV heaps, shader-visible descriptor heaps, root signatures, shader bytecode, graphics PSOs with optional input layouts, and render-target views alive across calls. It can close, reset, and execute owned command lists with fence signaling, query completed fence values, perform CPU-side fence waits before allocator reuse, queue GPU-side waits between graphics/compute/copy queues, create flip-model HWND swapchains, own back buffers, present, resize, transition swapchain back buffers, transition standalone textures, clear swapchain and texture render targets with requested clear colors, create CBV/SRV/UAV and sampler descriptor heaps, create descriptor-table root signatures, write CBV/SRV/UAV and sampler descriptor views, bind descriptor heaps, bind graphics root signatures, bind graphics descriptor tables with sampler table splitting by heap kind, set swapchain back buffers or standalone textures as render targets, bind graphics PSOs, bind vertex/index buffers through input-assembler views, write upload-buffer bytes, record buffer and aligned buffer/texture copies, map readback buffers for CPU bytes, create shared committed textures when `TextureUsage::shared` is requested, export shareable textures through first-party `D3d12SharedTextureHandle` values, and record direct or indexed draws. It now also exposes an initial `IRhiDevice` bridge for backend-neutral buffer/texture creation, sampler handle creation, upload-buffer writes, descriptor set layout/allocation/update validation backed by split CBV/SRV/UAV and sampler shader-visible descriptor arenas plus native descriptor writes, shader bytecode ownership, pipeline layout creation, graphics PSO creation with vertex input metadata, buffer and aligned buffer/texture copy recording, CPU buffer readback, standalone texture transition recording with first-party texture state validation, committed-resource-backed transient leases, surface-backed HWND swapchain creation and resize through first-party `SurfaceHandle`, explicit `SwapchainFrameHandle` acquisition/release before swapchain render passes, graphics-queue-only queued presentation during submission, swapchain render-pass-complete-before-present validation, duplicate-present rejection, single-pending-frame throttling until release or present/submit completion, swapchain and texture color render pass recording with non-black clear, first-triangle, and sampled-texture-plus-sampler CPU readback assertions in tests, private `Format::depth24_stencil8` DSV ownership, sampled depth textures backed by private typeless resources with D24 DSV views and R24 SRV views, explicit `depth_write` to `shader_read` transition validation, a D3D12 readback proof that samples a previously written depth value, RTV+DSV render-pass binding, depth clear validation, native `DepthStencilStateDesc` PSO mapping, active color/depth format validation at bind, a readback-observable overlapping-triangle depth-test proof, descriptor set command binding across split root tables, vertex/index buffer command binding, direct and indexed draw recording, command-list close/submit, fence waits, backend-neutral fence wait/failure/last-submitted/last-completed stats, shared texture export/failure stats, and renderer-facing frame submission plus viewport display/readback through `RhiFrameRenderer` and `RhiViewportSurface`.

D3D12 texture aliasing barrier command support currently validates distinct committed texture handles and records `texture_aliasing_barriers` intent/stat counters without emitting a native placed-resource aliasing barrier. Native `D3D12_RESOURCE_BARRIER_TYPE_ALIASING` emission remains tied to a future placed/reserved resource allocation slice that can prove shared heap mapping.

`MK_rhi_vulkan` is an SDK-independent Vulkan backend scaffold. It keeps loader, instance, physical-device, logical-device, command-pool, buffer, texture, sampler, shader-module, descriptor, pipeline, swapchain, frame-sync, and readback owners behind private runtime types while exposing only first-party probes and RHI handles. Its `create_rhi_device` bridge is gated by `build_rhi_device_mapping_plan`, which now requires descriptor binding, visible clear/readback, visible draw/readback, visible texture-sampling/readback, and visible depth/readback proof before Vulkan is advertised as a complete visible `IRhiDevice` backend. Native sampled-image, sampler, and depth attachment details stay private; depth textures may be planned with both depth-stencil attachment and sampled image usage, sampled-depth descriptor updates are covered through the private RHI bridge when a Vulkan runtime is available, and `depth_write` to `shader_read` maps to a backend-private synchronization2 shader-read barrier. Runtime material texture sampling, depth-ordered rendering, and sampled-depth readback are proven only by environment-gated tests when local Vulkan runtime plus real SPIR-V artifacts are configured. Texture aliasing barrier command support records a conservative `vkCmdPipelineBarrier2` memory dependency through private synchronization2 mirrors without claiming Vulkan memory alias allocation.

RHI Vulkan Compute Dispatch Foundation v1 adds backend-private Vulkan compute pipeline ownership through `VulkanRuntimeComputePipeline`, requires compute-capable queue selection plus `vkCreateComputePipelines` and `vkCmdDispatch` command resolution, maps `VulkanRhiDevice::create_compute_pipeline`, records compute pipeline and descriptor set binding with the compute bind point, and records `vkCmdDispatch` through public `IRhiCommandList::dispatch`. `MK_VULKAN_TEST_COMPUTE_SPV` can point at a real SPIR-V compute shader to prove deterministic storage-buffer readback through public RHI buffers. Runtime RHI Compute Morph Vulkan Proof v1 is complete for `RuntimeMorphMeshComputeBinding` POSITION readback through `MK_VULKAN_TEST_COMPUTE_MORPH_SPV`; Runtime RHI Compute Morph Renderer Consumption Vulkan v1 is complete for drawing that compute-written output through `MK_VULKAN_TEST_COMPUTE_MORPH_RENDER_VERTEX_SPV` / `MK_VULKAN_TEST_COMPUTE_MORPH_RENDER_FRAGMENT_SPV`; Runtime RHI Compute Morph NORMAL/TANGENT Output Vulkan v1 is complete for optional tangent-frame output readback through `RuntimeMorphMeshComputeBindingOptions::output_normal_usage`, `output_tangent_usage`, and `MK_VULKAN_TEST_COMPUTE_MORPH_TANGENT_FRAME_SPV`; Generated 3D Compute Morph Package Smoke Vulkan v1 is complete for generated `DesktopRuntime3DPackage` POSITION compute morph package smoke through `SdlDesktopPresentationVulkanSceneRendererDesc`, `--require-compute-morph`, and explicit SPIR-V artifacts. Vulkan NORMAL/TANGENT package smoke, tangent-frame renderer/package consumption, async overlap/performance, Metal parity, and broad renderer quality are not claimed by this foundation.


Metal readiness is now more than identity probing but still SDK-safe on non-Apple hosts. `validate_shader_library_artifact` validates non-empty `metallib` inputs, `build_runtime_readiness_plan` gates Apple host support, runtime availability, default-device creation, command-queue creation, and shader-library readiness through first-party diagnostics, `build_texture_synchronization_plan` maps backend-neutral texture states into Metal render/blit/present encoder intent and usage diagnostics, and `diagnose_platform_availability` separates unsupported hosts from Apple hosts missing Objective-C++ SDK linkage. CMake gates Apple-only Objective-C++ native sources so `create_native_device_and_command_queue`, `create_native_command_buffer`, `create_native_texture_target`, `acquire_native_drawable`, `create_native_render_encoder`, `present_native_drawable`, `commit_and_wait_native_command_buffer`, and `read_native_texture_bytes` can own `MTLDevice`, `MTLCommandQueue`, `MTLCommandBuffer`, `MTLTexture`, `CAMetalDrawable`, `MTLRenderCommandEncoder`, drawable present scheduling, private command-buffer submission, and blit-backed texture readback through retained private objects on Apple hosts while non-Apple builds return deterministic diagnostics without compiling Metal SDK headers.

`record_runtime_dynamic_rendering_clear` and `record_runtime_dynamic_rendering_draw` now record native Vulkan dynamic-rendering clear/draw slices. Clear recording uses private `vkCmdBeginRendering`/`vkCmdEndRendering` with a clear load op and no shader pipeline, while draw recording adds private viewport/scissor setup, graphics pipeline binding, optional `vkCmdBindVertexBuffers`/`vkCmdBindIndexBuffer`, `vkCmdDraw` or `vkCmdDrawIndexed`, and end-rendering calls. The Vulkan path now accepts optional private depth attachments through the existing first-party depth contract: `depth24_stencil8` textures create depth-aspect image views, `ResourceState::depth_write` maps to a depth-stencil attachment layout/barrier, dynamic rendering receives `pDepthAttachment`, and public `DepthStencilStateDesc` is mapped into native pipeline depth state. Both accept only first-party runtime owners and keep `VkRenderingInfo`, `VkRenderingAttachmentInfo`, `VkCommandBuffer`, `VkPipeline`, `VkImageView`, buffers, and native function pointers backend-private.

`record_runtime_swapchain_frame_barrier`, `record_runtime_texture_barrier`, and `submit_runtime_command_buffer` now cover the first synchronization2 runtime slices. Swapchain and standalone texture barriers map first-party `VulkanFrameSynchronizationBarrier` or backend-neutral `ResourceState` intent into private `VkImageMemoryBarrier2`/`VkDependencyInfo` mirrors for `vkCmdPipelineBarrier2`, including transfer-write to shader-read visibility for runtime texture uploads and undefined-to-depth-write barriers for depth attachments. Command-buffer submission maps first-party frame-sync owners into private `VkSemaphoreSubmitInfo`, `VkCommandBufferSubmitInfo`, and `VkSubmitInfo2` mirrors for `vkQueueSubmit2`, and a requested queue-idle wait must succeed before the result is treated as submitted. Queue handles, semaphore/fence handles, command buffers, images, raw `VkResult` values, and native synchronization structs stay behind `MK_rhi_vulkan`.

Vulkan readback now has a native foundation through `read_runtime_buffer`, `create_runtime_readback_buffer`, `record_runtime_swapchain_image_readback`, and `read_runtime_readback_buffer`. The runtime maps first-party readback-domain buffers for `IRhiDevice::read_buffer`, creates a host-visible/coherent transfer-destination buffer for swapchain image copies, records a tightly packed swapchain image copy through private image-to-buffer copy structures, and maps CPU bytes through first-party result types; buffer, memory, memory properties, copy regions, and mapped pointers remain backend-private.

Vulkan descriptors now have a native foundation through `create_runtime_descriptor_set_layout`, `create_runtime_descriptor_set`, `update_runtime_descriptor_set`, `create_runtime_pipeline_layout`, and `record_runtime_descriptor_set_binding`. Descriptor binding descriptions and buffer/texture/sampler resources remain first-party RHI structs, while `VkDescriptorSetLayout`, `VkDescriptorPool`, `VkDescriptorSet`, `VkWriteDescriptorSet`, `VkDescriptorBufferInfo`, `VkDescriptorImageInfo`, `VkSampler`, `vkUpdateDescriptorSets`, and `vkCmdBindDescriptorSets` stay private to `MK_rhi_vulkan` PIMPL owners. The current update path supports uniform/storage buffer, sampled texture, and sampler descriptors through private runtime owners.

Vulkan visible-frame smoke coverage now includes a shader-free dynamic-rendering clear path through `record_runtime_dynamic_rendering_clear`. On Windows hosts with a usable Vulkan runtime, tests create a hidden Win32 surface-backed swapchain, acquire an image, transition it, clear it through dynamic rendering, copy it to a host-visible readback buffer, assert non-zero bytes, and present or accept resize-required presentation. `MK_backend_scaffold_tests` also includes gated backend-neutral visible draw/readback, raw texture-sampling, depth-ordered draw/readback, sampled-depth readback, postprocess-depth readback, directional shadow receiver readback, compute dispatch storage-buffer readback, `RhiFrameRenderer` runtime material texture-sampling, and cooked runtime scene GPU palette proofs: when `MK_VULKAN_TEST_VERTEX_SPV`/`MK_VULKAN_TEST_FRAGMENT_SPV`, `MK_VULKAN_TEST_TEXTURE_VERTEX_SPV`/`MK_VULKAN_TEST_TEXTURE_FRAGMENT_SPV`, `MK_VULKAN_TEST_DEPTH_VERTEX_SPV`/`MK_VULKAN_TEST_DEPTH_FRAGMENT_SPV`, `MK_VULKAN_TEST_DEPTH_SAMPLE_VERTEX_SPV`/`MK_VULKAN_TEST_DEPTH_SAMPLE_FRAGMENT_SPV`, `MK_VULKAN_TEST_POSTPROCESS_DEPTH_VERTEX_SPV`/`MK_VULKAN_TEST_POSTPROCESS_DEPTH_FRAGMENT_SPV`, `MK_VULKAN_TEST_SHADOW_RECEIVER_VERTEX_SPV`/`MK_VULKAN_TEST_SHADOW_RECEIVER_FRAGMENT_SPV`, `MK_VULKAN_TEST_COMPUTE_SPV`, `MK_VULKAN_TEST_RUNTIME_MATERIAL_VERTEX_SPV`/`MK_VULKAN_TEST_RUNTIME_MATERIAL_FRAGMENT_SPV`, or `MK_VULKAN_TEST_RUNTIME_SCENE_VERTEX_SPV`/`MK_VULKAN_TEST_RUNTIME_SCENE_FRAGMENT_SPV` point at real SPIR-V shaders, the Vulkan `IRhiDevice` bridge clears a texture, records a draw, samples a texture through a sampler descriptor, writes then depth-tests geometry, transitions that depth texture to shader-read, samples the depth texture through private sampled-image descriptors, samples scene depth in a second postprocess pass, darkens a receiver pass from sampled shadow depth, dispatches a compute shader into a storage buffer and reads it back, submits a runtime material descriptor binding through the renderer, or builds a cooked scene GPU palette and creates the graphics pipeline from the exact palette material layout before readback assertions. The tests are skipped when those artifacts are not configured so default validation remains usable on hosts without Vulkan SPIR-V tooling.

Vulkan now exposes a `create_rhi_device` bridge for a valid `VulkanRuntimeDevice` plus a supported `VulkanRhiDeviceMappingPlan`. This bridge creates backend-neutral `BufferHandle`, `TextureHandle`, `SamplerHandle`, `ShaderHandle`, `DescriptorSetLayoutHandle`, `DescriptorSetHandle`, `PipelineLayoutHandle`, `GraphicsPipelineHandle`, and surface-backed `SwapchainHandle` resources plus transient leases, explicit swapchain frame acquire/release, swapchain resize, descriptor set updates for buffer, sampled-texture, and sampler bindings, standalone texture transitions, buffer copies, buffer-to-texture copies, texture-to-buffer copies, standalone texture render-target clears and draws, graphics pipeline binding with vertex input metadata, descriptor set binding that must match the currently bound pipeline layout, vertex/index buffer binding, indexed draw recording, graphics command-list recording, shader-free swapchain clear render passes, queued present during submit, copy-only submit, immediate fence-value stats/waits, and CPU readback for readback-domain buffers. The bridge backs those handles with private runtime owners. Environment-gated tests provide backend-neutral visible draw/readback, raw texture-sampling, depth-ordered draw/readback, sampled-depth readback, postprocess-depth readback, directional shadow receiver readback, renderer/runtime material texture-sampling proof, and cooked scene GPU palette proof when real SPIR-V artifacts and a Vulkan runtime are configured; default availability remains guarded by `build_rhi_device_mapping_plan`, shader artifact validation, DXC SPIR-V CodeGen, and `spirv-val` readiness before Vulkan is advertised as a complete visible backend for generated games.

Frame Graph package executor evidence is package-visible but backend-neutral: selected desktop package smokes now expose `framegraph_passes_executed` and `framegraph_barrier_steps_executed`, with exact two-frame budgets of `4/9` for depth-aware postprocess and `6/15` for directional-shadow paths. `evaluate_sdl_desktop_presentation_quality_gate` mirrors that as `renderer_quality_expected_framegraph_barrier_steps` and `renderer_quality_framegraph_barrier_steps_ok` without exposing D3D12/Vulkan barrier structs, layouts, queues, or native handles.

Frame Graph Transient Texture Lease Binding v1 binds the conservative alias plan to the existing backend-neutral transient lease surface: `acquire_frame_graph_transient_texture_lease_bindings` acquires one `IRhiDevice::acquire_transient_texture` lease per alias group, emits `FrameGraphTextureBinding` rows for every group resource with shared texture handles and `ResourceState::undefined`, accepts empty plans without allocation, rejects malformed alias groups before acquisition, and releases already acquired leases on later acquisition failure. Frame Graph Shared Texture State Handoff v1 makes the RHI texture executor shared-handle aware for those existing `TextureHandle` rows: successful barrier, pass-target, and final-state transitions update every row sharing the handle, while conflicting initial shared-handle states are rejected before recording. Frame Graph Texture Aliasing Barrier Command v1 adds explicit `IRhiCommandList::texture_aliasing_barrier` recording plus Frame Graph resource-name helper validation; NullRHI counts it without changing tracked texture states, D3D12 records a conservative null-resource `D3D12_RESOURCE_BARRIER_TYPE_ALIASING` after validating two public texture handles because current backend textures are committed resources, and Vulkan records a conservative synchronization2 memory barrier through private runtime owners. This is not native transient heap allocation, D3D12 placed-resource aliasing, Vulkan/Metal memory alias execution, automatic executor insertion, wildcard aliasing barriers, multi-queue graph scheduling, package streaming, or production render graph ownership.

## Backend Model

The RHI vocabulary follows modern explicit graphics APIs:

- command lists are recorded before submission
- work is submitted to command queues
- resource state transitions are explicit
- graphics pipeline descriptions own explicit vertex input layouts/attributes before backend PSO creation
- buffer copies and buffer/texture transfers are explicit commands recorded outside render passes
- buffer/texture transfers use `BufferTextureCopyRegion`; `buffer_row_length` and `buffer_image_height` follow the common zero-means-tight convention used by modern copy APIs
- `buffer_texture_copy_required_bytes` computes the required host/staging buffer footprint and rejects invalid row or image pitches before backend code is written
- CPU readback is explicit through `IRhiDevice::read_buffer` and requires a buffer with `copy_destination` usage
- render pass clear values are explicit through `ClearColorValue` on `RenderPassColorAttachment`
- transient buffers and textures are acquired through explicit leases and become invalid after `release_transient`
- descriptor set layouts describe shader-visible resource tables before pipeline layout creation
- descriptor sets are allocated from a descriptor set layout, updated with typed buffer/texture resources, and bound against a compatible pipeline layout
- swapchain resize is explicit through `IRhiDevice::resize_swapchain` and is rejected while an acquired or rendered frame is pending
- swapchain frame ownership is explicit through `IRhiDevice::acquire_swapchain_frame` and `IRhiDevice::release_swapchain_frame`; render passes and presents use `SwapchainFrameHandle`, not the persistent `SwapchainHandle`
- swapchain presentation is an explicit graphics command through `SwapchainFrameHandle`, not an implicit side effect of ending a render pass; a frame must complete a render pass before `present`
- backend-neutral swapchains currently accept one acquired frame at a time; a second acquire is rejected until the prior frame is manually released or presented and submitted
- render passes bracket graphics pipeline binding and draw submission
- fence values represent submitted GPU work
- `RhiStats` exposes fence wait counts, wait failures, last submitted fence values, and last completed fence values so fence lifecycle coverage can be asserted across `NullRhiDevice` and native backends

This maps directly onto Direct3D 12 command queues/lists/fences plus root signature descriptor tables, resource barriers, future placed/aliased resources, and copy commands; Vulkan command buffers/queues/synchronization plus descriptor set layouts, buffer/image copy regions, and transient attachments; and Metal command buffers/queues plus argument tables, argument buffers, heaps, and blit passes.

## Current Public Concepts

- `BackendKind`: `null`, `d3d12`, `vulkan`, `metal`
- `RhiResourceLifetimeRegistry`, `RhiResourceRegistrationResult`, `RhiResourceHandle`, `RhiResourceKind`, `RhiResourceLifetimeRecord`, `RhiResourceLifetimeDiagnostic`, and `RhiResourceLifetimeEvent` from Renderer/RHI Resource Foundation v1
- `FrameGraphV1Desc`, `FrameGraphResourceAccess`, `FrameGraphBarrier`, `compile_frame_graph_v1`, `FrameGraphTransientTextureDesc`, `FrameGraphTransientTextureAliasPlan`, `FrameGraphTransientTextureLeaseBindingResult`, `FrameGraphTextureAliasingBarrier`, `FrameGraphTextureAliasingBarrierRecordResult`, `plan_frame_graph_transient_texture_aliases`, `acquire_frame_graph_transient_texture_lease_bindings`, `release_frame_graph_transient_texture_lease_bindings`, `record_frame_graph_texture_aliasing_barriers`, `FrameGraphRhiTextureExecutionDesc`, `FrameGraphRhiTextureExecutionResult`, `execute_frame_graph_rhi_texture_schedule`, `RhiUploadStagingPlan`, `RhiUploadAllocationHandle`, `RhiUploadCopyRecord`, `RhiUploadDiagnostic`, and `RhiUploadDiagnosticCode::stale_generation` from Frame Graph and Upload/Staging Foundation v1
- `BackendCapabilityProfile`, `BackendProbePlan`, `BackendProbeResult`, `RhiHostPlatform`, `ShaderArtifactFormat`, and backend capability/probe helpers from `mirakana/rhi/backend_capabilities.hpp`
- `QueueKind`: `graphics`, `compute`, `copy`
- `BufferDesc`, `TextureDesc`, `SurfaceHandle`, and `SwapchainDesc`
- `VertexFormat`, `VertexInputRate`, `VertexSemantic`, `VertexBufferLayoutDesc`, `VertexAttributeDesc`, `VertexBufferBinding`, and `IndexBufferBinding`
- `IRhiDevice::create_swapchain`, `IRhiDevice::resize_swapchain`, `IRhiDevice::acquire_swapchain_frame`, and `IRhiDevice::release_swapchain_frame`
- `BufferCopyRegion`, `BufferTextureCopyRegion`, `bytes_per_texel`, and `buffer_texture_copy_required_bytes`
- `IRhiDevice::read_buffer`
- `TransientBuffer`, `TransientTexture`, and `TransientResourceHandle`
- `ShaderDesc`, `PipelineLayoutDesc`, and `GraphicsPipelineDesc`
- `DescriptorSetLayoutDesc`, `DescriptorBindingDesc`, `DescriptorWrite`, and descriptor set/resource handles
- `ClearColorValue` and `RenderPassDesc`
- `ResourceState`
- `IRhiCommandList`
- `IRhiDevice`
- `NullRhiDevice`
- Windows-only `MK_rhi_d3d12` scaffold
- SDK-independent `MK_rhi_vulkan` scaffold
- `VulkanCommandScope`, `VulkanCommandRequest`, `VulkanCommandAvailability`, `VulkanCommandResolutionPlan`, `vulkan_backend_command_requests`, and `build_command_resolution_plan`
- `VulkanRuntimeGlobalCommandProbeResult` and `probe_runtime_global_commands`
- `VulkanRuntimeInstanceCapabilityProbeResult`, `VulkanRuntimeInstanceCommandProbeResult`, `VulkanRuntimeInstance`, `VulkanRuntimeInstanceCreateResult`, `encode_vulkan_api_version`, `decode_vulkan_api_version`, `probe_runtime_instance_capabilities`, `probe_runtime_instance_commands`, and `create_runtime_instance`
- `VulkanRuntimePhysicalDeviceCountProbeResult` and `probe_runtime_physical_device_count`
- `VulkanRuntimePhysicalDeviceSnapshot`, `VulkanRuntimePhysicalDeviceSnapshotProbeResult`, `VulkanRuntimePhysicalDeviceSelectionProbeResult`, `probe_runtime_physical_device_snapshots`, `make_physical_device_candidate`, and `probe_runtime_physical_device_selection`
- `VulkanRuntimeSurfaceSupportProbeResult`, `vulkan_surface_instance_extensions`, and `probe_runtime_surface_support`
- `VulkanInstanceCreateDesc`, `VulkanInstanceCreatePlan`, and `build_instance_create_plan`
- `VulkanPhysicalDeviceCandidate`, `VulkanQueueFamilyCandidate`, `VulkanDeviceSelection`, and `select_physical_device`
- `VulkanLogicalDeviceCreateDesc`, `VulkanDeviceQueueCreatePlan`, `VulkanLogicalDeviceCreatePlan`, and `build_logical_device_create_plan`
- `VulkanRuntimeDevice`, `VulkanRuntimeDeviceCreateResult`, `vulkan_device_command_requests`, and `create_runtime_device`
- `VulkanRuntimeCommandPoolDesc`, `VulkanRuntimeCommandPool`, `VulkanRuntimeCommandPoolCreateResult`, and `create_runtime_command_pool`
- `VulkanRuntimeShaderModuleDesc`, `VulkanRuntimeShaderModule`, `VulkanRuntimeShaderModuleCreateResult`, and `create_runtime_shader_module`
- `VulkanRuntimeDescriptorSetLayoutDesc`, `VulkanRuntimeDescriptorSetLayout`, `VulkanRuntimeDescriptorSetLayoutCreateResult`, and `create_runtime_descriptor_set_layout`
- `VulkanRuntimeDescriptorSetDesc`, `VulkanRuntimeDescriptorSet`, `VulkanRuntimeDescriptorSetCreateResult`, `VulkanRuntimeDescriptorSetBindDesc`, `VulkanRuntimeDescriptorSetBindResult`, `create_runtime_descriptor_set`, and `record_runtime_descriptor_set_binding`
- `VulkanRuntimePipelineLayoutDesc`, `VulkanRuntimePipelineLayout`, `VulkanRuntimePipelineLayoutCreateResult`, and `create_runtime_pipeline_layout`
- `VulkanRuntimeGraphicsPipelineDesc`, `VulkanRuntimeGraphicsPipeline`, `VulkanRuntimeGraphicsPipelineCreateResult`, and `create_runtime_graphics_pipeline`
- `VulkanRuntimeSwapchainDesc`, `VulkanRuntimeSwapchain`, `VulkanRuntimeSwapchainCreateResult`, and `create_runtime_swapchain`
- `VulkanRuntimeFrameSyncDesc`, `VulkanRuntimeFrameSync`, `VulkanRuntimeFrameSyncCreateResult`, `VulkanRuntimeSwapchainAcquireDesc`, `VulkanRuntimeSwapchainAcquireResult`, `VulkanRuntimeSwapchainPresentDesc`, `VulkanRuntimeSwapchainPresentResult`, `create_runtime_frame_sync`, `acquire_next_runtime_swapchain_image`, and `present_runtime_swapchain_image`
- `VulkanRuntimeDynamicRenderingClearDesc`, `VulkanRuntimeDynamicRenderingClearResult`, `record_runtime_dynamic_rendering_clear`, `VulkanRuntimeDynamicRenderingDrawDesc`, `VulkanRuntimeDynamicRenderingDrawResult`, and `record_runtime_dynamic_rendering_draw`
- `VulkanRuntimeSwapchainFrameBarrierDesc`, `VulkanRuntimeSwapchainFrameBarrierResult`, `VulkanRuntimeCommandBufferSubmitDesc`, `VulkanRuntimeCommandBufferSubmitResult`, `record_runtime_swapchain_frame_barrier`, and `submit_runtime_command_buffer`
- `VulkanRuntimeBufferReadDesc`, `VulkanRuntimeBufferReadResult`, `read_runtime_buffer`, `VulkanRuntimeReadbackBufferDesc`, `VulkanRuntimeReadbackBuffer`, `VulkanRuntimeReadbackBufferCreateResult`, `VulkanRuntimeSwapchainReadbackDesc`, `VulkanRuntimeSwapchainReadbackResult`, `VulkanRuntimeReadbackBufferReadDesc`, `VulkanRuntimeReadbackBufferReadResult`, `create_runtime_readback_buffer`, `record_runtime_swapchain_image_readback`, and `read_runtime_readback_buffer`
- `VulkanSurfaceCapabilities`, `VulkanSurfaceFormatCandidate`, `VulkanPresentMode`, `VulkanSwapchainSupport`, `VulkanSwapchainCreateDesc`, `VulkanSwapchainCreatePlan`, `VulkanSwapchainResizePlan`, `build_swapchain_create_plan`, and `build_swapchain_resize_plan`
- `VulkanRhiDeviceMappingDesc`, `VulkanRhiDeviceMappingPlan`, and `build_rhi_device_mapping_plan`, including visible texture-sampling/readback and visible depth/readback readiness before `create_rhi_device` promotion
- `VulkanSpirvShaderArtifactDesc`, `VulkanSpirvShaderArtifactValidation`, and `validate_spirv_shader_artifact`
- SDK-independent `MK_rhi_metal` scaffold
- Renderer-facing `RhiViewportSurface` owns viewport color textures and `RhiViewportReadbackFrame` copies through `IRhiDevice`

## Backend Assumptions

- Game code should not depend on API-specific handles.
- Native handles are backend implementation details unless an explicit interop design is accepted later.
- Direct3D 12 is the Windows-specific backend path.
- Backend capability/probe state is API-independent and lives in `mirakana/rhi/backend_capabilities.hpp`; native backends must update this contract before editor availability or AI agent manifests claim support.
- `ShaderDesc::bytecode` is optional for GPU-free validation backends such as `NullRhiDevice`; real shader-owning backends require a non-null bytecode pointer and non-zero `bytecode_size`.
- Shader compile command planning is backend-aware but execution is not implicit. Commands are represented as executable-plus-argument vectors, not shell strings. The current runner validation allowlists compiler executables `dxc`, `metal`, and `metallib`, rejects unsafe output paths, records dry-run invocations, captures compiler stdout/stderr in `ShaderToolRunResult`, and can convert shader includes into asset dependency graph edges. Platform process execution is available through `mirakana::ProcessCommand` and `mirakana::Win32ProcessRunner`; `mirakana::ShaderToolProcessRunner` maps shader commands to that runner, supports reviewed executable overrides for PATH names, project-relative paths, or absolute installed SDK/tool paths, and enforces artifact output-root policy before invocation. `mirakana::discover_shader_tools` finds deterministic `dxc`, `spirv-val`, `metal`, and `metallib` descriptors with an explicit `dxc.spirv-codegen` capability marker for deterministic toolchain roots, and `mirakana::evaluate_shader_toolchain_readiness` reports D3D12 DXIL, Vulkan SPIR-V, DXC SPIR-V CodeGen, and Metal library readiness before backend promotion. `tools/check-shader-toolchain.ps1` additionally reads Process/User/Machine environment state, accepts `MK_SHADER_TOOLCHAIN_ROOT`, `VULKAN_SDK`/`VK_SDK_PATH`, vcpkg tool roots, and PATH tools before falling back to Windows SDK DXC, then runs a real `dxc -spirv -fspv-target-env=vulkan1.3` probe so Windows SDK DXC builds that expose `-spirv` but lack compiled SPIR-V CodeGen are reported as Vulkan blockers. `mirakana::make_spirv_shader_validation_command`, `mirakana::ShaderArtifactValidationProcessRunner`, and `mirakana::execute_shader_artifact_validation_action` provide a reviewed `spirv-val` path for compiled SPIR-V artifacts. `mirakana::build_shader_artifact_provenance`, `mirakana::is_shader_artifact_cache_valid`, `mirakana::ShaderArtifactCacheIndex`, and `mirakana::execute_shader_compile_action` record compiler/tool/input fingerprints, skip valid cached artifacts, write `.geprovenance` files, create deterministic artifact markers for tests, and require a real artifact when marker writes are disabled for compiler-backed runs. `mirakana::build_shader_pipeline_cache_plan` batches selected compile requests into deterministic hot-reload, compile-required, pipeline-recreation, cache-entry-current, and cache-index-repair rows; `mirakana::reconcile_shader_pipeline_cache_index` repairs selected first-party cache index files from current provenance without invoking compilers or writing artifacts. Editor-native viewport readiness is additionally gated by `mirakana::editor::ViewportShaderArtifactState`, which checks non-empty D3D12 vertex/fragment DXIL and Vulkan vertex/fragment SPIR-V artifacts before native backend promotion.
- The D3D12 backend starts with compile-time SDK integration, runtime probing, bootstrap validation, committed resource ownership probes, and persistent `DeviceContext` ownership for command queues, command allocators, command lists, resources, fences, CPU-side fence waits, GPU-side cross-queue waits, HWND swapchains, RTV heaps/views, shader-visible descriptor heaps, descriptor-table root signatures, native CBV/SRV/UAV descriptor view writes, shader bytecode, graphics PSOs, back-buffer transitions, standalone texture transitions, requested-color swapchain and texture render-target clears, buffer and aligned buffer/texture copies, readback buffer mapping, and first triangle draw recording. Its initial `IRhiDevice` bridge owns backend-neutral buffers, textures, descriptor set layout/allocation/update validation with native descriptor writes, shader modules, pipeline layouts, graphics pipelines, buffer copy commands, aligned buffer/texture copy commands, CPU buffer readback, standalone texture transition commands with tracked before/after state validation and stale command-list snapshot rejection, committed-resource-backed transient leases, surface-backed swapchains, backend-neutral swapchain resize, explicit swapchain frame acquire/release, graphics-queue-only queued presentation, swapchain render-pass-complete-before-present sequencing, single-pending-frame throttling, swapchain and texture render pass/draw recording, descriptor set command binding, command lists, submission fences, waits, backend-neutral fence lifecycle stats, renderer-facing frame submission, and renderer-owned viewport surface readback. Tests now assert non-black D3D12 render-target clear bytes, first-triangle draw bytes, texture state rejection, stale cross-queue texture-state submission rejection, descriptor/root-signature/PSO validation failures, swapchain present sequencing, swapchain resize, explicit acquire/release accounting, duplicate pending-frame rejection, and fence wait/failure/submitted/completed stats through CPU readback, stats assertions, or HWND-backed runtime paths. The Windows editor display path can use a D3D12 shared texture through an editor-private SDL bridge, with CPU readback retained as the deterministic fallback.
- D3D12 native COM objects remain hidden behind PIMPL; public game/engine code only sees first-party handles and backend-neutral contracts until an explicit interop design is accepted.
- `tools/check-public-api-boundaries.ps1` is part of `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/validate.ps1` and rejects native D3D12/DXGI/Win32 symbols, COM pointer types, and native graphics headers in public include trees.
- D3D12 command allocators are reset only when their command list is closed and no submitted fence is still pending.
- D3D12 HWND swapchains use `CreateSwapChainForHwnd` with a direct command queue, flip discard, one sample, two to sixteen buffers, and `rgba8_unorm` or `bgra8_unorm` back buffers.
- D3D12 swapchain back buffers create first-party-owned RTV descriptors. Back-buffer resource transitions and clears are recorded on graphics command lists before presentation.
- D3D12 standalone texture transitions use `ResourceBarrier` on graphics command lists and reject undefined or no-op transitions before native recording.
- D3D12 render-target textures create first-party-owned RTV descriptors when the texture is created. Texture render passes set viewport/scissor from the texture extent and do not expose native descriptor handles through the public API.
- D3D12 shader-visible descriptor heaps are currently first-party-owned CBV/SRV/UAV and sampler heaps. Backend-neutral descriptor sets allocate ranges from separate CBV/SRV/UAV and sampler descriptor arenas, and command-list descriptor binding maps them to split root descriptor tables that match the graphics pipeline layout.
- D3D12 buffer/texture copies use `CopyTextureRegion` with a first-party generated `D3D12_PLACED_SUBRESOURCE_FOOTPRINT`. The public copy region remains backend-neutral, while the D3D12 bridge currently requires the buffer offset to be 512-byte aligned and the row pitch to be 256-byte aligned.
- D3D12 root signatures are generated from first-party descriptor set layout descriptions. Each descriptor set becomes one descriptor table root parameter, descriptor ranges use the set index as register space, and optional push constants become 32-bit root constants in a reserved register space.
- D3D12 graphics PSOs are generated from first-party root signature, shader bytecode, color/depth formats, primitive topology, `DepthStencilStateDesc`, and optional vertex input layout/attribute descriptions. The current path supports vertex/pixel shader bytecode owned by `DeviceContext`, one color target, optional private `depth24_stencil8` DSV-backed depth attachment, shader-readable sampled depth resources through backend-private typeless DSV/SRV views, RTV+DSV render-pass binding, depth clears while the texture is in `depth_write`, and triangle or line topology. Postprocess-depth reads, sampled/visible shadow-map rendering, and Metal native depth recording remain separate slices; command-list pipeline binding validates the pipeline color/depth formats against the active render pass.
- D3D12 first draw recording binds a PSO, sets primitive topology, sets the current swapchain back buffer as render target with viewport/scissor, records `DrawInstanced`, waits for the fence, and presents.
- Vulkan is the preferred cross-platform backend path for Windows/Linux/Android.
- Vulkan backend availability must validate host support, runtime loading through `mirakana::rhi::vulkan::probe_runtime_loader`, loader/global command readiness through `mirakana::rhi::vulkan::probe_runtime_global_commands`, runtime instance API/extension capability probing through `mirakana::rhi::vulkan::probe_runtime_instance_capabilities`, transient native instance creation/destruction and instance command readiness through `mirakana::rhi::vulkan::probe_runtime_instance_commands`, persistent private instance ownership through `mirakana::rhi::vulkan::create_runtime_instance`, count-only physical device enumeration through `mirakana::rhi::vulkan::probe_runtime_physical_device_count`, physical device properties, queue-family, device-extension, dynamic rendering feature support, and synchronization2 feature support through `mirakana::rhi::vulkan::probe_runtime_physical_device_snapshots`, snapshot-to-candidate selection through `mirakana::rhi::vulkan::probe_runtime_physical_device_selection`, surface extension planning through `mirakana::rhi::vulkan::vulkan_surface_instance_extensions`, surface-specific present queue probing through `mirakana::rhi::vulkan::probe_runtime_surface_support`, remaining device command readiness through `mirakana::rhi::vulkan::build_command_resolution_plan`, instance API/extension readiness through `mirakana::rhi::vulkan::build_instance_create_plan`, physical device enumeration, Vulkan 1.3 API support, `VK_KHR_swapchain`, dynamic rendering support, synchronization2 support, queue family graphics/present capabilities through `mirakana::rhi::vulkan::make_physical_device_candidate` and `mirakana::rhi::vulkan::select_physical_device`, logical device queue/extension/feature readiness through `mirakana::rhi::vulkan::build_logical_device_create_plan`, native logical device ownership and device command loading through `mirakana::rhi::vulkan::create_runtime_device` plus `mirakana::rhi::vulkan::vulkan_device_command_requests`, command pool and primary command buffer ownership through `mirakana::rhi::vulkan::create_runtime_command_pool`, first-party buffer usage/memory-domain planning and native buffer/memory ownership through `mirakana::rhi::vulkan::build_runtime_buffer_create_plan`, `mirakana::rhi::vulkan::create_runtime_buffer`, and `mirakana::rhi::vulkan::read_runtime_buffer`, first-party texture usage planning and native image/memory/image-view ownership through `mirakana::rhi::vulkan::build_runtime_texture_create_plan` and `mirakana::rhi::vulkan::create_runtime_texture` including private depth-aspect image views for sampled `depth24_stencil8` descriptors and depth|stencil image-barrier aspect masks for D24S8 layout transitions, initial backend-neutral buffer/texture/transient/shader/descriptor/pipeline/swapchain creation plus sampled-texture/sampler descriptor updates and swapchain clear/present command submission through `mirakana::rhi::vulkan::create_rhi_device`, shader module ownership through `mirakana::rhi::vulkan::create_runtime_shader_module`, pipeline layout and graphics pipeline ownership through `mirakana::rhi::vulkan::create_runtime_pipeline_layout` and `mirakana::rhi::vulkan::create_runtime_graphics_pipeline` including public `DepthStencilStateDesc` depth-test/write/compare mapping, swapchain/image-view/acquire/resize planning through `mirakana::rhi::vulkan::build_swapchain_create_plan` and `mirakana::rhi::vulkan::build_swapchain_resize_plan`, native surface/swapchain/image-view ownership through `mirakana::rhi::vulkan::create_runtime_swapchain`, native semaphore/fence frame sync ownership through `mirakana::rhi::vulkan::create_runtime_frame_sync`, first-party swapchain image acquire through `mirakana::rhi::vulkan::acquire_next_runtime_swapchain_image`, first-party queue present through `mirakana::rhi::vulkan::present_runtime_swapchain_image`, dynamic rendering command/attachment planning through `mirakana::rhi::vulkan::build_dynamic_rendering_plan`, native dynamic rendering color/depth clear/draw recording through `mirakana::rhi::vulkan::record_runtime_dynamic_rendering_clear`, `mirakana::rhi::vulkan::record_runtime_dynamic_rendering_draw`, `mirakana::rhi::vulkan::record_runtime_texture_rendering_clear`, and `mirakana::rhi::vulkan::record_runtime_texture_rendering_draw`, standalone texture `depth_write` synchronization2 barriers through `mirakana::rhi::vulkan::build_texture_transition_barrier` and `mirakana::rhi::vulkan::record_runtime_texture_barrier` with usage/format/state compatibility validation, frame synchronization2 ordering through `mirakana::rhi::vulkan::build_frame_synchronization_plan`, readback buffer ownership/copy/mapping through `mirakana::rhi::vulkan::create_runtime_readback_buffer`, `mirakana::rhi::vulkan::record_runtime_swapchain_image_readback`, and `mirakana::rhi::vulkan::read_runtime_readback_buffer`, Vulkan RHI mapping readiness including descriptor binding plus visible clear/readback, visible draw/readback, visible texture-sampling/readback proof, and visible depth/readback proof through `mirakana::rhi::vulkan::build_rhi_device_mapping_plan`, plus separate environment-gated sampled-depth readback, directional shadow receiver readback, renderer/runtime material sampling, and depth-ordered draw/readback proofs when real SPIR-V artifacts are configured, and `mirakana::rhi::vulkan::validate_spirv_shader_artifact` before native shader module creation.
- Vulkan descriptor work must go through first-party layout/allocation/update/bind wrappers: `create_runtime_descriptor_set_layout`, `create_runtime_descriptor_set`, `update_runtime_descriptor_set`, descriptor-aware `create_runtime_pipeline_layout`, and `record_runtime_descriptor_set_binding`; native descriptor layouts, pools, sets, write descriptors, buffer infos, update commands, and bind commands stay private.
- Vulkan HLSL shader commands must be generated as executable-plus-argument vectors through DXC with `-spirv` and `-fspv-target-env=vulkan1.3`; do not use shell strings or omit the target environment when producing engine-owned SPIR-V artifacts.
- Vulkan native depth proof is implemented for the backend-neutral public depth contract without exposing Vulkan handles. `Format::depth24_stencil8` textures are accepted for `TextureUsage::depth_stencil` and `TextureUsage::depth_stencil | TextureUsage::shader_resource` in runtime create planning, create private depth image views for sampled descriptors, use depth|stencil aspect masks for D24S8 image barriers unless a later separate-depth/stencil-layout feature slice explicitly enables more granular layouts, update sampled-texture descriptors for sampled-depth textures through private descriptor writes when a Vulkan runtime is available, transition through `ResourceState::depth_write` into a depth-stencil attachment layout using enabled synchronization2 depth test stage/access masks, map `depth_write` to `shader_read` as a shader-read barrier for sampled-depth follow-up passes, bind as dynamic-rendering depth attachments, validate depth clear range/extent/format/state, and map `DepthStencilStateDesc` into native pipeline depth-stencil state. Vulkan command-list pipeline binding validates the pipeline color/depth formats against the active render pass before marking a graphics pipeline bound. Env-gated readback coverage writes a sampled depth texture, transitions it to shader-read, samples it through private descriptor writes in a second pass, and validates the center pixel through CPU readback when `MK_VULKAN_TEST_DEPTH_VERTEX_SPV`, `MK_VULKAN_TEST_DEPTH_FRAGMENT_SPV`, `MK_VULKAN_TEST_DEPTH_SAMPLE_VERTEX_SPV`, and `MK_VULKAN_TEST_DEPTH_SAMPLE_FRAGMENT_SPV` are configured; Postprocess Depth Input Readback Foundation v0 reuses those depth-write artifacts plus `MK_VULKAN_TEST_POSTPROCESS_DEPTH_VERTEX_SPV` and `MK_VULKAN_TEST_POSTPROCESS_DEPTH_FRAGMENT_SPV` to prove scene-depth sampling in a postprocess pass; the package-visible depth-aware postprocess sample smoke proves the selected SDL3 D3D12/Vulkan package can require `postprocess_depth_input_ready=1`; directional shadow receiver coverage reuses those depth-write artifacts plus `MK_VULKAN_TEST_SHADOW_RECEIVER_VERTEX_SPV` and `MK_VULKAN_TEST_SHADOW_RECEIVER_FRAGMENT_SPV` to prove sampled shadow depth darkens a receiver pass, and the shared shader now uses fixed 3x3 sampled-depth PCF. SSAO, depth of field, fog, temporal history, stencil features, hardware comparison shadow samplers, and Metal native depth recording remain separate slices.
- Shadow Map Foundation v0 is a renderer planning contract, not a general native shadow renderer. `mirakana::build_shadow_map_plan` validates one directional shadow-casting light, non-zero extent, `Format::depth24_stencil8`, `TextureUsage::depth_stencil | TextureUsage::shader_resource`, caster/receiver counts, deterministic diagnostics, and stable shadow-depth-before-receiver pass declaration through Frame Graph v0. `mirakana::build_directional_shadow_light_space_plan` adds Stable Directional Light-Space Policy v0 as backend-neutral `DirectionalShadowLightSpaceDesc` / `DirectionalShadowLightSpacePlan` value data with deterministic texel-snapped orthographic light-space matrices and validation for light direction, focus center, focus radius, and depth radius. `mirakana::build_shadow_receiver_plan` adds deterministic receiver-side sampled-depth and sampler descriptor layout policy, depth-bias/intensity/filter validation, fixed PCF mode metadata, and receiver counts without native handles; D3D12 and env-gated Vulkan RHI readback tests prove that sampled shadow depth can darken a receiver pass, and D3D12 readback proves a fixed 3x3 PCF boundary pixel distinct from fully lit and fully shadowed pixels. `MK_scene_renderer` can adapt a `SceneRenderPacket` into the shadow map plan by choosing the first directional shadow-casting light and counting visible meshes as v0 casters/receivers, and can adapt `SceneRenderPacket` plus `ShadowMapPlan` into the stable light-space policy by deriving mesh-center bounds and the selected directional light direction. This does not add hardware comparison filtering, cascades, atlases, Metal presentation, editor authoring, or gameplay-visible RHI/native handles.
- Package-Visible Directional Shadow Filtering v0 now gives `sample_desktop_runtime_game` a selected D3D12/Vulkan package proof for that fixed one-light path. The host creates a private shadow depth target, receiver descriptor set, shadow pipeline, scene receiver pass, and postprocess pass through `RhiDirectionalShadowSmokeFrameRenderer`; its declared shadow-depth, scene receiver, and postprocess Frame Graph schedule now runs inter-pass texture transitions, pass callbacks, and reusable depth final-state restoration through `execute_frame_graph_rhi_texture_schedule`. Installed validation can require `--require-directional-shadow`, `--require-directional-shadow-filtering`, `--require-renderer-quality-gates`, `directional_shadow_status=ready`, `directional_shadow_ready=1`, `directional_shadow_filter_mode=fixed_pcf_3x3`, `directional_shadow_filter_taps=9`, `directional_shadow_filter_radius_texels=1`, `renderer_quality_status=ready`, `renderer_quality_framegraph_execution_budget_ok=1`, and `framegraph_passes=3`. This is still not hardware comparison samplers, cascades, atlases, Metal shadow presentation, editor authoring, production shadow rendering, native transient heap allocation, native alias execution, multi-queue scheduling, or general production renderer quality.
- Treat Vulkan SPIR-V toolchain readiness as incomplete until DXC is available, DXC SPIR-V CodeGen is proven available, and `spirv-val` is available; use `MK_SHADER_TOOLCHAIN_ROOT` for user-local DXC/SPIR-V tool installs, `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-shader-toolchain.ps1` for local diagnostics, and `tools/check-shader-toolchain.ps1 -RequireAll` when a CI lane must fail on missing tools.
- Metal is the Apple platform backend path.
- Metal backend availability must validate Apple host support, Metal runtime loading, `MTLCreateSystemDefaultDevice`/default device creation, command queue creation, and non-empty Metal library artifacts before it is called available.
- Metal texture synchronization planning must keep RHI `ResourceState` transitions in first-party terms; render-target/depth writes map to render encoder work, copy source/destination maps to blit work, shader reads require proper texture usage, and present requires a drawable texture scheduled through `present_native_drawable`.
- Descriptor bindings are typed and stage-visible so D3D12 root signatures can stay small, Vulkan set layouts can preserve compatibility, and Metal can choose per-stage argument table binding or argument buffers.
- Transient leases are exposed through first-party handles. The current D3D12 `IRhiDevice` bridge backs them with committed resources and invalidates the public resource handle on release; later allocator work can replace this with placed-resource heap aliasing without changing the public lease contract.
- Synchronization must be explicit and testable at the RHI boundary before a real GPU backend is added.

## Official References

- Direct3D 12 work submission: https://learn.microsoft.com/en-us/windows/win32/direct3d12/command-queues-and-command-lists
- Direct3D 12 command list recording: https://learn.microsoft.com/en-us/windows/win32/direct3d12/recording-command-lists-and-bundles
- Direct3D 12 synchronization: https://learn.microsoft.com/en-us/windows/win32/direct3d12/executing-and-synchronizing-command-lists
- Direct3D 12 command queue waits: https://learn.microsoft.com/en-us/windows/win32/api/d3d12/nf-d3d12-id3d12commandqueue-wait
- Direct3D 12 fence completion events: https://learn.microsoft.com/en-us/windows/win32/api/d3d12/nf-d3d12-id3d12fence-seteventoncompletion
- Direct3D 12 device creation: https://learn.microsoft.com/en-us/windows/win32/api/d3d12/nf-d3d12-d3d12createdevice
- Direct3D 12 swap chains: https://learn.microsoft.com/en-us/windows/win32/direct3d12/swap-chains
- DXGI `CreateSwapChainForHwnd`: https://learn.microsoft.com/en-us/windows/win32/api/dxgi1_2/nf-dxgi1_2-idxgifactory2-createswapchainforhwnd
- DXGI swapchain description: https://learn.microsoft.com/en-us/windows/win32/api/dxgi1_2/ns-dxgi1_2-dxgi_swap_chain_desc1
- Direct3D 12 render-target views: https://learn.microsoft.com/en-us/windows/win32/api/d3d12/nf-d3d12-id3d12device-createrendertargetview
- Direct3D 12 resource barriers: https://learn.microsoft.com/en-us/windows/win32/api/d3d12/ns-d3d12-d3d12_resource_barrier
- Direct3D 12 `ID3D12GraphicsCommandList::ResourceBarrier`: https://learn.microsoft.com/en-us/windows/win32/api/d3d12/nf-d3d12-id3d12graphicscommandlist-resourcebarrier
- Direct3D 12 clear render target: https://learn.microsoft.com/en-us/windows/win32/api/d3d12/nf-d3d12-id3d12graphicscommandlist-clearrendertargetview
- Direct3D 12 descriptor heaps: https://learn.microsoft.com/en-us/windows/win32/direct3d12/descriptor-heaps
- Direct3D 12 descriptor heap creation: https://learn.microsoft.com/en-us/windows/win32/direct3d12/creating-descriptor-heaps
- Direct3D 12 descriptor heap binding: https://learn.microsoft.com/en-us/windows/win32/direct3d12/setting-descriptor-heaps
- Direct3D 12 descriptor overview: https://learn.microsoft.com/en-us/windows/win32/direct3d12/descriptors-overview
- Direct3D 12 descriptor creation: https://learn.microsoft.com/en-us/windows/win32/direct3d12/creating-descriptors
- Direct3D 12 `CreateConstantBufferView`: https://learn.microsoft.com/en-us/windows/win32/api/d3d12/nf-d3d12-id3d12device-createconstantbufferview
- Direct3D 12 `CreateShaderResourceView`: https://learn.microsoft.com/en-us/windows/win32/api/d3d12/nf-d3d12-id3d12device-createshaderresourceview
- Direct3D 12 `CreateUnorderedAccessView`: https://learn.microsoft.com/en-us/windows/win32/api/d3d12/nf-d3d12-id3d12device-createunorderedaccessview
- Direct3D 12 `CreateDepthStencilView`: https://learn.microsoft.com/en-us/windows/win32/api/d3d12/nf-d3d12-id3d12device-createdepthstencilview
- Direct3D 12 `ClearDepthStencilView`: https://learn.microsoft.com/en-us/windows/win32/api/d3d12/nf-d3d12-id3d12graphicscommandlist-cleardepthstencilview
- Direct3D 12 `OMSetRenderTargets`: https://learn.microsoft.com/en-us/windows/win32/api/d3d12/nf-d3d12-id3d12graphicscommandlist-omsetrendertargets
- Direct3D 12 resource states: https://learn.microsoft.com/en-us/windows/win32/api/d3d12/ne-d3d12-d3d12_resource_states
- Direct3D 12 `GetGPUVirtualAddress`: https://learn.microsoft.com/en-us/windows/win32/api/d3d12/nf-d3d12-id3d12resource-getgpuvirtualaddress
- Direct3D 12 root signatures: https://learn.microsoft.com/en-us/windows/win32/direct3d12/root-signatures-overview
- Direct3D 12 descriptor tables: https://learn.microsoft.com/en-us/windows/win32/direct3d12/descriptor-tables-overview
- Direct3D 12 graphics pipeline state: https://learn.microsoft.com/en-us/windows/win32/direct3d12/managing-graphics-pipeline-state-in-direct3d-12
- Direct3D 12 `CreateGraphicsPipelineState`: https://learn.microsoft.com/en-us/windows/win32/api/d3d12/nf-d3d12-id3d12device-creategraphicspipelinestate
- Direct3D 12 `IASetPrimitiveTopology`: https://learn.microsoft.com/en-us/windows/win32/api/d3d12/nf-d3d12-id3d12graphicscommandlist-iasetprimitivetopology
- Direct3D 12 `DrawInstanced`: https://learn.microsoft.com/en-us/windows/win32/api/d3d12/nf-d3d12-id3d12graphicscommandlist-drawinstanced
- Direct3D 12 `CopyTextureRegion`: https://learn.microsoft.com/en-us/windows/win32/api/d3d12/nf-d3d12-id3d12graphicscommandlist-copytextureregion
- Direct3D 12 texture copy locations: https://learn.microsoft.com/en-us/windows/win32/api/d3d12/ns-d3d12-d3d12_texture_copy_location
- Direct3D 12 subresource footprints: https://learn.microsoft.com/en-us/windows/win32/api/d3d12/ns-d3d12-d3d12_subresource_footprint
- Direct3D 12 placed subresource footprints: https://learn.microsoft.com/en-us/windows/win32/api/d3d12/ns-d3d12-d3d12_placed_subresource_footprint
- Direct3D 12 resource uploading: https://learn.microsoft.com/en-us/windows/win32/direct3d12/uploading-resources
- Direct3D 12 aliasing barriers: https://learn.microsoft.com/en-us/windows/win32/api/d3d12/ns-d3d12-d3d12_resource_aliasing_barrier
- Direct3D 12 memory management strategies: https://learn.microsoft.com/en-us/windows/win32/direct3d12/memory-management-strategies
- Direct3D 12 `CreateSharedHandle`: https://learn.microsoft.com/en-us/windows/win32/api/d3d12/nf-d3d12-id3d12device-createsharedhandle
- Direct3D 12 shared heaps/resources: https://learn.microsoft.com/en-us/windows/win32/direct3d12/shared-heaps
- SDL3 texture creation properties: https://wiki.libsdl.org/SDL3/SDL_CreateTextureWithProperties
- SDL3 renderer properties: https://wiki.libsdl.org/SDL3/SDL_GetRendererProperties
- Win32 `CreateProcessW`: https://learn.microsoft.com/en-us/windows/win32/api/processthreadsapi/nf-processthreadsapi-createprocessw
- Win32 redirected child-process I/O: https://learn.microsoft.com/en-us/windows/win32/procthread/creating-a-child-process-with-redirected-input-and-output
- Microsoft C command-line argument parsing: https://learn.microsoft.com/en-us/cpp/c-language/parsing-c-command-line-arguments
- Microsoft DirectX Shader Compiler: https://github.com/microsoft/DirectXShaderCompiler
- Microsoft DirectX Shader Compiler SPIR-V CodeGen: https://github.com/microsoft/DirectXShaderCompiler/wiki/SPIR%E2%80%90V-CodeGen
- Microsoft HLSL compiler overview: https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx-graphics-hlsl-part1
- Vulkan HLSL guide: https://github.khronos.org/Vulkan-Site/guide/latest/hlsl.html
- Vulkan descriptor sets: https://docs.vulkan.org/spec/latest/chapters/descriptorsets.html
- Vulkan descriptor set layout creation: https://docs.vulkan.org/refpages/latest/refpages/source/vkCreateDescriptorSetLayout.html
- Vulkan descriptor set layout create info: https://docs.vulkan.org/refpages/latest/refpages/source/VkDescriptorSetLayoutCreateInfo.html
- Vulkan descriptor set layout binding: https://docs.vulkan.org/refpages/latest/refpages/source/VkDescriptorSetLayoutBinding.html
- Vulkan descriptor pool creation: https://docs.vulkan.org/refpages/latest/refpages/source/vkCreateDescriptorPool.html
- Vulkan descriptor pool create info: https://docs.vulkan.org/refpages/latest/refpages/source/VkDescriptorPoolCreateInfo.html
- Vulkan descriptor set allocation: https://docs.vulkan.org/refpages/latest/refpages/source/vkAllocateDescriptorSets.html
- Vulkan descriptor set update command: https://docs.vulkan.org/refpages/latest/refpages/source/vkUpdateDescriptorSets.html
- Vulkan write descriptor set: https://docs.vulkan.org/refpages/latest/refpages/source/VkWriteDescriptorSet.html
- Vulkan descriptor buffer info: https://docs.vulkan.org/refpages/latest/refpages/source/VkDescriptorBufferInfo.html
- Vulkan descriptor set binding command: https://docs.vulkan.org/refpages/latest/refpages/source/vkCmdBindDescriptorSets.html
- Vulkan devices and queues: https://docs.vulkan.org/spec/latest/chapters/devsandqueues.html
- Vulkan initialization and command function pointers: https://docs.vulkan.org/spec/latest/chapters/initialization.html
- Vulkan `vkGetInstanceProcAddr`: https://docs.vulkan.org/refpages/latest/refpages/source/vkGetInstanceProcAddr.html
- Vulkan `vkGetDeviceProcAddr`: https://docs.vulkan.org/refpages/latest/refpages/source/vkGetDeviceProcAddr.html
- Vulkan instance version query: https://docs.vulkan.org/refpages/latest/refpages/source/vkEnumerateInstanceVersion.html
- Vulkan instance extension enumeration: https://docs.vulkan.org/refpages/latest/refpages/source/vkEnumerateInstanceExtensionProperties.html
- Vulkan API version packing: https://docs.vulkan.org/refpages/latest/refpages/source/VK_MAKE_API_VERSION.html
- Vulkan extension properties: https://docs.vulkan.org/refpages/latest/refpages/source/VkExtensionProperties.html
- Vulkan instance create info: https://docs.vulkan.org/refpages/latest/refpages/source/VkInstanceCreateInfo.html
- Vulkan application info: https://docs.vulkan.org/refpages/latest/refpages/source/VkApplicationInfo.html
- Vulkan instance creation: https://docs.vulkan.org/refpages/latest/refpages/source/vkCreateInstance.html
- Vulkan instance destruction: https://docs.vulkan.org/refpages/latest/refpages/source/vkDestroyInstance.html
- Vulkan physical device enumeration: https://docs.vulkan.org/refpages/latest/refpages/source/vkEnumeratePhysicalDevices.html
- Vulkan physical device properties query: https://docs.vulkan.org/refpages/latest/refpages/source/vkGetPhysicalDeviceProperties2.html
- Vulkan physical device properties chain: https://docs.vulkan.org/refpages/latest/refpages/source/VkPhysicalDeviceProperties2.html
- Vulkan physical device properties: https://docs.vulkan.org/refpages/latest/refpages/source/VkPhysicalDeviceProperties.html
- Vulkan physical device type: https://docs.vulkan.org/refpages/latest/refpages/source/VkPhysicalDeviceType.html
- Vulkan queue family properties command: https://docs.vulkan.org/refpages/latest/refpages/source/vkGetPhysicalDeviceQueueFamilyProperties.html
- Vulkan queue family properties structure: https://docs.vulkan.org/refpages/latest/refpages/source/VkQueueFamilyProperties.html
- Vulkan device extension enumeration: https://docs.vulkan.org/refpages/latest/refpages/source/vkEnumerateDeviceExtensionProperties.html
- Vulkan physical device feature query: https://docs.vulkan.org/refpages/latest/refpages/source/vkGetPhysicalDeviceFeatures2.html
- Vulkan physical device features chain: https://docs.vulkan.org/refpages/latest/refpages/source/VkPhysicalDeviceFeatures2.html
- Vulkan dynamic rendering feature: https://docs.vulkan.org/refpages/latest/refpages/source/VkPhysicalDeviceDynamicRenderingFeatures.html
- Vulkan extension enabling guide: https://docs.vulkan.org/guide/latest/enabling_extensions.html
- Vulkan surface extension: https://docs.vulkan.org/refpages/latest/refpages/source/VK_KHR_surface.html
- Vulkan Win32 surface extension: https://docs.vulkan.org/refpages/latest/refpages/source/VK_KHR_win32_surface.html
- Vulkan Win32 surface creation: https://docs.vulkan.org/refpages/latest/refpages/source/vkCreateWin32SurfaceKHR.html
- Vulkan Win32 surface create info: https://docs.vulkan.org/refpages/latest/refpages/source/VkWin32SurfaceCreateInfoKHR.html
- Vulkan surface support query: https://docs.vulkan.org/refpages/latest/refpages/source/vkGetPhysicalDeviceSurfaceSupportKHR.html
- Vulkan surface destruction: https://docs.vulkan.org/refpages/latest/refpages/source/vkDestroySurfaceKHR.html
- Vulkan physical devices and queue families: https://docs.vulkan.org/tutorial/latest/03_Drawing_a_triangle/00_Setup/03_Physical_devices_and_queue_families.html
- Vulkan device creation: https://docs.vulkan.org/refpages/latest/refpages/source/vkCreateDevice.html
- Vulkan device destruction: https://docs.vulkan.org/refpages/latest/refpages/source/vkDestroyDevice.html
- Vulkan device create info: https://docs.vulkan.org/refpages/latest/refpages/source/VkDeviceCreateInfo.html
- Vulkan device queue create info: https://docs.vulkan.org/refpages/latest/refpages/source/VkDeviceQueueCreateInfo.html
- Vulkan device queue retrieval: https://docs.vulkan.org/refpages/latest/refpages/source/vkGetDeviceQueue.html
- Vulkan device idle wait: https://docs.vulkan.org/refpages/latest/refpages/source/vkDeviceWaitIdle.html
- Vulkan swapchain extension: https://docs.vulkan.org/refpages/latest/refpages/source/VK_KHR_swapchain.html
- Vulkan swapchain creation: https://docs.vulkan.org/refpages/latest/refpages/source/vkCreateSwapchainKHR.html
- Vulkan swapchain create info: https://docs.vulkan.org/refpages/latest/refpages/source/VkSwapchainCreateInfoKHR.html
- Vulkan swapchain image enumeration: https://docs.vulkan.org/refpages/latest/refpages/source/vkGetSwapchainImagesKHR.html
- Vulkan image view creation: https://docs.vulkan.org/refpages/latest/refpages/source/vkCreateImageView.html
- Vulkan image view create info: https://docs.vulkan.org/refpages/latest/refpages/source/VkImageViewCreateInfo.html
- Vulkan image acquisition: https://docs.vulkan.org/refpages/latest/refpages/source/vkAcquireNextImageKHR.html
- Vulkan semaphore creation: https://docs.vulkan.org/refpages/latest/refpages/source/vkCreateSemaphore.html
- Vulkan semaphore create info: https://docs.vulkan.org/refpages/latest/refpages/source/VkSemaphoreCreateInfo.html
- Vulkan fence creation: https://docs.vulkan.org/refpages/latest/refpages/source/vkCreateFence.html
- Vulkan fence create info: https://docs.vulkan.org/refpages/latest/refpages/source/VkFenceCreateInfo.html
- Vulkan queue presentation: https://docs.vulkan.org/refpages/latest/refpages/source/vkQueuePresentKHR.html
- Vulkan present info: https://docs.vulkan.org/refpages/latest/refpages/source/VkPresentInfoKHR.html
- Vulkan dynamic rendering extension: https://docs.vulkan.org/refpages/latest/refpages/source/VK_KHR_dynamic_rendering.html
- Vulkan begin dynamic rendering command: https://docs.vulkan.org/refpages/latest/refpages/source/vkCmdBeginRendering.html
- Vulkan end dynamic rendering command: https://docs.vulkan.org/refpages/latest/refpages/source/vkCmdEndRendering.html
- Vulkan dynamic rendering info: https://docs.vulkan.org/refpages/latest/refpages/source/VkRenderingInfo.html
- Vulkan dynamic rendering attachment info: https://docs.vulkan.org/refpages/latest/refpages/source/VkRenderingAttachmentInfo.html
- Vulkan dynamic rendering pipeline create info: https://docs.vulkan.org/refpages/latest/refpages/source/VkPipelineRenderingCreateInfo.html
- Vulkan pipeline depth/stencil state: https://docs.vulkan.org/refpages/latest/refpages/source/VkPipelineDepthStencilStateCreateInfo.html
- Vulkan compare operations: https://docs.vulkan.org/refpages/latest/refpages/source/VkCompareOp.html
- Vulkan image aspect flags: https://docs.vulkan.org/refpages/latest/refpages/source/VkImageAspectFlagBits.html
- Vulkan image layouts: https://docs.vulkan.org/refpages/latest/refpages/source/VkImageLayout.html
- Vulkan bind pipeline command: https://docs.vulkan.org/refpages/latest/refpages/source/vkCmdBindPipeline.html
- Vulkan set viewport command: https://docs.vulkan.org/refpages/latest/refpages/source/vkCmdSetViewport.html
- Vulkan set scissor command: https://docs.vulkan.org/refpages/latest/refpages/source/vkCmdSetScissor.html
- Vulkan draw command: https://docs.vulkan.org/refpages/latest/refpages/source/vkCmdDraw.html
- Vulkan synchronization2 extension: https://docs.vulkan.org/refpages/latest/refpages/source/VK_KHR_synchronization2.html
- Vulkan pipeline barrier2 command: https://docs.vulkan.org/refpages/latest/refpages/source/vkCmdPipelineBarrier2.html
- Vulkan pipeline stage flags2: https://docs.vulkan.org/refpages/latest/refpages/source/VkPipelineStageFlagBits2.html
- Vulkan access flags2: https://docs.vulkan.org/refpages/latest/refpages/source/VkAccessFlagBits2.html
- Vulkan dependency info: https://docs.vulkan.org/refpages/latest/refpages/source/VkDependencyInfo.html
- Vulkan image memory barrier2: https://docs.vulkan.org/refpages/latest/refpages/source/VkImageMemoryBarrier2.html
- Vulkan queue submit2 command: https://docs.vulkan.org/refpages/latest/refpages/source/vkQueueSubmit2.html
- Vulkan submit info2: https://docs.vulkan.org/refpages/latest/refpages/source/VkSubmitInfo2.html
- Vulkan command buffer submit info: https://docs.vulkan.org/refpages/latest/refpages/source/VkCommandBufferSubmitInfo.html
- Vulkan semaphore submit info: https://docs.vulkan.org/refpages/latest/refpages/source/VkSemaphoreSubmitInfo.html
- Vulkan buffer creation: https://docs.vulkan.org/refpages/latest/refpages/source/vkCreateBuffer.html
- Vulkan buffer create info: https://docs.vulkan.org/refpages/latest/refpages/source/VkBufferCreateInfo.html
- Vulkan physical-device memory properties: https://docs.vulkan.org/refpages/latest/refpages/source/vkGetPhysicalDeviceMemoryProperties.html
- Vulkan memory allocation: https://docs.vulkan.org/refpages/latest/refpages/source/vkAllocateMemory.html
- Vulkan memory allocation guide: https://docs.vulkan.org/guide/latest/memory_allocation.html
- Vulkan memory allocate info: https://docs.vulkan.org/refpages/latest/refpages/source/VkMemoryAllocateInfo.html
- Vulkan buffer memory binding: https://docs.vulkan.org/refpages/latest/refpages/source/vkBindBufferMemory.html
- Vulkan image-to-buffer copy command: https://docs.vulkan.org/refpages/latest/refpages/source/vkCmdCopyImageToBuffer.html
- Vulkan buffer image copy region: https://docs.vulkan.org/refpages/latest/refpages/source/VkBufferImageCopy.html
- Vulkan memory mapping: https://docs.vulkan.org/refpages/latest/refpages/source/vkMapMemory.html
- Vulkan memory unmapping: https://docs.vulkan.org/refpages/latest/refpages/source/vkUnmapMemory.html
- Vulkan buffer copy command: https://docs.vulkan.org/refpages/latest/refpages/source/vkCmdCopyBuffer.html
- Vulkan buffer-to-image copy command: https://docs.vulkan.org/refpages/latest/refpages/source/vkCmdCopyBufferToImage.html
- Vulkan shader module creation: https://registry.khronos.org/vulkan/specs/latest/man/html/vkCreateShaderModule.html
- Vulkan shader module create info: https://registry.khronos.org/vulkan/specs/latest/man/html/VkShaderModuleCreateInfo.html
- Vulkan shader modules: https://docs.vulkan.org/spec/latest/chapters/shaders.html
- Vulkan pipeline layout creation: https://registry.khronos.org/vulkan/specs/latest/man/html/vkCreatePipelineLayout.html
- Vulkan graphics pipeline creation: https://registry.khronos.org/vulkan/specs/latest/man/html/vkCreateGraphicsPipelines.html
- Vulkan graphics pipeline create info: https://registry.khronos.org/vulkan/specs/latest/man/html/VkGraphicsPipelineCreateInfo.html
- Vulkan SPIR-V environment: https://docs.vulkan.org/spec/latest/appendices/spirvenv.html
- Vulkan copy commands: https://github.khronos.org/Vulkan-Site/spec/latest/chapters/copies.html
- Vulkan synchronization guide: https://github.khronos.org/Vulkan-Site/guide/latest/synchronization.html
- Metal shader libraries: https://developer.apple.com/documentation/metal/shader_libraries
- Metal `MTLDevice`: https://developer.apple.com/documentation/Metal/MTLDevice
- Metal default GPU device: https://developer.apple.com/documentation/metal/gpu_devices_and_work_submission/getting_the_default_gpu
- Metal `MTLCreateSystemDefaultDevice`: https://developer.apple.com/documentation/Metal/MTLCreateSystemDefaultDevice%28%29
- Metal command-line shader library compilation: https://developer.apple.com/documentation/metal/building-a-shader-library-by-precompiling-source-files
- Metal argument buffers: https://developer.apple.com/documentation/metal/improving-cpu-performance-by-using-argument-buffers
- Metal blit passes: https://developer.apple.com/documentation/metal/blit-passes
- Metal heaps: https://developer.apple.com/documentation/metal/memory-heaps
- Metal render command encoder resource binding: https://developer.apple.com/documentation/metal/mtlrendercommandencoder
- Metal command structure: https://developer.apple.com/documentation/metal/setting-up-a-command-structure
