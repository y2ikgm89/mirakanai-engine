# GameEngine Roadmap

## AI Reading Contract

Use this file as a short status and priority route, not as the exhaustive implementation ledger. For exact current truth, prefer:

- `engine/agent/manifest.json` for machine-readable active plan, supported recipes, host gates, readiness, and `unsupportedProductionGaps`.
- [Current capabilities](current-capabilities.md) for the concise human-readable supported surface.
- [Production completion master plan](superpowers/master-plans/2026-05-03-production-completion-master-plan-v1.md) for current 1.0 verdict, post-1.0 backlog, and projection chapters.
- [Implementation plan registry](superpowers/plans/README.md) for active vs retained dated plan evidence.

Long capability prose lower in this file is retained historical/static-check evidence. Do not use it to override the manifest, current-capabilities summary, or active plan registry.

Latest manifest-led environment commercial aggregate status: Environment Highest Commercial Readiness v1 Tasks 1-32 now close the exact clean-break commercial environment matrix. Selected strict Vulkan aggregate readiness, Apple-host Metal aggregate readiness, clean-break Metal aggregate readiness, backend parity v2, Windows D3D12, Windows Vulkan, Linux Vulkan, macOS Metal, iOS Metal, Android Vulkan, exact platform readiness, exact all-platform readiness, broad measured optimization, AAA preset asset library, full OpenEXR/KTX2/Basis asset pipeline, physical weather simulation, and production artist workflow are ready through reviewed recipes and validators. Task 32 closes Android Vulkan through `tools/validate-android-vulkan-runtime-host.ps1 -RequireReady` on an Android Emulator primary `x86_64` ABI with official Khronos validation-layer `jniLibs`, `AndroidAbi` / Gradle `mirakanai.androidAbi` ABI matching, APK-packaged `lib/x86_64/libVkLayer_khronos_validation.so`, `VK_LAYER_KHRONOS_validation` enumeration, clean logcat validation output, and offscreen Vulkan readback. `tools/validate-environment-highest-commercial-readiness.ps1 -RequireReady` now reports `environment_highest_commercial_ready=1`, `environment_commercial_ready=1`, `environment_commercial_ready_rows=16`, `environment_host_gated_rows=0`, `environment_dependency_gated_rows=0`, `environment_blocked_rows=0`, `environment_unsupported_rows=0`, `environment_missing_rows=0`, `environment_all_platform_unconditional_ready=1`, `environment_broad_optimization_ready=1`, and `environment_ready=0`. Broad `environment_ready` remains intentionally unclaimed.

2D Production Engine Capability Gap Cluster v1 is complete and live execution returns to the production-completion selection gate: `engine/agent/manifest.json.aiOperableProductionLoop.currentActivePlan` points at `docs/superpowers/master-plans/2026-05-03-production-completion-master-plan-v1.md`; `recommendedNextPlan.id = next-production-gap-selection`; `unsupportedProductionGaps = []`. Phase 1 PR #716 proved the first-party runtime gameplay execution loop; Phase 2 PR #721 plus PR #724 proved cooked-only sprite atlas runtime residency; Phase 3 PR #725 plus PR #728 proved dense sprite throughput; Phase 4 PR #730 proved first-party 2D physics runtime extensions; Phase 5 PR #733 proved input device production UX; Phase 6 PR #735 plus PR #736 proved the selected production workload matrix; Phase 7 PR #737 proved selected package playtest productization and merged to `origin/main` at `db745fc05b9eeab5931bae7a9e0f4f66faa11a7e`. The milestone preserves legal clean-room constraints, official docs category-only usage, package-visible counters, full validation evidence, hosted PR Gate evidence, and explicit non-claims for broad all-feature/all-platform 2D engine readiness, backend parity, Metal inference, renderer/RHI ownership, native handles, package-script execution, and active-session hot reload.

Retained MAVG Mesh Shader Capability Gate v1 (`mavg-mesh-shader-capability-gate-v1`) remains completed evidence in this branch: it adds `mavg_mesh_shader_capability_gate.hpp`, `RuntimeMavgMeshShaderCapabilityGateDesc`, `RuntimeMavgMeshShaderCapabilityBackendRow`, `RuntimeMavgMeshShaderCapabilityGateResult`, and `evaluate_runtime_mavg_mesh_shader_capability_gate` in `MK_runtime_rhi`. The gate is value-only: it records selected D3D12 `D3D12_FEATURE_D3D12_OPTIONS7` / `MeshShaderTier` evidence and selected Vulkan `VK_EXT_mesh_shader` rows through `VkPhysicalDeviceMeshShaderFeaturesEXT` and `VkPhysicalDeviceMeshShaderPropertiesEXT`, then requires compute/indirect fallback readiness before emitting package-visible `mavg_mesh_shader_capability_gate_ready=1`. The package smoke proves two backend rows, two ready backend rows, two feature-query rows, two pipeline-statistics rows, D3D12/Vulkan capability readiness, fallback-to-conventional-indexed-draws readiness, and `mesh shader execution remains 0`. Native handles, backend execution, Metal readiness, Nanite equivalence/superiority, broad MAVG backend readiness, and broad optimization remain unclaimed.

Retained mainline production-completion closeout: main returned `engine/agent/manifest.json.aiOperableProductionLoop.currentActivePlan` to `docs/superpowers/master-plans/2026-05-03-production-completion-master-plan-v1.md` with `recommendedNextPlan.id = next-production-gap-selection` and `unsupportedProductionGaps = []` after MAVG Async Overlap Performance Proof v1 (`mavg-async-overlap-performance-proof-v1`) completed through PR #714 / merge commit `c524a00200e9375250f70a031141f8a2760e6950` and closeout PR #715 / merge commit `ea949467`. The retained selected measured-sample async-overlap performance proof adds `mavg_async_overlap_performance_proof.hpp`, `RuntimeMavgAsyncOverlapPerformanceProofDesc`, `RuntimeMavgAsyncOverlapPerformanceSample`, `RuntimeMavgAsyncOverlapPerformanceProofResult`, and `prove_runtime_mavg_async_overlap_performance`, validates ready `RuntimeMavgStreamingUploadOverlapEvidenceResult` identity and caller-owned serial/overlapped sample spans, computes p95 ticks and speedup basis points, and promotes only the selected `proved_async_overlap_performance` row. `tools/validate-mavg-async-overlap-performance-proof.ps1 -RequireReady` proves the package-visible `sample_desktop_runtime_game --require-mavg-async-overlap-performance-proof` counters: `mavg_async_overlap_performance_proof_ready=1`, 10 samples, five overlapped samples, serial p95 140, overlapped p95 105, speedup 2500 basis points, zero native handle exposure, zero GPU DirectStorage destinations, zero GDeflate, zero mesh shader execution, zero Metal readiness, zero Nanite equivalence, and zero broad optimization. MAVG Win32 DirectStorage SDK Adapter v1 (`mavg-win32-directstorage-sdk-adapter-v1`) remains retained completed evidence for the first-party Win32 DirectStorage SDK adapter over Microsoft DirectStorage SDK 1.3.0 / vcpkg `dstorage` 1.3.0. GPU destinations, GDeflate, DirectStorage 1.4 preview APIs, autonomous streaming-service performance beyond selected samples, mesh shaders, Metal readiness, Nanite equivalence/superiority, public native handles, broad MAVG backend readiness, and broad optimization remain unclaimed.

MAVG Advanced Backend Evidence Closeout v1 Task 6 (`mavg-async-overlap-performance-v1`) now has a host-gated measured async-overlap performance artifact contract through `mavg_async_overlap_performance.hpp`, `RuntimeMavgAsyncOverlapMeasuredPerformanceDesc`, `RuntimeMavgAsyncOverlapRunEvidence`, `RuntimeMavgAsyncOverlapProfilerArtifactEvidence`, `RuntimeMavgAsyncOverlapMeasuredPerformanceResult`, `evaluate_runtime_mavg_async_overlap_measured_performance`, `schemas/mavg-async-overlap-performance.schema.json`, `GameEngine.MavgAsyncOverlapPerformanceEvidence.v1`, and `tools/validate-mavg-async-overlap-performance.ps1`. The validator requires comparable `sync_baseline` and `async_scheduler` runs, at least 30 warmup frames and 120 measured frames, non-captured internal timing counters, matching replay hash, memory budget compliance, p95/p99 thresholds, and reviewed official profiler artifact rows from PIX timing captures, Nsight Graphics GPU Trace, Radeon GPU Profiler, Intel GPA, or Apple Metal Tools. Current branch evidence remains `mavg_async_overlap_measured_performance_status=host_evidence_required` and `mavg_async_overlap_measured_performance_ready=0` because no retained host profiler artifact row is committed; timing-window-only evidence, native handles, GPU DirectStorage destinations, GDeflate, mesh shader execution, Metal readiness, Nanite equivalence/superiority, broad MAVG backend readiness, and broad CPU/GPU/memory optimization are still non-claims.

MAVG Advanced Backend Evidence Closeout v1 Task 7 (`mavg-deformation-integration-v1`) now has a runtime scene RHI deformation policy gate through `mavg_deformation_integration.hpp`, `MavgDeformationIntegrationDesc`, `MavgDeformationClusterBoundsRow`, `MavgDeformationIntegrationResult`, `plan_mavg_deformation_integrated_clusters`, and `tools/validate-mavg-deformation-integration.ps1`. The focused tests prove selected rigid transform, linear blend skinning, and morph target policy rows only when stable cluster ids, conservative deformed AABBs, material roots, resident page validity, and fallback draw ranges are preserved. The gate rejects topology-changing deformation, runtime-generated triangle topology, unbounded vertex displacement, excessive joint influence counts, native handles, and broad deformation readiness. Current evidence is intentionally `mavg_deformation_policy_ready=1`, `mavg_deformation_integration_status=backend_execution_required`, and `mavg_deformation_integration_ready=0`; backend dynamic vertex/payload execution rows, ray tracing, mesh shader execution, Metal readiness, Nanite compatibility/equivalence/superiority, broad MAVG backend readiness, and broad CPU/GPU/memory optimization remain non-claims.

MAVG Advanced Backend Evidence Closeout v1 Task 8 (`mavg-ray-tracing-integration-v1`) now has a runtime scene RHI ray tracing policy gate through `mavg_ray_tracing_integration.hpp`, `MavgRayTracingGeometryPolicy`, `MavgRayTracingBlasInputRow`, `MavgRayTracingIntegrationResult`, `plan_mavg_ray_tracing_blas_inputs`, and `tools/validate-mavg-ray-tracing-integration.ps1`. The focused tests prove explicit resident-cluster BLAS and fallback-mesh BLAS rows only when stable replay hash, geometry byte count, index format, transform row, material alpha policy, resident page evidence, and fallback mesh match evidence are present. The gate rejects implicit geometry mode switches, fallback mode mismatches, alpha-blended material policy, native handles, Metal readiness inference, and broad ray tracing readiness. Current evidence is intentionally `mavg_ray_tracing_policy_ready=1`, `mavg_ray_tracing_integration_status=backend_acceleration_structure_build_required`, and `mavg_ray_tracing_integration_ready=0`; D3D12 `BuildRaytracingAccelerationStructure`, Vulkan `VK_KHR_acceleration_structure` build execution, mesh shader execution, Metal readiness, Nanite compatibility/equivalence/superiority, broad MAVG backend readiness, and broad CPU/GPU/memory optimization remain non-claims.

MAVG Advanced Backend Evidence Closeout v1 Task 9A (`mavg-metal-mesh-lod-host-evidence-v1`) now has an Apple-host Metal MAVG mesh LOD host evidence gate through `metal_mavg_mesh_lod.hpp`, `MetalMavgMeshLodHostEvidenceDesc`, `MetalMavgMeshLodHostEvidenceResult`, `evaluate_mavg_metal_mesh_lod_host_evidence`, and `tools/check-mavg-metal-host-evidence.ps1`. The gate follows Apple Metal Feature Set Tables plus Apple's object/mesh shader LOD sample shape, but retained Windows validation intentionally emits `mavg_metal_mesh_lod_status=host_evidence_required` and `mavg_metal_mesh_lod_ready=0` until an Apple host proves full Xcode/metal/metallib tooling, Apple GPU family support, object/mesh shader support, a selected first-party MAVG workload, object/mesh shader pipeline execution, deterministic readback hash, and package-visible output. cross-backend inference, simulator-only evidence, native handles, Metal ray tracing conflation, Nanite compatibility/equivalence/superiority, package-visible `mavg_mesh_shader_lod_ready`, broad MAVG backend readiness, and broad CPU/GPU/memory optimization remain non-claims.

MAVG Advanced Backend Evidence Closeout v1 Task 10 (`mavg-broad-optimization-evidence-v1`) now has a host-gated broad CPU/GPU/memory optimization artifact contract through `mavg_broad_optimization_evidence.hpp`, `RuntimeMavgBroadOptimizationEvidenceDesc`, `RuntimeMavgBroadOptimizationEvidenceRow`, `RuntimeMavgBroadOptimizationMetrics`, `evaluate_runtime_mavg_broad_optimization_evidence`, `GameEngine.MavgBroadOptimizationArtifacts.v1`, `schemas/mavg-broad-optimization-artifacts.schema.json`, and `tools/validate-mavg-broad-optimization.ps1`. The gate requires 14 official-profiler artifact rows across D3D12 NVIDIA/AMD/Intel, Vulkan NVIDIA/AMD/Intel, and Apple-host Metal for both MAVG stress and first-party gameplay packages, and records profiler support source ids, capture overhead, CPU/GPU/IO/memory/queue timelines, replay hashes, p95 improvements, VRAM peak improvement, and p99 regression limits. Current evidence intentionally emits `mavg_broad_cpu_gpu_memory_optimization_status=host_evidence_required`, `mavg_broad_cpu_gpu_memory_optimization_ready=0`, `mavg_broad_optimization_required_rows=14`, `mavg_broad_optimization_intel_gpa_eol=1`, and `mavg_broad_optimization_intel_vulkan_supported_profiler_rows=0`; package-visible broad MAVG backend readiness, Nanite compatibility/equivalence/superiority, `environment_ready`, `renderer_ready`, and broad engine optimization remain non-claims.

MAVG Advanced Backend Evidence Closeout v1 Task 11 (`mavg-nanite-comparison-report-v1`) now has a clean-room Nanite comparison report through `docs/specs/2026-06-21-mavg-nanite-comparison-taxonomy-v1.md`, `GameEngine.MavgNaniteComparisonReport.v1`, `schemas/mavg-nanite-comparison-report.schema.json`, and `tools/validate-mavg-nanite-comparison.ps1 -RequireReady`. The report uses only public Epic Nanite documentation and first-party MIRAIKANAI/generated assets, covers 12 public-doc axes, and emits `mavg_nanite_comparison_report_ready=1` with `mavg_nanite_public_docs_only=1`, `mavg_nanite_unreal_source_used=0`, `mavg_nanite_private_format_used=0`, `mavg_nanite_epic_sample_assets_used=0`, `mavg_nanite_compatible=0`, `mavg_nanite_equivalent=0`, `mavg_nanite_superior=0`, and `mavg_nanite_marketing_claim_allowed=0`. This is a comparison taxonomy only; Nanite interoperability, replacement equivalence, superiority, broad MAVG backend readiness, broad optimization, renderer readiness, environment readiness, and commercial readiness remain non-claims.

MAVG Advanced Backend Evidence Closeout v1 Task 12 closes the plan bookkeeping: the plan registry, roadmap, current-capabilities summary, task-specific schemas, manifest command rows, and static AI-integration chapters agree on the exact ready rows and non-claims. This closeout does not promote additional runtime/backend readiness; `currentActivePlan` stays on the production-completion selection gate, missing host evidence remains host-gated, and Nanite compatibility/equivalence/superiority product claims remain false.

MAVG package-visible backend readiness closeout is now a selected stacked slice through `mavg_backend_readiness_closeout.hpp`, `MavgBackendReadinessCloseoutResult`, `evaluate_mavg_backend_readiness_closeout`, `sample_desktop_runtime_game --require-mavg-backend-readiness`, and `tools/validate-mavg-backend-readiness.ps1 -RequireReady`. It promotes only `mavg_package_visible_backend_readiness_ready=1` when nine exact rows are ready with zero diagnostics/native handles/Metal inference and `mavg_package_visible_backend_readiness_broad_backend_readiness=0`; it does not claim mesh shader execution, Metal MAVG readiness, Nanite compatibility/equivalence/superiority, deformation, ray tracing, autonomous scheduler breadth, or broad CPU/GPU/memory optimization.

MAVG Advanced Backend Evidence Closeout v1 Task 3 is now a focused host-gated D3D12 mesh shader LOD execution slice through `mavg_mesh_shader_lod.hpp`, `D3d12MavgMeshShaderLodDispatchResult`, `probe_d3d12_mavg_mesh_shader_lod_capability`, and `execute_d3d12_mavg_mesh_shader_lod`. Focused tests prove backend-neutral meshlet task planning, D3D12 `D3D12_FEATURE_D3D12_OPTIONS7` / `MeshShaderTier` gating, AS/MS/PS shader models `as_6_5` / `ms_6_5` / `ps_6_0`, direct `DispatchMesh` readback evidence when the host supports it, no IA input layout, no index-buffer dependency, no `ExecuteIndirect` mesh dispatch, diagnostic text, and supplemental pipeline-statistics host-gating. This remains D3D12-local execution evidence only; package-visible `mavg_mesh_shader_lod_ready`, Vulkan execution, Metal readiness, Nanite compatibility/equivalence/superiority, deformation, ray tracing, autonomous scheduler breadth, and broad optimization remain unclaimed.

MAVG Advanced Backend Evidence Closeout v1 Task 4 is now a selected Vulkan mesh shader LOD direct-dispatch evidence slice through `vulkan_mavg_mesh_shader_lod.hpp`, `VulkanMavgMeshShaderLodDispatchResult`, backend-private `VulkanRuntimeMeshGraphicsPipeline`, `create_runtime_mesh_graphics_pipeline`, and `record_runtime_texture_rendering_mesh_tasks_draw`. Focused tests prove `VK_EXT_mesh_shader` feature/property probing, logical-device mesh/task feature enablement, direct `vkCmdDrawMeshTasksEXT`, task/mesh/fragment SPIR-V entry-point binding, dynamic rendering with no vertex input layout and no index buffer, synchronization2 render/readback barriers, and nonzero readback hash evidence when a selected host provides validated `MK_VULKAN_TEST_MAVG_MESH_SHADER_LOD_*_SPV` artifacts. This remains selected Vulkan direct-dispatch evidence only: indirect mesh task dispatch, indirect-count dispatch, package-visible `mavg_mesh_shader_lod_ready`, Metal readiness, Nanite compatibility/equivalence/superiority, deformation, ray tracing, autonomous scheduler breadth, broad MAVG backend readiness, and broad optimization remain unclaimed.

2026-06-21 First-Party Runtime UI Standard Widgets v1 adds first-party `MK_ui` value-model APIs for HUD meters, pause/settings menus, standard HUD composition, and provenance review. The selected 2D and 3D package smokes use `--require-runtime-ui-standard-widgets` to prove three meter rows, two menu screens, four menu actions, nineteen `UiDocument` elements, accessibility/localization/style-token evidence, official-documentation rows used only as category research, one first-party design row, and zero external engine code/assets, UI middleware, or diagnostics. This does not copy Unity, Unreal Engine, Godot, third-party UI middleware, public UI system names, samples, marketplace assets, themes, or layouts, and it does not claim production text/font/IME/accessibility/renderer-upload or broad platform UI parity readiness.

2026-06-18 Task 4/5 note, tightened through 2026-06-21 Task 32: Linux Vulkan, Android Vulkan, and iOS Metal now use explicit host validators instead of Task 3 skeleton output. Linux routes through `environment-platform-linux-vulkan-host-gate`, `environment-platform-linux-vulkan-package`, `tools/validate-linux-vulkan-runtime-host.ps1`, and `vulkan-strict-linux`; Task 28 promotes Linux only through hosted Linux package/readback/clean-log evidence. Android routes through `environment-platform-android-vulkan-package` / `tools/validate-android-vulkan-runtime-host.ps1` and requires Android SDK/NDK, adb device or emulator, Vulkan feature declarations, `android_gpu_debuggable_ready=1`, host-supplied `ValidationLayerJniLibs`, `AndroidAbi`, Gradle `mirakanai.androidAbi` / `mirakanai.validationLayerJniLibs`, `jniLibs.addStaticSourceDirectory`, device primary ABI matching, `android_package_primary_abi=x86_64`, APK-packaged `lib/x86_64/libVkLayer_khronos_validation.so`, `android_validation_layer_jni_libs_ready=1`, `android_validation_layer_apk_packaged=1`, `VK_LAYER_KHRONOS_validation_ready=1`, package smoke, `android_vulkan_readback_ready=1`, `android_vulkan_validation_layer_enumerated=1`, and `android_vulkan_validation_log_clean=1`. AGI GPU debug-layer settings remain diagnostic evidence only. iOS routes through `environment-platform-ios-metal-package` / `tools/validate-apple-metal-platform-host.ps1` and requires `host=macos`, full Xcode, iOS Simulator SDK/runtime or device evidence, iOS package smoke, app-written Metal feature-set, command queue, compute pipeline, command buffer, and readback counters, plus `environment_platform_requires_ios_metal_host_evidence=0`. The current Win32 `sample_desktop_runtime_game` / `tools/package-desktop-runtime.ps1` lane still cannot promote Linux, Android, or iOS platform readiness by inference.

2026-06-20 Task 24 update: `sample_desktop_runtime_game` now uses a Linux-specific package entrypoint on Linux through `HOST_BACKEND linux` and `games/sample_desktop_runtime_game/linux_main.cpp`, with explicit `--require-linux-vulkan-presentation-smoke`, `--require-linux-vulkan-readback`, and `--require-linux-vulkan-validation-log` package-smoke flags. The entrypoint currently emits fail-closed counters, so `environment_platform_linux_vulkan_ready=0`, `environment_commercial_ready=0`, and broad `environment_ready=0` remain correct until real Linux XCB Vulkan swapchain/readback/clean-log evidence is produced.

2026-06-20 Task 25 update: `LinuxDesktopVulkanPresentationProbeDesc` / `probe_linux_desktop_vulkan_presentation` now drive a private Linux XCB Vulkan package probe through `SurfacePlatform::xcb`, runtime surface support, device creation, swapchain creation, acquire, dynamic rendering clear, readback, and present. The probe intentionally leaves validation-log capture fail-closed, so it does not promote `environment_platform_linux_vulkan_ready`, strict Vulkan aggregate, all-platform, commercial readiness, or broad `environment_ready`.

2026-06-19 Task 10 update: `environment-physical-weather-simulation-closeout` now routes through `tools/validate-environment-weather-physics.ps1 -RequireReady` plus `EnvironmentPhysicalWeatherSimulationCloseoutDesc` / `evaluate_environment_physical_weather_simulation_closeout`. It emits `environment_physical_weather_simulation_ready=1`, `environment_weather_simulation_production_solver_ready=1`, `environment_weather_simulation_backend_parity_ready=1`, and `environment_weather_simulation_validation_failures=0` after CPU/D3D12/Vulkan/Metal solver parity, 13 coupled physical fields, 12 canonical datasets, 12 canonical images, conservation/stability thresholds, and zero validation/budget/visual/inference/native-handle failures. It keeps `environment_ready=0` and `environment_commercial_ready=0`; Android Vulkan, strict Vulkan aggregate commercial v2, unconditional all-platform readiness, commercial aggregate, and broad `environment_ready` remain unclaimed or host-gated. iOS Metal, Linux Vulkan, production artist workflow, and broad optimization are promoted later by exact hosted/artifact evidence.

2026-06-19 Task 11 update: `environment-artist-workflow-production-closeout` now routes through `tools/validate-environment-artist-workflow-production.ps1 -RequireReady` plus `EnvironmentArtistWorkflowProductionCloseoutDesc` / `evaluate_environment_artist_workflow_production_closeout`. It emits `environment_artist_workflow_production_ready=1` after 14 package-visible workflow rows for OpenEXR import, KTX2/Basis import, glTF material import, USD/MaterialX/OCIO review, package cooking, D3D12/Vulkan/Metal-host live preview, weather timeline editing, preset batch apply, validation report review, profiler artifact review, undo/redo revision safety, and operator review. It keeps `environment_ready=0` and `environment_commercial_ready=0`; Android Vulkan, strict Vulkan aggregate commercial v2, unconditional all-platform readiness, commercial aggregate, and broad `environment_ready` remain unclaimed or host-gated. iOS Metal, Linux Vulkan, and broad optimization are promoted later by exact hosted/artifact evidence.

Retained selected environment commercial status: Phase 3 slices 17-22 add selected D3D12 WARP and strict Vulkan RGBA8 runtime upload/readback execution, selected D3D12 WARP and strict Vulkan backend-target BC7 compressed upload/readback execution, Metal ASTC 4x4 planner/native-owner scaffold, and host-gated selected Apple-host Metal ASTC compressed package upload/readback counters through `execute_runtime_environment_texture_payload_upload`, `RuntimeEnvironmentTextureBackendPayloadUploadPlanResult`, `Format::astc_4x4_*`, `upload_native_texture_bytes`, `read_native_texture_bytes`, package-visible `--require-environment-texture-asset-pipeline-d3d12-upload`, package-visible `--require-environment-texture-asset-pipeline-vulkan-upload`, package-visible `--require-environment-texture-asset-pipeline-d3d12-compressed-upload`, package-visible `--require-environment-texture-asset-pipeline-vulkan-compressed-upload`, and host-gated package-visible `--require-environment-texture-asset-pipeline-metal-compressed-upload` counters. The Metal lane records official Apple Metal Feature Set Tables / `MTLPixelFormatASTC_4x4_sRGB` evidence, `MTLBlitCommandEncoder copyFromBuffer` upload, compact readback, texture usage rows, 4x4/16-byte footprint rows, and zero public native-handle/strict-Vulkan/backend-parity/broad-ready counters on Apple hosts. Phase 3 also retains optional-gated real OpenEXR scanline pixel decode and KTX2/Basis selected RGBA8 transcode evidence; default builds fail closed without optional dependencies. Task 10 closeout is the current physical-weather ready claim, and Phase 11 slice 5 keeps the editor-core visible artist workflow execution/review value rows. Selected package-visible backend parity readiness is promoted by Phase 12D and the Task 6 backend parity v2 closeout, exact macOS Metal platform readiness by Phase 12E, iOS Metal by Task 13, Linux Vulkan by Task 28, broad optimization by Task 17, production artist workflow by Task 11, and Android Vulkan plus the final commercial aggregate by Task 32. Broad `environment_ready` remains unclaimed. `unsupportedProductionGaps` stays `[]`.

Retained Environment Highest Commercial Readiness v1 closeout evidence: Tasks 1-32 have replaced the highest-commercial skeleton set with exact validators/producers and retained evidence for the official-source gate, clean-break commercial v2 contract, Linux/Android/iOS platform host rows, backend parity v2, retained cross-backend optimization artifacts, production AAA preset asset library, full OpenEXR/KTX2/Basis asset pipeline, physical weather simulation, production artist workflow, final aggregate, D3D12, strict Vulkan, Apple-host Metal retained optimization artifacts, broad optimization dependency-row promotion, hosted Linux Vulkan readiness, official Android NDK packaged validation-layer readiness, Android ABI matching, and Android Emulator primary `x86_64` package/readback/log evidence. Task 12 routes `environment-highest-commercial-readiness-closeout` through `tools/validate-environment-highest-commercial-readiness.ps1`; the gate builds/runs `MK_environment_commercial_readiness_v2_tests`, reads all 16 clean-break commercial v2 dependency rows from `environmentCommercialClaimMatrix`, consumes `tools/validate-environment-optimization-artifacts.ps1` for 21 retained optimization measurement rows, and now emits `environment_highest_commercial_ready=1` plus `environment_commercial_ready=1` with `environment_commercial_ready_rows=16`, `environment_host_gated_rows=0`, `environment_dependency_gated_rows=0`, `environment_blocked_rows=0`, `environment_unsupported_rows=0`, `environment_missing_rows=0`, `environment_native_handle_access=0`, `environment_commercial_diagnostics=0`, `environment_all_platform_unconditional_ready=1`, physical-weather backend parity, visible-shell workflow execution, `runtime_source_parsing=0`, and `environment_ready_unchanged=1`. Environment Commercial Excellence v1 is retained as selected input evidence only; it does not rename, bridge, or implicitly carry selected rows into commercial v2. Broad `environment_ready` remains intentionally unclaimed. Long capability prose below is retained historical/static-check evidence and should not override this summary.

Retained selected Environment System evidence: Environment System v1 completed selected D3D12 rain package evidence through `--require-environment-precipitation-package-evidence`, `environment_precipitation_status=ready`, and stable particle/depth/sampler/constants binding slots `8/9/8/7`; it also retained strict host-gated Vulkan height-fog shader/tooling evidence through `MK_VULKAN_TEST_HEIGHT_FOG_DEPTH_VERTEX_SPV`, `MK_VULKAN_TEST_HEIGHT_FOG_DEPTH_FRAGMENT_SPV`, `MK_VULKAN_TEST_HEIGHT_FOG_VERTEX_SPV`, `MK_VULKAN_TEST_HEIGHT_FOG_FRAGMENT_SPV`, and `VK_LAYER_KHRONOS_validation`. Later selected recipes add narrower environment rows, but broad backend parity, broad optimization, and broad `environment_ready` remain unclaimed unless a selected recipe explicitly proves a narrower row.
MAVG Streamed Cluster GPU Upload v1 (`mavg-streamed-cluster-gpu-upload-v1`) adds `mavg_streamed_cluster_gpu_upload.hpp`, `RuntimeMavgStreamedClusterGpuUploadResult`, and `upload_runtime_mavg_streamed_cluster_pages` so committed `RuntimeMavgClusterStreamingSafePointAdoptionResult` rows plus `RuntimeResidentCatalogCacheV2` resident mesh rows and caller-owned page payloads can publish page-level `MeshGpuBinding` rows through existing runtime mesh upload. It preserves fail-closed non-claims for candidate reload, streaming-state mutation, DirectStorage, backend execution, mesh shaders, native handles, async-overlap/performance proof, Nanite readiness, and broad optimization.
MAVG Streaming Upload Overlap Evidence v1 (`mavg-streaming-upload-overlap-evidence-v1`) adds `mavg_streaming_upload_overlap_evidence.hpp`, `RuntimeMavgStreamingUploadOverlapEvidenceDesc`, `RuntimeMavgStreamingUploadOverlapEvidenceResult`, `RuntimeMavgStreamingUploadEvidenceWindow`, `background_load_window`, `gpu_upload_window`, and `plan_runtime_mavg_streaming_upload_overlap_evidence` so callers can record deterministic temporal overlap from caller-owned timing windows between successful closeout background-load evidence and streamed-cluster GPU upload evidence after safe-point adoption. It reports `recorded_temporal_overlap_evidence` and `overlap_tick_count` while keeping `claimed_speedup=false` and `proved_async_overlap_performance=false`, preserving fail-closed non-claims for persistent/autonomous streaming services, DirectStorage, backend execution, mesh shaders, Metal readiness, Nanite equivalence/superiority, native handles, measured speedup, broad async-overlap/performance proof, and broad optimization.
MAVG Streamed Cluster Backend Draw Execution v1 (`mavg-streamed-cluster-backend-draw-execution-v1`) adds `RuntimeMavgStreamedSceneLodSubmitDesc` and `plan_runtime_mavg_streamed_scene_lod_mesh_commands` in `MK_runtime_scene_rhi` so ready streamed page `MeshGpuBinding` rows become page-asset `MeshCommand` rows and range-aware `RhiFrameRenderer` indexed draws. It preserves fail-closed non-claims for candidate reload, streaming-state mutation, DirectStorage, autonomous streaming services, mesh shaders, native handles, async-overlap/performance proof, Nanite readiness, and broad optimization.
MAVG Streaming Upload Overlap Evidence v1 (`mavg-streaming-upload-overlap-evidence-v1`) adds `mavg_streaming_upload_overlap_evidence.hpp`, `RuntimeMavgStreamingUploadOverlapEvidenceDesc`, `RuntimeMavgStreamingUploadOverlapEvidenceResult`, `RuntimeMavgStreamingUploadEvidenceWindow`, `background_load_window`, `gpu_upload_window`, and `plan_runtime_mavg_streaming_upload_overlap_evidence` so callers can record deterministic temporal overlap from caller-owned timing windows between successful closeout background-load evidence and streamed-cluster GPU upload evidence after safe-point adoption. It reports `recorded_temporal_overlap_evidence` and `overlap_tick_count` while keeping `claimed_speedup=false` and `proved_async_overlap_performance=false`, preserving fail-closed non-claims for persistent/autonomous streaming services, DirectStorage, backend execution, mesh shaders, Metal readiness, Nanite equivalence/superiority, native handles, measured speedup, broad async-overlap/performance proof, and broad optimization.
Retained exact MAVG runtime LoD roadmap tokens include `mavg_page_streaming.hpp`, `RuntimeMavgPageStreamingPlanResult`, `RuntimeMavgPageStreamingDrainResult`, `plan_runtime_mavg_page_streaming_requests`, `RuntimeMavgResidentPageMountRow`, `RuntimeMavgPageStreamingSelectedClusterRow`, `RuntimeMavgPageStreamingEvictionReviewResult`, `review_runtime_mavg_page_streaming_evictions`, `execute_runtime_mavg_page_streaming_request_safe_point`, `RuntimeMavgPageStreamingBackgroundLoadDesc`, `RuntimeMavgPageStreamingBackgroundLoadResult`, `RuntimeMavgPageStreamingBackgroundLoadedRow`, `dispatch_runtime_mavg_page_streaming_background_loads`, `RuntimeMavgPageStreamingBackgroundServiceState`, `RuntimeMavgPageStreamingBackgroundServiceTickResult`, `tick_runtime_mavg_page_streaming_background_service`, `RuntimeMavgPageStreamingAutomaticEvictionPlanDesc`, `plan_runtime_mavg_page_streaming_automatic_evictions`, `planned_automatic_eviction_policy`, `automatic_eviction_candidate_count`, `protected_eviction_candidate_skip_count`, MAVG Runtime Inferred Page Use Generation v1, `RuntimeMavgPageStreamingRecencyRow`, `RuntimeMavgResidentPageUseGenerationDesc`, `RuntimeMavgResidentPageUseGenerationResult`, `infer_runtime_mavg_resident_page_use_generations`, `mavg_conventional_upload.hpp`, `RuntimeMavgConventionalMeshUploadResult`, `upload_runtime_mavg_conventional_mesh_binding`, `mavg_gpu_culling.hpp`, `MavgGpuCullingIndirectPlan`, and `plan_mavg_gpu_culling_indirect_commands`, plus `mavg_gpu_memory_residency.hpp`, `RuntimeMavgGpuMemoryResidencyDesc`, `RuntimeMavgGpuMemoryResidencyResult`, and `plan_runtime_mavg_gpu_memory_pressure_residency`; the retained D3D12/Vulkan GPU-culling ledger includes selected `ExecuteIndirect` and count-buffer Vulkan execution evidence while keeping the value-only planner boundary explicit. These remain caller-reviewed and value-only where applicable, preserve selected/fallback page protection, persistent caller-owned service tick state, and deterministic automatic candidate ordering without runtime-inferred LRU/frequency eviction policy, and do not claim autonomous background package scheduling, mesh shaders, or Nanite readiness.

Retained MAVG RHI indirect draw evidence includes `mavg-rhi-indirect-draw-v1`, `indirect_draw.hpp`, `IndexedIndirectDrawDesc`, `BufferUsage::indirect`, Null RHI argument/count-buffer execution, selected D3D12 `ExecuteIndirect`, and count-buffer Vulkan execution. It remains selected D3D12/Vulkan evidence and does not claim mesh shaders, Metal readiness, Nanite equivalence/superiority, or broad optimization.

Retained MAVG indirect/culling execution child evidence includes `mavg-d3d12-indexed-indirect-draw-execution-v1` for D3D12 `ExecuteIndirect` with CPU-generated upload argument buffers and count-buffer follow-up non-claims, `mavg-vulkan-indexed-indirect-draw-execution-v1` for Vulkan `vkCmdDrawIndexedIndirect` with CPU-generated upload argument buffers and count-buffer follow-up non-claims, `mavg-d3d12-count-buffer-indirect-execution-v1` for D3D12 `ExecuteIndirect` count-buffer execution, `mavg-vulkan-count-buffer-indirect-execution-v1` for Vulkan `vkCmdDrawIndexedIndirectCount`, `mavg-gpu-culling-dispatch-v1` for D3D12 compute dispatch through `dispatch_mavg_gpu_culling_indirect`, `MavgGpuCullingIndirectPlan`, and `MK_mavg_gpu_culling_dispatch_tests`, `mavg-d3d12-compute-generated-indirect-consumption-v1` for D3D12 compute-generated indirect consumption through `MK_mavg_d3d12_compute_generated_indirect_consumption_tests` and PR #560, `mavg-vulkan-gpu-culling-dispatch-v1` for Vulkan `VK_KHR_synchronization2` compute dispatch through `MK_mavg_vulkan_gpu_culling_dispatch_tests`, and `mavg-vulkan-compute-generated-indirect-consumption-v1` / MAVG Vulkan Compute-Generated Indirect Consumption v1 through `MK_mavg_vulkan_compute_generated_indirect_consumption_tests`, `is_compute_generated_indexed_indirect_buffer`, and `leave_indirect_argument_state_for_consumption`. The retained non-claims remain package-visible MAVG backend readiness, Vulkan compute dispatch where only D3D12 was proven, MAVG Vulkan Compute-Generated where only pre-consumption Vulkan dispatch was proven, compute-generated where only CPU upload was proven, mesh shaders, Metal readiness, Nanite readiness, and broad optimization.
MAVG Resident Page Recency Eviction Order v1 (`mavg-resident-page-recency-eviction-order-v1`) adds caller-supplied recency through `RuntimeMavgPageStreamingAutomaticEvictionPolicyKind::caller_supplied_recency`, `RuntimeMavgPageStreamingRecencyRow`, `resident_page_last_used_generation`, `applied_caller_supplied_recency_policy`, `recency_eviction_candidate_count`, `duplicate_recency_row_count`, and `missing_recency_row_count` without runtime-inferred LRU/frequency eviction policy, DirectStorage execution, or Nanite superiority claims; MAVG GPU Memory Pressure Residency v1 can feed that reviewed eviction planner with a renderer-policy-derived resident byte budget, but does not mutate mounts, refresh catalogs, upload GPU resources, or execute backend memory eviction. MAVG Cluster Streaming Safe Point Adoption v1 (`mavg-cluster-streaming-safe-point-adoption-v1`) adds `mavg_cluster_streaming_safe_point_adoption.hpp`, `RuntimeMavgClusterStreamingSafePointAdoptionResult`, and `execute_runtime_mavg_cluster_streaming_safe_point_adoption` so successful closeout `background_load.loaded_rows` can be adopted into caller-owned `RuntimeResidentPackageMountSetV2` mounts and `RuntimeResidentCatalogCacheV2` through `plan_runtime_resident_package_evictions_v2` at a safe point, without reloading package candidates, DirectStorage, GPU upload, backend execution, renderer/RHI handle access, mesh shaders, Nanite readiness, or broad optimization.
MAVG Payload Byte-Range Page Loader v1 (`mavg-payload-byte-range-page-loader-v1`) adds `mavg_cluster_payload_format_v1`, graph `invalid_page_byte_range` validation, `mavg_payload_page_loader.hpp`, `RuntimeMavgPayloadPageLoadDesc`, `RuntimeMavgPayloadPageLoadResult`, `RuntimeMavgPayloadPageRow`, and `load_runtime_mavg_payload_pages` so reviewed `.mavgpayload` page byte ranges can be copied deterministically from caller-owned payload bytes. MAVG Payload Filesystem Byte-Range IO v1 (`mavg-payload-filesystem-byte-range-io-v1`) adds `IFileSystem::read_binary_range`, `RuntimeMavgPayloadFilesystemPageLoadDesc`, and `load_runtime_mavg_payload_pages_from_filesystem` for first-party filesystem byte-range reads. MAVG DirectStorage Page IO Execution v1 (`mavg-directstorage-page-io-execution-v1`) adds `IByteRangeIoExecutor`, `ByteRangeIoBackendKind::direct_storage`, `RuntimeMavgPayloadDirectStoragePageLoadDesc`, and `load_runtime_mavg_payload_pages_from_direct_storage` so reviewed page ranges can execute through a caller-owned DirectStorage byte-range executor while failing closed without filesystem fallback. MAVG Win32 DirectStorage SDK Adapter v1 (`mavg-win32-directstorage-sdk-adapter-v1`) completes the first-party Windows implementation over Microsoft DirectStorage SDK 1.3.0 through private `MK_platform_win32` SDK objects, `Win32DirectStorageByteRangeExecutor`, system-memory destinations, and package counters proving two completed ranges with zero native handle exposure, GPU destinations, GDeflate, or async-overlap/performance proof. MAVG Background Streaming Dispatch v1 (`mavg-background-streaming-dispatch-v1`) adds `RuntimeMavgPageStreamingBackgroundLoadDesc`, `RuntimeMavgPageStreamingBackgroundLoadedRow`, `RuntimeMavgPageStreamingBackgroundLoadResult`, and `dispatch_runtime_mavg_page_streaming_background_loads`; MAVG Persistent Background Streaming Service v1 (`mavg-persistent-background-streaming-service-v1`) adds `RuntimeMavgPageStreamingBackgroundServiceState`, `RuntimeMavgPageStreamingBackgroundServiceTickResult`, and `tick_runtime_mavg_page_streaming_background_service` without autonomous background streaming, async-overlap/performance proof, DirectStorage default execution beyond the selected Win32 SDK adapter, GPU memory pressure mutation, mesh shaders, Metal readiness, Nanite readiness, or broad optimization.

Retained MAVG residency closeout evidence includes `mavg-gpu-memory-pressure-residency-v1` / MAVG GPU Memory Pressure Residency v1 through `mavg_gpu_memory_residency.hpp`, `RuntimeMavgGpuMemoryResidencyResult`, `plan_runtime_mavg_gpu_memory_pressure_residency`, `GpuMemoryPolicyPlan`, and `RuntimeResourceResidencyBudgetV2::max_resident_content_bytes`; `mavg-cluster-streaming-residency-closeout-v1` / MAVG Cluster Streaming Residency Closeout v1 through `mavg_cluster_streaming_residency_closeout.hpp`, `RuntimeMavgClusterStreamingResidencyCloseoutResult`, `plan_runtime_mavg_cluster_streaming_residency_closeout`, `load_runtime_mavg_payload_pages_from_filesystem`, `dispatch_runtime_mavg_page_streaming_background_loads`, `plan_runtime_mavg_gpu_memory_pressure_residency`, and deterministic degradation; and `mavg-cluster-streaming-safe-point-adoption-v1` / MAVG Cluster Streaming Safe Point Adoption v1 through `mavg_cluster_streaming_safe_point_adoption.hpp`, `RuntimeMavgClusterStreamingSafePointAdoptionResult`, `execute_runtime_mavg_cluster_streaming_safe_point_adoption`, `background_load.loaded_rows`, `RuntimeResidentPackageMountSetV2`, `RuntimeResidentCatalogCacheV2`, and `plan_runtime_resident_package_evictions_v2`. These rows do not execute DirectStorage by default, GPU upload where explicitly excluded, backend execution, async-overlap/performance proof, mesh shaders, Nanite readiness, or broad optimization.
Environment System v1 is completed historical evidence for a clean first-party environment system through `MK_environment`, scene/runtime binding, renderer policy packets, package evidence, and first-party editor-core authoring. Current environment follow-up claims are the selected recipe rows recorded in the manifest and plan registry. `desktop-runtime-sample-game-environment-ready-aggregate` completed through PR #588 and proves selected D3D12 primary `environment_ready=1` only while keeping strict Vulkan aggregate readiness, Metal host aggregate readiness, backend parity, broad optimization, snow-by-inference, native/backend handles, and all-platform readiness unclaimed. Each new slice must refresh official docs/Context7 for the APIs it touches and must not infer Vulkan or Metal readiness from D3D12 evidence, reintroduce SDL3 or Dear ImGui, expose native/backend handles, or select optional OpenEXR/KTX-class dependencies without dependency/legal/vcpkg/package proof.

The completed editor shell capability remains current evidence: private visible D3D12 viewport display, `editor_shell_panels=11`, `editor_shell_docking_status=single_window_ready`, `editor_shell_dock_tab_headers=11`, `editor_shell_dock_split_gutters=3`, `editor_shell_dock_active_panels=4`, `editor_shell_dock_focusable_controls=11`, `editor_shell_viewport_status=d3d12_texture_ready`, `editor_shell_viewport_visible_texture_composites`, `editor_shell_viewport_native_handles_exposed=0`, and visible compositor presentation stay on the first-party `desktop-editor` lane.

Renderer Metal Apple Host Validation Recipe v1 subsequently completed through PR #385 / merge commit `b5e1fdac6cde3250ccddfb4d46143928bc98fd06`, adding the reviewed `renderer-metal-apple-host-evidence` recipe. Renderer Metal CI Host Recipe v1 then completed through PR #386 / merge commit `12fde1bf7ea369e97a45d95a0ee271b4e2515b3d`, wiring the hosted `macOS Metal CMake` lane to run that recipe before the full `ci-macos-appleclang` configure/build/CTest path; the hosted log reached `apple-host-evidence-check: ready` and `renderer-metal-apple-check: ok`. `metal-apple`, broad Metal parity, and broad renderer quality remain host-gated or unclaimed.

Previous optimization-foundation closeouts: Job Execution Worker Pool v1 completed through PR #381 / merge commit `8c6a56f8`; it added a first-party C++23 `MK_core` worker-thread pool with bounded queue execution, RAII `std::thread` lifetime, cooperative stop, `execute(batch)` drain semantics, worker-local scratch, diagnostic evidence, and selected package-visible `job_execution_foundation_*` counters. Job Execution Topology Policy v1 completed through PR #382 / merge commit `f277e990dfc27afea4492c7a1990969fd47a7c39`; it adds portable `MK_core` worker-count selection over observed logical processors, fallback/cap/reserve rules, derived `JobExecutionPoolDesc`, processor-group and NUMA host-evidence diagnostics, and selected `sample_desktop_runtime_game --require-job-execution-topology-policy` package-visible `job_execution_topology_policy_*` counters. Job Execution Work Stealing v1 completed through PR #389 / merge commit `ec6239873e2997bd5a42272683f8e191bcd3905f`; it adds opt-in worker-queue stealing and selected package-visible `job_execution_work_stealing_*` evidence without OS affinity or vendor side effects. Job Execution Placement Policy v1 adds host-independent placement policy rows, ready OS-default selection, fail-closed NUMA/hybrid/SMT/manual-affinity diagnostics, and selected package-visible `job_execution_placement_policy_*` evidence without OS affinity, NUMA allocation, SIMD, GPU async, CUDA, HIP, or SYCL execution side effects. Windows CPU Set Worker Placement v1 adds host-owned Windows CPU Sets worker-start placement through value-only `MK_core` callback rows, `MK_platform_win32` CPU Set query/selection/application rows, and selected package-visible `windows_cpu_set_worker_placement_*` evidence without native thread handle exposure or Linux affinity / NUMA allocation / hybrid-SMT / SIMD / GPU async / CUDA / HIP / SYCL side effects. Frame/Thread Scratch v1 completed through PR #373, PR #374, and PR #375 / merge commit `c37d3ea2`; it added scratch diagnostic counters, first-party `ScratchArena` / `ScratchLease` APIs, safe-point reset evidence, false-sharing diagnostics, and selected package-visible framegraph aliasing evidence. Memory Diagnostics v1 completed the dependency-free `MK_core` memory diagnostics model and selected package-visible `memory_diagnostics_*` evidence for package resident CPU, resident GPU, upload/staging, and transient GPU classes. Memory Lifetime Taxonomy v1 completed the ownership semantics, raw pointer non-ownership, `std::unique_ptr`, `std::span`, backend handle opacity, allocator/job/NUMA/GPU memory boundary, and memory-class taxonomy prerequisite for memory diagnostics while keeping broad CPU/GPU/memory optimization unsupported. Performance Baseline v1 completed the first reproducible measurement/evidence lane with selected package frame budgets, p95/p99 diagnostics counters, `installed-2d-performance-baseline-smoke`, Trace Event JSON artifact references, docs, manifest rows, and static checks while keeping broad CPU/GPU/memory optimization and cross-vendor/backend parity unsupported. AI-Operable Performance Budget And Evidence v1 completed through PR #361 / merge commit `e344326a270d39526be7e56d989bf96d86c01808`; it added schema-backed `game.agent.json.performanceBudgets`, manifest `performanceBudgetEvidenceLoops`, selected sample/template rows, docs, and static checks so AI-generated games can distinguish budget/evidence-backed optimization from unsupported broad CPU/GPU/memory claims. First-Party UI Editor Production Stack v1 remains the previous editor/UI closeout; Phase 9 locks `editor.cross_platform.adapter.*` future gates for Core Text/InputMethodKit/NSAccessibility, AT-SPI/IBus/Fcitx, Android InputMethodService/accessibility, iOS UITextInput/UIAccessibility, and HarfBuzz/FreeType/ICU-class dependency adapters. Direct2D GPU text rendering, broad editable rich text, full UIA parity, cross-platform accessibility/editor parity, Vulkan/Metal editor texture-display parity, broader material-preview GPU parity, and public native handles remain future focused selections.

## Principle

Build a clean C++23 engine with no early backward-compatibility burden. Prefer official platform guidance, explicit module boundaries, deterministic tests, and AI-readable contracts. Separate developer-facing tooling from production runtime UI: the completed SDL3 removal stays closed, the completed Dear ImGui visible editor shell has been replaced by a first-party retained editor shell, `MK_editor_core` remains the supported editor logic surface with core-backed `EditorDockLayout` / `editor_dock_panel_catalog` dock and panel identity rows plus value-only dock command planners for show/hide/activate/move/split/reset and clean-break workspace v2 dock persistence, and future editor/game UI surfaces should follow first-party retained contracts similar in role to Unreal Slate/UMG/Common UI, Unity UI Toolkit, and Godot Control while low-level text, IME, accessibility, font, image, and platform services stay behind official-SDK or audited adapters.

## Core-First MVP Closure

`core-first-mvp` is complete for the current MVP scope when the repository default build/test/agent contract is validated and the remaining work is explicitly classified as host-gated or next-phase. This is not a claim that GameEngine is a complete commercial game engine.

The closure plan is `Core First MVP Closure`. Closure requires `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/validate.ps1`, `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/build.ps1`, agent/schema checks, and API-boundary verification where public API or backend interop surfaces are touched. The current MVP foundation includes the source-tree default validation lane, the optional Win32 desktop game runtime lane, manifest-driven desktop runtime package metadata, generated desktop package scaffolds, cooked-scene package scaffolds, material/shader package scaffolds, and Windows D3D12 plus Windows/toolchain-gated Vulkan proofs behind private backend ownership and first-party diagnostics.

The following remain outside the MVP closure claim: Apple-hosted iOS simulator/device validation, device signing, backend-neutral Metal visible presentation, production material/shader graph authoring, live shader generation, broader authored/cooked packaged-game conventions, production text/font/IME/accessibility/image adapters, richer postprocess and production shadow systems, broad GPU skinning/backend compute morph parity beyond the reviewed D3D12 lanes, navmesh asset import and persistent/full crowd simulation, advanced physics controllers, dynamic-vs-dynamic TOI, rotational CCD, rotational rigid-body constraints, 2D CCD, persistent joint assets, broad vehicle dynamics/persistent vehicle simulation APIs, ragdolls, and broader authored collision assets, streaming codec integration, HRTF execution/parity, broad DSP graph execution, and crash/telemetry/profiling/trace workflows beyond the current diagnostics and GPU marker foundations.

## Desktop Runtime Productization

`Desktop Runtime Productization` is complete as the first practical Windows desktop vertical slice after `core-first-mvp`: source-tree desktop runtime validation, the default desktop runtime package lane, and the selected `sample_desktop_runtime_game` package lane with installed config, cooked scene package files, target-specific shader artifacts, scene GPU binding reports, depth-aware postprocess status, and fixed 3x3 PCF directional-shadow smoke reports.

Optional vcpkg-backed lanes now use explicit dependency bootstrap rather than configure-time restore: `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/bootstrap-deps.ps1` owns `vcpkg install` for `desktop-runtime`, `asset-importers`, `physics-jolt`, and `network-enet`, while CMake presets consume `${sourceDir}/vcpkg_installed` with `VCPKG_MANIFEST_INSTALL=OFF`. The first-party `desktop-editor` lane is dependency-free and does not have a Dear ImGui or UI-middleware vcpkg feature.

D3D12 is the primary verified Windows package lane for this slice. The current Windows host also validates the strict selected Vulkan package command, but Vulkan remains host/runtime/toolchain-gated for other environments. Apple/iOS/Metal productization, production renderer quality, material/shader graph authoring, live shader generation, broader authored/cooked packaged-game conventions, editor UX expansion, concrete runtime UI adapters, telemetry/crash reporting, allocator diagnostics, and GPU marker adapters remain next-phase work.

## AI-Operable Production Roadmap

The next broad production direction is `docs/specs/2026-05-01-ai-operable-game-engine-production-roadmap-v1-design.md`. It organizes the next phase around AI-operable game creation for both 2D and 3D games, with clean breaking changes allowed where MVP-era contracts would block a production data model.

The current project-wide completion roadmap is `docs/superpowers/master-plans/2026-05-03-production-completion-master-plan-v1.md`. It rechecks the repository after the post-2026-05-02 slices, maps every manifest-level unsupported production gap and host gate into phase-gated child-plan candidates, and keeps broad or host-gated work out of ready claims until focused validation evidence exists. The 2026-05-19 gameplay/physics/navigation/AI gap-cluster plan is completed: deterministic traversal now includes scene-ref navmesh dynamic-obstacle routing, advanced movement/physics interaction uses first-party policy counters, and selected 2D/3D package gameplay-system smokes provide integrated evidence while broad navmesh import, persistent/full crowd simulation beyond value-only batch planning, animation-aware steering, networked crowd determinism, native physics middleware, and broad generated-game production readiness remain future work. Phase 4 (renderer/material/shader/quality) and Phase 5 (source import/atlas/cook foundations) closure sequencing was tracked in the phase-gated milestone `Phase 4 5 Closure Milestone v1` (now **completed**; see `Phase 4 5 Milestone Closure Evidence Index v1`). Future phase-gated milestones should use new dated plans rather than reopening this ledger row. The previous `Engine Excellence Roadmap` remains historical wave evidence and should not receive new production tasks.

`Engine 1.0 Gap Matrix v1` completed the docs/manifest governance selection slice, the selected `renderer-backend-parity-v1` strict Vulkan follow-up landed package-generated mapping compute SPIR-V proof plus reviewed validation-recipe execution, and `Renderer Production VFX Profiling Host Gate v1` completed selected D3D12 and strict Vulkan host-evidence-backed renderer/VFX/profiling counters. `Engine General Production Quality Expansion v1` is now completed as the selected post-1.0 production breadth milestone for renderer quality, runtime UI text/font/IME/accessibility, broad reviewed import, physics/navigation breadth, networking execution/security, audio production evidence, generated package proof, and final agent-surface closeout. Phase 1 added `Renderer General Quality Matrix v1` through `plan_renderer_quality_matrix` and `renderer_quality_matrix_*` package counters for backend-local D3D12/Vulkan readiness plus Metal host-gated rows with `renderer_quality_matrix_status=host_evidence_required`, `renderer_quality_matrix_dependency_gated_rows=0`, `renderer_quality_matrix_unsupported_rows=0`, and `renderer_quality_matrix_general_renderer_quality_ready=0` without claiming general renderer quality; the clean-break follow-up completed explicit ready/host-gated/dependency-gated/unsupported row taxonomy, backend-local renderer parity proof rows, selected debug CPU zone / trace handoff evidence, GPU memory budget / residency pressure evidence, selected package counters, and hosted PR evidence through PR #261, PR #262, and PR #263. Runtime UI Text Platform Stack v1 completed through PR #264, PR #265, PR #266, PR #267, and PR #268 while broad runtime UI platform parity remains host/dependency-gated or unclaimed. `First-Party Desktop Platform And SDL3 Removal v1` is completed as the Windows-first clean-break desktop milestone: active SDL3 source, tests, vcpkg dependency records, CMake/export/install hooks, ready claims, and agent contracts were removed or retargeted to first-party Win32/WASAPI modules. `Reviewed Importers Codecs And Shader Generation v1` is completed for selected KTX2/Basis review, glTF scene import review, source image/audio codec review, and reviewed shader generation/cache evidence rows. `Physics Navigation Commercial Coverage v1` is completed evidence for optional adapter boundary gates over physics/navigation breadth rows. `Renderer Backend Parity Metal Apple Evidence v1` is completed through PR #296 with local agent/static validation and hosted PR Gate, macOS Metal CMake, iOS Simulator smoke, Windows MSVC, Linux CMake, full repository static analysis, and CodeQL evidence; `unsupportedProductionGaps = []` remains the current Engine 1.0 ready-surface truth. `Renderer Postprocess Tone Mapping Evidence v1` is completed through PR #298 with local `tools/validate.ps1` evidence and hosted PR Gate, Agent Static Guards, Windows MSVC, Linux CMake, Linux Coverage, Linux Clang ASan/UBSan, full repository static analysis, macOS Metal CMake, iOS Simulator smoke, and CodeQL evidence; it narrows `renderer-postprocess-v1` with backend-local HDR/tone-mapping evidence rows while keeping Metal host-gated, native handles hidden, SDL3 absent, and broad postprocess quality unclaimed. `Generic 2D Sandbox Runtime Foundation v1` adds deterministic in-memory `build_runtime_sandbox_world`, `sample_runtime_sandbox_cell`, and `snapshot_runtime_sandbox_world` evidence over reviewed chunk/cell rows. `Generic 2D Sandbox Mutation Execution v1` adds `RuntimeSandboxWorldMutationExecutionStatus`, `RuntimeSandboxWorldDirtyRegion`, `RuntimeSandboxWorldMutationExecutionResult`, and `apply_runtime_sandbox_world_mutations` for copied-world placement/destruction execution with deterministic dirty regions and explicit zero side-effect flags while keeping persistence IO, package loading, streaming execution, renderer/platform/editor APIs, threads, native handles, and game-specific content rules out of the runtime contract. `Generic 2D Sandbox Production Tile Renderer v1` adds `plan_tile_chunk_renderer` with deterministic visible-cell sprite rows, opaque/transparent draw grouping, dirty rebuild budget rows, renderer-neutral lightmap rows, selected 2D package `--require-production-tile-renderer` `tile_chunk_renderer_*` counters, and explicit zero backend submission/native texture ownership invocation while keeping renderer/RHI residency, native handles, SDL3, and broad renderer quality out of the claim. `Sandbox World Network And Modding Gate v1` is the active child plan for sandbox mutation/delta replication review and reviewed modding policy rows; `unsupportedProductionGaps = []` remains intact, SDL3 remains excluded, and broad online multiplayer/modding readiness remains unclaimed.

`Generic 2D Sandbox Tile Simulation Primitives v1` adds `RuntimeSandboxTileMaterialRow`, `RuntimeSandboxTileSimulationDesc`, `RuntimeSandboxTileSimulationPlan`, `RuntimeSandboxTileCollisionSpanRow`, `RuntimeSandboxLightPropagationRow`, `RuntimeSandboxLiquidFlowRow`, `RuntimeSandboxScheduledTileUpdateRow`, and `plan_runtime_sandbox_tile_simulation` for value-only tile collision, platform, liquid, trigger, light propagation, bounded liquid-flow, scheduled-update, and replay-hash planning over already-built runtime sandbox worlds. It keeps physics upload, renderer lightmap upload, full fluid simulation quality, persistence IO, package loading, streaming execution, renderer/platform/editor APIs, threads, native handles, legacy desktop middleware, and game-specific content rules outside the claim.

`Generic 2D Sandbox Persistence Snapshot v1` adds `RuntimeSandboxWorldPersistenceDocumentPlan`, `RuntimeSandboxWorldMigrationReviewPlan`, `RuntimeSandboxWorldAtomicSavePlan`, `plan_runtime_sandbox_world_persistence_document`, `plan_runtime_sandbox_world_snapshot_diff`, `review_runtime_sandbox_world_migration`, and `plan_runtime_sandbox_world_atomic_save` for value-only canonical snapshot text, chunk/layer/changed-cell rows, dirty-chunk diff planning, schema migration/recovery review, safe project-relative atomic-save operation intent, replay hashes, and explicit zero filesystem/platform/thread side-effect flags over already-built runtime sandbox worlds. It keeps actual filesystem writes, Win32 execution, `IFileSystem` execution, package mutation, migration execution, streaming execution, cloud save, native handles, legacy desktop middleware, and game-specific save serialization outside the claim.

`Generic 2D Sandbox Streaming Residency v1` adds `RuntimeSandboxWorldStreamingPlan`, `RuntimeSandboxWorldStreamingSourceRow`, `RuntimeSandboxWorldAddressableDependencyRow`, `RuntimeSandboxWorldStreamingSafePointDesc`, `RuntimeSandboxWorldStreamingSafePointResult`, `plan_runtime_sandbox_world_streaming`, and `execute_runtime_sandbox_world_streaming_safe_point` for reviewed source-range chunk selection, dirty resident chunk pinning, addressable tile-atlas/biome-material/audio/prefab dependencies, world-region residency budgets, and host safe-point adoption over reviewed package candidates. It is sandbox-specific safe-point evidence, not broad `world-streaming-and-large-scenes` open-world parity; background streaming, async addressable execution, automatic LRU eviction, renderer/RHI residency, native handles, legacy desktop middleware, and game-specific biome/block-art rules remain outside the claim.

`Generic 2D Sandbox Gameplay Integration Rows v1` extends `plan_runtime_sandbox_world_mutation` with `RuntimeSandboxTileDropRow`, `RuntimeSandboxConstructionCostConsumptionRow`, `RuntimeSandboxToolEffectivenessRow`, `RuntimeSandboxSpawnRegionRow`, `RuntimeSandboxDayNightEventRow`, and `RuntimeSandboxTriggerRow` so generic sandbox worlds can hand value-only tile drop, cost consumption, tool category, spawn region, day/night, and trigger evidence to game-owned inventory/crafting/AI/spawn/interaction systems. The selected 2D package now reports `sandbox_world_cost_consumption_rows=1`, `sandbox_world_tile_drop_rows=1`, `sandbox_world_tool_effectiveness_rows=1`, `sandbox_world_spawn_region_rows=1`, `sandbox_world_day_night_event_rows=1`, and `sandbox_world_trigger_rows=1` while rejecting boss/NPC catalogs, hardcoded damage formulas, and economy balance hooks. It does not execute game rules, mutate inventory, spawn actors, write persistence, expose native handles, or reintroduce SDL3.

`Sandbox World Package Validation And Performance Budgets v1` (`sandbox-world-package-validation-performance-budgets-v1`) adds selected package-budget evidence to `sample_2d_desktop_runtime_package`: source-tree and installed smokes can require Win32 runtime host, D3D12 presentation, WASAPI evidence, sandbox-world runtime/persistence/streaming, production tile renderer, production authoring workflow, runtime UI renderer atlas handoff, `--require-sandbox-package-budgets`, and the selected D3D12 `--require-performance-baseline` lane. The package prints deterministic `sandbox_package_budget_*` counters for row/byte limits, chunk bytes, dirty chunks, renderer draw rows, tile draw calls, light rows, persistence rows, streaming load/unload rows, positive replay hash, zero unsupported side effects, plus deterministic `performance_baseline_*` warm-up/sample-count/counter/profile/p95/p99 fields; long-run host soak keeps sprite budget, draw, and texture-bind row accounting representative so package-budget evidence does not become a frame-count accumulator; a source-tree-only `--force-sandbox-package-budget-overflow` CTest proves the fail-closed budget-limited path. This does not mutate packages, execute source-image decoding, open arbitrary platform/native handles, broaden renderer quality, or claim broad CPU/GPU/memory optimization, cross-vendor/backend performance parity, or broad online/modding readiness.

`Renderer Backend Parity Host Recipe Proof v1` tightens the backend parity value contract by requiring `BackendRendererParityProofRow::host_validation_recipe_id` on Metal rows, failing closed with `missing_host_validation_recipe` when the Apple-host proof path is unnamed, and failing closed with `unreviewed_host_validation_recipe` when an arbitrary valid id is supplied. Reviewed recipe ids include `shader-toolchain`, `mobile-packaging`, `renderer-metal-apple-host-evidence`, and `ios-simulator-smoke`; the renderer recipe remains Apple-host-gated and routes through `check-apple-host-evidence.ps1 -RequireReady` plus the `ci-macos-appleclang` `MK_backend_scaffold_tests` / `MK_renderer_quality_matrix_tests` CTest path. `BackendRendererParityAppleMetalEnvironmentEvidenceDesc` and `make_backend_renderer_parity_apple_metal_environment_proofs` now allow that selected Apple-host recipe evidence to feed only backend-local `synchronization`, `shader_validation`, and `package_evidence` proof rows, while `memory_residency` and `profiling_capture` remain separate proof rows. The work keeps `metal-apple` host-gated, keeps native handles hidden, and keeps `unsupportedProductionGaps = []`.

Phase 2 of that plan completed Runtime UI Production Stack Evidence v1: `plan_runtime_ui_production_stack` and `sample_2d_desktop_runtime_package --require-runtime-ui-production-stack` provide side-effect-free selected package counters for text shaping, font rasterization, glyph atlas, renderer handoff, IME, and accessibility evidence, including shaping direction/script/language rows, glyph ids/clusters/advances/offsets/fallback/bidi/line-break rows, glyph bitmap/metrics/pixel-format rows, atlas placement/budget/eviction rows, renderer upload handoff, dependency/host gates, unsupported-claim rejection, and ready/host-gated/dependency-gated/skipped/adapter-invoked/unsupported row counters, while keeping actual production text engines, font dependencies, native IME, OS accessibility, renderer upload execution, and broad platform UI parity host/dependency gated. The renderer atlas handoff slice adds `review_ui_renderer_atlas_handoff` and `sample_2d_desktop_runtime_package --require-runtime-ui-renderer-atlas-handoff` counters over cooked image/glyph UI atlas metadata, placement/budget/eviction rows, texture handoff review rows, renderer sprite submission counters, unsupported-claim rejection, zero side effects, and replay evidence without promoting renderer texture upload APIs, live glyph atlas generation, source image decoding, text shaping, font rasterization, native handles, or broad runtime UI renderer quality.

`Broad Reviewed Asset Import And Cook Pipeline` Phase 3 now has a value-only `MK_assets` review surface through `review_asset_import_production_readiness`. This is a promotion gate for declared source roots, importer ids, selected extension/package/license/provenance/hash evidence, validator/dependency/legal rows, offline shader command review rows, exact `vcpkg.ktx` dependency evidence before KTX2/Basis readiness, exact `toolchain.dxc` plus `toolchain.spirv-tools` evidence before shader rows can be ready, and an execution-readiness matrix for reviewed, dependency-gated, host-gated, package-mutation, and unsupported-claim rows. It is not importer execution, live shader generation, runtime source parsing, external asset download, package mutation, parser/compiler/native handle exposure, broad source image/audio codec support, or broad codec readiness.

`glTF Scene Import Review Package Evidence v1` adds a selected package-evidence review gate through `review_gltf_scene_import_readiness`, and `Generated 3D glTF Scene Import Review Package Smoke v1` exposes the generated 3D package counters behind `--require-gltf-scene-import-review`. The gate requires source-root policy, parser validation, geometry/material/animation payload, external resource policy, source provenance, package output, deterministic hash, selected package evidence, and exact `vcpkg.asset-importers` dependency/legal records before `gltf_scene_import_review_status=ready`; it keeps external network fetches, runtime source parsing, parser type leakage, native handle exposure, package mutation, and broad glTF scene import readiness unsupported.

`Source Image Audio Codec Review Package Evidence v1` adds a selected package-evidence review gate through `review_source_image_audio_codec_readiness`, and generated 2D/3D package smokes expose counters behind `--require-source-image-audio-codec-review`. The gate requires source-root policy, PNG decode rows, RGBA8 pixel-format diagnostics, audio decode rows, sample-format diagnostics, source provenance, package output, deterministic hashes, selected package evidence, and exact `vcpkg.libspng` plus `vcpkg.miniaudio` dependency/legal records before `source_image_audio_codec_review_status=ready`; it keeps SVG/vector decoding, broad image/audio codec readiness, background decode streaming, HRTF/middleware execution, runtime source parsing, native handle exposure, and package mutation unsupported.

`Reviewed Shader Generation Cache Execution v1` adds a selected shader generation/cache review gate through `review_shader_generation_cache_execution`, and the generated material/shader package smoke exposes `shader_generation_cache_*` counters behind `--require-material-graph-authoring`. The gate reviews exact offline DXC D3D12 DXIL and DXC Vulkan SPIR-V command rows, `spirv-val --target-env vulkan1.3` validation rows, target profiles/environments, inputs, outputs, cache keys, artifact provenance, and diagnostics. The default package keeps Vulkan shader generation host-gated with two ready D3D12 rows and two host-gated Vulkan rows; the strict `-RequireVulkanShaders` package recipe proves four ready rows and SPIR-V validation/cache/provenance evidence without claiming Vulkan renderer residency, live shader generation, runtime compiler execution, native cache handles, Metal library generation, or broad shader readiness.

Phase 4 completed first-party physics/navigation production breadth through `review_physics_production_breadth` and `review_navigation_production_breadth`, plus selected 2D/3D package counters for advanced controllers, character dynamics, constraints, kinematic motion, simple vehicle rows, navmesh readiness, crowd readiness, and determinism gates without claiming middleware parity or every commercial genre feature. Phase 5 completed Runtime Network Production Security Gate v1 through `plan_runtime_network_production_security_gate`: threat-modeled value-only session/loopback/replication security evidence, optional ENet loopback host evidence, and selected 2D package `network_production_security_*` counters, while secure transport/authentication, matchmaking, NAT traversal, cloud services, internet-facing execution, rollback execution, and broad online readiness remain unclaimed. Phase 6 completed Audio Production Playback/Streaming/DSP/Spatialization Evidence v1 through `review_audio_production_readiness`, WASAPI lifecycle evidence via `wasapi_audio_device_lifecycle_evidence`, and selected 2D package `audio_production_*` counters with host-gated device/HRTF rows. Phase 7 extended that selected package proof to `sample_generated_desktop_runtime_3d_package --require-audio-production`, using the cooked generated 3D gameplay-audio payload and installed validation for the same audio production counters; broad codec support, middleware parity, HRTF execution parity, background streaming execution, native handles, and general production audio quality remain unclaimed. Phase 8 closed the milestone by reconciling docs, manifest pointers, validation recipes, game guidance, static checks, and host-gated non-claims.

`Generated Game Studio v1` is completed for its successor game-surface milestone. It composes the existing AI game design, generation, package review, validation preflight, evidence import, remediation, quality rubric, and editor AI command surfaces into one reviewable 2D/3D studio loop through `EditorAiGeneratedGameStudioV1Model` / `make_editor_ai_generated_game_studio_v1_model` and retained `generated_game_studio` rows while keeping engine internals, arbitrary shell, native handles, renderer/RHI residency, Metal readiness, and broad editor productization outside the claim. The prior `General Purpose Game Production v1` milestone completed its Phases 1-10; rendering/VFX/profiling evidence remains host-gated for Metal and does not imply broad renderer readiness.

General Purpose Game Production v1 retained evidence includes `genre-rpg-systems-pack-v1` through `plan_runtime_rpg_systems`, `genre-sandbox-world-pack-v1` through `plan_runtime_sandbox_world_mutation` plus `Generic 2D Sandbox Runtime Foundation v1` through `build_runtime_sandbox_world`, `sample_runtime_sandbox_cell`, and `snapshot_runtime_sandbox_world`, `Generic 2D Sandbox Mutation Execution v1` through `apply_runtime_sandbox_world_mutations` and `RuntimeSandboxWorldDirtyRegion`, `Generic 2D Sandbox Gameplay Integration Rows v1` through `RuntimeSandboxTileDropRow`, `RuntimeSandboxConstructionCostConsumptionRow`, `RuntimeSandboxToolEffectivenessRow`, `RuntimeSandboxSpawnRegionRow`, `RuntimeSandboxDayNightEventRow`, and `RuntimeSandboxTriggerRow`, `Generic 2D Sandbox Tile Simulation Primitives v1` through `plan_runtime_sandbox_tile_simulation`, `RuntimeSandboxTileSimulationPlan`, `RuntimeSandboxLightPropagationRow`, `RuntimeSandboxLiquidFlowRow`, and `RuntimeSandboxScheduledTileUpdateRow`, `Generic 2D Sandbox Persistence Snapshot v1` through `plan_runtime_sandbox_world_persistence_document`, `plan_runtime_sandbox_world_snapshot_diff`, `review_runtime_sandbox_world_migration`, `plan_runtime_sandbox_world_atomic_save`, and `RuntimeSandboxWorldAtomicSavePlan`, and `Generic 2D Sandbox Streaming Residency v1` through `RuntimeSandboxWorldStreamingPlan`, `plan_runtime_sandbox_world_streaming`, and `execute_runtime_sandbox_world_streaming_safe_point`; `genre-simulation-management-pack-v1` through `plan_runtime_simulation_management`, `production-network-replication-v1` through `plan_runtime_network_replication`, and `production-rendering-vfx-profiling-v1` through `plan_renderer_production_vfx_profiling` with selected debug profiling CPU zone / trace handoff evidence and GPU memory budget / residency pressure evidence.

Documentation authority split: keep narrowed ready claims and `unsupportedProductionGaps` rows authoritative in `engine/agent/manifest.json` (composed from `engine/agent/manifest.fragments/` via `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/compose-agent-manifest.ps1 -Write`). The production-completion master plan holds the **1.0 definition and gap ledger** with a slim Current Verdict plus links into dated evidence plans and gap-stream indexes. `docs/current-capabilities.md` summarizes the operator-facing supported surface in prose that must stay aligned with the manifest. Treat roadmap capability prose as retained evidence unless a current-truth document points here. `First-Party UI Editor Production Stack v1` (`first-party-ui-editor-production-stack-v1`, `2026-05-31-first-party-ui-editor-production-stack-v1.md`) is the active clean-break plan after `First-Party Editor Shell v1` (`first-party-editor-shell-v1`, `2026-05-31-first-party-editor-shell-v1.md`) completed the replacement of the Native Win32 Editor Shell v1 Dear ImGui implementation with a first-party retained editor shell. The active plan keeps `unsupportedProductionGaps` empty while targeting first-party docking, rich text, completed Windows DirectWrite/TSF/UIA adapter evidence, full viewport/material GPU display parity, and AI-operable editor UX expansion without SDL3, UI middleware, compatibility shims, or native handle exposure.

The first Phase-1 child implementation plan completed from that master plan is `Full Clang Tidy Warning Cleanup v1` (full-repository clang-tidy warning cleanup under strict compile database availability). Additional completed Phase-1 slices include `Cmake Install Export And Cxx Modules Audit v1`, `Sanitizer And CI Matrix Hardening v1`, and `Coverage Threshold Policy v1`. CI Matrix Contract Check v1 adds `tools/check-ci-matrix.ps1` / `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-ci-matrix.ps1` to the default validation path so reviewed Windows/Linux/sanitizer/macOS/iOS workflow lanes, package artifacts, coverage artifacts, and host-gated iOS evidence cannot drift silently; C++23 Release Package Artifact CI Evidence v1 extends the existing Windows C++23 release lane so CPack ZIP/SHA-256/archive payload validation runs before artifact upload, while broader build/package CI execution evidence beyond that Windows lane and static analyzer profile expansion remain follow-up work. Runtime UI Accessibility Publish Plan v1 adds host-independent `AccessibilityPublishPlan`, `AccessibilityPublishResult`, `plan_accessibility_publish`, and `publish_accessibility_payload` so `AccessibilityPayload` rows are validated before `IAccessibilityAdapter` dispatch while OS accessibility bridge publication, native accessibility objects, and platform SDK calls remain follow-up work. Runtime UI IME Composition Publish Plan v1 adds host-independent `ImeCompositionPublishPlan`, `ImeCompositionPublishResult`, `plan_ime_composition_update`, and `publish_ime_composition` so `ImeComposition` rows are validated before `IImeAdapter` dispatch while Win32/TSF, Cocoa text input, ibus/Fcitx, Android/iOS IME sessions, and platform SDK calls remain follow-up work. Runtime UI Platform Text Input Session Plan v1 adds host-independent `PlatformTextInputSessionPlan`, `PlatformTextInputSessionResult`, `PlatformTextInputEndPlan`, `PlatformTextInputEndResult`, `begin_platform_text_input`, and `end_platform_text_input` so first-party text-input begin/end rows are validated before `IPlatformIntegrationAdapter` dispatch while native text-input objects, virtual keyboard/session behavior, Win32/TSF, Cocoa, Android/iOS IME sessions, and platform SDK calls remain follow-up work. Runtime UI Win32 Platform Text Input Adapter v1 adds optional `Win32PlatformIntegrationAdapter` begin/end dispatch through Win32 text input area/start/stop calls, Runtime UI Win32 Text Input Event Translation v1 and Text Input Proof Rows v1 add copied first-party Win32 message rows for committed text and editing commands, Runtime UI Platform IME Composition Proof Rows v1 keeps composition publication behind first-party adapters, and Runtime UI Win32 Committed Text Edit Apply v1 adds `TextEditState`, `CommittedTextInput`, `plan_committed_text_input`, `apply_committed_text_input`, `win32_committed_text_from_message`, and `apply_win32_committed_text_message` so copied text-input rows can apply committed UTF-8 insertion/replacement without moving platform APIs into `MK_ui` or claiming full text editing widget behavior, candidate UI, or native IME parity. Runtime UI Text Edit Command Apply v1 adds host-independent `TextEditCommandKind`, `TextEditCommand`, `plan_text_edit_command`, and `apply_text_edit_command` so validated `TextEditState` rows can apply scalar-boundary cursor movement and backward/forward deletion while platform key mapping, key repeat, selection UI, copy/cut/paste widgets, word movement, grapheme-cluster editing, candidate UI, and native IME parity remain follow-up work. Runtime UI Clipboard Text Request v1 adds host-independent `IClipboardTextAdapter`, `ClipboardTextWriteRequest`, `ClipboardTextReadRequest`, `plan_clipboard_text_write`, `write_clipboard_text`, `plan_clipboard_text_read`, and `read_clipboard_text` plus `Win32ClipboardTextAdapter` over `Win32Clipboard` so text clipboard rows validate target and UTF-8 data without moving platform APIs or `engine/platform` into `MK_ui`. Runtime UI Text Edit Clipboard Command Apply v1 adds host-independent `TextEditClipboardCommandKind`, `TextEditClipboardCommand`, `plan_text_edit_clipboard_command`, and `apply_text_edit_clipboard_command` so caller-owned text edit selections can copy, cut, and paste through `IClipboardTextAdapter`; full text editing widgets, platform shortcut mapping, selection UI, rich clipboard formats, and native text services remain follow-up work. Runtime UI Text Shaping Request Plan v1 adds host-independent `TextShapingRequestPlan`, `TextShapingResult`, `plan_text_shaping_request`, and `shape_text_run` so first-party text layout requests and adapter-returned runs are validated around `ITextShapingAdapter` dispatch while production shaping implementation, bidi reordering, production line breaking, font fallback, renderer texture upload, and platform SDK calls remain follow-up work. Runtime UI Font Rasterization Request Plan v1 adds host-independent `FontRasterizationRequestPlan`, `FontRasterizationResult`, `plan_font_rasterization_request`, and `rasterize_font_glyph` so first-party font-family, glyph, pixel-size, and adapter allocation rows are validated around `IFontRasterizerAdapter` dispatch while real font loading/rasterization, glyph atlas generation, renderer texture upload, and platform SDK calls remain follow-up work. Runtime UI Image Decode Request Plan v1 adds host-independent `ImageDecodeRequestPlan`, `ImageDecodeDispatchResult`, `plan_image_decode_request`, and `decode_image_request` so first-party image byte requests and explicit RGBA8 adapter output rows are validated around `IImageDecodingAdapter` dispatch; Runtime UI PNG Image Decoding Adapter v1 adds optional `MK_tools` `PngImageDecodingAdapter` over `decode_audited_png_rgba8` / `libspng` for reviewed PNG bytes while broader runtime image decoding, source codecs beyond the reviewed PNG lane, SVG/vector parsing, atlas packing, renderer texture upload, and platform SDK calls remain follow-up work. Runtime Input Rebinding Presentation Rows v1 adds host-independent `RuntimeInputRebindingPresentationToken`, `RuntimeInputRebindingPresentationRow`, `RuntimeInputRebindingPresentationModel`, `present_runtime_input_action_trigger`, `present_runtime_input_axis_source`, and `make_runtime_input_rebinding_presentation` so runtime/game menus can display reviewed base/profile action and axis bindings with deterministic labels and symbolic glyph lookup keys. Engine Input Action Contexts v1 now has selected 2D/3D package evidence under `--require-gameplay-systems` for context stack planning, profile overlays, action capture, axis capture, focused capture consumption/retention, presentation rows, and glyph lookup keys through `input_context_rebinding_*` and `input_rebinding_*` counters, while full runtime/game rebinding panels, real platform input glyph generation/assets/rendering, keyboard-layout localization, device assignment, native handles, and file/cloud/binary saves remain follow-up work. Frame Graph RHI Texture Barrier Recording v1 adds `FrameGraphTextureBinding`, `FrameGraphTextureBarrierRecordResult`, `frame_graph_texture_state_for_access`, and `record_frame_graph_texture_barriers` so successful Frame Graph v1 schedules can record explicit texture transitions through public `IRhiCommandList` calls from caller-owned resource bindings, and Frame Graph Pass Callback Execution v1 adds `FrameGraphPassExecutionBinding`, `FrameGraphExecutionCallbacks`, `FrameGraphExecutionResult`, and `execute_frame_graph_v1_schedule` so caller-owned barrier/pass callbacks can run a linear v1 schedule with deterministic diagnostics while production render graph ownership, renderer-owned pass migration, multi-queue scheduling, package streaming integration, and renderer-wide manual-transition removal remain follow-up work. Phase 2 work: `Broad Dependency Cook Plan v1` (completed), `Package Mount And Resident Cache v1` (completed), `Runtime Resource Residency Budget Execution v1` (completed), `Renderer RHI Safe Point Resource Teardown v1` (completed), `AI Cook Package Command Surface v1` (completed), and `Phase 2 Manifest Schema Docs Skills Validation Recipes` (completed) advance cook closure, resident overlay mounts, deterministic resident byte/record budget execution, host-owned runtime scene GPU binding safe-point teardown, manifest `registeredSourceAssetCookTargets` descriptors aligned with `cook-registered-source-assets` plus `prefabScenePackageAuthoringTargets`, and the Phase 2 checklist closure slice for architecture/Cursor-skill sync plus `run-validation-recipe` evidence. Phase 3 child plans through `Metal Visible Presentation Apple Host v1` are complete (native RHI resource lifetime Phases A-D, GPU markers/timestamps, allocator/residency diagnostics, frame graph v1 execution, upload ring/staging pool, D3D12 production hardening, Vulkan debug-object names, Metal `-[MTLObject setLabel:]` on Apple owners; full Metal `IRhiDevice` parity remains future work). Phase 6 now also includes `glTF Node Transform Animation Import v1`, which imports LINEAR glTF node translation, z-axis-only rotation, and positive scale channels into `MK_animation` keyframe rows, `glTF Node Transform Animation Float Clip Bridge v1`, which bridges those node TRS rows into first-party scalar float clip source/cook/runtime payload rows, `Animation Float Transform Application v1`, which applies sampled scalar curves to caller-owned `Transform3D` rows through explicit bindings, `Animation Transform Binding Source v1`, which adds `GameEngine.AnimationTransformBindingSource.v1` / `AnimationTransformBindingSourceDocument` text rows plus `serialize_animation_transform_binding_source_document` for authored curve-target to named-transform-component mapping, `glTF Node Transform Animation Binding Source Bridge v1`, which derives matching `GameEngine.AnimationTransformBindingSource.v1` rows from glTF node TRS targets and node-name fallbacks, `Runtime Scene Animation Transform Binding v1`, which adds `resolve_runtime_scene_animation_transform_bindings` and `apply_runtime_scene_animation_transform_samples` for `RuntimeSceneInstance` node-name resolution and validated sampled-value application, plus `glTF Animation Float Clip Bridge v1`, which bridges LINEAR glTF morph weights into first-party scalar float clip source rows and runtime/gameplay byte-row sampling. Authoritative narrow-slice pointers live in `engine/agent/manifest.json` and the plan registry; after AI Gameplay Authoring Tools v1 implementation completed through PR #174 / commit `9bffa2b8`, selection-gate closeout pointer sync completed through PR #175 / commit `f05136e2`, Post-1.0 Capability Program v1 closed at Phase 2 through PR #178 / commit `f4faf787`, Renderer Production Quality And Backend Parity v1 closed through PR #261, PR #262, and PR #263, and Runtime UI Text Platform Stack v1 closed through PR #264, PR #265, PR #266, PR #267, and PR #268, First-Party Desktop Platform And SDL3 Removal v1 closed the Windows-first native desktop replacement path, and the current composed manifest selects `recommendedNextPlan.id = native-win32-editor-shell-v1` for Native Win32 Editor Shell v1 while `Reviewed Importers Codecs And Shader Generation v1` is paused and `unsupportedProductionGaps` remains empty.

Runtime RHI Upload Submission Fence Rows v1 adds `submitted_fence` rows to runtime texture, mesh, skinned mesh, morph mesh, and material-factor upload results, preserves actual scene upload submit order in `RuntimeSceneGpuBindingResult::submitted_upload_fences`, and derives `submitted_upload_fence_count` plus `last_submitted_upload_fence` in `RuntimeSceneGpuUploadExecutionReport`. This narrows `upload-staging-v1` by preserving first-party upload submission evidence without claiming native async upload execution, runtime-wide ring-backed upload integration beyond reviewed texture/buffer uploads, package streaming, backend overlap, or renderer ownership changes.

Runtime Texture Upload Multi-Queue Final-State Evidence v1 routes byte-backed `upload_runtime_texture` copy command recording, pass target-state preparation, and final-state restoration through `execute_frame_graph_rhi_multi_queue_schedule`: one command list is submitted for the selected upload queue, one pass target-state barrier prepares the texture as `copy_destination`, one callback records the buffer-to-texture copy, and one final-state barrier restores `shader_read`. The result counters expose command-list, queue-wait, barrier, final-state, and callback evidence while metadata-only allocations remain zero-evidence; broad/background streaming, runtime-wide ring ownership beyond reviewed texture/buffer uploads, production graph ownership, async overlap, and public native/RHI handles remain unsupported. Runtime Ring-Backed Texture Upload v1 adds `RuntimeTextureUploadOptions::upload_ring` so byte-backed `upload_runtime_texture` can use caller-owned `RhiUploadRing`/`RhiUploadStagingPlan` staging, reuse one `copy_source` ring buffer, preserve Frame Graph/fence counters, and fail before texture upload side effects on ring exhaustion; package streaming texture transactions inherit the path through existing source upload options. Runtime Buffer Ring-Backed Uploads v1 adds `RuntimeMeshUploadOptions::upload_ring`, `RuntimeSkinnedMeshUploadOptions::upload_ring`, and `RuntimeMorphMeshUploadOptions::upload_ring` so runtime mesh/skinned/morph uploads can reuse caller-owned ring staging, preserve Frame Graph/fence counters, fail before destination buffer or upload side effects on ring exhaustion, and keep runtime-scene teardown from releasing caller-owned ring buffers. Package mesh/skinned/morph upload binding transactions add `upload_runtime_package_streaming_mesh_gpu_bindings`, `upload_runtime_package_streaming_skinned_mesh_gpu_bindings`, and `upload_runtime_package_streaming_morph_mesh_gpu_bindings` so committed package streaming state can validate live resident static, skinned, and morph mesh rows plus explicit payload rows, upload through the matching runtime upload helpers including caller-owned ring options, and return upload rows, renderer GPU binding rows, submitted fences, uploaded-byte totals, Frame Graph command-list counters, and Runtime Upload Queue Wait v1 graphics-queue wait evidence for async copy-queue uploads.

Runtime Mesh Frame Graph Command Evidence v1 routes byte-backed `upload_runtime_mesh` vertex/index copy command recording and command-list submission through `execute_frame_graph_rhi_multi_queue_schedule`, returning `RuntimeMeshUploadResult` evidence for one submitted command list, zero queue waits, zero texture barriers, and one pass callback while preserving selected queue and submitted fence behavior. This moves mesh upload command ownership under the Frame Graph RHI executor without adding buffer barriers, ring-backed mesh upload staging, native async upload execution, production graph ownership, async overlap, or public native/RHI handles.

Runtime Skinned Mesh Frame Graph Command Evidence v1 routes byte-backed `upload_runtime_skinned_mesh` vertex/index/joint-palette copy command recording and command-list submission through `execute_frame_graph_rhi_multi_queue_schedule`, returning `RuntimeSkinnedMeshUploadResult` evidence for one submitted command list, zero queue waits, zero texture barriers, and one pass callback while preserving selected queue, submitted fence behavior, uploaded bytes, copy regions, and the joint palette descriptor binding path. Runtime Buffer Ring-Backed Uploads v1 later adds caller-owned ring staging for this path; native async upload execution, production graph ownership, async overlap, and public native/RHI handles remain unsupported.

Runtime Morph Mesh Frame Graph Command Evidence v1 routes byte-backed `upload_runtime_morph_mesh_cpu` POSITION, optional NORMAL, optional TANGENT, and morph-weight copy command recording plus command-list submission through `execute_frame_graph_rhi_multi_queue_schedule`, returning `RuntimeMorphMeshUploadResult` evidence for one submitted command list, zero queue waits, zero texture barriers, and one pass callback while preserving selected queue, submitted fence behavior, uploaded bytes, copy regions, and the morph descriptor binding path. Runtime Buffer Ring-Backed Uploads v1 later adds caller-owned ring staging for this path; native async upload execution, production graph ownership, async overlap, and public native/RHI handles remain unsupported.

Runtime Material Factor Frame Graph Command Evidence v1 routes material-factor uniform buffer copy command recording plus command-list submission in `create_runtime_material_gpu_binding` through `execute_frame_graph_rhi_multi_queue_schedule`, returning `RuntimeMaterialGpuBinding` evidence for one submitted command list, zero queue waits, zero texture barriers, and one pass callback while preserving existing graphics-queue wait behavior, submitted fence rows, uploaded factor bytes, and descriptor binding paths. This keeps material-factor ring-backed upload staging, native async upload execution, production graph ownership, async overlap, and public native/RHI handles unsupported.

Runtime Package Streaming RHI Upload Binding Transaction v1 adds a host-owned `upload_runtime_package_streaming_frame_graph_texture_bindings` helper that validates committed package streaming state, live resident texture rows, and explicit caller-reviewed `RuntimeTexturePayload` rows before uploading through `upload_runtime_texture` and returning both upload evidence and imported `FrameGraphTextureBinding` rows. Package mesh/skinned/morph upload binding transactions add matching helpers for live resident static mesh, skinned mesh, and morph mesh CPU rows plus explicit payload rows, returning upload evidence plus renderer `MeshGpuBinding`, `SkinnedMeshGpuBinding`, or `MorphMeshGpuBinding` rows; Runtime Upload Queue Wait v1 lets those package transactions record graphics-queue waits for async copy-queue upload fences without CPU waits. Package Resource Update Readiness v1 adds first-party resource update rows over committed streaming state, live catalog records, submitted upload fences, and graphics-queue wait evidence. Selected D3D12 generated 3D package upload staging evidence adds `execute_runtime_package_upload_staging_evidence`, the `--require-package-upload-staging` smoke flag, installed validation for `package_upload_staging_*` fields including resource update counters, and CMake package smoke args for the committed generated 3D package sample. These narrow package streaming integration without adding broad/background streaming, renderer-owned residency, runtime-wide upload ring ownership beyond reviewed texture/buffer uploads, runtime-wide staging-pool ownership beyond explicit lease-backed upload rings, production graph ownership, async overlap/performance claims, or gameplay-visible `IRhiDevice` access.

Frame Graph Production Ownership Boundary Selection v1 adds `FrameGraphProductionOwnershipCapability`, `FrameGraphProductionOwnershipBoundary`, `FrameGraphProductionOwnershipCandidate`, `FrameGraphProductionOwnershipPlan`, and `plan_frame_graph_production_ownership_boundary` to classify reviewed capabilities before migration. The planner marks completed executor surfaces such as texture transitions, render pass envelopes, queue waits, multi-queue command submission, runtime upload commands, package-streaming texture-binding handoff, and Vulkan transient alias memory evidence as `frame_graph_owned`; keeps swapchain acquire/present, viewport readback, and native overlay preparation renderer-owned; keeps package residency runtime-host-owned; keeps Metal memory alias allocation host-gated; and fails closed for production multi-queue graph adoption, broad/background package streaming, data inheritance/content preservation, async overlap/performance, and public native handles. This is boundary evidence only and does not claim broad production graph ownership or broad renderer readiness.

Frame Graph RHI Texture Schedule Execution v1 now includes `FrameGraphRhiTextureExecutionDesc`, `FrameGraphRhiTextureExecutionResult`, `FrameGraphTexturePassTargetAccess`, `build_frame_graph_texture_pass_target_accesses`, `FrameGraphTextureFinalState`, `FrameGraphTexturePassTargetState`, `final_state_barriers_recorded`, `pass_target_state_barriers_recorded`, and `execute_frame_graph_rhi_texture_schedule` so caller-owned RHI texture transitions, declared pass target-state preparation validated against declared writer access rows, pass callbacks, and declared reusable final-state transitions execute in one deterministic `FrameGraphExecutionStep` order. The executor is shared-`TextureHandle` aware for existing backend-neutral texture handles: successful barrier, pass-target, and final-state transitions propagate to every `FrameGraphTextureBinding` row with the same handle, and conflicting initial shared-handle states are rejected before recording. Frame Graph Automatic Aliasing Barrier Insertion v1 extends the executor with `FrameGraphTransientTextureLifetime` input and `aliasing_barriers_recorded`, automatically recording one backend-neutral `IRhiCommandList::texture_aliasing_barrier` before each later resource lifetime in a non-overlapping alias group; Frame Graph Transient Alias Content Initialization v1 rejects resource-backed transient first render-pass `LoadAction::load` before command recording or callbacks. Frame Graph Texture Aliasing Barrier Command v1 also keeps `FrameGraphTextureAliasingBarrier` / `record_frame_graph_texture_aliasing_barriers` explicit helper recording without changing tracked texture states, and Public Null Aliasing Barriers v1 treats `TextureHandle{}` plus empty Frame Graph aliasing resource names as wildcard endpoints. D3D12 now has backend-private same-offset placed alias-group command/state evidence for those distinct handles, and Vulkan transient alias groups prove alias-flagged images bound to one shared backend-private memory allocation. `RhiFrameRenderer` now queues raw primary sprite/mesh work during draw calls and records it from an executor-owned `primary_color` callback with writer-access-backed primary texture/depth target-state preparation plus a render pass envelope in `end_frame()`, so completed raw frames report one executor-invoked frame graph pass while swapchain acquire/present and command-list submission stay renderer-owned. `RhiViewportSurface::render_clear_frame()` now uses an executor-owned `viewport.clear` render pass envelope plus writer-access-backed target-state and final copy-source rows while display/readback ownership stays renderer-owned. Frame Graph v1 is closed for the Engine 1.0 Windows-default ready surface by `Frame Graph v1 1 0 Scope Closeout v1`; broad production render graph scheduling, broad/background package streaming, Metal memory alias allocation, async overlap/performance, actual content preservation, public native handles, and broad renderer quality remain future or host-gated non-goals rather than active blockers.

Frame Graph Render Pass Package Evidence v1 makes `RendererStats::framegraph_render_passes_recorded` package-visible for selected postprocess/depth and directional-shadow desktop package smokes through `framegraph_render_passes_recorded`, `renderer_quality_expected_framegraph_render_passes`, and `renderer_quality_framegraph_render_passes_ok`, without exposing native render pass objects or broad renderer quality claims.

Frame Graph Transient Texture Alias Planning v1 adds `FrameGraphTransientTextureDesc`, `FrameGraphTransientTextureAliasPlan`, and `plan_frame_graph_transient_texture_aliases` so `MK_renderer` can validate used transient texture descriptors, compute first/last pass lifetimes from a successful `FrameGraphV1Desc`, group only non-overlapping exact `TextureDesc` matches, and report unaliased/aliased byte estimates. Frame Graph Backend-Neutral Distinct Alias-Group Lease Binding v1 adds `rhi::TransientTextureAliasGroup` plus `IRhiDevice::acquire_transient_texture_alias_group`, and updates `acquire_frame_graph_transient_texture_lease_bindings` so each alias group acquires one backend-neutral alias-group lease and returns distinct resource-name `FrameGraphTextureBinding` rows with malformed-plan, zero/duplicate/wrong-count backend-return, and partial-failure cleanup guards. Frame Graph Shared Texture State Handoff v1 remains for binding rows that share one backend-neutral `TextureHandle`. Frame Graph Texture Aliasing Barrier Command v1 plus Public Null Aliasing Barriers v1 add `IRhiCommandList::texture_aliasing_barrier` plus `FrameGraphTextureAliasingBarrier` / `record_frame_graph_texture_aliasing_barriers` explicit dependency recording for distinct bound texture handles or wildcard/null endpoints without changing tracked states; D3D12 records a conservative backend-private null-resource `D3D12_RESOURCE_BARRIER_TYPE_ALIASING` after public non-zero handle validation when the handles are not a proven placed-resource alias pair, maps explicit public wildcard/null endpoints to `nullptr` native aliasing endpoints, tracks it with `DeviceContextStats::null_resource_aliasing_barriers`, records backend-private non-null barriers for proven same alias-group placed pairs, and Vulkan records a conservative synchronization2 memory dependency. D3D12 transient texture leases now allocate one backend-private heap and one `CreatePlacedResource` texture per one-texture lease with first-use activation barriers, allocation evidence counters, and `transient_texture_placed_resources_alive` retirement evidence; multi-texture backend-neutral alias-group leases use D3D12 backend-private same-offset placed textures on one heap for non-null barrier/state evidence and Vulkan backend-private distinct `VkImage` owners with `VK_IMAGE_CREATE_ALIAS_BIT` bound to one shared `VkDeviceMemory` allocation for allocation/stat evidence. Automatic aliasing-barrier insertion is implemented through `FrameGraphTransientTextureLifetime` rows in both the single-command-list texture executor and `execute_frame_graph_rhi_multi_queue_schedule`; the multi-queue path exposes `FrameGraphRhiMultiQueueExecutionDesc::transient_texture_lifetimes`, records the aliasing barrier after producer waits and before scheduled texture barriers/target states/callbacks, and reports `FrameGraphRhiMultiQueueExecutionResult::aliasing_barriers_recorded`. Transient alias content initialization rejects first render-pass `LoadAction::load` for resource-backed transient lifetimes before command recording on both executor paths. Frame Graph Render Pass Envelope v1 adds executor-owned render pass begin/end scopes for selected postprocess passes through `FrameGraphRhiRenderPassDesc`, Frame Graph Directional Shadow Render Pass Envelope v1 extends that ownership to directional-shadow callbacks, Frame Graph Remaining Render Pass Envelopes v1 moves the raw primary and viewport clear pass scopes into the same envelope contract, Frame Graph Primary Pass Target-State Evidence v1 routes primary texture/depth target prep through declared writer-access-backed pass target-state rows while keeping synchronization in explicit barriers, and Frame Graph RHI Multi-Queue Texture Barrier Execution v1 lets the multi-queue executor opt into `FrameGraphRhiMultiQueueExecutionDesc::texture_bindings`, prevalidate scheduled texture barriers before command recording, and record consumer-pass transitions after producer waits while reporting `FrameGraphRhiMultiQueueExecutionResult::barriers_recorded`; Frame Graph Multi-Queue Final-State Execution v1 adds `FrameGraphRhiMultiQueueExecutionDesc::final_states` and `FrameGraphRhiMultiQueueExecutionResult::final_state_barriers_recorded` so final texture transitions record on the last scheduled resource pass command list. Metal alias memory execution, actual data inheritance/content preservation, broader production graph pass ownership beyond selected executor slices, production multi-queue graph adoption, async overlap/performance evidence, package streaming integration, and Metal readiness remain follow-up work.

Runtime Resident Package Eviction Plan v1 adds `plan_runtime_resident_package_evictions_v2` as a host-independent reviewed planner over explicit `RuntimeResidentPackageMountIdV2` candidates. It returns no-op success when the current resident view fits the target budget, rejects invalid/duplicate/missing/protected candidates before partial planning, and returns deterministic `budget_unreachable` diagnostics when reviewed candidates are insufficient. This contributes to the closed `runtime-resource-v2` safe-point/controller surface without claiming arbitrary/LRU eviction, background package streaming, broad disk/VFS package mount orchestration beyond reviewed candidate resident mount/replacement, renderer/RHI ownership, upload/staging integration, or GPU allocator enforcement.

Runtime Resident Package Reviewed Eviction Commit v1 adds `commit_runtime_resident_package_reviewed_evictions_v2` as the matching host-independent execution safe point for caller-reviewed eviction candidates. It projects the resident mount/cache state, applies only the reviewed unmount order, refreshes the projected catalog and budget, and commits atomically with status, diagnostic, byte/count, generation, eviction-plan, and catalog-refresh evidence while preserving live state on reviewed-candidate, insufficient-candidate, budget, or catalog-refresh failure. This remains standalone resident eviction execution only; package discovery/loading, broad/background streaming, hot reload productization, renderer/RHI ownership, upload/staging integration, arbitrary/LRU eviction policy, allocator/GPU budgets, inferred content roots, and native handles remain follow-up claims.

Runtime Package Index Discovery v1 adds `discover_runtime_package_indexes_v2` to `MK_runtime` for deterministic reviewed `.geindex` candidate discovery under a caller-owned `IFileSystem` root. It returns sorted candidate rows with `package_index_path`, caller-provided `content_root`, and root-relative labels, reports invalid descriptor, missing-root, and scan diagnostics, and does not read package contents, load packages, mount resident packages, refresh resident catalogs, stream in the background, or touch renderer/RHI/native handles.

Runtime Package Candidate Load v1 adds `load_runtime_package_candidate_v2` to `MK_runtime` for turning one reviewed selected `.geindex` candidate into a typed `RuntimeAssetPackageLoadResult` plus record count and resident-byte estimate. It validates direct-call candidate rows before loader invocation and converts low-level loader failures, invalid index text, and filesystem read exceptions into typed diagnostics. It does not stage stores, mount resident packages, refresh resident catalogs, stream in the background, infer content roots, or touch renderer/RHI/native handles.

Runtime Package Candidate Resident Mount v1 adds `commit_runtime_package_candidate_resident_mount_v2` as the reviewed disk/VFS candidate-to-resident-mount execution path. It validates invalid/duplicate mount ids before filesystem reads, loads the selected candidate through `load_runtime_package_candidate_v2`, refreshes a projected resident catalog cache with the caller's overlay and budget, and commits the mount set/cache only after all preflight succeeds. It does not use package-streaming descriptors, background workers, hot reload controllers, upload/staging integration, renderer/RHI ownership, inferred content roots, native handles, or arbitrary eviction policy.

Runtime Package Candidate Resident Mount Reviewed Evictions v1 adds `commit_runtime_package_candidate_resident_mount_with_reviewed_evictions_v2` as the reviewed candidate-to-resident-mount path that can free resident budget using only caller-provided eviction candidates. It loads the selected candidate, mounts it into a projected resident view, automatically protects the new mount id, plans explicit unmounts through `plan_runtime_resident_package_evictions_v2`, refreshes the final projected resident catalog cache, and commits mount/cache state only after all preflight succeeds. It does not infer LRU/arbitrary eviction policy, stream in the background, hot reload, upload/stage renderer resources, enforce allocator/GPU budgets, expose native handles, or take renderer/RHI ownership.

Runtime Package Candidate Resident Replace v1 adds `commit_runtime_package_candidate_resident_replace_v2` as the reviewed disk/VFS candidate-to-resident-replacement execution path. It validates invalid/missing existing mount ids before filesystem reads, loads the selected candidate through `load_runtime_package_candidate_v2`, delegates to the slot-preserving `commit_runtime_resident_package_replace_v2` path, and commits mount/cache state only after candidate load, replacement, budget, and catalog refresh preflight succeeds. It does not use package-streaming descriptors, background workers, hot reload controllers, upload/staging integration, renderer/RHI ownership, inferred content roots, native handles, or arbitrary eviction policy.

Runtime Package Candidate Resident Replace Reviewed Evictions v1 adds `commit_runtime_package_candidate_resident_replace_with_reviewed_evictions_v2` as the reviewed candidate-to-existing-resident-replacement path that can free resident budget using only caller-provided eviction candidates. It validates the existing mount id before filesystem reads, loads the selected candidate, replaces the resident mount in projected state while preserving slot/order/label, automatically protects the replacement mount id, plans explicit unmounts through `plan_runtime_resident_package_evictions_v2`, refreshes the final projected resident catalog cache, and commits mount/cache state only after all preflight succeeds. It does not infer LRU/arbitrary eviction policy, stream in the background, hot reload, upload/stage renderer resources, enforce allocator/GPU budgets, expose native handles, take renderer/RHI ownership, or add discovery/package-streaming orchestration.

Runtime Package Discovery Resident Commit v1 adds `commit_runtime_package_discovery_resident_v2` as the reviewed discovery-root-to-resident-commit orchestration path. It discovers `.geindex` rows through `discover_runtime_package_indexes_v2`, selects one exact package index path, validates the requested mount/replace id before discovery or package reads, delegates to the existing candidate resident mount/replace commit helpers, reports discovery/selection/delegated commit evidence, and preserves live mount/cache state on discovery, selection, load, budget, or catalog-refresh failures. It narrows broad disk/VFS orchestration only to reviewed exact-candidate resident mount/replacement and does not add background streaming, hot reload, arbitrary/LRU eviction, upload/staging integration, renderer/RHI ownership, allocator/GPU budget enforcement, inferred content roots, or native handles.

Runtime Package Discovery Resident Mount Reviewed Evictions v1 adds `commit_runtime_package_discovery_resident_mount_with_reviewed_evictions_v2` as the reviewed discovery-root-to-eviction-assisted-resident-mount path. It discovers `.geindex` rows through `discover_runtime_package_indexes_v2`, selects one exact package index path, validates the selected path and new mount id before discovery or package reads, delegates to `commit_runtime_package_candidate_resident_mount_with_reviewed_evictions_v2`, reports discovery/selection/delegated mount/eviction-plan/catalog-refresh evidence, and preserves live mount/cache state on discovery, selection, load, eviction-plan, budget, or catalog-refresh failures. It narrows broad disk/VFS orchestration only to reviewed exact-candidate resident mounting with caller-reviewed evictions and does not add replace-mode eviction assistance, background streaming, hot reload, arbitrary/LRU eviction, upload/staging integration, renderer/RHI ownership, allocator/GPU budget enforcement, inferred content roots, or native handles.

Runtime Package Discovery Resident Replace Reviewed Evictions v1 adds `commit_runtime_package_discovery_resident_replace_with_reviewed_evictions_v2` as the reviewed discovery-root-to-eviction-assisted-resident-replacement path. It discovers `.geindex` rows through `discover_runtime_package_indexes_v2`, selects one exact package index path, validates the selected path and existing mount id before discovery or package reads, delegates to `commit_runtime_package_candidate_resident_replace_with_reviewed_evictions_v2`, reports discovery/selection/delegated replacement/eviction-plan/catalog-refresh evidence, and preserves live mount/cache state on discovery, selection, load, eviction-plan, budget, or catalog-refresh failures. It narrows broad disk/VFS orchestration only to reviewed exact-candidate resident replacement with caller-reviewed evictions and does not add package-streaming integration, background streaming, hot reload, arbitrary/LRU eviction, upload/staging integration, renderer/RHI ownership, allocator/GPU budget enforcement, inferred content roots, or native handles.

Runtime Package Hot Reload Reviewed Replacement v1 formalizes that same `commit_runtime_package_discovery_resident_replace_with_reviewed_evictions_v2` contract as the host-driven reviewed hot-reload replacement safe point. The runtime side begins only after a host/operator has reviewed a replacement `.geindex`, explicit existing mount id, and optional eviction candidates; file watching, recook execution, background streaming, arbitrary/LRU eviction, renderer/RHI ownership, upload/staging, allocator/GPU budgets, inferred content roots, and native handles remain follow-up work.

Runtime Package Hot Reload Candidate Review v1 adds `plan_runtime_package_hot_reload_candidate_review_v2` to `MK_runtime` as the host-independent planner between externally observed package/index/content path changes and later safe-point replacement review. It maps caller-reviewed relative VFS changed paths against already-discovered `RuntimePackageIndexDiscoveryCandidateV2` rows, returns sorted/de-duplicated candidate review rows, package-index/content match evidence, invalid and unmatched path diagnostics, and matched counts without taking `IFileSystem`, reading packages, mutating resident state, watching files, running recook/build steps, streaming in the background, choosing eviction policy, touching renderer/RHI/upload resources, enforcing allocator/GPU budgets, inferring content roots, or exposing native handles.

Runtime Package Hot Reload Recook Change Review v1 adds `plan_runtime_package_hot_reload_recook_change_review_v2` as the no-read handoff from reviewed `AssetHotReloadApplyResult` rows to candidate review. It accepts only `staged` and `applied` apply rows whose paths are already-reviewed runtime package/index/content relative VFS paths, rejects failed/unknown/invalid apply rows before candidate review, delegates deterministic matching to `plan_runtime_package_hot_reload_candidate_review_v2`, and reports no-file-watch/no-recook/no-package-load/no-resident-commit evidence without adding recook execution, package reads, resident mutation, renderer/RHI ownership, upload/staging, allocator/GPU enforcement, inferred content roots, or native handles.

Runtime Hot Reload Recook Package Replacement Execution v1 adds the `MK_tools` `execute_asset_runtime_package_hot_reload_replacement_safe_point` bridge for the reviewed host/editor step that actually runs asset recook, feeds staged runtime package rows into `commit_runtime_package_hot_reload_recook_replacement_v2`, and commits only selected package matched `AssetRuntimeReplacementState` rows after the resident package replacement succeeds. This narrows the live-iteration path without adding automatic file watching, automatic target inference, package scripts, renderer/RHI ownership, upload/staging, arbitrary eviction, allocator/GPU budget enforcement, native handles, or broad hot reload productization.

Runtime Hot Reload Registered Asset Watch Tick v1 adds the `MK_tools` `execute_asset_runtime_package_hot_reload_registered_asset_watch_tick_safe_point` bridge for host/editor polling over registered asset sources before the reviewed recook-to-resident replacement path. It scans through caller-owned `IFileSystem`, advances caller-owned `AssetHotReloadTracker` and `AssetHotReloadRecookScheduler` state, primes the first baseline by default, forwards dependency invalidated recook requests, keeps ready scheduler rows retryable after downstream failure, and invokes the existing replacement helper only after debounced requests are ready. This narrows hot-reload orchestration without owning native file watchers, inferring replacement targets, running background work or package scripts, touching renderer/RHI/upload resources, enforcing allocator/GPU budgets, exposing native handles, or claiming broad hot reload productization.

Runtime Package Streaming Candidate Resident Mount v1 adds `execute_selected_runtime_package_streaming_candidate_resident_mount_safe_point` as the selected descriptor-to-resident-mount bridge. It builds the reviewed candidate from `RuntimePackageStreamingExecutionDesc`, validates the new mount id before filesystem reads, loads the candidate through `load_runtime_package_candidate_v2`, validates residency hints, commits through projected `RuntimeResidentPackageMountSetV2` plus `RuntimeResidentCatalogCacheV2` state, and reports `candidate_load`, `resident_mount`, and `resident_catalog_refresh` evidence. It does not add broad/background package streaming, broad disk/VFS package mount orchestration beyond reviewed candidate resident mount/replacement, hot reload productization, arbitrary eviction, upload/staging integration, renderer/RHI ownership, allocator/GPU budget enforcement, native handles, or inferred content roots.

Runtime Package Streaming Candidate Resident Mount Reviewed Evictions v1 adds `execute_selected_runtime_package_streaming_candidate_resident_mount_with_reviewed_evictions_safe_point` as the selected descriptor-to-eviction-assisted-resident-mount bridge. It builds the reviewed candidate from `RuntimePackageStreamingExecutionDesc`, validates the new mount id before filesystem reads, delegates projected mounting plus caller-reviewed eviction candidates to `commit_runtime_package_candidate_resident_mount_with_reviewed_evictions_v2`, validates residency hints before live commit, and reports `candidate_load`, `resident_mount`, `eviction_plan`, and `resident_catalog_refresh` evidence. It narrows package-streaming breadth only to reviewed candidate resident mounting with caller-reviewed evictions and does not add replacement-mode eviction assistance, broad/background streaming, hot reload productization, arbitrary/LRU eviction, upload/staging integration, renderer/RHI ownership, allocator/GPU budget enforcement, native handles, or inferred content roots.

Runtime Package Streaming Candidate Resident Replace Reviewed Evictions v1 adds `execute_selected_runtime_package_streaming_candidate_resident_replace_with_reviewed_evictions_safe_point` as the selected descriptor-to-eviction-assisted-resident-replacement bridge. It builds the reviewed candidate from `RuntimePackageStreamingExecutionDesc`, validates runtime scene preflight, delegates projected replacement plus caller-reviewed eviction candidates to `commit_runtime_package_candidate_resident_replace_with_reviewed_evictions_v2`, validates residency hints before live commit, and reports `candidate_load`, `resident_replace`, `eviction_plan`, and `resident_catalog_refresh` evidence. It narrows package-streaming breadth only to reviewed candidate resident replacement with caller-reviewed evictions and does not add broad/background streaming, hot reload productization, arbitrary/LRU eviction, upload/staging integration, renderer/RHI ownership, allocator/GPU budget enforcement, native handles, or inferred content roots.

Runtime Package Streaming Candidate Resident Replace v1 adds `execute_selected_runtime_package_streaming_candidate_resident_replace_safe_point` as the selected descriptor-to-resident-replacement bridge. It builds the reviewed candidate from `RuntimePackageStreamingExecutionDesc`, validates the existing mount id before filesystem reads, loads the candidate through `load_runtime_package_candidate_v2`, validates residency hints, delegates to the slot-preserving `commit_runtime_resident_package_replace_v2` path, and reports `candidate_load`, `resident_replace`, and `resident_catalog_refresh` evidence. It does not add broad/background package streaming, broad disk/VFS package mount orchestration beyond reviewed candidate resident mount/replacement, hot reload productization, arbitrary eviction, upload/staging integration, renderer/RHI ownership, allocator/GPU budget enforcement, native handles, or inferred content roots.

Runtime UI Platform Text Input Proof Rows v1 keeps IME/text-input candidate, composition, committed-text, and clipboard rows behind first-party `MK_ui` contracts. The selected Windows adapter publishes copied Win32 message rows through `Win32CopiedTextInputMessage`, `win32_committed_text_from_message`, and `Win32ClipboardTextAdapter`; it does not claim UI-side candidate display, native IME parity, virtual keyboard policy, or cross-platform text services.

Runtime UI Win32 Committed Text Edit Apply v1 extends that bridge with `TextEditState`, `CommittedTextInput`, `plan_committed_text_input`, `apply_committed_text_input`, `win32_committed_text_from_message`, and `apply_win32_committed_text_message`, applying copied Win32 char-input rows as committed UTF-8 insertion/replacement after target, cursor, selection, scalar-boundary, and committed-text validation. Runtime UI Text Edit Command Apply v1 adds `TextEditCommandKind`, `TextEditCommand`, `plan_text_edit_command`, and `apply_text_edit_command` for host-independent scalar-boundary cursor movement plus backward/forward deletion on the same `TextEditState`. Runtime UI Win32 Text Edit Command Key Events v1 maps copied Win32 key rows for Left, Right, Home, End, Backspace, and Delete, including repeated key rows, through `win32_text_edit_command_from_input_event` and `apply_win32_text_edit_command_event`. Runtime UI Clipboard Text Request v1 adds the host-independent `IClipboardTextAdapter` request/dispatch boundary plus the Windows `Win32ClipboardTextAdapter` bridge. Runtime UI Text Edit Clipboard Command Apply v1 adds `TextEditClipboardCommandKind`, `TextEditClipboardCommand`, `plan_text_edit_clipboard_command`, and `apply_text_edit_clipboard_command` for host-independent copy/cut/paste application over caller-owned `TextEditState` selection rows. Runtime UI Win32 Text Edit Clipboard Command Key Events v1 maps copied Win32 Ctrl+C/Ctrl+X/Ctrl+V rows through `win32_text_edit_clipboard_command_from_input_event` and `apply_win32_text_edit_clipboard_command_event`.

`Cooked Animation Quaternion Clip v1` adds the host-independent cooked/runtime quaternion clip path: `GameEngine.AnimationQuaternionClipSource.v1`, `GameEngine.CookedAnimationQuaternionClip.v1`, `AssetKind::animation_quaternion_clip`, `runtime_animation_quaternion_clip_payload`, `AnimationJointTrack3dByteSource`, `make_animation_joint_tracks_3d_from_f32_bytes`, and `import_gltf_node_transform_animation_quaternion_clip` for LINEAR glTF node TRS 3D rows. `Generated 3D Quaternion Animation Package Smoke v1` extends committed and generated `DesktopRuntime3DPackage` package smokes with deterministic cooked `animation_quaternion_clip` rows, `--require-quaternion-animation`, byte-row decode, and one-joint `AnimationPose3d` sampling counters. Animation graphs, retargeting, authored IK assets, skeletal renderer deformation, renderer/RHI execution, and broad skeletal animation production readiness remain follow-up work; host-independent 3D local rotation limits/twist controls now live in `MK_animation` as a separate pose/IK clamp contract.

The recommended order is `A -> C -> B`: first the AI-operable production loop and machine-readable contracts, then 2D/3D playable vertical slices, then editor productization on top of the same first-party authoring model. `AI Operable Production Loop Foundation v1` completed the first contract slice by adding `aiOperableProductionLoop`, enforced recipe ids, command surfaces, unsupported gaps, host gates, validation mappings, and top-level `agent-context` output. `Scene Component Prefab Schema v2` implements Scene/Component/Prefab Schema v2 as a contract-only `MK_scene` data spine with stable authoring ids, schema-driven component rows, deterministic validation/serialization, and stable prefab property override paths. `Asset Identity Runtime Resource v2` implemented the initial Asset Identity v2 and Runtime Resource v2 foundation contracts for stable asset keys, deterministic `GameEngine.AssetIdentity.v2` text IO, generation-checked cooked-package resource handles, and stale-handle rejection; Asset Identity v2 is now closed by the 2026-05-12 reference-cleanup milestone and Runtime Resource v2 is now closed for the Engine 1.0 safe-point/controller surface by the 2026-05-16 closeout. `Renderer RHI Resource Foundation v1` plus follow-up RHI slices now close Renderer/RHI Resource Foundation v1 for the Engine 1.0 Windows-default foundation surface through `RhiResourceLifetimeRegistry`, owner labels, debug names, deferred-release records, queue-qualified fence retirement, deterministic diagnostics, marker-style events, NullRHI lifetime wiring, D3D12/Vulkan deferred native teardown, GPU debug markers/timestamp-frequency rows, read-only memory diagnostics, editor Resources diagnostics, and reviewed capture handoff evidence. `Frame Graph Upload Staging Foundation v1` implements Frame Graph and Upload/Staging Foundation v1 as foundation-only `MK_renderer`/`MK_rhi` planning contracts through `FrameGraphV1Desc`, barrier intent rows, `RhiUploadStagingPlan`, staging allocation rows, buffer/texture copy rows, submitted `FenceValue` tracking, and completed-fence retirement. `AI Command Surface Foundation v1` implements AI Command Surface Foundation v1 as a descriptor contract; current ready apply surfaces are limited to reviewed `runtimePackageFiles` registration, cooked UI atlas metadata package row updates, first-party material instance package row updates, explicit material graph -> runtime material package binding, first-party scene package row updates, Scene/Prefab v2 authoring commands, first-party source asset registration, and the Scene v2 runtime package migration bridge. `2D Playable Vertical Slice Foundation v1` implements the ready source-tree `2d-playable-source-tree` recipe through runtime input actions, orthographic scene camera plus sprite validation, scene sprite renderer submission, first-party HUD submission, device-independent audio cue rendering, and `NullRenderer` proof. `2D Desktop Runtime Package Proof v1` implements the separate host-gated `2d-desktop-runtime-package` proof for cooked 2D scene, sprite, HUD, audio, Win32 desktop host execution, installed package validation, and deterministic `NullRenderer` fallback. `3D Playable Vertical Slice Foundation v1` implements the host-gated `3d-playable-desktop-package` sample proof with cooked 3D scene package loading, D3D12 and strict Vulkan scene GPU binding, depth-aware postprocess, and fixed 3x3 PCF directional shadow package smoke. `Native GPU UI Overlay Foundation v1` implements the host-gated `native-gpu-runtime-ui-overlay` proof for renderer-owned final overlay drawing of `MK_ui_renderer` box/image-placeholder sprites on D3D12 and strict Vulkan selected package lanes. `Native UI Textured Sprite Atlas Foundation v1` implements the host-gated `native-ui-textured-sprite-atlas` proof for cooked texture/atlas-backed UI image sprites on the same D3D12 and strict Vulkan selected package lanes. `Native UI Atlas Package Metadata Foundation v1` implements the host-gated `native-ui-atlas-package-metadata` proof for first-party cooked `ui_atlas` package records that drive `UiRendererImagePalette` bindings. `UI Atlas Metadata Authoring Tooling v1` implements the ready cooked-metadata authoring surface for `GameEngine.UiAtlas.v1`, `AssetKind::ui_atlas`, and `ui_atlas_texture` rows through `mirakana::UiAtlasMetadataDocument`, `mirakana::author_cooked_ui_atlas_metadata`, and `mirakana::verify_cooked_ui_atlas_package_metadata`. `UI Atlas Metadata Apply Tooling v1` adds the narrow `MK_tools` dry-run/apply helpers for updating `.uiatlas` and `.geindex` together. `Material Instance Apply Tooling v1` adds the narrow `MK_tools` dry-run/apply helpers for updating material instance `.material` content and `.geindex` material/texture dependency rows together. `Material Graph Package Binding v1` adds the narrow `MK_tools` dry-run/apply helpers for lowering explicit `GameEngine.MaterialGraph.v1` text or a selected graph file into runtime `GameEngine.Material.v1` `.material` content plus `.geindex` material/texture dependency rows. `Scene Package Apply Tooling v1` adds the narrow `MK_tools` dry-run/apply helpers for updating first-party `GameEngine.Scene.v1` `.scene` content and `.geindex` scene mesh/material/sprite dependency rows together. `Source Asset Registration Command Tooling v1` adds `GameEngine.SourceAssetRegistry.v1` plus the narrow `MK_tools` dry-run/apply helpers for registering first-party texture, mesh, audio, material, and scene source asset identity/import intent without executing importers or mutating cooked package rows. `Scene v2 Runtime Package Migration v1` adds the narrow `MK_tools` dry-run/apply bridge from authored `GameEngine.Scene.v2` plus registered source asset keys into the existing `GameEngine.Scene.v1` package update helper. These slices are not claims that renderer/RHI residency, native GPU uploads beyond the reviewed texture ring path, runtime-wide upload rings, staging pools, production render graph scheduling, package streaming beyond the reviewed safe-point/controller surface, GPU allocator enforcement/residency budgets, production 2D atlas/tilemap/runtime-image/native GPU output, production UI atlas packing, production text/font/image/accessibility, shader graphs, live shader generation, broad automatic material graph cooking beyond the reviewed package update surface, broad 3D production renderer, editor productization, nested prefab propagation/merge resolution UX, broad package cooking, dependent asset cooking, external importer execution from migration, or Metal readiness is implemented.

RHI Upload Stale Generation Diagnostics v1 narrows `upload-staging-v1` by returning the existing `RhiUploadDiagnosticCode::stale_generation` row for same-id/wrong-generation staging handles and by making `RhiUploadRing` ownership checks generation-aware; it does not claim native async upload execution or package-streaming readiness.

RHI Native Async Upload Execution v1 narrows `upload-staging-v1` by adding `execute_upload_gpu_batch_async` and `RhiUploadGpuBatchExecutionResult` so a caller-owned `RhiUploadStagingPlan` / `RhiUploadRing` batch can prevalidate targets and ring ownership, submit copies on a selected backend-neutral queue, mark staging allocations with the returned `FenceValue`, and avoid helper-owned CPU waits. Runtime Upload Queue Wait v1 adds `wait_for_runtime_uploads_on_queue` and package mesh/skinned/morph `upload_queue_waits_recorded` evidence so copy-queue uploads can be consumed by graphics bindings through backend-neutral GPU-side waits without CPU waits. Staging Pool Lease Adoption v1 adds `RhiStagingBufferLease`, `RhiStagingBufferPool::try_acquire_lease`, `RhiStagingBufferPool::release(RhiStagingBufferLease)`, and `RhiUploadRingDesc::buffer` so host-owned pool chunks can back caller-owned upload rings without extra ring allocation. Package Resource Update Readiness v1 and selected D3D12 generated 3D package upload staging evidence are complete through `--require-package-upload-staging`; allocator/GPU budget enforcement, public native handles, runtime-wide staging-pool ownership beyond explicit leases, and async overlap/performance remain unsupported.

Full Repository Quality Gate 1.0 Closeout v1 closes `full-repository-quality-gate` for the Engine 1.0 Windows-default ready surface by accepting the local full `tools/validate.ps1` gate, CI Matrix Contract Check v1, Full Repository Static Analysis CI Contract v1, Linux coverage threshold policy, sanitizer lane documentation, C++23 Release Package Artifact CI Evidence v1, installed SDK validation, and release package artifact validation. Broader build/package CI execution evidence beyond reviewed contracts, broader analyzer profile expansion, full cross-platform package execution evidence, signing, notarization, upload, release distribution, crash/telemetry backend publication, and non-Windows host execution remain future or host-gated release work.

The later Scene/Prefab authoring slice extends the ready source authoring surface beyond those cooked-package rows with reviewed `GameEngine.Scene.v2` and `GameEngine.Prefab.v2` dry-run/apply commands. The source asset registration slice adds a reviewed `GameEngine.SourceAssetRegistry.v1` command for source identity/import intent. The registered source asset cook/package slice adds reviewed explicit-row cooked artifact and `.geindex` updates through existing import/package helpers. The Scene v2 runtime package migration slice adds the narrow reviewed bridge into the existing Scene v1 package update surface. Broad package cooking, dependent asset cooking, editor productization, nested prefab merge/resolution UX, renderer/RHI residency, package streaming, and Metal readiness still remain outside the ready claim.

`Validation Recipe Runner Tooling v1` completed the reviewed non-mutating `run-validation-recipe` dry-run/execute surface for resolving only allowlisted manifest-declared validation recipes into reviewed command builders with host-gate diagnostics. It does not evaluate arbitrary shell input or raw manifest command strings, and it does not broaden editor productization, package mutation, runtime source import, material/shader graphs, live shader generation, renderer/RHI residency, package streaming, public native handle, Metal, or renderer quality claims.

`Scene Prefab Authoring Command Tooling v1` completed the reviewed Scene/Component/Prefab Schema v2 authoring command surface for create scene, node/component edits, prefab creation, and prefab instantiation through `mirakana::plan_scene_prefab_authoring` and `mirakana::apply_scene_prefab_authoring`; the later `refresh-prefab-instance` slice adds reviewed stable-id prefab instance `.scene` refresh writes on the same helper. Scene v1 cooked package updates remain on `update-scene-package`, while the reviewed runtime package migration bridge stays on `migrate-scene-v2-runtime-package`. Source asset import execution, broad package cooking, dependent asset cooking, editor productization, nested prefab merge/resolution UX, renderer/RHI residency, package streaming, public native handles, Metal, and general production renderer quality remain planned or host-gated.

`Source Asset Registration Command Tooling v1` completed the reviewed source asset registration dry-run/apply surface for first-party source asset identity and deterministic import intent. `Scene v2 Runtime Package Migration v1` completed the narrow reviewed bridge from authored `GameEngine.Scene.v2` plus `GameEngine.SourceAssetRegistry.v1` rows into the existing runtime-loadable `GameEngine.Scene.v1` package update surface. `Registered Source Asset Cook Package Command Tooling v1` adds the reviewed `cook-registered-source-assets` dry-run/apply surface for explicitly selected source registry rows, deterministic cooked artifacts, and `.geindex` package updates through existing import/package helpers, while keeping broad dependency traversal, renderer/RHI residency, package streaming, material/shader graphs, live shader generation, editor productization, public native handles, Metal, and general production renderer quality planned or host-gated.

`Scene v2 Registered Asset Runtime Workflow Validation v1` completed a host-independent workflow validation slice proving the validated authored-to-runtime workflow `register-source-asset -> cook-registered-source-assets -> migrate-scene-v2-runtime-package -> validate-runtime-scene-package` without turning into broad package cooking, renderer/RHI residency, package streaming, runtime source parsing, material/shader graph work, live shader generation, editor productization, public native handles, Metal, or general production renderer quality claims. `Scene v2 Command Authored Runtime Workflow Validation v1` extends that proof so reviewed Scene/Prefab v2 authoring commands feed the same workflow before runtime package validation. `Scene v2 Prefab Source Provenance v1` adds deterministic `prefab_source` provenance rows for instantiated Scene/Prefab v2 node/component copies. `Scene v2 Stable Id Prefab Refresh Plan v1` adds contract-only stable-id `plan_scene_prefab_instance_refresh_v2` planning against `PrefabDocumentV2` through `source_node_id` / `source_component_id`; `Scene v2 Prefab Refresh Integrity v1` makes the planner fail closed with `duplicate_prefab_source_identity` diagnostics for ambiguous copied source identities; `Scene v2 Prefab Refresh Nested Guard v1` makes nested prefab roots fail closed with `unsupported_nested_prefab_instance`; `Scene v2 Prefab Refresh Local Ownership Guard v1` makes local child nodes/components fail closed with `unsupported_local_prefab_child` / `unsupported_local_prefab_component`; `Scene v2 Prefab Refresh Apply v1` adds pure value `apply_scene_prefab_instance_refresh_v2` / `ScenePrefabInstanceRefreshResultV2` apply over reviewed stable-id rows; and `Scene v2 Prefab Refresh Command Surface v1` exposes that apply through reviewed `refresh-prefab-instance` authored `.scene` writes while keeping editor actions, runtime prefab semantics, nested propagation, editor productization, broad package cooking, renderer/RHI residency, and native handles unsupported.

`Runtime Scene Package Validation Command Tooling v1` completed the reviewed `validate-runtime-scene-package` surface for non-mutating runtime scene package validation through `mirakana::plan_runtime_scene_package_validation` and `mirakana::execute_runtime_scene_package_validation`, covering explicit runtime `.geindex` package load plus `MK_runtime_scene` instantiation diagnostics so agents can validate the final authored-to-runtime package step without inventing broad package cooking, renderer/RHI residency, package streaming, runtime source parsing, editor productization, native handle, Metal, or general renderer claims.

`Gameplay Authoring Foundation v1` Phase 1 adds Runtime Scene Gameplay Binding v1 in `MK_runtime_scene`: `RuntimeSceneGameplayBindingSourceRow` plus `resolve_runtime_scene_gameplay_bindings` let generated gameplay resolve reviewed scene node names to `SceneNodeId` rows with required component gates for `camera`, `light`, `mesh_renderer`, `sprite_renderer`, or `any_renderable`, while failing closed for invalid ids, duplicate binding ids, missing or duplicate node names, and missing required components. Selected 2D/3D package smokes and installed validation now require `gameplay_systems_scene_binding_ready=1`, 3 source rows, 3 resolved binding rows, 3 gameplay systems, 3 component-backed rows, and clean diagnostics under `--require-gameplay-systems`. This starts the developer-owned `gameplay-authoring-foundation-v1` capability after the Engine 1.0 zero-gap closeout and does not reopen `unsupportedProductionGaps`, add a gameplay scheduler, editor command surface, renderer/RHI execution, package format change, or broad generated-game production readiness.

`Gameplay Authoring Foundation v1` Phase 2 adds Runtime Scene Gameplay Interaction Framework v1 in `MK_runtime_scene`: `RuntimeSceneGameplayInteractionSourceRow` plus `plan_runtime_scene_gameplay_interactions` layer deterministic trigger, pickup, damage, heal, objective, win/loss, and restart intent rows over resolved gameplay bindings. Planning consumes `RuntimeSceneGameplayBindingRow` values, resolves source/target `SceneNodeId` references, applies ordered session-state transitions, and fails closed for invalid/duplicate action ids, missing source or target bindings, missing objective ids, invalid amounts, and rejected terminal-state transitions. `sample_2d_playable_foundation` and `sample_gameplay_foundation` show 2D and headless gameplay-family consumers; selected 2D/3D package smokes now require 3 scene interaction rows, clean diagnostics, and `gameplay_systems_scene_interaction_final_session_state=won`. This is still value-only gameplay authoring infrastructure; it does not add a gameplay scheduler, script runtime, physics/event dispatcher, editor command surface, renderer/RHI execution, package format change, or broad generated-game production readiness.

`Engine Asset Placeholder Generation v1` Phase 1 adds Asset Placeholder Generation v1 in `MK_tools`: `PlaceholderAssetBundleRequest` plus `plan_placeholder_asset_bundle` plan deterministic first-party texture, mesh, material, and audio source documents, synchronized `GameEngine.SourceAssetRegistry.v1` text, changed-file hashes, and provenance rows. This is a reviewed legal placeholder planning primitive for missing generated-game assets; it does not download external assets, run arbitrary image/audio generation, execute importers, cook packages, create renderer/RHI residency, stream packages, or define final game-specific art direction.

`Engine Asset Placeholder Generation v1` Phase 2 adds `PlaceholderAssetCookPackageRequest` plus `plan_placeholder_asset_cook_package` in `MK_tools`, routing generated placeholder source documents into the reviewed registered source cook/package planner before runtime package consumption. The promoted foundation also proves reviewed replacement recooks against existing source registry/package index content, stable asset keys, source revisions, content hashes, provenance/license rows, `game.agent.json.aiWorkflow.placeholderAssetPipeline.replacementWorkflow`, and package handoff counters for selected sample/generated package manifests. This remains a package-authoring helper, not broad automatic cook promotion, external asset download, renderer/RHI residency, package streaming, runtime source-registry parsing, or a final art direction system.

`Engine Gameplay Debug Overlay v1` adds Runtime Gameplay Debug Overlay v1 in `MK_ui`: `RuntimeGameplayDebugOverlayRowDesc` plus `plan_runtime_gameplay_debug_overlay` validate deterministic gameplay/input/audio/physics/navigation/AI/package/session/custom debug rows and fail closed for missing or duplicate row ids, missing labels, unsupported categories, and unsupported row kinds. `sample_2d_playable_foundation` adopts the planner and reports `debug_overlay_rows=12`. This is value-only debug overlay infrastructure; it does not render UI, execute debug commands, expose Dear ImGui/editor/native handles, create renderer/RHI residency, stream packages, or define game-specific debug schemas. Later developer-owned milestones are tracked in the plan registry; the composed manifest owns the current active pointer.

`Engine Save Settings Profile v1` Phase 1 adds Runtime Session Profile Path Policy v1 in `MK_runtime`: `RuntimeSessionProfilePathRequest` plus `plan_runtime_session_profile_paths` derive deterministic project-relative `save.gesave`, `settings.settings`, and `input.geinputprofile` paths under one game-local profile root. Planning fails closed for invalid game ids, profile ids, root paths, absolute paths, parent traversal, path separators in ids, and control characters without mutating the filesystem. This is value-only save/settings/profile foundation work; it does not add platform user-directory resolution, cloud saves, binary saves, native handles, filesystem mutation, or broad save migration/backup workflow.

`Engine Save Settings Profile v1` Phase 2 adds Runtime Session Profile Document Bundle v1 in `MK_runtime`: `RuntimeSessionProfileDocuments` plus `load_runtime_session_profile_documents` / `write_runtime_session_profile_documents` let generated games load save/settings/input-profile documents, default missing optional documents, distinguish corrupt from unsupported-version rows, and reset/write reviewed defaults without deleting unrelated profile-root files. `sample_gameplay_foundation` demonstrates the source-tree load/write path. This remains a value-oriented session-profile primitive and does not add cloud saves, binary saves, platform user-directory resolution, save migration/backup workflow, native handles, or a game-specific save schema.

`Simulation Persistence v1` adds Runtime Simulation Persistence Plan v1 in `MK_runtime`: `RuntimeSimulationPersistenceRequest` plus `plan_runtime_simulation_persistence` review already-loaded save document evidence for stable save-slot/world/snapshot/tick state, deterministic `RuntimeSimulationPersistentEntityRow` rows, supported reachable `RuntimeSimulationPersistenceMigrationStep` chains, and corrupt-save remediation recommendations through `RuntimeSimulationPersistenceRemediationAction`. Non-save profile document failures do not block valid save recovery because this planner only reviews simulation state evidence. This remains a value-only review surface; it does not add cloud saves, cross-device sync, multiplayer replication, binary compatibility policy, platform user-directory resolution, automatic migration execution, filesystem/package mutation, native handles, or game-specific component serialization beyond reviewed save keys.

`Engine UI Game Menu Hud v1` adds Runtime Menu HUD Intent Model v1 in `MK_ui`: `RuntimeMenuHudRowDesc` plus `plan_runtime_menu_hud` let generated games validate label, counter, prompt, `dialogue_box`, `input_binding_prompt`, and command rows before building UI elements or executing menu commands. Planning returns deterministic `RuntimeMenuHudDisplayRow` / `RuntimeMenuHudCommandRow` values and fails closed for missing or duplicate row ids, unsupported row kinds, missing or duplicate command ids, invalid command intents, and invalid command targets. `sample_ui_audio_assets` demonstrates the source-tree HUD/menu intent path, while `sample_2d_desktop_runtime_package` and `sample_generated_desktop_runtime_3d_package` expose selected `--require-runtime-menu-hud` counters for `runtime_menu_hud_ready`, display rows, command rows, dialogue rows, and input-binding prompt rows. This remains a value-only game-owned UI intent primitive and does not add a widget toolkit, UI renderer, focus/navigation system, command dispatcher, full input rebinding panel, platform UI service, native handles, or game-specific menu schema.

`Engine Audio Gameplay Mixer v1` adds selected package-visible Audio Gameplay Mix Planner evidence in `MK_audio`: `AudioGameplayMixRequest` plus `plan_gameplay_audio_mix` validate music/SFX buses, pause, fade, looping, triggers, and optional spatial cue metadata before game code creates mixer-ready voices. `sample_2d_desktop_runtime_package` and `sample_generated_desktop_runtime_3d_package` load cooked package audio payloads before exposing `--require-audio-gameplay-mixer` counters for `audio_gameplay_mixer_ready`, bus/cue/trigger/command rows, paused/faded bus rows, looping/spatial command rows, deterministic render command/frame/sample rows, sample absolute sums, and sample/payload diagnostics. This remains a first-party value and device-independent PCM evidence path; it does not add broad codec decoding, native device ownership, HRTF, DSP graphs, device hotplug/selection, middleware, or mixer authoring UI.

`Engine Gameplay Debug Overlay v1` Phase 1 adds Runtime Gameplay Debug Overlay v1 in `MK_ui`: `RuntimeGameplayDebugOverlayRowDesc` plus `plan_runtime_gameplay_debug_overlay` let generated games validate deterministic gameplay debug overlay rows before rendering or reporting them. Planning returns `RuntimeGameplayDebugOverlayPlan` / `RuntimeGameplayDebugOverlayRow` values and fails closed through `RuntimeGameplayDebugOverlayDiagnosticCode` for missing or duplicate row ids, missing labels, unsupported categories, and unsupported row kinds. This remains a value-only debug overlay primitive and does not add a widget toolkit, UI renderer, renderer/RHI execution path, debug command dispatcher, Dear ImGui/editor surface, platform UI service, native handles, telemetry backend, package streaming, or game-specific debug schema.

`Engine Quest Dialogue State v1` Phase 2 adds Runtime Quest Dialogue State v1 in `MK_runtime`: `RuntimeQuestDialogueState`, `RuntimeQuestDialogueStateValidationResult`, and `validate_runtime_quest_dialogue_state` validate saved progress rows, while `RuntimeQuestDialogueTransitionRequest`, `RuntimeQuestDialogueTransitionRow`, `RuntimeQuestDialogueTransitionResult`, and `advance_runtime_quest_dialogue_state` classify deterministic `set_flag`, `complete_objective`, and `choose_dialogue` transitions as accepted, ignored, blocked, completed, or invalid over validated quest/dialogue documents and state. `sample_2d_desktop_runtime_package --require-gameplay-systems` reports package-visible `gameplay_systems_quest_dialogue_*` counters including transition rows, completed objectives, flags, dialogue nodes, action ids, reward ids, and save-state rows. This remains value-only narrative/progression infrastructure; it does not execute rewards/actions, render dialogue UI, add scripting, mutate saves, add editor graph tooling, change package formats, or broaden generated-game production readiness.

`Engine Inventory Items Crafting v1` Phase 2 adds Runtime Inventory Items Crafting v1 in `MK_runtime`: `RuntimeItemCatalogDocument`, `RuntimeItemDesc`, `RuntimeItemCostDesc`, `RuntimeItemCatalogValidationContext`, `RuntimeItemCatalogValidationResult`, and `validate_runtime_item_catalog_document` validate reusable item ids, stack limits, categories, tags, localization keys, optional placement ids, and placement cost references before game-owned inventory, placement, economy, or crafting systems execute. `RuntimeInventoryState`, `RuntimeInventoryStackDesc`, `RuntimeInventoryStateValidationResult`, `validate_runtime_inventory_state`, `RuntimeCraftingRecipeDocument`, `RuntimeInventoryTransitionRequest`, `RuntimeInventoryTransitionStatus`, and `advance_runtime_inventory_state` add value-only add/remove/craft transitions that normalize stacks, preserve state on blocked/invalid rows, and classify rows as accepted, ignored, blocked, completed, or invalid. `Engine Construction Placement v1` Phase 1 adds `RuntimeConstructionPlacementSurfaceDesc`, `RuntimeConstructionPlacementCandidateDesc`, `RuntimeConstructionPlacementValidationContext`, `RuntimeConstructionPlacementValidationResult`, `RuntimeConstructionPlacementDiagnostic`, `RuntimeConstructionPlacementValidationRow`, and `validate_runtime_construction_placement` for value-only construction placement previews over item placement metadata, supported surfaces, finite grid/world positions, candidate row grid/world origins, positive footprints, duplicate occupied cells, and provided cost rows without mutating scene state. Phase 2 adds `RuntimeSceneConstructionPlacementIntentDesc`, `RuntimeSceneConstructionPlacementIntentContext`, `RuntimeSceneConstructionPlacementIntentPlan`, and `plan_runtime_scene_construction_placement_intents` in `MK_runtime_scene` for reviewed node/component placement intent rows classified as accepted, blocked, invalid, or already_occupied; it validates transforms against scene validity and placement world position and rejects same-batch occupied-cell collisions without implicit scene mutation. `sample_2d_desktop_runtime_package --require-gameplay-systems` reports package-visible item pickup, inventory state validation, recipe completion, placement preview, and scene-intent counters such as `gameplay_systems_inventory_items_transition_rows`, `gameplay_systems_inventory_items_final_workbench_quantity`, `gameplay_systems_construction_placement_validation_rows`, `gameplay_systems_construction_placement_intent_rows`, `gameplay_systems_construction_placement_intent_accepted_rows`, and `gameplay_systems_construction_placement_intent_occupied_cells`. This remains value-only catalog/state/transition/placement-validation/placement-intent infrastructure; it does not mutate persistence storage, define item art or balance, add shop/loot/equipment semantics, add scripting, add editor item tooling, change package formats, or broaden generated-game production readiness.

`Engine Procedural Generation v1` Phase 1 adds Runtime Procedural Generation Seed/Plan Contract v1 in `MK_runtime`: `RuntimeProceduralSeedStream`, `make_runtime_procedural_seed_stream`, `advance_runtime_procedural_seed`, `RuntimeProceduralGenerationRequest`, `RuntimeProceduralGenerationContext`, `RuntimeProceduralGenerationPlan`, and `plan_runtime_procedural_generation` produce deterministic value-only map tile, encounter, loot, and object rows from reviewed generator ids, non-zero seeds, map extents, output budgets, and supported content ids. The plan emits reproducible output rows and a `replay_hash`, and fails closed for invalid generator ids, invalid seeds, invalid map extents, output-budget overflow, invalid counts, duplicate content ids, unsupported content ids, and unsupported content kinds. Phase 2 adds Runtime Scene Procedural Construction Placement Bridge v1 in `MK_runtime_scene`: `RuntimeSceneProceduralConstructionPlacementIntentDesc` and `plan_runtime_scene_procedural_construction_placement_intents` attach reviewed object/encounter/loot procedural output ids, anchors, and package-visible evidence to the existing construction placement intent planner, preserving fail-closed diagnostics for missing/duplicate output ids, missing anchors, unsupported map-tile placement attempts, package-invisible rows, invalid transforms, occupied cells, and invalid node/component requirements. Phase 3 adds selected `sample_2d_desktop_runtime_package --require-procedural-generation` evidence for deterministic procedural generation counters, diagnostics, object/encounter/loot row counts, replay hash, package-visible adoption rows, and procedural placement intent rows. This does not mutate scenes/packages/files, call external services, add game-specific generation rules, execute scripts, expose renderer/RHI handles, or claim content quality/broad generated-game production readiness.

`Engine World Region Streaming v1` completed the developer-owned scale-enabler world-region streaming milestone after procedural generation. It adds deterministic world-region catalog rows, safe-point load/unload planning, residency budget diagnostics, reviewed package bridge evidence, and selected package counters while keeping `unsupportedProductionGaps = []`. `World Streaming Large Scene Readiness v1` extends that foundation with `RuntimeWorldStreamingLargeSceneReadinessReport`, selected load/keep/unload package counters, missing-region diagnostics, budget evidence, and navigation path-cache readiness evidence while keeping background streaming, open-world parity, platform async job systems, and native handles as future work.

`Engine Entity Scale And Culling v1` completed the developer-owned scale-enabler milestone after world-region streaming. It adds deterministic entity rows, culling decisions, LOD/update/draw budget intent, selected `sample_2d_desktop_runtime_package --require-entity-scale-culling` counters, generated-game guidance, and full validation evidence while keeping `unsupportedProductionGaps = []`. `Generated 3D Entity Scale Culling Package Evidence v1` extends the same package evidence to `sample_generated_desktop_runtime_3d_package` through `installed-d3d12-3d-entity-scale-culling-smoke` without scene GPU or renderer selection. `Renderer GPU Memory v1` completed the developer-owned GPU memory milestone with backend-neutral GPU memory policy diagnostics, package-visible GPU memory counters, and selected D3D12 GPU memory execution evidence while keeping `unsupportedProductionGaps = []`. `Renderer Debug Profiling v1` completed the developer-owned debug/profiling milestone with backend-neutral debug profiling policy diagnostics, package-visible counters, and selected D3D12 debug profiling execution evidence while keeping `unsupportedProductionGaps = []`. `Renderer Backend Parity v1` completed Vulkan debug profiling execution evidence, cross-backend proof guards, and installed-vulkan-scene-gpu-smoke promotion without D3D12 cross-backend claims. `Engine Scripting Sandbox v1` completed the developer-owned optional-adapter scripting sandbox policy foundation with selected `sample_2d_desktop_runtime_package --require-scripting-sandbox-policy` package evidence, hosted PR #170 checks, and merge commit `d17ac0ac`. `Engine Networking Foundation v1` completed the developer-owned optional-adapter networking foundation through PR #171 / merge commit `987016f1` with value-only networking policy rows and selected `sample_2d_desktop_runtime_package --require-networking-foundation-policy` package evidence. `Gameplay Simulation Orchestration v1` completed through PR #172 / merge commit `eb6a34b5`; it adds `RuntimeSimulationOrchestrationRequest`, deterministic fixed-step simulation rows, ordered input-command playback rows, budget-limited frame planning, replay-ready diagnostics, and selected `sample_2d_desktop_runtime_package --require-simulation-orchestration` counters without game-rule scheduling, renderer frame pacing, networking I/O, rollback/lockstep claims, threads, package mutation, editor runtime hosts, or native handles. `AI Gameplay Authoring Tools v1` implementation completed through PR #174 / merge commit `9bffa2b8`, with selection-gate closeout pointer sync through PR #175 / merge commit `f05136e2`; it promotes selected `sample_2d_desktop_runtime_package --require-gameplay-authoring-review` counters for reviewed feature rows, mutation-ledger rows, remediation rows, and fail-closed unsupported evidence/claim diagnostics without cooked-package mutation or autonomous commercial game-design claims. `AI Engine Capability Handoff v1` adds value-only `MK_tools` handoff review and `game.agent.json.aiWorkflow.engineCapabilityHandoffs` descriptor rows so unsupported reusable engine capability requests become developer-owned backlog handoffs instead of engine-internal game-creation edits. `AI Game Design Spec v1` adds fail-closed `game.agent.json.aiWorkflow.gameDesignSpec` rows and static checks so generated 2D/3D package work records gameplay family, template, camera, input, loop, scene/assets/systems, package targets, validation recipe ids, quality gates, and unsupported claims before expansion. `AI Game Generation Orchestrator v1` adds `tools/create-game-recipe.ps1` as a reviewed dry-run/apply wrapper over `tools/new-game.ps1` for `DesktopRuntime2DPackage` and `DesktopRuntime3DPackage` scaffolds, with deterministic JSON file lists and no arbitrary shell execution or external asset generation. `AI Safe Content Mutation Ledger v1` adds fail-closed `game.agent.json.aiWorkflow.contentMutationLedger` rows for generated/sample 2D/3D package games so game-owned roots, generated files, reviewed command surfaces, forbidden shared paths, and remediation actions are explicit before AI content mutation. `AI Placeholder Asset Pipeline v1` adds fail-closed `game.agent.json.aiWorkflow.placeholderAssetPipeline` rows so generated/sample 2D/3D package games map design asset requests to first-party placeholder assets, replacement workflows, package handoff counters, validation recipes, and runtime package files without external downloads, runtime source parsing, renderer/RHI residency, or broad quality claims.

`Production Authoring Workflows v1` adds value-only `MK_tools` production workflow review through `review_production_authoring_workflow` plus selected `sample_2d_desktop_runtime_package --require-production-authoring-workflows` counters for six reviewed scene placement, quest/dialogue, item/economy, AI behavior, world region, and validation repair workflow rows, six mutation-ledger rows, one validation-repair row, and fail-closed forbidden-scope diagnostics. It does not mutate files or cooked packages, execute shell commands, expose native/editor/RHI handles, or claim autonomous production authoring.

`Game Manifest Runtime Scene Validation Targets v1` completed descriptor-only `game.agent.json.runtimeSceneValidationTargets` rows so agents can select reviewed `validate-runtime-scene-package` inputs from manifests without scraping smoke-test command strings or adding a new execution command. `2D Packaged Playable Generation Loop v1` completed the generated packaged 2D scaffold contract through `tools/new-game.ps1 -Template DesktopRuntime2DPackage`, cooked sprite/material/audio/orthographic scene payloads, manifest runtime package files, manifest runtime scene validation targets, public input/UI/audio/package code, and focused static scaffold checks. `Generated 2D Native RHI Sprite Package Scaffold v1` extends that generated scaffold with host-gated D3D12 native 2D sprite package smoke metadata and generated shader source, without broadening the production renderer claim. `2D Sprite Batch Planning Contract v1` adds 2D Sprite Batch Planning Contract v1, a host-independent `MK_renderer` planning contract for order-preserving adjacent-run sprite batch plans and invalid texture metadata diagnostics. `2D Sprite Batch Package Telemetry v1` adds 2D Sprite Batch Package Telemetry v1, a `MK_scene_renderer` adapter and `sample_2d_desktop_runtime_package` `sprite_batch_plan_*` smoke counters for cooked scene sprites. `Sprite Batching Renderer v1` adds backend-neutral `SpriteBatchBudgetProfile` rows plus `sprite_batch_budget_*` package counters for world, UI, and effects lane budgets. `2D Playable Vertical Slice 1 0 Closeout v1` closes the Engine 1.0 Windows-default generated 2D package surface after D3D12/Vulkan package smokes, native 2D sprite execution, sprite animation, and tilemap runtime UX counters. Broad package cooking, dependent asset cooking, runtime source parsing, renderer/RHI residency beyond selected host-owned package smokes, package streaming, editor productization, atlas editor workflow, full tilemap editing canvas, broad production sprite batching readiness, native handles, Metal, and general renderer quality remain planned or host-gated.

Completion-uplift review on 2026-05-02: `3D Packaged Playable Generation Loop v1` completed a generated packaged 3D gameplay scaffold contract through `DesktopRuntime3DPackage` and the same reviewed package/validation pattern. `Editor Playtest Package Review Loop v1` completed Editor Playtest Package Review Loop v1, which sequences existing editor package candidates, reviewed `runtimePackageFiles` registration, manifest `runtimeSceneValidationTargets`, `validate-runtime-scene-package`, and host-gated desktop smoke without claiming broad package cooking, editor productization, renderer/RHI residency, package streaming, native handles, Metal readiness, or general renderer quality. `Renderer Resource Residency Upload Execution v1` completed Renderer Resource Residency Upload Execution v1, a host-gated upload/report loop for selected cooked package payloads with backend-neutral scene GPU counters and no gameplay native-handle exposure. `Material Shader Authoring Loop v1` completed Material Shader Authoring Loop v1 through descriptor-only `materialShaderAuthoringTargets`, manifest `materialShaderAuthoringLoops`, generated template rows, static checks, and D3D12 host-gated generated material/shader package smoke. `Package Streaming Residency Budget Contract v1` completed Package Streaming Residency Budget Contract v1 through `packageStreamingResidencyTargets`, manifest `packageStreamingResidencyLoops`, generated/sample rows, and explicit blocked execution for broad background streaming, arbitrary eviction, native handles, Metal, and renderer quality claims. `Safe Point Package Unload Replacement Execution v1` completed the initial safe-point package replacement helper through `mirakana::runtime::commit_runtime_package_safe_point_replacement`, catalog-generation stale-handle tests, invalid replacement rollback, manifest `safePointPackageReplacementLoops`, and docs/static checks without broad async streaming, eviction, renderer/RHI teardown execution, public native/RHI handles, Metal, or renderer quality claims. `Host Gated Package Streaming Execution v1` completed Host-Gated Package Streaming Execution v1 through `mirakana::runtime::execute_selected_runtime_package_streaming_safe_point`, host-gated-safe-point manifest rows, tests, docs/static checks, and explicit unsupported claims for broad/background streaming, arbitrary eviction, allocator/GPU budget enforcement, renderer/RHI teardown execution, public native/RHI handles, Metal, and renderer quality. `2D Atlas Tilemap Package Authoring v1` completed 2D Atlas Tilemap Package Authoring v1 through `GameEngine.Tilemap.v1`, `AssetKind::tilemap`, `tilemap_texture` dependency rows, `atlasTilemapAuthoringTargets`, generated 2D package files, runtime tilemap payload inspection, docs/static checks, and explicit unsupported claims for source image decoding, production atlas packing, full tilemap editor UX, native GPU sprite batching, package streaming execution, public native/RHI handles, Metal, and renderer quality. `3D Prefab Scene Package Authoring v1` completed 3D Prefab Scene Package Authoring v1 through `prefabScenePackageAuthoringTargets`, generated and sample source Scene/Prefab v2 files, selected SourceAssetRegistry rows, schema/static checks, and explicit unsupported claims for broad importer execution, broad/dependent cooking, runtime source parsing, material/shader graphs, live shader generation, skeletal animation/GPU skinning, public native/RHI handles, Metal, and renderer quality. `Editor AI Package Authoring Diagnostics v1` completed Editor AI Package Authoring Diagnostics v1 through a diagnostics-only editor-core model, manifest `reviewLoops`, tests, docs/static checks, and explicit blocked mutation/execution claims. `Editor AI Validation Recipe Preflight v1` completed Editor AI Validation Recipe Preflight v1 through `EditorAiValidationRecipePreflightModel`, manifest `reviewLoops`, tests, docs/static checks, and explicit blocked raw shell, raw manifest command, package execution, play-in-editor, renderer/RHI handle, Metal, and renderer quality claims. `Editor AI Playtest Operator Workflow UX v1` is completed with plan-recorded `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/validate.ps1` PASS evidence; `Desktop Runtime Shippable RHI Window v1` is completed for host-gated D3D12/Vulkan RHI-backed package window presentation; `2D Native RHI Sprite Package Proof v1` is completed for the installed D3D12 native 2D sprite package proof; `Generated 2D Native RHI Sprite Package Scaffold v1` is completed for generated `DesktopRuntime2DPackage` native sprite smoke metadata and shader-source scaffolding; `2D Sprite Batch Planning Contract v1` is completed for host-independent order-preserving sprite batch planning diagnostics. At that time the active pointer moved through the sprite batch planning slice; current pointers are manifest-led and recorded in `engine/agent/manifest.json`.

`3D Playable Vertical Slice 1 0 Closeout v1` closes the Engine 1.0 Windows-default generated 3D package surface after generated desktop package proof, D3D12/Vulkan package smokes, visible 3D aggregate counters, scene GPU/postprocess/depth/shadow/morph/frame graph counters, compute morph/skinned compute package evidence, and native UI overlay/atlas package counters. Runtime source parsing, broad dependency cooking, broad/background streaming, live shader generation, Metal, native handles, and general renderer quality remain planned, host-gated, or explicit non-goals.

Editor AI Playtest Readiness Report v1 completed in `Editor AI Playtest Readiness Report v1` through `EditorAiPlaytestReadinessReportModel`, a read-only aggregation of package authoring diagnostics and validation recipe preflight into ready, blocked, or host-gated operator-validation status. `Editor AI Playtest Operator Handoff v1` completed Editor AI Playtest Operator Handoff v1 through `EditorAiPlaytestOperatorHandoffModel`, deterministic external-operator rows with reviewed command display, argv plan data, host gates, blockers, and readiness dependency. `Editor AI Playtest Evidence Summary v1` completed Editor AI Playtest Evidence Summary v1 through `EditorAiPlaytestEvidenceSummaryModel`, deterministic per-recipe `passed`, `failed`, `blocked`, `host_gated`, or `missing` rows over externally supplied validation evidence. `Editor AI Playtest Remediation Queue v1` completed Editor AI Playtest Remediation Queue v1 through `EditorAiPlaytestRemediationQueueModel`, deterministic read-only remediation category rows and next-action text for failed, blocked, missing, and host-gated evidence. `Editor AI Playtest Remediation Handoff v1` completed Editor AI Playtest Remediation Handoff v1 through `EditorAiPlaytestRemediationHandoffModel`, deterministic external-owner/action-kind handoff rows with handoff text for remediation queue items. Editor AI Playtest Operator Workflow v1 now consolidates the loop, the remediation evidence-review and closeout-report candidates were retired because closeout is represented by rerunning the existing evidence summary and observing no remediation queue/handoff rows, and `EditorAiPlaytestOperatorWorkflowReportModel` provides a structured report surface with stage rows, source model names, source row counts, source row ids, host gates, blockers, closeout state, and unsupported-claim diagnostics. These models do not add validation execution, package script execution, evidence mutation, remediation mutation, fix execution, free-form manifest edits, arbitrary shell, raw/free-form command evaluation, play-in-editor productization, renderer/RHI handle exposure, Metal readiness, or general renderer quality.

Play-In-Editor Session Isolation v1 completed in `Play In Editor Session Isolation v1` through `EditorPlaySession`, an isolated simulation scene snapshot from `SceneAuthoringDocument`, source-scene edit blocking reports, simulation tick counts, and deterministic begin/pause/resume/tick/stop transitions. This is editor-core isolation only; gameplay execution inside Play-In-Editor, package scripts, renderer/RHI uploads, native handles, hot reload, package streaming, and full editor productization remain follow-up work.

Play-In-Editor Gameplay Driver v1 completed in `Play In Editor Gameplay Driver v1` through `IEditorPlaySessionDriver`, `EditorPlaySessionTickContext`, driver begin/tick/end callbacks against the isolated simulation scene, finite positive tick delta validation, pause rejection, last-delta reporting, and driver detach on stop. This is still editor-core callback execution only; dynamic game module loading, `DesktopGameRunner` embedding, package scripts, validation recipe execution, renderer/RHI uploads, native handles, hot reload, package streaming, and full editor productization remain follow-up work.

Play-In-Editor Visible Viewport Wiring v1 completed in `Play In Editor Visible Viewport Wiring v1` through `EditorPlaySessionControlsModel`, visible Run menu and Viewport toolbar commands over `EditorPlaySession`, simulation-scene viewport rendering while active, per-viewport-frame play ticks, and source-scene authoring plus undo/redo blocking. Dynamic game module loading, `DesktopGameRunner` embedding, package scripts, validation recipe execution, renderer/RHI uploads from editor core, native handles, hot reload, package streaming, and full editor productization remain follow-up work.

Editor Runtime Host Playtest Launch v1 completed in `Editor Runtime Host Playtest Launch v1` through `EditorRuntimeHostPlaytestLaunchDesc`, `EditorRuntimeHostPlaytestLaunchModel`, `make_editor_runtime_host_playtest_launch_model`, retained `play_in_editor.runtime_host` rows, and visible Run/Viewport `Execute Runtime Host` controls. `MK_editor_core` reviews only argv, working directory, host gates, acknowledgements, and unsupported claims into a safe `mirakana::ProcessCommand`; future visible `MK_editor` owns explicit host-gate acknowledgement, Windows `mirakana::Win32ProcessRunner` execution, and transient exit/stdout/stderr evidence. Dynamic game-module loading, in-process runtime-host embedding, package scripts, arbitrary shell/raw manifest evaluation, free-form manifest edits, renderer/RHI/native handles, hot reload, package streaming, and full editor productization remain follow-up work.

Editor In-Process Runtime Host Review v1 adds `EditorInProcessRuntimeHostDesc`, `EditorInProcessRuntimeHostModel`, `EditorInProcessRuntimeHostBeginResult`, `make_editor_in_process_runtime_host_model`, `begin_editor_in_process_runtime_host_session`, `make_editor_in_process_runtime_host_ui_model`, retained `play_in_editor.in_process_runtime_host` rows, and visible Run/Viewport `Begin In-Process Runtime Host` diagnostics for an already-linked `IEditorPlaySessionDriver` handoff into the existing isolated `EditorPlaySession`. Editor Dynamic Game Module Driver Load v1, Editor Game Module Driver Safe Reload Review v1, Editor Game Module Driver Contract Metadata Review v1, and Editor Game Module Driver Dynamic Probe v1 add `mirakana::DynamicLibrary`, `mirakana::load_dynamic_library`, `mirakana::resolve_dynamic_library_symbol`, `EditorGameModuleDriverApi`, `EditorGameModuleDriverContractMetadataModel`, `make_editor_game_module_driver_contract_metadata_model`, `make_editor_game_module_driver_load_model`, `make_editor_game_module_driver_reload_model`, `make_editor_game_module_driver_from_symbol`, retained `play_in_editor.game_module_driver` / `play_in_editor.game_module_driver.reload` / `play_in_editor.game_module_driver.contract` rows, visible `Game Module Driver`, `Load Game Module Driver`, `Reload Game Module Driver`, and `Unload Game Module Driver` controls, a Windows-only `MK_editor_game_module_driver_probe` DLL fixture, and `MK_editor_game_module_driver_load_tests` evidence for visible-host loading of `mirakana_create_editor_game_module_driver_v1` from a reviewed absolute module path before enabling `Begin In-Process Runtime Host`. Editor Game Module Driver Reload Transaction Recipe MK Editor Evidence v1 adds retained `play_in_editor.game_module_driver.reload_transaction_recipe_evidence` plus visible reviewed `run-validation-recipe` argv lines for `dev-windows-editor-game-module-driver-load-tests` without `mirakana_editor` recipe execution. It records the same-engine-build ABI contract and unsupported stable-ABI/hot-reload boundaries without adding active-session hot reload, active-session reload/unload, stable third-party ABI, `DesktopGameRunner` embedding, package scripts, validation recipes, renderer/RHI uploads or handles, native handles, package streaming, or full editor productization.

Editor Runtime Scene Package Validation Execution v1 adds `EditorRuntimeScenePackageValidationExecutionDesc`, `EditorRuntimeScenePackageValidationExecutionModel`, `EditorRuntimeScenePackageValidationExecutionResult`, `make_editor_runtime_scene_package_validation_execution_model`, `execute_editor_runtime_scene_package_validation`, retained `playtest_package_review.runtime_scene_validation` rows, and visible AI Commands `Validate Runtime Scene Package` evidence for the existing non-mutating `validate-runtime-scene-package` command. It closes only the reviewed package-validation execution step before host-gated desktop smoke; package cooking, runtime source parsing, external importer execution, renderer/RHI residency, package streaming, validation recipe execution, arbitrary shell/raw manifest execution, manifest mutation, dynamic game-module loading, native handles, Metal readiness, and broad renderer quality remain unsupported.

Editor Resource Panel Diagnostics v1 completed in `Editor Resource Panel Diagnostics v1` through `EditorResourcePanelModel`, a hidden-by-default workspace `resources` panel, retained `MK_ui` resource panel model output, and future visible `MK_editor` Resources diagnostics over active viewport RHI stats, memory diagnostics, and optional lifetime registry summaries. Editor Resource Capture Request v1 completed in `Editor Resource Capture Request v1` through reviewed `resources.capture_requests` rows and visible transient acknowledgement/log state for PIX GPU capture and D3D12 debug-layer/GPU-validation host handoff. Editor Resource Capture Execution Evidence v1 completed in `Editor Resource Capture Execution Evidence v1` through `EditorResourceCaptureExecutionInput`, `EditorResourceCaptureExecutionRow`, retained `resources.capture_execution` rows, and visible host-gated waiting evidence for acknowledged requests. This is diagnostics/request/evidence handoff only; residency enforcement, allocator policy, eviction, package streaming, PIX/debug-layer/ETW process execution, backend destruction migration, public native handles, GPU capture execution, and broad renderer quality remain follow-up work.

Editor AI Command Diagnostics Panel v1 completed in `Editor AI Command Diagnostics Panel v1` through `EditorAiCommandPanelModel`, a hidden-by-default workspace `ai_commands` panel, retained `MK_ui` AI Commands output, and future visible `MK_editor` AI Commands diagnostics over the existing AI package/preflight/readiness/handoff/evidence/remediation/operator workflow models. This is read-only diagnostics only; validation recipe execution, arbitrary shell, raw manifest command evaluation, free-form manifest edits, package scripts, dynamic game-module loading or in-process runtime-host embedding, renderer/RHI handles, Metal readiness, and broad renderer quality remain follow-up work.

Editor AI Evidence Import Review v1 completed in `Editor AI Evidence Import Review v1` through `EditorAiPlaytestEvidenceImportModel`, `EditorAiPlaytestEvidenceImportReviewRow`, retained `ai_evidence_import` `MK_ui` rows, and future visible `MK_editor` AI Commands paste/review/import controls for externally supplied evidence. Imported evidence remains transient editor state and feeds the existing evidence summary; host-gated AI command execution, arbitrary shell, raw manifest command evaluation, free-form manifest edits, package scripts, dynamic game-module loading or in-process runtime-host embedding, renderer/RHI handles, Metal readiness, and broad renderer quality remain follow-up work.

Editor AI Reviewed Validation Execution v1 completed in `Editor AI Reviewed Validation Execution v1` through `EditorAiReviewedValidationExecutionModel`, `make_editor_ai_reviewed_validation_execution_plan`, retained `ai_commands.execution` rows, and future visible `MK_editor` AI Commands execution controls for host-gate-free reviewed `run-validation-recipe` rows. Host-Gated Validation Execution Ack v1 completes the explicit acknowledgement follow-up through `Editor AI Host Gated Validation Execution Ack v1`: host-gated rows stay unavailable until the operator checks the per-row host-gate acknowledgement, then reviewed `-HostGateAcknowledgements <gate>` arguments are appended before local execution. Editor AI Reviewed Validation Batch Execution v1 completed in `Editor AI Reviewed Validation Batch Execution v1` through `EditorAiReviewedValidationExecutionBatchModel`, retained `ai_commands.execution.batch` rows, and visible `Execute Ready` for all currently executable reviewed rows. Results become transient evidence rows; unacknowledged host-gated recipe execution, arbitrary shell, raw manifest command evaluation, free-form manifest edits, broad package script execution, dynamic game-module loading or in-process runtime-host embedding, renderer/RHI handles, Metal readiness, and broad renderer quality remain follow-up work.

Editor Profiler Trace Export v1 completed in `Editor Profiler Trace Export v1` through `EditorProfilerTraceExportModel`, `make_editor_profiler_trace_export_model`, retained `profiler.trace_export` rows, and visible `Copy Trace JSON` for non-empty diagnostics captures. Editor Profiler Trace File Save v1 completed in `Editor Profiler Trace File Save v1` through `EditorProfilerTraceFileSaveRequest`, `EditorProfilerTraceFileSaveResult`, `save_editor_profiler_trace_json`, retained `profiler.trace_file_save` rows, and visible `Trace Path` / `Save Trace JSON` for safe project-relative `.json` writes through `ITextStore`. Editor Profiler Native Trace Save Dialog v1 completed in `Editor Profiler Native Trace Save Dialog v1` through `EditorProfilerTraceSaveDialogModel`, `make_editor_profiler_trace_save_dialog_request`, `make_editor_profiler_trace_save_dialog_model`, retained `profiler.trace_save_dialog` rows, and visible `Browse Save Trace JSON` through `native shell file-dialog service` for in-project `.json` save selections. Editor Profiler Telemetry Handoff v1 completed in `Editor Profiler Telemetry Handoff v1` through `EditorProfilerTelemetryHandoffModel`, `make_editor_profiler_telemetry_handoff_model`, retained `profiler.telemetry` rows, and visible read-only `Profiler Telemetry Handoff` readiness over `mirakana::build_diagnostics_ops_plan` `telemetry_upload` artifacts. Editor Profiler Trace Import Review v1 completed in `Editor Profiler Trace Import Review v1` through `DiagnosticsTraceImportReview`, `review_diagnostics_trace_json`, `EditorProfilerTraceImportReviewModel`, `make_editor_profiler_trace_import_review_model`, retained `profiler.trace_import` rows, and visible `Trace JSON Review` / `Review Trace JSON` for pasted supported Chrome Trace Event JSON. Editor Profiler Trace File Import Review v1 completed in `Editor Profiler Trace File Import Review v1` through `EditorProfilerTraceFileImportRequest`, `EditorProfilerTraceFileImportResult`, `import_editor_profiler_trace_json`, retained `profiler.trace_file_import` rows, and visible `Trace Import Path` / `Import Trace JSON` for safe project-relative `.json` review through `ITextStore`. Editor Profiler Native Trace Open Dialog v1 completed in `Editor Profiler Native Trace Open Dialog v1` through `EditorProfilerTraceOpenDialogModel`, `make_editor_profiler_trace_open_dialog_request`, `make_editor_profiler_trace_open_dialog_model`, retained `profiler.trace_open_dialog` rows, and visible `Browse Trace JSON` through `native shell file-dialog service` for in-project `.json` open selections. Editor Profiler Trace Import Reconstruction v1 completed in `Editor Profiler Trace Import Reconstruction v1` through `DiagnosticsTraceImportResult`, `import_diagnostics_trace_json`, reconstructed capture rows on `EditorProfilerTraceImportReviewModel` / `EditorProfilerTraceFileImportResult`, and retained `profiler.trace_import.reconstructed_*` rows for the first-party exported `M`/`i`/`C`/`X` subset. The trace paths reuse `mirakana::export_diagnostics_trace_json`; broader editor native save/open dialogs outside Profiler, arbitrary JSON conversion beyond first-party exported Trace Event JSON reconstruction, telemetry SDK/upload execution, crash-report backends, GPU/backend timestamps, allocator diagnostics, native handles, and production flame graphs remain follow-up work.

Editor Content Browser Import Diagnostics v1 completed in `Editor Content Browser Import Diagnostics v1` through `EditorContentBrowserImportPanelModel`, retained `MK_ui` Assets panel output, and future visible `MK_editor` Assets panel rendering over Content Browser rows, selected asset details, import queue/progress, diagnostics, dependencies, thumbnail requests, material preview rows, and hot-reload summaries. Editor Content Browser Import Native Dialog v1 completed in `Editor Content Browser Import Native Dialog v1` through `EditorContentBrowserImportOpenDialogModel`, `make_content_browser_import_open_dialog_request`, `make_content_browser_import_open_dialog_model`, retained `content_browser_import.open_dialog` rows, and visible `Browse Import Sources` through `native shell file-dialog service` for in-project first-party `.texture`, `.mesh`, `.material`, `.scene`, and `.audio_source` selections plus reviewed `.png`, `.gltf`, `.glb`, `.wav`, `.mp3`, and `.flac` codec sources that replace the import plan without executing import automatically. Editor Content Browser Import External Copy Review v1 completed in `Editor Content Browser Import External Copy Review v1` through `EditorContentBrowserImportExternalSourceCopyModel`, `make_content_browser_import_external_source_copy_model`, `make_content_browser_import_external_source_copy_ui_model`, retained `content_browser_import.external_copy` rows, and visible `Copy External Sources` staging for reviewed external import sources before import-plan rebuild. Editor Content Browser Import Codec Adapter Review v1 completed in `Editor Content Browser Import Codec Adapter Review v1` by routing explicit import/recook execution for those reviewed codec selections through existing `mirakana::ExternalAssetImportAdapters`. Arbitrary importer adapters, arbitrary file import, automatic import execution, arbitrary shell, package scripts, validation execution, free-form manifest edits, package streaming, renderer/RHI/native handles, dynamic game-module Play-In-Editor, and broad editor/importer readiness remain follow-up work.

Editor Material Asset Preview Diagnostics v1 completed in `Editor Material Asset Preview Diagnostics v1` through `EditorMaterialAssetPreviewPanelModel`, retained `MK_ui` selected-material output, and future visible `MK_editor` Assets panel rendering over cooked material metadata, factor rows, texture dependency diagnostics, typed GPU payload texture rows, and D3D12/Vulkan material-preview shader readiness. Editor Material GPU Preview Execution Evidence v1 completed in `Editor Material GPU Preview Execution Evidence v1` through `EditorMaterialGpuPreviewExecutionSnapshot`, `apply_editor_material_gpu_preview_execution_snapshot`, retained `material_asset_preview.gpu.execution` rows, and future visible `MK_editor` host-owned cache snapshot reporting for selected-material GPU preview status/backend/display/frame evidence. Shader compiler execution, RHI upload/display from editor core, package scripts, arbitrary shell, package streaming, native handles, Vulkan display parity, Metal readiness, and broad editor/renderer readiness remain follow-up work.

Editor Prefab Variant Conflict Review v1, Reviewed Resolution v1, Missing Node Cleanup v1, Node Retarget Review v1, Batch Resolution Review v1, Source Mismatch Retarget Review v1, Source Mismatch Accept Current Review v1, Native Dialog v1, and Base Refresh Merge Review v1 completed in `Editor Prefab Variant Conflict Review v1`, `Editor Prefab Variant Reviewed Resolution v1`, `Editor Prefab Variant Missing Node Cleanup v1`, `Editor Prefab Variant Node Retarget Review v1`, `Editor Prefab Variant Batch Resolution Review v1`, `Editor Prefab Variant Source Mismatch Retarget Review v1`, `Editor Prefab Variant Source Mismatch Accept Current Review v1`, `Editor Prefab Variant Native Dialog v1`, and `Editor Prefab Variant Base Refresh Merge Review v1` through `PrefabVariantConflictReviewModel`, `PrefabVariantConflictRow`, `PrefabVariantConflictBatchResolutionPlan`, `make_prefab_variant_conflict_review_model`, `resolve_prefab_variant_conflict`, `resolve_prefab_variant_conflicts`, `make_prefab_variant_conflict_resolution_action`, `make_prefab_variant_conflict_batch_resolution_action`, `PrefabNodeOverride::source_node_name`, `source_node_mismatch`, `accept_current_node`, `deserialize_prefab_variant_definition_for_review`, `EditorPrefabVariantFileDialogModel`, `make_prefab_variant_open_dialog_request`, `make_prefab_variant_save_dialog_request`, `make_prefab_variant_open_dialog_model`, `make_prefab_variant_save_dialog_model`, retained `MK_ui` `prefab_variant_conflicts`, `prefab_variant_conflicts.batch_resolution`, `prefab_variant_file_dialog.open`, `prefab_variant_file_dialog.save`, and `prefab_variant_base_refresh` output, and future visible `MK_editor` Prefab Variant Authoring conflict rows with explicit `Apply` cleanup for redundant, later duplicate, and missing-node stale override rows, reviewed unique source-node retarget for missing-node or existing-node source mismatch rows, explicit `Accept current node N` hint repair that updates only `source_node_name` when no safe retarget exists, one undoable `Apply All Reviewed` batch path, explicit `plan_prefab_variant_base_refresh` / `apply_prefab_variant_base_refresh` embedded-base refresh review rows, plus `Browse Load Variant` / `Browse Save Variant` through `native shell file-dialog service` for in-project `.prefabvariant` selections. Strict `MK_scene` composition remains index-based. Nested prefab propagation beyond explicit base refresh, fuzzy matching, automatic merge/rebase/resolution UX, execution from the review model, package scripts, validation recipes, dynamic game-module loading or in-process runtime-host embedding, renderer/RHI uploads, native handles, package streaming, broader editor native save/open dialogs outside Profiler and Prefab Variant, and broad editor productization remain follow-up work.

Editor Scene Native Dialog v1 adds `EditorSceneFileDialogModel`, `make_scene_open_dialog_request`, `make_scene_save_dialog_request`, `make_scene_open_dialog_model`, `make_scene_save_dialog_model`, and `make_scene_file_dialog_ui_model` for retained `scene_file_dialog.open` / `scene_file_dialog.save` review rows. The future visible `MK_editor` File menu and Scene panel expose `Open Scene...`, `Save Scene As...`, `Browse Open Scene`, and `Browse Save Scene As` through `native shell file-dialog service` for in-project `.scene` selections, update `SceneAuthoringDocument::set_scene_path` on save-as, and reuse existing scene load/save helpers while keeping native handles, path conversion, filesystem IO, in-process runtime-host embedding, renderer/RHI uploads, package scripts, and validation recipes out of editor core. Broader editor native save/open dialogs outside Profiler, Scene, and Prefab Variant remain follow-up work.

Editor Project Native Dialog v1 adds `EditorProjectFileDialogModel`, `EditorProjectFileDialogMode`, `make_project_open_dialog_request`, `make_project_save_dialog_request`, `make_project_open_dialog_model`, `make_project_save_dialog_model`, and `make_project_file_dialog_ui_model` for retained `project_file_dialog.open` / `project_file_dialog.save` review rows. The future visible `MK_editor` File menu and Project Settings panel expose `Open Project...` and `Save Project As...` through `native shell file-dialog service` for in-store `.geproject` selections, convert accepted native paths back to safe store-relative project paths, infer companion workspace/startup-scene bundle paths, and reuse existing `load_project_bundle` / `save_project_bundle` helpers while keeping native handles, path conversion, filesystem IO, in-process runtime-host embedding, renderer/RHI uploads, package scripts, and validation recipes out of editor core. Broader editor native save/open dialogs outside Profiler, Scene, Prefab Variant, and Project remain follow-up work.

`2D Sprite Batch Package Telemetry v1` completed the follow-up 2D Sprite Batch Package Telemetry v1 slice: `MK_scene_renderer` exposes `mirakana::plan_scene_sprite_batches`, the committed 2D package smoke emits `sprite_batch_plan_*` counters, and installed validation requires those counters as telemetry-only evidence. `Sprite Batching Renderer v1` now adds `mirakana::plan_sprite_batch_budget_profile` and committed 2D package `sprite_batch_budget_*` counters for world, UI, and effects lanes while keeping broader production sprite batching, material policy, backend quality, and performance claims out of scope. At that time the active pointer moved through this selected slice; current pointers are manifest-led and recorded in `engine/agent/manifest.json`.

`3D Scene Mesh Package Telemetry v1` completed the post-review-selected 3D Scene Mesh Package Telemetry v1 slice: `MK_scene_renderer` exposes `mirakana::plan_scene_mesh_draws`, the committed 3D package smoke emits `scene_mesh_plan_*` counters, and installed validation requires those counters without claiming broad generated 3D production readiness, renderer execution, skeletal animation, GPU skinning, material/shader graphs, package streaming, native handles, Metal, or renderer quality.

`Generated 3D Mesh Telemetry Scaffold v1` completed the generated-scaffold follow-up: `tools/new-game.ps1 -Template DesktopRuntime3DPackage` now emits `scene_mesh_plan_*` counters in generated package smoke output and static checks require those fields, keeping generated 3D scaffolds aligned with the committed package telemetry proof without broad generated 3D production readiness, renderer execution, skeletal animation/GPU skinning, material/shader graphs, package streaming, native handles, Metal, or renderer quality. At that time the active pointer moved through this selected slice and returned to the selection gate because the post-review found only broad or host-gated remaining gaps; current pointers are manifest-led and recorded in `engine/agent/manifest.json`.

`Generated 3D Transform Animation Scaffold v1` adds the generated 3D transform-animation scaffold follow-up: `MK_scene_renderer` exposes `mirakana::sample_and_apply_runtime_scene_render_animation_float_clip` for sampled cooked scalar clip playback against `RuntimeSceneRenderInstance` data through first-party transform binding rows, and `tools/new-game.ps1 -Template DesktopRuntime3DPackage` now ships a tiny cooked `animation_float_clip` payload plus `--require-transform-animation` smoke counters for `PackagedMesh` translation. This keeps source authoring files out of `runtimePackageFiles` and does not claim broad generated 3D production readiness, authored animation graphs, GPU morph, full 3D orientation, package streaming, native handles, Metal, or renderer quality.

`Generated 3D Morph Package Consumption v1` adds the generated 3D morph package consumption follow-up: `MK_scene_renderer` exposes `mirakana::sample_runtime_morph_mesh_cpu_animation_float_clip` for sampled cooked morph-weight clip playback against `RuntimeMorphMeshCpuPayload` data, and `tools/new-game.ps1 -Template DesktopRuntime3DPackage` now ships a tiny cooked `morph_mesh_cpu` payload plus `--require-morph-package` smoke counters. This keeps source authoring files out of `runtimePackageFiles`; package-visible GPU drawing is covered by the later generated visible-deformation bridge, and broad generated 3D production readiness, authored animation graphs, package streaming, native handles, Metal, and renderer quality remain outside this CPU counter slice.

`GPU Morph D3D12 Proof v1` adds the D3D12 primary-lane GPU morph follow-up: `MK_runtime_rhi` can upload `RuntimeMorphMeshCpuPayload` POSITION deltas into storage buffers plus 256-byte-backed weight uniforms, `MK_renderer` exposes `MorphMeshGpuBinding`, `MeshCommand::gpu_morphing`, and `RhiFrameRendererDesc::morph_graphics_pipeline`, and the D3D12 RHI test visibly draws a base triangle only after the morph shader applies the uploaded delta. Later slices cover direct NORMAL/TANGENT reads, generated package integration, runtime compute output consumption, and generated D3D12 POSITION compute morph package smoke; Vulkan/Metal parity and broad animation/renderer readiness remain separate.

`GPU Morph Normal Tangent D3D12 Proof v1` adds the D3D12 primary-lane NORMAL/TANGENT GPU morph follow-up: `RuntimeMorphMeshUploadResult` records optional `uploaded_normal_delta_bytes` and `uploaded_tangent_delta_bytes`, `MorphMeshGpuBinding` carries `normal_delta_buffer` and `tangent_delta_buffer`, `attach_morph_mesh_descriptor_set` binds them at vertex-stage storage-buffer bindings `2` and `3`, and the D3D12 RHI readback test proves uploaded NORMAL and TANGENT deltas can change lit output through `RhiFrameRenderer`. This is still a direct graphics-pipeline proof; generated package smoke and direct compute NORMAL/TANGENT output are covered by later slices, while scene-schema morph authoring, async compute scheduling, skin+morph composition, directional-shadow morph rendering, Vulkan/Metal parity, and broad animation/renderer readiness remain separate.

`Runtime Scene RHI Morph GPU Palette v1` adds the package bridge follow-up for that proof: `MK_runtime_scene_rhi` accepts explicit `RuntimeSceneGpuBindingOptions::morph_mesh_assets`, verifies selected records are cooked `morph_mesh_cpu` payloads, uploads them through `MK_runtime_rhi`, retains `SceneMorphGpuBindingPalette` entries plus morph descriptor set layout state, reports morph upload counts/bytes, and tears retained morph buffers/descriptors down through the existing NullRHI safe-point path. Generated visible-deformation, NORMAL/TANGENT graphics, and NORMAL/TANGENT compute package slices cover the narrow generated D3D12 smoke path; scene schema morph authoring, async compute scheduling, Vulkan/Metal parity, and broad animation/renderer readiness remain separate.

`Generated 3D Morph GPU Palette Smoke v1` adds the generated 3D morph GPU palette smoke follow-up: `tools/new-game.ps1 -Template DesktopRuntime3DPackage` now passes `packaged_morph_mesh_asset_id()` into D3D12/Vulkan scene renderer `morph_mesh_assets`, emits `scene_gpu_morph_mesh_bindings`, `scene_gpu_morph_mesh_uploads`, and `scene_gpu_uploaded_morph_bytes`, and requires positive morph GPU palette counters when `--require-morph-package --require-scene-gpu-bindings` are both selected. Generated D3D12 visible-deformation, NORMAL/TANGENT graphics, and NORMAL/TANGENT compute package slices cover the narrow shader/payload smoke; scene schema morph authoring, async compute scheduling, Vulkan/Metal visible morph parity, and broad generated 3D readiness remain separate.

`Generated 3D Morph Visible Deformation v1` adds the generated 3D morph visible-deformation bridge: SDL desktop scene renderer descriptors now carry explicit mesh-to-morph binding rows and `morph_vertex_shader` bytecode, the scene GPU binding injector resolves those rows into `MeshCommand::gpu_morphing`, `RhiPostprocessFrameRenderer` records morph descriptor binds and draw counters through a host-owned morph scene pipeline, and `tools/new-game.ps1 -Template DesktopRuntime3DPackage` emits `vs_morph` artifacts plus `scene_gpu_morph_mesh_resolved`, `renderer_gpu_morph_draws`, and `renderer_morph_descriptor_binds` smoke fields on the D3D12 primary package path. That slice introduced the POSITION-delta generated package proof; later generated slices extend payloads, shader reads, and tangent-frame compute output, while scene schema morph authoring, async compute scheduling, skin+morph composition, directional-shadow morph rendering, Vulkan/Metal parity, and broad generated 3D readiness remain separate.

`Generated 3D Morph Normal Tangent Package Smoke v1` adds the generated 3D NORMAL/TANGENT morph package smoke follow-up: `tools/new-game.ps1 -Template DesktopRuntime3DPackage` now emits cooked/source `morph_mesh_cpu` payloads with bind NORMAL/TANGENT rows plus NORMAL/TANGENT target deltas, aligns the generated scene vertex input with the cooked tangent-frame mesh layout, and generates `vs_morph` shader source that reads POSITION/NORMAL/TANGENT morph delta buffers at descriptor bindings `0`, `2`, and `3`. This remains a D3D12-primary graphics morph package smoke; scene-schema morph authoring, skin+morph composition, directional-shadow morph rendering, Vulkan/Metal parity, and broad generated 3D readiness remain separate.

`RHI Compute Dispatch Foundation v1` adds the backend-neutral RHI compute dispatch foundation for the next morph slice: `ComputePipelineHandle`, `ComputePipelineDesc`, `IRhiDevice::create_compute_pipeline`, `IRhiCommandList::bind_compute_pipeline`, compute descriptor binding, and `IRhiCommandList::dispatch` are now public first-party contracts, NullRHI validates invalid compute ordering and records dispatch stats, and D3D12 has a deterministic compute shader buffer readback proof. Generated-package D3D12 POSITION compute morph smoke is covered by the later generated 3D package slice; this foundation alone is not async compute overlap, frame-graph scheduling, Vulkan/Metal parity, skin+morph composition, or broad renderer quality.

`Runtime RHI Compute Morph D3D12 Proof v1` adds the first D3D12 primary-lane runtime RHI compute morph output proof: `MK_runtime_rhi` exposes `RuntimeMorphMeshComputeBinding` and `create_runtime_morph_mesh_compute_binding` to bind the base position buffer, morph position delta buffer, morph weight uniform, and compute-write output position buffer. A D3D12 compute shader dispatched through `IRhiCommandList::dispatch` writes deterministic `base + weighted deltas` bytes and reads them back through public RHI buffers. Renderer consumption, optional NORMAL/TANGENT compute output, and generated-package D3D12 compute morph smoke are covered by later slices; async compute overlap, Vulkan/Metal parity, skin+morph composition, and broad animation/renderer readiness remain separate.

`Runtime RHI Compute Morph Renderer Consumption D3D12 v1` adds the D3D12 renderer-consumption follow-up: `make_runtime_compute_morph_output_mesh_gpu_binding` maps the compute-written POSITION output buffer plus the original index buffer into `MeshGpuBinding`, and a D3D12 `RhiFrameRenderer` draw/readback test proves the compute output can be consumed as vertex input. Generated D3D12 package smoke and direct NORMAL/TANGENT compute output are covered by later slices; async compute overlap, Vulkan/Metal parity, skin+morph composition, directional-shadow morph rendering, and broad animation/renderer readiness remain separate.

`Generated 3D Compute Morph Package Smoke D3D12 v1` adds the generated D3D12 compute morph package smoke: `tools/new-game.ps1 -Template DesktopRuntime3DPackage` emits `vs_compute_morph`/`cs_compute_morph_position` artifacts and `--require-compute-morph`, `MK_runtime_host_win32_presentation` dispatches the selected cooked morph row into a POSITION output buffer, and package smoke reports `scene_gpu_compute_morph_mesh_bindings`, `scene_gpu_compute_morph_dispatches`, `scene_gpu_compute_morph_mesh_resolved`, and `scene_gpu_compute_morph_draws`. The later generated tangent-frame compute slice adds optional `--require-compute-morph-normal-tangent` output and `scene_gpu_compute_morph_tangent_frame_output`; async compute overlap, Vulkan/Metal parity, skin+morph composition, directional-shadow morph rendering, scene-schema compute-morph authoring, and broad renderer readiness remain separate.

`Generated 3D Compute Morph Normal Tangent Package Smoke D3D12 v1` adds the generated D3D12 tangent-frame compute morph package smoke: `tools/new-game.ps1 -Template DesktopRuntime3DPackage` emits `vs_compute_morph_tangent_frame` / `cs_compute_morph_tangent_frame` artifacts, the Win32 D3D12 scene renderer descriptor explicitly enables NORMAL/TANGENT compute output buffers, and package smoke reports `scene_gpu_compute_morph_tangent_frame_output=1` when requested. The Vulkan NORMAL/TANGENT and Vulkan skin+compute package promotions are recorded by the later Vulkan slices, while async compute overlap, Metal parity, directional-shadow morph rendering, scene-schema compute-morph authoring, and broad renderer readiness remain separate.

`Runtime RHI Compute Morph Normal Tangent Output D3D12 v1` adds Runtime RHI Compute Morph NORMAL/TANGENT Output D3D12 v1, the D3D12 primary-lane direct NORMAL/TANGENT compute output follow-up: `RuntimeMorphMeshComputeBindingOptions` can opt into `output_normal_usage` and `output_tangent_usage`, `RuntimeMorphMeshComputeBinding` reports optional output normal/tangent buffers and byte counts, `create_runtime_morph_mesh_compute_binding` binds morph normal/tangent deltas plus compute-write output normal/tangent buffers, and a D3D12 readback test proves normalized morphed NORMAL/TANGENT rows. This is direct RHI proof only; the generated package promotion is covered by the later tangent-frame package slice, while async compute scheduling, Vulkan/Metal parity, skin+morph composition, directional-shadow morph rendering, and broad renderer readiness remain separate.

`Generated 3D Compute Morph Normal Tangent Package Smoke D3D12 v1` adds Generated 3D Compute Morph NORMAL/TANGENT Package Smoke D3D12 v1. It carries the completed direct POSITION/NORMAL/TANGENT compute output proof into generated `DesktopRuntime3DPackage` D3D12 smoke with `--require-compute-morph-normal-tangent` and `scene_gpu_compute_morph_tangent_frame_output`, while Vulkan/Metal parity, async compute scheduling, skin+morph composition, directional-shadow morph rendering, scene-schema compute-morph authoring, and broad renderer readiness remain separate.

`Runtime RHI Compute Morph Queue Synchronization D3D12 v1` adds Runtime RHI Compute Morph Queue Synchronization D3D12 v1. `IRhiDevice::wait_for_queue(QueueKind, FenceValue)` and `RhiStats::queue_waits` / `queue_wait_failures` provide backend-neutral queue-wait contracts, NullRHI validates them deterministically, D3D12 routes them through private queue/fence primitives, and the compute morph renderer-consumption proof now orders graphics queue work on the compute fence without a CPU wait between compute submit and graphics consumption.

`Generated 3D Compute Morph Queue Sync Package Smoke D3D12 v1` adds Generated 3D Compute Morph Queue Sync Package Smoke D3D12 v1. Generated `DesktopRuntime3DPackage` D3D12 smoke now emits and requires `scene_gpu_compute_morph_queue_waits`, proving package-visible queue-wait evidence for the host-owned compute morph path without exposing RHI/native handles.

`Runtime RHI Compute Morph Skin Composition D3D12 v1` adds Runtime RHI Compute Morph Skin Composition D3D12 v1. `make_runtime_compute_morph_skinned_mesh_gpu_binding` composes compute-written POSITION output with a first-party skinned binding, `skin_attribute_vertex_buffer` keeps joint indices/weights in a separate vertex stream, and D3D12 readback proves the distinct joint-palette descriptor path.

`Runtime Scene RHI Compute Morph Skin Palette D3D12 v1` adds Runtime Scene RHI Compute Morph Skin Palette D3D12 v1. `RuntimeSceneGpuBindingOptions::compute_morph_skinned_mesh_bindings` carries selected skinned mesh to morph mesh rows, `MK_runtime_scene_rhi` extracts POSITION-only compute base uploads from cooked skinned payloads, retains `RuntimeMorphMeshComputeBinding` resources, and inserts composed bindings into `SceneSkinnedGpuBindingPalette` with first-party report counters. Generated 3D Compute Morph Skin Package Smoke D3D12 v1 completes the generated package skin+compute smoke with `--require-compute-morph-skin`, skinned compute shader artifacts, a `skinned_mesh` package row, and package-visible `scene_gpu_compute_morph_skinned_*` counters. Generated 3D Compute Morph Skin Package Smoke Vulkan v1 extends that boundary to toolchain-gated Vulkan SPIR-V artifacts, `Win32DesktopPresentationVulkanSceneRendererDesc::compute_morph_skinned_shader`, explicit `compute_morph_skinned_mesh_bindings`, and the same first-party skin+compute package counters. Generated 3D Package Streaming Safe Point Smoke v1 completes the generated package selected safe-point proof with `--require-package-streaming-safe-point` and first-party `package_streaming_*` counters while keeping broad/background streaming out of scope. Runtime RHI Compute Morph Async Telemetry D3D12 v1 completes first-party queue-submit and queue-wait fence sequencing telemetry through `RhiStats` and D3D12 context stats. Generated 3D Compute Morph Async Telemetry Package Smoke D3D12 v1 completes generated D3D12 package validation for `--require-compute-morph-async-telemetry` and `scene_gpu_compute_morph_async_*` counters without exposing native handles, GPU timestamps, or performance claims. Runtime RHI Compute Morph Async Overlap Evidence D3D12 v1 completes `RhiAsyncOverlapReadinessDiagnostics`, which reports `not_proven_serial_dependency` for the current same-frame graphics wait. Runtime RHI Compute Morph Pipelined Output Ring D3D12 v1 is complete and adds `RuntimeMorphMeshComputeBindingOptions::output_slot_count`, `RuntimeMorphMeshComputeOutputSlot`, and D3D12 descriptor-range slot proof; Runtime RHI Compute Morph Pipelined Scheduling D3D12 v1 is complete and adds `RhiPipelinedComputeGraphicsScheduleEvidence` plus previous-slot graphics consumption diagnostics; RHI D3D12 Per-Queue Fence Synchronization v1 is complete and adds first-party `FenceValue` queue identity plus backend-private D3D12 per-queue fence timelines. RHI D3D12 Queue Timestamp Measurement Foundation v1, RHI D3D12 Queue Clock Calibration Foundation v1, RHI D3D12 Calibrated Queue Timing Diagnostics v1, Runtime RHI Compute Morph Calibrated Overlap Diagnostics D3D12 v1, RHI D3D12 Submitted Command Calibrated Timing Scopes v1, Runtime RHI Compute Morph Submitted Overlap Diagnostics D3D12 v1, and RHI Vulkan Compute Dispatch Foundation v1 are complete; Runtime RHI Compute Morph Vulkan Proof v1 is complete for Vulkan POSITION-output readback through `MK_VULKAN_TEST_COMPUTE_MORPH_SPV`; Runtime RHI Compute Morph Renderer Consumption Vulkan v1 is complete through `MK_VULKAN_TEST_COMPUTE_MORPH_RENDER_VERTEX_SPV` / `MK_VULKAN_TEST_COMPUTE_MORPH_RENDER_FRAGMENT_SPV`; Runtime RHI Compute Morph NORMAL/TANGENT Output Vulkan v1 is complete through `MK_VULKAN_TEST_COMPUTE_MORPH_TANGENT_FRAME_SPV`; Generated 3D Compute Morph Package Smoke Vulkan v1 is complete for generated `DesktopRuntime3DPackage` POSITION compute morph package smoke through host-owned Win32 presentation descriptors, `--require-compute-morph`, and explicit SPIR-V artifacts; Generated 3D Compute Morph NORMAL/TANGENT Package Smoke Vulkan v1 is complete through `Win32DesktopPresentationVulkanSceneRendererDesc::enable_compute_morph_tangent_frame_output`, `--require-compute-morph-normal-tangent`, tangent-frame SPIR-V artifacts, and package-visible `scene_gpu_compute_morph_tangent_frame_output`. 2D Native Sprite Batching Execution v1 is complete for renderer-owned native sprite batching execution counters such as `native_2d_sprite_batches_executed`. 2D Sprite Animation Package v1 is complete for deterministic generated `DesktopRuntime2DPackage` sprite animation frame sampling/application counters such as `sprite_animation_frames_sampled`; 2D Tilemap Editor Runtime UX v1 is complete for package-visible tilemap runtime/editor counters and diagnostics through `sample_runtime_tilemap_visible_cells`, `make_editor_tilemap_package_diagnostics_model`, `--require-tilemap-runtime-ux`, `tilemap_cells_sampled`, and `tilemap_diagnostics`. Broad/background package streaming, graphics morph+skin composition beyond recorded package evidence, generated-package measured async compute overlap/performance claims, Metal compute morph parity, broad production frame graph scheduling beyond selected executor/package evidence, directional-shadow morph rendering, scene-schema compute-morph authoring, source image decoding, production atlas packing, full tilemap editor UX, and broad renderer readiness remain separate.

`Animation CPU Skinned Normal Stream v1` completed a focused 3D-playable prerequisite in `MK_animation`: `AnimationCpuSkinningDesc` accepts optional bind normals, validation rejects normal-count mismatches and invalid normals, and `skin_animation_vertices_cpu` emits deterministic skinned normal streams alongside positions. This is CPU-only animation data support, not morph deformation, GPU skinning/upload, renderer/RHI integration, glTF skin import, broad skeletal animation readiness, or generated 3D production readiness. `engine/agent/manifest.json.aiOperableProductionLoop.currentActivePlan` then pointed at that selected slice; `recommendedNextPlan` returned to `next-production-gap-selection`.

`Animation CPU Skinned Tangent Stream v1` completed the next focused 3D-playable prerequisite in `MK_animation`: `AnimationCpuSkinningDesc` accepts optional bind tangents, validation rejects tangent-count mismatches and invalid tangents, and `skin_animation_vertices_cpu` emits deterministic skinned tangent streams alongside positions and optional normals. This is CPU-only `Vec3` tangent data support, not bitangent/handedness reconstruction, morph deformation, GPU skinning/upload, renderer/RHI integration, glTF skin import, broad skeletal animation readiness, or generated 3D production readiness. `engine/agent/manifest.json.aiOperableProductionLoop.currentActivePlan` then pointed at that selected slice; `recommendedNextPlan` returned to `next-production-gap-selection`.

`Animation CPU Skinned Bitangent Handedness v1` completed the next focused 3D-playable prerequisite in `MK_animation`: `AnimationCpuSkinningDesc` accepts optional bind tangent handedness signs, validation rejects missing normal/tangent prerequisites, handedness count mismatches, and non-finite/non-sign handedness values, and `skin_animation_vertices_cpu` emits deterministic skinned bitangents reconstructed from skinned normal/tangent rows. This is CPU-only tangent-frame data support, not tangent-space material rendering, cooked mesh schema changes, morph deformation, GPU skinning/upload, renderer/RHI integration, glTF skin import, broad skeletal animation readiness, or generated 3D production readiness. At that time the active pointer moved through this selected slice and returned to `next-production-gap-selection`; current pointers are manifest-led and recorded in `engine/agent/manifest.json`.

`Animation Root Motion Foundation v1` completed the next focused animation prerequisite in `MK_animation`: `AnimationRootMotionSampleDesc` plus `validate_animation_root_motion_sample` / `is_valid_animation_root_motion_sample` / `sample_animation_root_motion` first provided deterministic translation-only root-joint start/end/delta sampling over existing joint tracks.

`Animation Root Rotation Delta Foundation v1` completed the follow-up Z-rotation slice in `MK_animation`: `AnimationRootMotionSample` now also reports `start_rotation_z_radians`, `end_rotation_z_radians`, and `delta_rotation_z_radians` using the same clamped interpolation and rest fallback as local-pose sampling. This is root translation plus existing Z-rotation-only sampling, not full 3D joint orientation, angular wrap normalization, IK, broader morph GPU/render execution, cooked animation gameplay workflows, animation graph authoring, renderer/RHI integration, or broad skeletal animation production readiness.

`Animation Root Motion Loop Accumulation Foundation v1` completed the next narrow root-motion slice in `MK_animation`: `AnimationRootMotionAccumulationDesc` plus `validate_animation_root_motion_accumulation` / `is_valid_animation_root_motion_accumulation` / `accumulate_animation_root_motion` provide deterministic forward loop accumulation for root-joint translation and Z-rotation deltas over finite non-negative intervals with an explicit positive clip duration. This does not add full 3D joint orientation, angular wrap normalization, backward accumulation, negative-time wrapping, inferred clip duration, IK, broader morph production, cooked animation asset workflows, animation graph authoring, renderer/RHI integration, or broad skeletal animation production readiness. `engine/agent/manifest.json.aiOperableProductionLoop.currentActivePlan` returns to the production-completion master plan until the next dated Phase 6 child slice is filed; `recommendedNextPlan` remains the `root-motion-ik-and-morph-foundation-v1` umbrella.

## Current Implemented Foundation

This section is retained historical/static-check evidence. It is intentionally not the primary read path for AI agents; use the AI Reading Contract above and [Current capabilities](current-capabilities.md) first.

Completed editor milestone note: `First-Party UI Editor Production Stack v1` Phase 8 adds AI-operable editor UX rows to `EditorAiOperationSnapshot.status_rows` for `editor.ai.dock.selected_panel`, `editor.ai.rich_text.documents`, `editor.ai.text_input.focused_target`, `editor.ai.adapter.text_font`, `editor.ai.ime.session`, `editor.ai.accessibility.uia_provider`, `editor.ai.viewport.display`, and `editor.ai.material_preview.display`. The reviewed rich-text command ids are `<rich_text_document_id>.copy_plain_text` and `<rich_text_document_id>.copy_selection_plain_text`; native handles, shell/process execution, validation-recipe execution, screen coordinates, stale revisions, target mismatches, missing confirmation, unsupported parameters, disabled commands, and unknown command ids remain fail-closed.

- `MK_core`: app lifecycle, logging, dependency-free diagnostics/counter/profile capture, p95/p99 budget summaries, deterministic Trace Event JSON subset export, `DiagnosticsOpsPlan` operations handoff rows, fixed timestep, entity/component registry, memory diagnostics, frame/worker scratch arenas, value-only job scheduling evidence rows, and real bounded `JobExecutionPool::execute(batch)` worker execution with explicit RAII-owned `std::thread` workers, opt-in work stealing, cooperative stop, worker-local `ScratchArena`, deterministic publish evidence, runtime steal attempt/success/wait counters, and fail-closed invalid-task/queue/dependency/exception diagnostics
- `MK_assets`: logical asset ids, Asset Identity v2 foundation-only stable `AssetKeyV2` rows, deterministic validation, stable `AssetId` derivation, deterministic `GameEngine.AssetIdentity.v2` text IO, host-independent placement resolution through `plan_asset_identity_placements_v2`, scene-side Scene v2 runtime package migration placement evidence through `SceneV2RuntimePackageMigrationModelMutation::placement_rows`, `GameEngine.SourceAssetRegistry.v1` validation/serialization plus Asset Identity v2 and import metadata projection for texture, mesh, audio, material, and scene source rows, asset registry, asset dependency graph, hot reload snapshot/event tracking, debounce-based recook request scheduling with dependency invalidation, failed-reload rollback state, runtime asset replacement staging with safe-point commits, recook import plan extraction, cooked package index serialization with content hashes, dependency edges, and deterministic recook decisions, shader source metadata, generated shader artifact tracking, shader compiler command planning including DXC Vulkan SPIR-V commands with `-spirv` and `-fspv-target-env=vulkan1.3`, shell-free shader tool runner validation, compiler output capture contracts, shader include dependency discovery, shader include dependency graph edges, deterministic shader artifact manifests, texture/mesh/audio/material/scene import metadata, scene mesh/material/sprite dependency edge kinds, first-party texture/mesh/audio source document decoding with optional deterministic hex byte payloads, asset import planning, material definition serialization, material instance serialization/composition, explicit backend-neutral material binding metadata, and the dependency-audited optional `asset-importers` lane for `libspng` PNG, `fastgltf` glTF, and `miniaudio` audio source importers
- `MK_animation`: keyframe validation, clamped linear sampling for float and `Vec3` tracks, sorted float curve binding samples, deterministic clip/state/trigger/blend animation state machines, weighted blend tree samples, 1D blend graph samples, layered animation samples with additive flags, authored timeline event tracks, timeline event playback, retargeting policy evaluation, Skeleton Pose Foundation v0 with validated skeleton hierarchies, rest poses, local joint transforms using translation, positive scale, and Z-rotation-only radians, joint-track bindings, deterministic pose diagnostics, local pose sampling with missing tracks falling back to rest pose, Animation Skin Payload Foundation v0 with skin joint palette entries, per-vertex influence rows, diagnostics, normalization, model-space joint matrices, and skinning palette construction, Animation CPU Skinning Foundation v0 plus Animation CPU Skinned Normal/Tangent/Bitangent Handedness v1 with deterministic skinned-position, optional skinned-normal/skinned-tangent deformation, and optional skinned-bitangent reconstruction from bind streams, skin influence rows, palette matrices, and tangent handedness signs, Animation Morph POSITION+NORMAL+TANGENT CPU v0 additive morph target evaluation for glTF-style streams, AnimationMorphWeightsTrackDesc sampling plus cooked scalar float clip byte-row conversion, planar two-bone IK with explicit bend-side plus pole-vector branch selection, analytic two-bone IK 3D orientation basis output through orthonormal parent/child frames, 3D FABRIK chain IK position solving, 3D FABRIK solution -> unit-quaternion local pose write-back, unit-quaternion 3D local-pose/model-pose matrix construction, quaternion keyframe sampling and selected 3D joint-track local-pose application, multi-segment XY FABRIK chain IK with optional bend-side plus pole-vector branch selection, FABRIK solution -> local `AnimationPose` Z-rotation write-back, local IK rotation limits, solver-integrated XY FABRIK segment local Z limits, signed-pi IK local angle normalization, and Animation Root Motion Foundation v1 root-joint translation plus Z-rotation start/end/delta sampling and explicit-duration forward loop accumulation
- `MK_ai`: standard-library-only Behavior Tree Foundation v0 plus Behavior Tree Blackboard Conditions v0, AI Behavior Authoring Foundation v1, and AI Perception Services v1 with value-type tree/node/leaf-result/tick-result contracts, typed blackboard value rows for bool/signed-integer/finite-double/string facts, deterministic `BehaviorTreeBlackboard` snapshots, blackboard condition bindings, `BehaviorAuthoringDocument` / `BehaviorAuthoringValidationContext` / `validate_behavior_authoring_document` reusable behavior document validation against available blackboard keys and supported game-owned action ids, `BehaviorAuthoringReadinessConfig` / `BehaviorAuthoringReadinessReport` / `evaluate_behavior_authoring_readiness` validation, trace, action-binding, blackboard-condition, and budget summaries, explicit `AiPerceptionAgent2D` / `AiPerceptionTarget2D` request rows, deterministic `AiPerceptionSnapshot2D` sight/hearing classification, stable primary-target selection, `build_ai_perception_snapshot_2d`, `write_ai_perception_blackboard`, `AiPerceptionReadinessConfig` / `AiPerceptionReadinessReport` / `evaluate_ai_perception_readiness_2d` stable-primary, blackboard-projection, visible/audible-target, and target-budget summaries, memoryless `sequence`, `selector`, `action`, and `condition` evaluation, externally supplied action statuses, visited-node traces, behavior-authoring trace rows, missing-root/duplicate-id/missing-child/empty-composite/leaf-has-children/missing-leaf/invalid-leaf/duplicate-leaf/duplicate-blackboard-key/invalid-blackboard-value/missing-blackboard-key/type-mismatch/invalid-condition/cycle/max-depth/duplicate-behavior-id/invalid-authoring-node-id/unsupported-action/invalid-perception-row/duplicate-perception-target/invalid-perception-blackboard-key diagnostics, and no scene, physics, navigation, async task, editor, platform, visual scripting, arbitrary code execution, ML inference, network replication, or third-party dependency
- `MK_audio`: device-independent audio bus/voice mix planning, `AudioGameplayMixRequest` / `plan_gameplay_audio_mix` fail-closed gameplay cue/bus planning for generated games, spatial voice/listener planning for distance attenuation and stereo pan metadata, decoded streaming queue append/consume/starvation contracts, clip metadata registration, sample-frame voice scheduling, streaming buffer state, device format conversion intent, underrun diagnostics, first-party interleaved float clip sample validation, deterministic interleaved float PCM rendering with gain, selectable nearest/linear resampling, channel conversion, and clamping, plus `AudioDeviceStreamRequest` / `AudioDeviceStreamPlan` queue-fill planning through `plan_audio_device_stream` and `render_audio_device_stream_interleaved_float` for bounded host-adapter PCM render chunks
- `MK_audio_wasapi`: Windows WASAPI shared-mode audio adapter that opens the default render endpoint through private COM/WASAPI owners, queues silence or `AudioDeviceStreamPlan`-driven rendered interleaved float PCM, reports queued frames, supports pause/resume/clear, records `wasapi_audio_device_lifecycle_evidence`, and keeps native handles out of public engine APIs
- `MK_audio_wasapi`: Windows WASAPI shared-mode audio adapter that opens the default render endpoint through private COM/WASAPI owners, queues silence or `AudioDeviceStreamPlan`-driven rendered interleaved float PCM, reports queued frames, supports pause/resume/clear, records `wasapi_audio_device_lifecycle_evidence`, and keeps native handles out of public engine APIs
- `MK_math`: vectors, matrices, and rotation-aware transforms
- `MK_platform`: virtual key input, mouse/touch/pen-style pointer input with distinct canceled touch state, first-party named touch gesture recognition, virtual gamepad input, display/DPI/safe-area contracts, monitor selection policy, window placement policy, mobile lifecycle/touch/orientation/safe-area/storage/permission contracts, text clipboard contracts, asynchronous native-style file dialog contracts, cursor state contracts, application lifecycle contracts, headless window, in-memory/rooted filesystem contracts, file watcher backend selection with deterministic polling fallback events, native backend family reporting, Windows `ReadDirectoryChangesW`, Linux `inotify`, and macOS `FSEvents` native file watcher events behind PIMPL/stubbed host-unavailable contracts, shell-free process command validation, recording process runner, and Windows stdout/stderr process execution
- `MK_runtime`: game-facing cooked package reader using `IFileSystem`, runtime payload hash/dependency validation, immutable first-party runtime asset handles, typed texture/mesh/audio/material/scene payload access, all-or-nothing active/staged package replacement at safe points, Safe-Point Package Unload Replacement Execution v1 through `commit_runtime_package_safe_point_replacement` for preloaded pending packages plus rebuilt `RuntimeResourceCatalogV2` commits with stale-handle invalidation counts and invalid replacement rollback, explicit `commit_runtime_package_safe_point_unload` / `RuntimePackageSafePointUnloadResult` active-package unload to an empty catalog with stale-handle invalidation and pending-discard reporting, Runtime Resource v2 safe-point/controller `RuntimeResourceCatalogV2` with generation-checked `RuntimeResourceHandleV2` values over cooked package records, duplicate-package-asset diagnostics, lookup, catalog replacement, stale-handle rejection, `RuntimeResidentPackageMountSetV2` explicit in-memory mount ids, `RuntimeResidentCatalogCacheV2` cache-hit reuse over unchanged mount generation / overlay / budget with budget-failure catalog preservation, a resident-mount overload of `execute_selected_runtime_package_streaming_safe_point` that validates selected streaming descriptors, projected resident budgets, and raw loaded-package catalog builds before mounting already-loaded packages and refreshing the cached resident catalog, `execute_selected_runtime_package_streaming_resident_unmount_safe_point` for selected resident unmount execution, `commit_runtime_resident_package_replace_v2` slot-preserving resident package replacement plus cache refresh after raw candidate catalog preflight, and `commit_runtime_resident_package_unmount_v2` copy-then-commit unmount/cache refresh that preserves the live mount set and cached catalog on invalid, missing, budget, or catalog failure, deterministic runtime diagnostic reports for package/payload/scene/session failures, deterministic runtime save data and settings documents, localization catalogs, `GameEngine.RuntimeInputActions.v4` action mapping documents with context-scoped keyboard, pointer, gamepad button, scalar key-pair, and scalar gamepad-axis sources, and `GameEngine.RuntimeInputRebindingProfile.v1` persisted full-row action/axis overlay profiles validated and applied over caller-supplied base maps through `validate_runtime_input_rebinding_profile`, `apply_runtime_input_rebinding_profile`, `serialize_runtime_input_rebinding_profile`, `deserialize_runtime_input_rebinding_profile`, `load_runtime_input_rebinding_profile`, and `write_runtime_input_rebinding_profile`; input maps evaluate through `RuntimeInputStateView`, `RuntimeInputContextStack`, default-context `bind_key`, `bind_pointer`, `bind_gamepad_button`, `bind_key_axis`, `bind_gamepad_axis`, and evaluate helpers, explicit `bind_key_in_context`, `bind_pointer_in_context`, `bind_gamepad_button_in_context`, `bind_key_axis_in_context`, and `bind_gamepad_axis_in_context` methods, and context-aware `action_down` / `axis_value` overloads over first-party `VirtualInput`, `VirtualPointerInput`, and `VirtualGamepadInput`; `RuntimeInputRebindingCaptureRequest` / `RuntimeInputRebindingCaptureResult` capture first-party digital action candidates, while `RuntimeInputRebindingAxisCaptureRequest` / `RuntimeInputRebindingAxisCaptureResult` plus `capture_runtime_input_rebinding_axis` capture first-party gamepad-axis override candidates, and `RuntimeInputRebindingFocusCaptureRequest` / `RuntimeInputRebindingFocusCaptureResult` plus `capture_runtime_input_rebinding_action_with_focus` add a narrow focused capture guard with deterministic `gameplay_input_consumed` and `focus_retained` rows for armed menu capture flows; active context stacks scan in order and use `default` only when empty, scalar action axes include deterministic clamp/deadzone handling, `VirtualPointerInput::touch_gesture` exposes active touch count, centroid, centroid delta, and two-touch pinch scale primitives over first-party touch pointers, `TouchGestureRecognizer` exposes tap, double-tap, long-press, pan, swipe, pinch, rotate, and canceled gesture events, and broad/background package streaming, native watcher ownership, automatic target inference, package scripts, async eviction, renderer/RHI teardown execution, renderer/RHI resource ownership, upload/staging outside host-owned lanes, native handles, broad hot reload productization, full interactive runtime/game rebinding panels outside the reviewed editor action capture lane, broad UI focus integration, global input consumption/bubbling beyond focused rebinding capture, radial stick deadzones, response curves, pointer delta axes, gesture-to-RuntimeInputActions binding, input glyphs, multiplayer device assignment, and per-device profiles remain follow-up work
- Runtime Input Rebinding Presentation Rows v1 is implemented in `MK_runtime` through `RuntimeInputRebindingPresentationToken`, `RuntimeInputRebindingPresentationRow`, `RuntimeInputRebindingPresentationModel`, `present_runtime_input_action_trigger`, `present_runtime_input_axis_source`, and `make_runtime_input_rebinding_presentation`; it provides deterministic labels and symbolic glyph lookup keys for reviewed base/profile display rows, while platform input glyph generation/assets/rendering and keyboard-layout localization remain follow-up work.
- Runtime Input Context Stack Planner v1 is implemented in `MK_runtime` through `RuntimeInputContextStackRequest`, `RuntimeInputContextLayerDesc`, `RuntimeInputContextStackPlan`, and `plan_runtime_input_context_stack`; it validates gameplay/menu/dialogue/rebinding/capture/overlay layers before producing a `RuntimeInputContextStack`, reports duplicate/invalid context diagnostics, supports default-context fallback and modal lower-priority blocking, and exposes `gameplay_input_available`, `gameplay_input_consumed`, `ui_context_active`, and `capture_context_active` evidence without adding a full UI focus/navigation system or platform/native input handles.
- `sample_2d_playable_foundation` now proves the generated-game adoption path by routing movement and jump evaluation through a planned passive HUD overlay plus gameplay stack before `RuntimeInputActionMap`, routing jump audio through `plan_gameplay_audio_mix`, planning gameplay/input/audio/session debug overlay rows through `plan_runtime_gameplay_debug_overlay`, and reporting `input_contexts=6`, `audio_cues=1`, and `debug_overlay_rows=12` in the source-tree smoke.
- `MK_runtime_rhi`: backend-neutral bridge from typed runtime payloads to RHI texture resources, mesh vertex/index buffers, renderer `MeshGpuBinding` payloads with owner-device validation data, derived position-only and position+normal+uv mesh vertex layout contracts from cooked mesh metadata, material descriptor set layout descriptions, caller-owned descriptor set layout allocation for shared material pipeline layouts, and material descriptor bindings, with upload-buffer writes plus copy-command upload paths when cooked byte payloads are present and explicit texture metadata-only behavior when richer payload bytes are not yet cooked
- `MK_runtime_scene_rhi`: backend-neutral runtime scene RHI bridge that turns a cooked `RuntimeAssetPackage` plus `SceneRenderPacket` into a retained `SceneGpuBindingPalette` by uploading unique referenced mesh, texture, and material payloads through `MK_runtime_rhi`, preserving derived mesh layout metadata such as position-only stride 12 and lit tangent-space stride 48 in renderer-visible bindings, retaining upload/binding resources, de-duplicating asset references, using one scene-shared material pipeline layout, supporting caller-owned material descriptor set layouts while creating a matching pipeline layout, and reporting deterministic per-asset failures without adding RHI dependencies to `MK_runtime` or `MK_scene`
- `MK_runtime_host`: editor-independent desktop game host contract with `DesktopGameRunner`, `DesktopHostServices`, `DesktopRunConfig`, `DesktopRunResult`, and `IDesktopEventPump` so `mirakana::GameApp` can run through public window, virtual input/lifecycle, and renderer contracts without SDL, OS, GPU, RHI, or editor handles in game code; it can optionally record `runtime_host.frame` CPU profile samples and backend-neutral renderer counters into a host-supplied `DiagnosticsRecorder`, and it also provides a host-independent packaged shader bytecode loader through `IFileSystem` for runtime host samples and tests
- `MK_runtime_host_win32`: optional Windows desktop game event-pump adapter behind `MK_ENABLE_DESKTOP_RUNTIME` that owns Win32 message pumping, forwards translated events through first-party window/input contracts, and keeps native event polling out of game code; the visible editor shell no longer implies a game runtime lane
- `MK_runtime_host_win32_presentation`: optional Windows desktop game presentation and reusable game-host adapter that owns renderer selection diagnostics, privately converts first-party Win32 surface candidates into D3D12/Vulkan surface handles, accepts host-supplied precompiled D3D12 DXIL or Vulkan SPIR-V shader bytecode through first-party `Win32DesktopPresentationDesc` fields, creates swapchain-backed `RhiFrameRenderer` instances when host/backend/toolchain gates pass, promotes cooked scene packages into host-owned scene GPU bindings through `MK_runtime_scene_rhi`, exposes backend-neutral `Win32DesktopPresentationReport` rows, and falls back deterministically to `NullRenderer` without exposing `IRhiDevice`, native handles, or GPU palettes to gameplay
- `MK_physics`: deterministic 2D and 3D body integration with gravity, force accumulation, damping, 2D/3D sweep-and-prune broadphase pairs, 2D AABB/circle contacts, 3D AABB/sphere/capsule contacts, collision layer/mask filtering, collision-bounds raycasts with nearest-hit selection and ray masks, trigger body flags plus non-resolving trigger overlap reporting, bounds-based 2D/3D shape sweeps with nearest-hit, ignored-body, trigger-inclusion, mask, and initial-overlap handling, exact 3D primitive sweeps through `PhysicsShape3DDesc::aabb`, `PhysicsShape3DDesc::sphere`, `PhysicsShape3DDesc::capsule`, `PhysicsQueryFilter3D`, `PhysicsWorld3D::exact_shape_sweep`, and `PhysicsWorld3D::exact_sphere_cast` for AABB/sphere/vertical-capsule query shapes against current AABB/sphere/capsule target primitives, deterministic contact manifolds through `PhysicsContactPoint3D`, `PhysicsContactManifold3D`, and `PhysicsWorld3D::contact_manifolds` with stable feature ids, warm-start-safe point rows, trigger/disabled exclusion, and solver consumption, opt-in deterministic CCD rows through `PhysicsContinuousStep3DConfig`, `PhysicsContinuousStep3DRow`, `PhysicsContinuousStep3DResult`, and `PhysicsWorld3D::step_continuous` for fast dynamic AABB/sphere/vertical-capsule bodies swept against current non-trigger AABB/sphere/capsule targets while `PhysicsWorld3D::step` remains discrete, generated-game character/dynamic policy rows through `PhysicsCharacterDynamicPolicy3DDesc`, `PhysicsCharacterDynamicPolicy3DRowKind`, `PhysicsCharacterDynamicPolicy3DRow`, `PhysicsCharacterDynamicPolicy3DResult`, and `evaluate_physics_character_dynamic_policy_3d`, value-only advanced controller planning through `PhysicsAdvancedController3DDesc`, `PhysicsMovingPlatform3DDesc`, `PhysicsAdvancedController3DResult`, and `plan_physics_advanced_controller_3d`, explicit distance-joint solving through `PhysicsJoint3DStatus`, `PhysicsJoint3DDiagnostic`, `PhysicsDistanceJoint3DDesc`, `PhysicsJointSolve3DConfig`, `PhysicsJointSolve3DDesc`, `PhysicsJointSolve3DRow`, `PhysicsJointSolve3DResult`, and `solve_physics_joints_3d`, opt-in distance/fixed/linear-axis positional constraint solving through `PhysicsConstraint3DStatus`, `PhysicsConstraint3DDiagnostic`, `PhysicsConstraint3DKind`, `PhysicsFixedConstraint3DDesc`, `PhysicsLinearAxisConstraint3DDesc`, `PhysicsConstraintSolve3DConfig`, `PhysicsConstraintSolve3DDesc`, `PhysicsConstraintSolve3DRow`, `PhysicsConstraintSolve3DResult`, and `solve_physics_constraints_3d`, default-unbounded `max_rows` row budgets plus `row_budget_exceeded` diagnostics for those constraints, value-only kinematic motion planning through `PhysicsKinematicMotion3DStatus`, `PhysicsKinematicMotion3DDiagnostic`, `PhysicsKinematicMotion3DRowKind`, `PhysicsKinematicMotion3DDesc`, `PhysicsKinematicMotion3DRow`, `PhysicsKinematicMotion3DResult`, and `plan_physics_kinematic_motion_3d`, simple vehicle policy rows through `PhysicsSimpleVehicle3DDesc`, `PhysicsSimpleVehicle3DResult`, and `plan_physics_simple_vehicle_3d`, host-independent replay signatures and count-based budget diagnostics through `PhysicsDeterminismGate3DStatus`, `PhysicsDeterminismGate3DDiagnostic`, `PhysicsDeterminismGate3DConfig`, `PhysicsDeterminismGate3DCounts`, `PhysicsReplaySignature3D`, `PhysicsDeterminismGate3DResult`, `make_physics_replay_signature_3d`, and `evaluate_physics_determinism_gate_3d`, iterative contact solver configuration with correction slop/percent, stacked-body coverage, replay-tested contact resolution, conservative `move_physics_character_controller_3d` capsule movement/contact rows, first-party `build_physics_world_3d_from_authored_collision_scene` in-memory collision rows, and a documented adapter decision that explicitly excludes Jolt/native middleware from the default/generated-game Physics 1.0 ready surface while exposing Jolt through the optional `physics-jolt` dependency/legal/notice-gated `MK_physics_jolt` lane with fail-closed capability negotiation
- `MK_navigation`: standard-library-only deterministic grid navigation with `NavigationGrid`, walkability and traversal-cost cells, cardinal A* pathfinding through `find_navigation_path`, stable tie-breaking, invalid/blocked endpoint diagnostics, no-path reporting, total cost, visited-node counts, `validate_navigation_grid_path` remaining-path diagnostics, `replan_navigation_grid_path` stale/blocked path recovery, `smooth_navigation_grid_path` deterministic walkable line-of-sight waypoint reduction, deterministic grid-path-to-point mapping, `NavigationGridAgentPathRequest` / `NavigationGridAgentPathPlan` / `plan_navigation_grid_agent_path` for generated-game route setup with optional smoothing, point-path mapping, planner diagnostics, point-count/cost evidence, and `NavigationAgentState` creation, `NavigationNavmeshPathRequest` / `plan_navigation_navmesh_path` for deterministic scene-ref polygon routes with dynamic obstacle blocking, `NavigationCrowdPlanRequest` / `plan_navigation_navmesh_crowd` plus `NavigationCrowdReadinessConfig` / `NavigationCrowdReadinessReport` / `evaluate_navigation_crowd_readiness` for value-only deterministic crowd rows over navmesh routes, dynamic-obstacle participation, local-avoidance aggregation, max-agent budget rejection, duplicate/invalid-agent diagnostics, deterministic source-order checks, row budgets, readiness summaries, and replay counters, explicit path-follow cursor advancement with arrival radius/max step/remaining distance reporting, single-agent arrive steering desired velocity/speed helpers, Navigation Agent Controller Foundation v0 value-type state/tick helpers for idle/moving/reached/cancelled/invalid movement status, path replacement, speed/delta clamping, final-destination slowing, remaining-distance reporting, and actual path-movement desired velocity, plus `calculate_navigation_local_avoidance` for deterministic single-agent local separation against explicit neighbor rows without owning scene or physics state. Navmesh asset import, persistent/full crowd simulation beyond value-only batch planning, animation-aware steering, networked crowd determinism, scene/physics integration, editor visualization, middleware, and background navigation jobs remain follow-up work.
- `MK_navigation` / `MK_runtime` large-world review: `NavigationHierarchicalWorldPathRequest` / `plan_navigation_hierarchical_world_path` provide value-only deterministic world-region portal routing over reviewed world-region refs, nav-data refs, portal rows, and endpoint scene refs, while `RuntimeWorldRegionNavigationRefReviewRequest`, `RuntimeWorldRegionNavigationPathCacheReviewRequest`, `review_runtime_world_region_navigation_refs`, and `review_runtime_world_region_navigation_path_cache` review package residency rows and fail-closed retained path-cache readiness against malformed route shapes, missing/not-resident package rows, stale mount/catalog generations, and unrefreshed `RuntimeResidentCatalogCacheV2` state without package reads, catalog refresh, streaming execution, nav baking, scene mutation, renderer/RHI ownership, or native handles.
- `MK_ui`: first-party retained-mode runtime UI contracts with deterministic document hierarchy, stable ids, semantic roles, visibility/enabled state, bounds, style/layout descriptors, headless row/column/stack layout solving with padding, margins, row/column gaps, anchors, fill, and size constraints, text metadata, image metadata, first-party renderer submission payloads with `RendererBox`, `RendererTextRun`, image placeholders, text adapter rows with line/span placeholders and unresolved-localization diagnostics, an opt-in dependency-free `MonospaceTextLayoutPolicy` overload for deterministic logical-order line/glyph placeholder boxes with simple ASCII whitespace wrapping plus clip/invalid-policy/unsupported-direction diagnostics, image adapter rows with stable resource and asset URI references, accessibility payload nodes carrying localization key, enabled, focusable, parent, and depth metadata, host-independent publish/session plans for accessibility, IME composition, platform text-input begin/end, font rasterization requests, and image decode requests, adapter boundaries for text/font/IME/accessibility/image/concrete rendering/platform integration, focus/navigation state, transition state, one-way text binding, and command invocation
- `MK_ui_renderer`: first-party adapter that maps styled `MK_ui` renderer submission boxes through theme tokens, deterministic monospace text glyph rows through `UiRendererGlyphAtlasPalette`, and resolved image placeholders through `UiRendererImagePalette` into `MK_renderer` `SpriteCommand` submissions while reporting text adapter row, text glyph, image placeholder, image sprite submission, missing glyph/image resource, accessibility node, and adapter diagnostic counts; glyph/image entries consume already-cooked atlas metadata through `build_ui_renderer_glyph_atlas_palette_from_runtime_ui_atlas` and `build_ui_renderer_image_palette_from_runtime_ui_atlas`, with font loading/rasterization, text shaping, renderer texture upload, broader image decoding/codecs, and accessibility OS integration kept behind adapters
- `MK_tools`: shader tool process adapter that maps shader compile commands and `spirv-val` validation commands to platform process execution with artifact output-root policy and reviewed executable override for PATH names, project-relative paths, or absolute installed SDK/tool paths, deterministic shader toolchain discovery for `dxc`, `spirv-val`, `metal`, and `metallib`, backend-specific shader toolchain readiness diagnostics including DXC SPIR-V CodeGen capability, shader compile and validation actions, provenance writes, persistent cache index storage, cache-hit reuse, deterministic test artifact marker writes, real artifact enforcement for compiler-backed runs, shader hot reload planning through artifact provenance plus pipeline recreation decisions, batch shader pipeline cache planning and first-party cache-index reconciliation through `build_shader_pipeline_cache_plan` / `reconcile_shader_pipeline_cache_index`, filesystem-backed texture/mesh/audio/material import execution for deterministic cooked artifacts including first-party texture/audio byte payload and mesh vertex/index byte payload preservation, first-party `GameEngine.Scene.v1` validation/canonicalization through `MK_scene`, reviewed `GameEngine.Scene.v2` / `GameEngine.Prefab.v2` authoring dry-run/apply helpers through `plan_scene_prefab_authoring` and `apply_scene_prefab_authoring`, reviewed Scene v2 runtime package migration helpers through `plan_scene_v2_runtime_package_migration` and `apply_scene_v2_runtime_package_migration` that reuse the existing Scene v1 package update helper, automatic cooked package assembly from successful import results into validated runtime-loadable `.geindex` files, optional `asset-importers` adapters for PNG/glTF/common-audio source files through `libspng`, `fastgltf`, and `miniaudio`, decoded PNG RGBA8 payloads, optional `PngImageDecodingAdapter`, packed decoded-image UI atlas package authoring through `author_packed_ui_atlas_from_decoded_images` and `plan_packed_ui_atlas_package_update`, decoded common-audio PCM payloads, glTF triangle `POSITION`/index mesh byte payload cooking for embedded/loaded buffers, runtime recook execution that stages safe replacements transactionally, and filesystem-backed asset snapshot scanning for hot reload
- `MK_platform_win32`: Windows runtime/window backend with display enumeration, first-party monitor selection and window placement policy, window display scale, pixel density, safe-area reporting, Unicode text clipboard access plus `Win32ClipboardTextAdapter`, native file dialog access, cursor visibility/grab/relative mode control, `Win32PlatformIntegrationAdapter` text-input area/start/stop dispatch, copied Win32 committed-text and text-edit command rows, application lifecycle event mapping, and keyboard, mouse, touch, and gamepad input mapping
- `platform/android`: toolchain-gated Android Gradle Plugin 9.1.0, Gradle 9.3.1, JDK 17, Android SDK Platform 36.1, Build Tools 36.0.0, Android SDK CMake 4.1.2, NDK CMake, Prefab, AndroidX AppCompat 1.7.1, AndroidX Core 1.18.0, and AndroidX GameActivity 3.0.4 package template with game manifest validation, generated asset packaging, private `ANativeWindow`/`VK_KHR_android_surface` bridge and surface creation entry point, private low-latency AAudio output adapter, Release signing validation with non-repository upload keys, deterministic `ANDROID_AVD_HOME` normalization, AVD readiness diagnostics, emulator install/launch smoke, Debug/Release package script selection, and explicit SDK/NDK/JDK/Gradle blocker diagnostics
- `platform/ios`: toolchain-gated CMake/Xcode UIKit/Metal bundle template with Metal-backed root view setup, sandbox save/cache/shared storage mapping, game manifest/resource bundling, bundle identifier/signing configuration inputs, simulator/device SDK selection, Debug/Release package script selection, game manifest validation, Simulator install/launch smoke script, dedicated macOS GitHub Actions iOS validation lane, and explicit macOS/Xcode blocker diagnostics
- `MK_renderer`: renderer contract, NullRenderer, sprite/mesh command counters, MeshCommand mesh/material asset identity plus optional mesh/material GPU binding payloads, deterministic Frame Graph v0 pass/resource declaration diagnostics, Frame Graph v1 foundation-only `FrameGraphV1Desc` resource access/imported/transient/barrier intent planning, Shadow Map Foundation v0 directional-light planning with `depth24_stencil8` sampled depth texture requirements, caster/receiver counts, deterministic diagnostics, and stable shadow-depth-before-receiver pass declaration, RHI-backed frame submission through `RhiFrameRenderer` with swapchain-backed resize propagation, optional host-owned depth texture forwarding through RHI Depth Attachment Contract v0, frame-between host-owned depth texture replacement after resize/recreation, and executor-owned raw `primary_color` render pass envelopes, `RhiPostprocessFrameRenderer` for the current color-only scene-color plus fullscreen postprocess two-pass path, material descriptor set binding, vertex/index buffer binding, indexed mesh draws, begin-frame swapchain failure cleanup, frame-between graphics pipeline replacement for shader hot reload, backend-neutral framegraph/postprocess pass counters, and renderer-owned RHI viewport surfaces with renderer-submitted frames, executor-owned color-state transitions to `render_target`, `copy_source`, and `shader_read`, executor-owned clear-frame render pass envelopes, shader-readable display frames, optional native display interop texture usage, and CPU readback fallback frames
- `MK_scene_renderer`: scene-packet-to-renderer bridge that converts visible scene meshes and sprites into renderer commands, resolves material definition and material instance base colors, loads cooked runtime scene render data, instantiates non-throwing runtime scene render packets with diagnostics, resolves optional `SceneGpuBindingPalette` mesh/material GPU bindings, extracts transform position/scale/z rotation, maps sprite size/tint, builds camera matrices, maps lights into renderer-neutral commands, and adapts `SceneRenderPacket` into Shadow Map Foundation v0 plans by choosing the first directional shadow-casting light and counting visible meshes as v0 casters/receivers while keeping `MK_scene` renderer-neutral
- `MK_rhi`: RHI contracts with Renderer/RHI Resource Foundation v1 `RhiResourceLifetimeRegistry` resource ids, owner labels, debug names, generation-checked handles, deferred-release records, queue-qualified fence retirement, diagnostics, marker-style events, NullRHI lifetime wiring, D3D12/Vulkan deferred native teardown for committed resources and pipeline-class objects, GPU debug-scope/marker stats, D3D12 graphics-queue timestamp-frequency reporting, read-only memory diagnostics, Upload/Staging Foundation v1 foundation-only `RhiUploadStagingPlan` staging allocation rows, buffer/texture copy rows, submitted `FenceValue` tracking, completed-fence retirement, `RhiUploadDiagnosticCode::stale_generation` for same-id/wrong-generation staging handles, generation-aware `RhiUploadRing` ownership/byte-offset lookup, NullRhiDevice, resources including shared texture usage intent, swapchains, explicit swapchain frame acquire/release, backend-neutral swapchain resize, shaders, descriptor set layouts, descriptor sets, copy/upload/readback commands with explicit texture footprints, CPU buffer readback, transient resource leases, presentation ownership and sequencing, single-pending-frame swapchain throttling, graphics pipelines with vertex input layouts/attributes plus backend-neutral depth-test/write/compare state, render passes with color attachments plus optional first-party depth attachments, command lists, resource transitions, vertex/index buffer binding, direct/indexed draw calls, D3D12 and Vulkan native depth attachment/depth-state proofs with active render-pass vs pipeline color/depth validation, fence semantics, backend-neutral fence wait/failure/last-submitted/last-completed stats, and shared texture export/failure stats; these registries/planners do not implement GPU allocator enforcement or residency budgets, package streaming, upload/staging package integration, production render graph scheduling, Metal `IRhiDevice` parity, public native/RHI handles, editor resource management/capture tooling execution beyond diagnostics/request handoff, production 2D atlas/batching/native GPU readiness, broad renderer quality, or 3D playable vertical-slice readiness
- `MK_rhi_d3d12`: Windows-only D3D12/DXGI backend scaffold with runtime probe, device bootstrap validation, committed resource checks, and PIMPL device/resource/command list/fence/swapchain/RTV/DSV/descriptor heap/root signature/shader/PSO ownership with CPU waits, GPU-side cross-queue waits, HWND presentation, resize, back-buffer transitions, standalone texture transitions, requested-color/depth clears, native CBV/SRV/UAV descriptor view writes, descriptor table binding, swapchain and texture render-target setup, buffer and aligned buffer/texture copy recording, shared committed textures plus first-party shared texture export handles for editor interop, first triangle draw recording, and an initial `IRhiDevice` bridge for backend-neutral resources, descriptor sets, shaders, pipelines, buffer copies, aligned buffer/texture copies, transient leases, surface-backed swapchains, explicit swapchain frame acquire/release, swapchain resize, graphics-queue-only queued presentation, render-pass-complete-before-present sequencing, duplicate-present rejection, single-pending-frame throttling, swapchain and texture render passes with optional private `depth24_stencil8` DSV attachments, native `DepthStencilStateDesc` PSO mapping, active color/depth format validation, descriptor set binding, vertex input metadata, vertex/index buffer binding, indexed draw recording, command submission, fence wait ownership, backend-neutral fence lifecycle stats, tracked texture state validation, non-black render-target clear readback proof, first-triangle texture readback proof, depth-tested overlapping-triangle texture readback proof, visible sampled-texture/sampler proof, visible runtime material texture sampling proof, visible uploaded runtime mesh vertex/index proof, visible cooked runtime scene GPU palette indexed draw/readback proof, installed Lit Material v0 sample package proof, shared texture export proof, and swapchain lifecycle proof
- `MK_rhi_vulkan`: SDK-independent Vulkan runtime owners through private PIMPL boundaries for instance/device, command pool/primary command buffer, first-party buffer create planning and buffer/memory ownership plus readback-domain CPU mapping, first-party texture create planning and image/memory/image-view ownership including private depth-aspect views for `depth24_stencil8`, shader module, descriptor set layout/pool/set allocation/update and binding, pipeline layout/graphics pipeline including backend-neutral `DepthStencilStateDesc` mapping, an initial `IRhiDevice` bridge for backend-neutral buffer/texture creation, transient leases, shader modules, descriptor set layout/allocation/update, standalone texture transitions including `depth_write`, copy commands/render-target clears/render-target draws with optional depth attachment binding, pipeline layout, graphics pipeline creation, surface-backed swapchain creation/resize, explicit swapchain frame acquire/release, shader-free swapchain clear render passes, graphics command-list close/submit, queued present, copy-only submit, immediate fence stats/waits, and readback-buffer reads backed by private runtime owners, Win32 surface/swapchain/image views, real surface capabilities/format/present-mode queries before swapchain creation, frame sync/acquire/present, dynamic rendering clear/readback smoke coverage, dynamic rendering draw recording with optional color/depth attachments plus vertex/index buffer binding and indexed draws, synchronization2 barrier/submission recording with shader-read texture hazards and depth-write barriers, command-list-local texture state commits with stale-submit rejection, host-visible readback buffer creation/copy/mapping, `IRhiDevice` mapping readiness gates for descriptors plus visible clear/readback, visible draw/readback, visible texture-sampling, environment-gated depth-ordered draw/readback proof, environment-gated sampled-depth readback proof, and environment-gated cooked scene GPU palette readback proof using real SPIR-V scene shaders, and Win32/Vulkan game-window plus scene GPU presentation proof with precompiled SPIR-V artifacts on a host with Vulkan runtime and surface support
- `MK_rhi_metal`: SDK-independent Metal backend scaffold with backend identity, host support, capability/probe contracts, non-empty `metallib` validation, runtime/default-device/command-queue/shader-library readiness planning, texture synchronization planning for render/blit/present encoder intent, platform availability diagnostics, and Apple-only Objective-C++ native default-device, command-queue, command-buffer, render-encoder, texture-target, `CAMetalLayer` drawable acquisition, drawable present scheduling, command-buffer submission, and texture-readback ownership behind PIMPL without public Metal handles
- `MK_scene`: scene nodes, hierarchy, rotation-aware transforms, camera/light/mesh/sprite renderer components, renderer-neutral render packet extraction with world transforms, backend-neutral 2D playable scene validation through `mirakana::validate_playable_2d_scene`, scene-subtree prefab creation, prefab instantiation, deterministic prefab serialization, dependency-free prefab variant/override diagnostics and composition, deterministic valid `GameEngine.PrefabVariant.v1` serialization/deserialization plus `deserialize_prefab_variant_definition_for_review` for editor cleanup review of stale variants, deterministic scene serialization, and Scene/Component/Prefab Schema v2 APIs for stable `AuthoringId` values, schema-driven component rows, deterministic `GameEngine.Scene.v2` and `GameEngine.Prefab.v2` text IO, validation diagnostics including sparse index, line-control, finite-transform, and non-zero-scale checks, and stable prefab property override paths without claiming broad/dependent package cooking, runtime prefab semantics, package streaming, or 3D vertical-slice readiness
- `MK_editor_core`: GUI-independent editor project document with shader tool settings, shader tool discovery state plus backend-specific shader toolchain readiness diagnostics, render backend preference/availability selection, project settings draft validation, project creation wizard, project migration, project bundle IO, shader artifact manifest storage, clean-break `GameEngine.Workspace.v2` workspace serialization with persisted dock layout rows, core-backed `EditorDockLayout` / `editor_dock_panel_catalog` dock and panel identity rows including `main_menu` shell chrome and `input_rebinding` non-native-shell classification, `EditorDockCommandKind` / `EditorDockCommandRequest` / `EditorDockCommandPlan` plus `plan_editor_dock_command` and `apply_editor_dock_command` for value-only show/hide/activate/move/split/reset dock mutation planning with validated result layouts, cycle/duplicate-tab rejection, panel state including `input_rebinding`, content browser state, asset import queue state, asset import progress summaries, import dependency display items, thumbnail requests including scene assets, selected cooked material preview status/artifact/diagnostic/material-factor/texture-dependency metadata, selected-material asset preview panel rows with typed GPU payload texture readiness and D3D12/Vulkan material-preview shader readiness, source material authoring documents for first-party material factor/texture-slot edits, `GameEngine.MaterialGraph.v1` material graph authoring documents with text-store save/load and undoable graph replace actions, registry-backed texture validation, deterministic texture dependency extraction, text-store save/load, dirty/staged state, undoable material edit actions, source scene/prefab authoring documents for hierarchy rows, selected-node state, text-store save/load, registry-backed mesh/material/sprite diagnostics, package candidate rows, package registration draft rows that classify add/already-registered/source/unsafe/duplicate states from caller-supplied `runtimePackageFiles` entries, reviewed package registration apply plans, a narrow `ITextStore` updater for `game.agent.json.runtimePackageFiles`, Editor Input Rebinding Profile Panel v1 through `EditorInputRebindingProfileReviewModel` and `EditorInputRebindingProfilePanelModel` diagnostics for reviewed `GameEngine.RuntimeInputRebindingProfile.v1` save readiness/conflicts/unsupported claims plus retained visible rebinding review rows, `EditorPlaySession` and `IEditorPlaySessionDriver` play-in-editor contracts over an isolated simulation scene copy with source-edit blocking reports, finite-delta driver tick callbacks, last-delta reporting, and discard-on-stop semantics, Prefab Variant reviewed resolution, batch resolution, and base refresh models through `PrefabVariantConflictBatchResolutionPlan`, `resolve_prefab_variant_conflicts`, `source_node_mismatch` conflict rows, `PrefabVariantBaseRefreshPlan`, `plan_prefab_variant_base_refresh`, and retained `prefab_variant_conflicts.batch_resolution` plus `prefab_variant_base_refresh` rows, and undoable scene/prefab edit actions, import diagnostics, asset pipeline panel aggregation, asset import execution result mapping, imported asset record registration helpers including scene records, asset hot reload event state, hot reload recook request state, hot reload applied/failed rollback result state, hot reload diagnostic labels, shader compile queue/status state, shader compile execution result mapping, default viewport shader compile request planning for D3D12 DXIL and Vulkan SPIR-V artifacts, D3D12 DXIL and Vulkan SPIR-V viewport shader artifact readiness state, viewport state, viewport run controls, viewport tool state, scene transform drafts, scene component drafts, viewport transform edit application, command registry, command palette model, first `MK_editor_ui` retained model helpers including timeline controls, event rows, and recorder-backed profiler panel rows from `MK_core` diagnostic captures, deterministic editor UI model serialization, dirty tracking, and undo/redo history
- `MK_editor`: optional Windows-native editor/developer shell is active through the dependency-free `desktop-editor` lane. The shell keeps Win32, first-party retained `mirakana::ui` / `MK_ui_renderer` document submission, Direct3D 12, DXGI, descriptor heaps, swapchains, fences, Win32 file-dialog, clipboard, reviewed process-runner service adapters, private visible D3D12 viewport display, private visible D3D12 material preview, value-only private D3D12 texture-display lifecycle gates, and private `native_texture_display_adapter.*` RHI evidence for requested texture display, offscreen targets, descriptor updates, resource barriers, fences, viewport resize-safe teardown, visible compositor consumption gates, and material-preview host-private frame preparation, and private visible compositor presentation; the first visible panel set renders Main Menu, Scene, Inspector, Assets, Console, Viewport, Resources, AI Commands, Profiler, Timeline, and Project Settings from `MK_editor_core` dock layout/catalog data stored on `Workspace` and reports deterministic `editor_shell_ui=first_party`, `editor_shell_backend=d3d12`, `editor_shell_imgui=0`, `editor_shell_panels=11`, `editor_shell_docking_status=single_window_ready`, `editor_shell_dock_tab_headers=11`, `editor_shell_dock_split_gutters=3`, `editor_shell_dock_active_panels=4`, `editor_shell_dock_focusable_controls=11`, `editor_shell_sdl3=0`, `editor_shell_file_dialog_service=win32`, `editor_shell_clipboard_service=win32`, `editor_shell_reviewed_process_runner=win32`, `editor_shell_viewport_status=d3d12_texture_ready`, `editor_shell_viewport_visible_texture_composites`, `editor_shell_viewport_native_handles_exposed=0`, `editor_shell_material_preview_status=d3d12_texture_ready`, `editor_shell_material_preview_visible_texture_composites`, `editor_shell_material_preview_native_handles_exposed=0`, `editor_shell_renderer_boxes_submitted`, and `editor_shell_renderer_text_runs_available` smoke counters. The previous SDL3/Dear ImGui docking shell and material-preview viewport support sources are removed from active build lanes and the inactive Dear ImGui implementation files are deleted; `MK_editor_core` remains the supported GUI-independent editor logic target for input rebinding models such as `EditorInputRebindingCaptureModel` action-row capture controls, `EditorInputRebindingAxisCaptureModel` axis-row capture controls, and in-memory profile application, scene/prefab/prefab-variant authoring, Play-In-Editor contracts, profiler/trace review rows, game-module driver policy rows, material preview `host-private-native` execution evidence, core dock layout/panel identity rows, value-only core dock command planners, clean-break dock persistence, AI operation snapshot/catalog/dry-run/apply overloads for reviewed dock layout rows and dock command ids, core rich-text document/selection/copy rows, and retained UI model data. Vulkan/Metal editor texture-display parity, broader material-preview GPU parity, multi-window drag/tear-off docking, editable/help rich text, broad rich text shaping/rendering, native IME candidate UI, full UIA pattern/event parity, runtime UI OS accessibility publication, production font fallback, and cross-platform shell work require focused phases with private backend handles, fresh static checks, official-SDK or audited-dependency adapter records, and validation evidence.
- `games/`: C++ game scaffold and executable samples for lifecycle, input/renderer, source-tree 2D playable foundation, physics, navigation, AI behavior-tree/navigation composition, animation, UI, audio, asset/material/runtime-scene foundations, and optional desktop runtime package targets whose CMake registration includes `GAME_MANIFEST`, finite smoke metadata, selected-target package file metadata, config and cooked scene package payloads for the non-shell desktop runtime sample, plus build-output D3D12 DXIL and Vulkan SPIR-V loading when `dxc` with SPIR-V CodeGen is available for the shell and cooked-scene desktop runtime samples; `sample_2d_playable_foundation` proves the ready `2d-playable-source-tree` recipe through runtime input actions, orthographic camera plus visible sprite scene validation, scene sprite renderer submission, first-party HUD submission, `plan_gameplay_audio_mix` cue/bus planning, `plan_runtime_gameplay_debug_overlay` gameplay/input/audio/session rows, device-independent audio cue rendering, and `NullRenderer` counters without desktop runtime/package/native-handle claims; `sample_2d_desktop_runtime_package` proves the host-gated `2d-desktop-runtime-package` recipe through a manifest-derived desktop package containing config, `.geindex`, cooked 2D scene, deterministic data-only tilemap metadata, first-party sprite animation rows, sprite texture/material, cooked audio payload, Win32 desktop host execution, sprite/HUD/audio submission counters, sprite animation counters, `tilemap_cells_sampled` / `tilemap_diagnostics` counters, deterministic `NullRenderer` fallback, packaged D3D12 DXIL window smoke, and toolchain/host-gated Vulkan SPIR-V window smoke without source image decoding, production atlas packing, a full tilemap editor, production sprite batching, public native/RHI handles, Metal, or broad renderer quality claims; `tools/new-game.ps1 -Template DesktopRuntimePackage` now generates a config-only Win32 desktop runtime package scaffold with `runtimePackageFiles` and `PACKAGE_FILES_FROM_MANIFEST`, proven by `sample_generated_desktop_runtime_package`; `tools/new-game.ps1 -Template DesktopRuntimeCookedScenePackage` generates a package-ready Win32 desktop scaffold with config, `.geindex`, first-party cooked texture/mesh/material/scene files, runtime package load, and renderer-neutral scene-submit smoke validation, proven by `sample_generated_desktop_runtime_cooked_scene_package`; `tools/new-game.ps1 -Template DesktopRuntime2DPackage` generates a host-gated packaged 2D gameplay scaffold with cooked sprite texture/material/audio/orthographic scene payloads, deterministic data-only tilemap metadata, first-party sprite animation rows, `runtimeSceneValidationTargets`, `atlasTilemapAuthoringTargets`, package residency descriptors, generated 2D sprite shader source, selected-target D3D12 native sprite package smoke metadata, sprite animation smoke, tilemap runtime UX smoke, and static scaffold checks; `tools/new-game.ps1 -Template DesktopRuntimeMaterialShaderPackage` generates an opt-in desktop scaffold with source material/HLSL authoring inputs, cooked scene payloads, selected-target D3D12 DXIL artifacts, and toolchain-gated Vulkan SPIR-V artifacts while keeping authoring files out of `runtimePackageFiles`, proven by `sample_generated_desktop_runtime_material_shader_package`; `tools/new-game.ps1 -Template DesktopRuntime3DPackage` generates a host-gated packaged 3D gameplay scaffold with cooked texture/mesh/material/perspective scene payloads, source Scene/Prefab v2 files, a selected SourceAssetRegistry v1 document, `runtimeSceneValidationTargets`, `prefabScenePackageAuthoringTargets`, primary camera/controller smoke, selected-target D3D12 shader artifact metadata, and Vulkan toolchain-gated package recipes while keeping source authoring files out of `runtimePackageFiles`; `tools/register-runtime-package-files.ps1` safely appends reviewed game-relative `runtimePackageFiles` entries for existing desktop runtime games before schema/package validation; `sample_ai_navigation` proves a generated-game pattern that combines `MK_ai` blackboard-driven behavior tree conditions with `MK_navigation` `plan_navigation_grid_agent_path` output and value-type navigation agent ticks through public APIs only, while `sample_desktop_runtime_game` is the current Lit Material v0 plus package-visible depth-aware Frame Graph/Postprocess v0 and Package-Visible Directional Shadow Filtering v0 proof with matching `prefabScenePackageAuthoringTargets`, source Scene/Prefab v2 fixtures, selected SourceAssetRegistry rows, a cooked position+normal+uv mesh, base-color material/texture payload, deterministic scene light payload, target-specific scene/postprocess/shadow shader artifacts, and D3D12/Vulkan package smokes that report scene GPU readiness plus `postprocess_status=ready`, `postprocess_depth_input_ready=1`, `directional_shadow_status=ready`, `directional_shadow_ready=1`, `directional_shadow_filter_mode=fixed_pcf_3x3`, `directional_shadow_filter_taps=9`, `directional_shadow_filter_radius_texels=1`, and `framegraph_passes=3`; broader post effects remain follow-up
- `sample_desktop_runtime_game` also proves Package-Visible Directional Shadow Filtering v0 on the same selected D3D12/Vulkan package lane through target-specific shadow receiver/shadow shader artifacts, first-party `--require-directional-shadow` and `--require-directional-shadow-filtering` request/report fields, `directional_shadow_status=ready`, `directional_shadow_ready=1`, `directional_shadow_filter_mode=fixed_pcf_3x3`, `directional_shadow_filter_taps=9`, `directional_shadow_filter_radius_texels=1`, and `framegraph_passes=3`. Directional cascaded atlased shadow depth, per-cascade viewport rendering, `pack_shadow_receiver_constants`, and Stable Directional Light-Space Policy v0 consumption ship on that smoke path (`Cascaded And Atlased Shadow Maps v1`). Hardware comparison samplers, Metal shadow presentation, and editor shadow authoring remain follow-up.
- `sample_ai_navigation` now also proves AI Perception Services v1 by building explicit `AiPerceptionAgent2D` / `AiPerceptionTarget2D` route-target rows, calling `build_ai_perception_snapshot_2d`, projecting facts through `write_ai_perception_blackboard`, and consuming those facts through `BehaviorTreeEvaluationContext` before value-type navigation ticks.
- Runtime Gameplay Systems Composition v1 expands `sample_gameplay_foundation` into the headless source-tree proof that composes `build_physics_world_3d_from_authored_collision_scene`, `move_physics_character_controller_3d`, `plan_navigation_grid_agent_path`, `build_ai_perception_snapshot_2d`, `write_ai_perception_blackboard`, `evaluate_behavior_tree`, `render_audio_device_stream_interleaved_float`, animation state transitions, and `HeadlessRunner` lifecycle in one game object without generated package shipping, scene/physics integration, middleware, Dear ImGui, or native handle claims.
- Generated 2D/3D Gameplay Systems Package Smoke v1 promotes selected public systems composition into `sample_2d_desktop_runtime_package` and `sample_generated_desktop_runtime_3d_package` through `--require-gameplay-systems`, package-visible `gameplay_systems_status=ready`, `gameplay_systems_navigation_plan_status=ready`, `gameplay_systems_navigation_navmesh_dynamic_obstacles=1`, `gameplay_systems_navigation_navmesh_readiness_status=ready`, `gameplay_systems_navigation_navmesh_readiness_diagnostics=0`, `gameplay_systems_navigation_navmesh_scene_refs=3`, `gameplay_systems_navigation_navmesh_visited_polygons=3`, `gameplay_systems_navigation_crowd_source_order_ready=1`, `gameplay_systems_navigation_crowd_applied_neighbors=2`, `gameplay_systems_navigation_crowd_readiness_status=ready`, `gameplay_systems_navigation_crowd_readiness_diagnostics=0`, `gameplay_systems_navigation_crowd_readiness_source_order_ready=1`, `gameplay_systems_navigation_crowd_readiness_applied_neighbors=2`, `gameplay_systems_navigation_crowd_readiness_dynamic_obstacles=2`, `gameplay_systems_local_avoidance_applied_neighbors`, `gameplay_systems_physics_policy_dynamic_pushes=1`, `gameplay_systems_kinematic_motion_status=constrained`, `gameplay_systems_kinematic_motion_rows=2`, `gameplay_systems_vehicle_status=grounded`, `gameplay_systems_vehicle_diagnostic=none`, `gameplay_systems_vehicle_wheel_rows=4`, `gameplay_systems_vehicle_grounded_wheels=4`, `gameplay_systems_vehicle_wheel_probe_hits=4`, `gameplay_systems_blackboard_status=ready`, `gameplay_systems_perception_readiness_status=ready`, `gameplay_systems_perception_readiness_diagnostic=none`, `gameplay_systems_perception_readiness_diagnostics=0`, `gameplay_systems_perception_stable_primary_target_ready=1`, `gameplay_systems_perception_blackboard_projection_ready=1`, `gameplay_systems_behavior_authoring_readiness_status=ready`, `gameplay_systems_behavior_authoring_readiness_diagnostic=none`, `gameplay_systems_behavior_authoring_readiness_diagnostics=0`, `gameplay_systems_behavior_authoring_deterministic_trace_ready=1`, `gameplay_systems_behavior_authoring_action_bindings=1`, `gameplay_systems_behavior_authoring_blackboard_conditions=2`, `gameplay_systems_scene_binding_ready=1`, `gameplay_systems_scene_binding_rows=3`, `gameplay_systems_scene_interaction_rows=3`, `gameplay_systems_scene_interaction_final_session_state=won`, `gameplay_systems_construction_placement_validation_rows=3`, `gameplay_systems_construction_placement_intent_rows=1`, `gameplay_systems_construction_placement_intent_accepted_rows=1`, `gameplay_systems_construction_placement_intent_occupied_cells=2`, `gameplay_systems_behavior_status=success`, and `gameplay_systems_audio_status=ready` fields without claiming scene/physics perception integration, navmesh asset import, persistent/full crowd simulation beyond value-only batch planning, animation-aware steering, visual scripting, arbitrary AI code execution, ML inference, networked AI replication, middleware, codec streaming, native audio devices, implicit scene mutation, dynamic-vs-dynamic TOI, rotational CCD, rotational rigid-body constraints, 2D CCD, persistent joint assets, broad vehicle dynamics/persistent vehicle simulation APIs, ragdolls, Metal, editor productization, native/RHI handles, or broad generated 3D production readiness.
- Engine Gameplay Interaction Framework v1 promotes reusable `MK_runtime` gameplay-state rows through `RuntimeGameplayInteractionEvent`, `RuntimeGameplayInteractionState`, `RuntimeGameplayInteractionPlan`, and `plan_runtime_gameplay_interactions` for trigger, damage, heal, pickup, objective, feedback, win, loss, and restart flows. Selected 2D/3D package smokes now expose `gameplay_systems_interaction_ready=1`, `gameplay_systems_interaction_rows=10`, `gameplay_systems_interaction_feedback_rows=10`, and `gameplay_systems_interaction_final_session_state=running` so generated-game quality rubrics can bind feedback and fail/restart evidence to deterministic package counters without claiming feedback execution, scene mutation, a gameplay scheduler, middleware, native/RHI handles, or broad production gameplay readiness.
- `sample_generated_desktop_runtime_3d_package` and generated `DesktopRuntime3DPackage` targets can require `--require-package-streaming-safe-point` in source-tree and installed smokes. The selected proof reports `package_streaming_status=committed`, `package_streaming_ready=1`, positive resident bytes, committed record counts, stale-handle counts, and diagnostics over the manifest `packageStreamingResidencyTargets` row, while the runtime safe-point execution path now rejects missing required preload assets and disallowed resident resource kinds before staging or committing, records the selected resident package-count hint counter, exposes a resident mount-set/catalog-cache commit overload for host-owned streaming controllers, pairs that with copy-then-commit resident unmount/cache refresh for explicit mount ids, adds slot-preserving resident package replacement for later hot reload/recook controllers, exposes `execute_selected_runtime_package_streaming_resident_replace_safe_point` so selected streaming controllers can replace one existing resident mount through the same copy-then-commit cache-refresh path, and exposes `execute_selected_runtime_package_streaming_resident_unmount_safe_point` so selected streaming controllers can remove one existing resident mount after projected remaining preload/kind/count checks. This does not claim broad async/background package streaming, arbitrary eviction, texture streaming, allocator/GPU budget enforcement, renderer/RHI teardown execution, Metal, or broad renderer quality.
- Generated 3D Renderer Quality Package Smoke v1 extends the committed/generated `DesktopRuntime3DPackage` installed package smoke with `--require-renderer-quality-gates` for the already selected scene GPU + postprocess path. It reports `renderer_quality_status=ready`, `renderer_quality_ready=1`, `renderer_quality_diagnostics=0`, `renderer_quality_expected_framegraph_passes=2`, `renderer_quality_expected_framegraph_render_passes=4`, `renderer_quality_framegraph_passes_ok=1`, `renderer_quality_framegraph_render_passes_ok=1`, `renderer_quality_framegraph_execution_budget_ok=1`, `renderer_quality_scene_gpu_ready=1`, and `renderer_quality_postprocess_ready=1`; selected depth-input and shadow extensions add their own gates without claiming GPU timestamps, backend-native stats, native/RHI handles, Metal readiness, production shadow quality, or broad renderer quality.
- Generated 3D Playable Package Smoke v1 extends the same committed/generated `DesktopRuntime3DPackage` installed package lane with `--require-playable-3d-slice`. It reports `playable_3d_status=ready`, `playable_3d_ready=1`, `playable_3d_diagnostics=0`, frame/game-frame readiness, scene mesh plan readiness, camera/controller readiness, transform/morph/quaternion readiness, selected package-streaming readiness, scene GPU/postprocess/renderer-quality readiness, and selected compute readiness without claiming runtime source import, broad dependency cooking, broad/background package streaming, Metal parity, native/RHI handles, general renderer quality, or broad generated 3D production readiness.
- Generated 3D Visible Production-Style Package Proof v1 extends the same committed/generated `DesktopRuntime3DPackage` installed package lane with `--require-visible-3d-production-proof` on D3D12 or `--require-vulkan-visible-3d-production-proof` on the Vulkan toolchain-gated recipe. It reports `visible_3d_status=ready`, `visible_3d_ready=1`, `visible_3d_diagnostics=0`, `visible_3d_presented_frames=2`, `visible_3d_d3d12_selected=1` or `visible_3d_vulkan_selected=1`, `visible_3d_null_fallback_used=0`, scene GPU/postprocess/renderer-quality/playable/native-UI readiness, and gameplay-system readiness without treating either backend proof as cross-backend evidence or claiming Metal parity, native/RHI handles, broad renderer quality, broad runtime UI readiness, or broad generated 3D production readiness.
- Physics Scene Package Collision Authoring v1 extends the committed/generated `DesktopRuntime3DPackage` installed package lane with `--require-scene-collision-package`. It ships `GameEngine.PhysicsCollisionScene3D.v1` as `AssetKind::physics_collision_scene`, preserves `backend.native=unsupported`, decodes `RuntimePhysicsCollisionScene3DPayload` rows with material and `body.N.compound` metadata, converts them through `build_physics_world_3d_from_runtime_collision_scene`, and reports `collision_package_status=ready`, `collision_package_bodies=3`, `collision_package_triggers=1`, positive contacts/trigger overlaps, `collision_package_world_ready=1`, `collision_query_batch_ready=1`, deterministic collision query batch hit/no-hit/invalid-request counters, query-budget rejection counters, and `gameplay_systems_collision_package_ready=1` without claiming dynamic-vs-dynamic TOI, rotational CCD, rotational rigid-body constraints, 2D CCD, persistent joint assets, broad vehicle dynamics/persistent vehicle simulation APIs, ragdolls, Jolt/native middleware in the default/generated-game Physics 1.0 ready surface, or broad physics beyond the first-party Physics 1.0 ready surface.
- Physics Collision Query v1 adds `PhysicsCollisionQueryBatchStatus`, `PhysicsCollisionQueryBatchDiagnostic`, `PhysicsCollisionQueryRowStatus`, and `PhysicsCollisionQueryRowDiagnostic` source-indexed rows through `PhysicsWorld2D::raycast_batch`, `PhysicsWorld3D::raycast_batch`, `PhysicsWorld2D::shape_sweep_batch`, and `PhysicsWorld3D::shape_sweep_batch` over existing public raycast and bounds-sweep primitives. The package-visible slice reports `collision_query_batch_ready=1` with deterministic rows, hit/no-hit/invalid-request counters, and query-budget rejection counters in the generated 3D scene collision package smoke. The first phase covers deterministic ordering, layer/mask filtering, trigger inclusion/exclusion for shape sweeps, invalid-query diagnostics, default-unbounded query counts, and explicit query-budget rejection without exposing native physics handles or broadening the first-party Physics 1.0 ready surface.
- Generated 3D Postprocess Depth Package Smoke v1 extends the committed/generated `DesktopRuntime3DPackage` installed package lane with `--require-postprocess-depth-input`. It reports `postprocess_depth_input_ready=1`, `renderer_quality_postprocess_depth_input_ready=1`, and `framegraph_passes=2` through host-owned D3D12/Vulkan scene renderer descriptor depth-input wiring and package postprocess shader `scene_depth_texture` / `scene_depth_sampler` bindings without claiming directional shadows, shadow filtering, GPU timestamps, backend-native stats, native/RHI handles, Metal readiness, broad production frame graph scheduling beyond selected executor evidence, broad renderer quality, or broad generated 3D production readiness.
- Generated 3D Directional Shadow Package Smoke v1 extends the committed/generated `DesktopRuntime3DPackage` installed package lane with a separate selected `--require-directional-shadow --require-directional-shadow-filtering` smoke. It reports `directional_shadow_status=ready`, `directional_shadow_ready=1`, `directional_shadow_filter_mode=fixed_pcf_3x3`, `directional_shadow_filter_taps=9`, `directional_shadow_filter_radius_texels=1`, `framegraph_passes=3`, `renderer_quality_directional_shadow_ready=1`, and `renderer_quality_directional_shadow_filter_ready=1` through host-owned D3D12/Vulkan scene renderer descriptor shadow wiring and generated shadow receiver/shadow map shader artifacts. It intentionally does not combine with generated morph/compute/playable smokes and does not claim hardware comparison samplers, production shadow quality, Metal shadow presentation, native/RHI handles, broad renderer quality, or broad generated 3D production readiness.
- Generated 3D Shadow Morph Composition Package Smoke v1 extends the same committed/generated `DesktopRuntime3DPackage` installed package lane with a separate selected `--require-shadow-morph-composition` D3D12 smoke. It reports positive `scene_gpu_morph_mesh_bindings`, `scene_gpu_morph_mesh_resolved`, `renderer_gpu_morph_draws`, and `renderer_morph_descriptor_binds` together with `directional_shadow_ready=1`, fixed `directional_shadow_filter_mode=fixed_pcf_3x3`, `framegraph_passes=3`, `framegraph_passes_executed=6`, `framegraph_render_passes_recorded=6`, and `framegraph_barrier_steps_executed=15` through shifted shadow receiver artifacts and host-owned morph descriptor binding. It remains a narrow graphics morph scene-pass plus directional shadow receiver proof and does not claim morph-deformed shadow-caster silhouettes, compute morph + shadow composition, graphics morph+skin+shadow composition, hardware comparison samplers, production shadow quality, Metal shadow presentation, native/RHI handles, broad renderer quality, or broad generated 3D production readiness.
- Generated 3D Native UI Overlay Package Smoke v1 extends the same committed/generated `DesktopRuntime3DPackage` installed package lane with selected D3D12 `--require-native-ui-overlay` evidence. It reports `hud_boxes=2`, `ui_overlay_requested=1`, `ui_overlay_status=ready`, `ui_overlay_ready=1`, `ui_overlay_sprites_submitted=2`, and `ui_overlay_draws=2` through a first-party `MK_ui` HUD box submitted via `MK_ui_renderer` into the host-owned native UI overlay path. It remains a narrow HUD box overlay proof; textured image-sprite atlas behavior is covered by the separate selected atlas smoke, and production text shaping, font rasterization, glyph atlas generation, runtime source image decoding, Metal overlay readiness, native/RHI handles, broad renderer quality, and broad generated 3D production readiness remain unsupported.
- Generated 3D Native UI Textured Sprite Atlas Package Smoke v1 extends the committed/generated `DesktopRuntime3DPackage` installed package lane with selected D3D12 `--require-native-ui-textured-sprite-atlas` evidence. It reports `hud_images=2`, `ui_atlas_metadata_status=ready`, `ui_atlas_metadata_pages=1`, `ui_atlas_metadata_bindings=1`, `ui_texture_overlay_atlas_ready=1`, `ui_texture_overlay_sprites_submitted=2`, `ui_texture_overlay_texture_binds=2`, and `ui_texture_overlay_draws=2` from cooked `GameEngine.UiAtlas.v1` metadata referencing the existing cooked base-color texture page, while production text/font/glyph UI, runtime source image decoding, source image atlas packing, Metal overlay parity, native/RHI handles, broad runtime UI readiness, and broad generated 3D readiness remain unsupported.
- Generated 3D Native UI Text Glyph Atlas Package Smoke v1 extends the committed/generated `DesktopRuntime3DPackage` installed package lane with selected D3D12 `--require-native-ui-text-glyph-atlas` evidence. It reports `hud_text_glyphs=2`, `text_glyphs_available=2`, `text_glyphs_resolved=2`, `text_glyphs_missing=0`, `text_glyph_sprites_submitted=2`, `ui_atlas_metadata_status=ready`, `ui_atlas_metadata_pages=1`, `ui_atlas_metadata_bindings=0`, `ui_atlas_metadata_glyphs=1`, `ui_texture_overlay_atlas_ready=1`, `ui_texture_overlay_sprites_submitted=2`, `ui_texture_overlay_texture_binds=2`, and `ui_texture_overlay_draws=2` from one fixed cooked `GameEngine.UiAtlas.v1` glyph row through `UiRendererGlyphAtlasPalette` and the host-owned native UI texture overlay path. It remains cooked metadata consumption only; production text shaping, real font loading/rasterization, glyph atlas generation from source fonts, runtime source image decoding, renderer texture upload as a gameplay API, Metal overlay readiness, native handles, broad runtime UI readiness, and broad generated 3D readiness remain unsupported.
- `sample_desktop_runtime_game` now backs the host-gated `3d-playable-desktop-package`, `native-gpu-runtime-ui-overlay`, and `native-ui-textured-sprite-atlas` recipes: cooked config, `.geindex`, texture, mesh, material, `GameEngine.Scene.v1`, static mesh scene submission, material instance intent, primary camera/controller smoke fields, HUD diagnostics counters, selected D3D12 package validation, strict Vulkan selected package validation, depth-aware postprocess, fixed 3x3 PCF directional shadow smoke, package-visible Lighting Shadow Policy v1 counters, renderer-owned native GPU overlay output for first-party `MK_ui_renderer` box/image-placeholder sprites, and a cooked texture/atlas-backed UI image sprite proof. This is not a broad generated 3D production recipe and does not make runtime source asset parsing, source image decoding, production atlas packing, material/shader graphs, skeletal animation, GPU skinning, package streaming, production text/font/accessibility, Metal readiness, public native/RHI handle access, editor productization, or general production renderer quality complete.
- Desktop runtime source-tree staging is target-isolated: `MK_add_desktop_runtime_game` places each registered game's executable, package payload copies, removed third-party runtime DLL, and source-tree shader artifacts under a target-specific output directory so generated samples with common `runtime/assets/generated/*` filenames cannot overwrite another target's smoke inputs.
- Frame Graph Package Executor Evidence v1 makes the selected desktop package proofs validate executor-owned counts in stdout: generated and sample postprocess-depth paths report `framegraph_passes_executed=4`, `framegraph_render_passes_recorded=4`, and `framegraph_barrier_steps_executed=9`, while directional-shadow paths report `framegraph_passes_executed=6`, `framegraph_render_passes_recorded=6`, `framegraph_barrier_steps_executed=15`, `renderer_quality_expected_framegraph_render_passes=6`, and `renderer_quality_expected_framegraph_barrier_steps=15`. This is evidence for the completed executor-routed postprocess and shadow package paths; viewport color-state execution is now executor-owned inside `RhiViewportSurface` but is not package-visible budget evidence. These slices are not production graph ownership, multi-queue scheduling, Metal parity, native handles, or broad renderer quality.
- D3D12 Postprocess Package Evidence v1 makes the selected `installed-d3d12-scene-gpu-smoke` recipe require `--require-d3d12-postprocess-evidence` with `postprocess_d3d12_execution_status=ready`, `postprocess_d3d12_execution_ready=1`, `postprocess_d3d12_execution_selected=1`, `postprocess_d3d12_execution_shader_evidence_ready=1`, and `postprocess_d3d12_execution_passes_ok=1`, and adds `--require-gpu-skinning` to the same D3D12 recipe for README alignment, without treating Vulkan `--require-vulkan-postprocess-evidence` proof as cross-backend evidence. This is selected per-backend package evidence only; Metal parity, new postprocess shader families, and public native/RHI handles remain unsupported.
- Vulkan GPU Skinning Package Evidence v1 makes the selected `sample_desktop_runtime_game` D3D12 package smoke require `--require-gpu-skinning` and the separate `installed-vulkan-scene-gpu-smoke` recipe require `--require-vulkan-gpu-skinning-evidence` with `vulkan_gpu_skinning_evidence_ready=1`, `vulkan_gpu_skinning_evidence_selected=1`, positive `scene_gpu_skinned_mesh_*` counters, positive `renderer_gpu_skinning_draws`, and positive `renderer_skinned_palette_descriptor_binds`, without treating D3D12 proof as cross-backend evidence. This is selected per-backend package evidence only; broad skeletal animation production, skin+morph composition, Metal parity, and public native/RHI handles remain unsupported.
- Frame Graph Multi-Queue Package Evidence v1 makes the selected `sample_desktop_runtime_game` D3D12 package smoke require `--require-framegraph-multiqueue-evidence` with `d3d12_framegraph_multiqueue_evidence_ready=1` and `d3d12_framegraph_multiqueue_evidence_selected=1`, and the separate `installed-vulkan-scene-gpu-smoke` recipe require `--require-vulkan-framegraph-multiqueue-evidence` with `vulkan_framegraph_multiqueue_evidence_ready=1` and `vulkan_framegraph_multiqueue_evidence_selected=1`, emit `framegraph_multiqueue_status=ready`, `framegraph_multiqueue_command_lists_submitted=4`, `framegraph_multiqueue_queue_waits_recorded=3`, `framegraph_multiqueue_barriers_recorded=4`, `framegraph_multiqueue_aliasing_barriers_recorded=1`, `framegraph_multiqueue_submitted_pass_fences=4`, positive queue submit/wait counters, and `framegraph_multiqueue_graphics_waited_for_copy=1`, and validate those fields in installed package smoke. Frame Graph Multi-Queue Render Pass Execution v1 extends the backend-neutral executor with optional pass target access/state rows and render-pass envelopes, records target-state transitions after waits/barriers and before graphics callbacks, wraps graphics callbacks in begin/end render-pass calls, and rejects swapchain/non-graphics render-pass rows before command recording. The package proof now runs transient alias-group lease bindings plus automatic aliasing barriers with an alias-induced cross-queue wait before later resource use. This is narrow evidence for the existing backend-neutral multi-queue executor, not production graph adoption, async overlap/performance proof, native queue/fence/semaphore exposure, Metal readiness, public RHI handles, or broad renderer quality.
- Generated desktop runtime material/shader authoring now has both source-tree and installed package coverage: `sample_generated_desktop_runtime_material_shader_package_shader_artifacts_smoke` proves DXC-built D3D12 scene/postprocess artifacts in the `desktop-game-runtime` preset when DXC is available, and `tools/package-desktop-runtime.ps1 -GameTarget sample_generated_desktop_runtime_material_shader_package` validates the selected installed package with manifest-derived config/cooked scene files, target-specific D3D12 scene/postprocess DXIL artifacts, host-owned scene GPU binding, `postprocess_status=ready`, and `framegraph_passes=2` on a ready Windows/DXC host.
- AI integration: Codex/Claude instructions, behaviorally equivalent project skills, project subagents including read-only `explorer`/review/architecture/audit roles, `engine/agent/manifest.json` with runtime backend readiness, importer capabilities, packaging targets, validation recipes, required AI surfaces, and `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/agent-context.ps1` output for public headers, module ownership, sample games, assets, platform targets, and validation recipes
- Infrastructure: GitHub Actions validation, Release CPack ZIP packaging with installed SDK consumer validation, optional desktop runtime Release CPack ZIP packaging with installed manifest-linked registered-game metadata, selected package files, selected game smoke validation, repo-local vcpkg downloads/binary-cache environment setup for optional vcpkg lanes, non-interactive closed-stdin tool invocation for validation wrappers, Clang/GCC sanitizer CMake preset, and LLVM `clang-format` command scripts

## Current Planning Source

The plan registry is `docs/superpowers/plans/README.md`. Read it before editing or extending any implementation plan.

The historical strategic roadmap ledger is `Engine Excellence Roadmap`; new production work is tracked through the master plan and dated child plans instead of appending to that historical file.

The current production-completion sequencing document is `docs/superpowers/master-plans/2026-05-03-production-completion-master-plan-v1.md`. **Authoritative narrow-slice pointers** (`aiOperableProductionLoop.currentActivePlan`, `recommendedNextPlan`) live in `engine/agent/manifest.json` and change as each dated child plan completes; do not rely on this paragraph for their exact paths. Summaries of completed slices and validation evidence belong in `docs/superpowers/plans/README.md`, the historical evidence archive, or the active dated plan while it is still live (`pwsh -NoProfile -ExecutionPolicy Bypass -File tools/validate.ps1`).

Editor Prefab Instance Source-Link Review v1 adds durable review-only prefab source-link metadata for instantiated scene nodes through `ScenePrefabSourceLink`, `PrefabInstantiateDesc`, `SceneNode::prefab_source`, `ScenePrefabInstanceSourceLinkModel`, and retained `scene_prefab_source_links` rows. This advances `editor-productization` diagnostics for future prefab merge work, but nested prefab propagation, automatic merge/rebase/resolution UX, fuzzy matching, runtime prefab instance semantics, package streaming, renderer/RHI work, and native handles remain future work.

The production-essential feature gap analysis is `docs/specs/2026-04-27-engine-essential-gap-analysis.md`. Treat it as historical MVP-era gap analysis; current work selection is manifest-led through `engine/agent/manifest.json`, the production-completion master plan, and dated capability/milestone plans.

The previous core-first MVP plan remains as history and implementation evidence. Completed dated plans are historical evidence and may be archived or deleted after current references move to durable docs. New production work should prefer a dated capability/gap-cluster/milestone plan, with phases for behavior/API/validation boundary decisions; create child plans only for distinct architecture/validation/review boundaries or phases too large for one safe context. Link the active plan from the plan registry and summarize it here after completion.

## Execution Waves

### Wave 0: Planning and Governance Synchronization

- keep `docs/roadmap.md`, `docs/workflows.md`, `engine/agent/manifest.json`, Codex skills, Claude rules/skills, and project subagent guidance aligned when engine capability changes
- keep C++23 policy centralized through `MK_CXX_STANDARD`, `MK_MSVC_CXX23_STANDARD_OPTION`, CMake module-scanning policy, and validation checks
- keep vcpkg manifest dependency policy and JSON contract checks as already-implemented governance, then expand them only when new dependency or manifest capability is added

### Wave 1: Engineering Quality Gates

- expand CI artifact publishing and cache policy
- keep the clang-tidy profile and strict compile database enforcement maintained; the default Windows Visual Studio `dev` preset now uses CMake File API codemodel data to synthesize `compile_commands.json` when the generator does not emit one, while missing clang-tidy/CMake tools remain local setup blockers
- add coverage reporting where the compiler/platform path can produce deterministic results
- keep Windows C++23 Release evaluation, Linux configure/build/CTest, and Linux ASan/UBSan coverage visible in workflow docs

### Wave 2: Direct3D 12 Visible Backend and Editor Viewport

- promote D3D12 from scaffold/initial `IRhiDevice` bridge to a visible clear/draw/readback/present backend
- continue proof tests beyond the implemented non-black clear, first-triangle texture readback, texture state validation, stale cross-queue texture-state rejection, swapchain render/present/resize lifecycle, explicit frame acquire/release, single-pending-frame throttling, descriptor/root-signature/PSO validation paths, and backend-neutral fence lifecycle stats
- use the implemented D3D12 shared-texture editor display path when SDL is using D3D12, and keep the CPU-readback display path as the deterministic fallback

### Wave 3: Vulkan Visible Backend

- maintain the implemented command pool, command buffer, first-party runtime buffer/memory ownership plus upload/readback-domain CPU mapping, first-party runtime texture/image/image-view ownership, backend-neutral `IRhiDevice` resource/shader/descriptor/pipeline/swapchain creation, upload-buffer write, and descriptor-update bridge, standalone texture transition/copy/render-target-clear/render-target-draw command bridge, swapchain clear render passes, graphics command-list close/submit, queued present, copy-only submit, immediate fence stats/waits, swapchain/image-view/frame-sync ownership, dynamic rendering clear/readback smoke coverage, dynamic rendering draw recording with optional vertex/index buffer binding and indexed draws, synchronization2 including shader-read texture barrier hazards, command-list-local texture state commits with stale-submit rejection, shader module validation, descriptor set layout/pool/set update/binding, pipeline layout/graphics pipeline ownership, image acquire, queue present, readback buffer/copy/mapping ownership, and `IRhiDevice` mapping readiness gates for visible clear/readback and visible draw/readback proof
- maintain the implemented real surface capability, format, and present-mode queries before native swapchain creation, and keep Win32/Vulkan game-window presentation as a host/runtime/toolchain-gated path that uses precompiled SPIR-V artifacts and deterministic `NullRenderer` fallback diagnostics
- keep the environment-gated Vulkan visible draw/readback proof running through `IRhiDevice` without public Vulkan headers or handles when local SPIR-V artifacts and a Vulkan runtime are available
- keep Vulkan 1.3, `VK_KHR_swapchain`, dynamic rendering, graphics/present queues, and SPIR-V artifact validation as availability gates
- keep DXC Vulkan SPIR-V compile commands pinned to `-spirv` plus `-fspv-target-env=vulkan1.3`
- keep shader toolchain readiness visible through `mirakana::evaluate_shader_toolchain_readiness`, `ShaderToolDiscoveryState::readiness`, and `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-shader-toolchain.ps1`, including the distinction between DXIL-capable DXC and DXC builds compiled with SPIR-V CodeGen; local strict lanes can use `MK_SHADER_TOOLCHAIN_ROOT` or SDK/vcpkg tool roots without committing compiler binaries

### Wave 4: Metal Native Backend

- continue from the implemented non-empty `metallib` validation, runtime/default-device/command-queue readiness plan, texture synchronization planning, platform availability diagnostics, Apple-only Objective-C++ CMake source gate, and native Metal device/command-queue/command-buffer/render-encoder/texture/drawable/present/readback ownership into shader library creation and visible backend integration
- keep Objective-C/Metal handles private behind `MK_rhi_metal`
- keep the macOS CI lane active for Apple-only Metal Objective-C++ source coverage

### Wave 5: Production Asset Import, Cook, and Material Binding

- use the completed importer dependency audit, optional `asset-importers` vcpkg feature (`libspng`, `fastgltf`, `miniaudio`), and `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/build-asset-importers.ps1` before expanding production PNG, glTF, and audio source importer behavior; current glTF mesh import supports triangle POSITION-only and interleaved POSITION/NORMAL/TEXCOORD_0/TANGENT cooked mesh payloads (see `cooked-mesh-tangent-frame-schema-v1`) but rejects primitives with `JOINTS_0`/`WEIGHTS_0` for static mesh cook; tooling can import skins/animations into `MK_animation` types via `import_gltf_skin_skeleton_and_skin_payload` / `import_gltf_animation_joint_tracks_for_skin` (see `gltf-animation-skin-import-v1`); generic node transform animation import via `import_gltf_node_transform_animation_tracks` maps LINEAR glTF `translation`, z-axis-only `rotation`, and positive `scale` channels into `MK_animation` keyframe rows (see `gltf-node-transform-animation-import-v1`), `import_gltf_node_transform_animation_float_clip` bridges those rows into `GameEngine.AnimationFloatClipSource.v1` scalar tracks for existing cook/package/runtime payloads, `import_gltf_node_transform_animation_binding_source` derives matching `GameEngine.AnimationTransformBindingSource.v1` rows from glTF node names or deterministic fallbacks, and `apply_float_animation_samples_to_transform3d` applies sampled scalar rows to caller-owned `Transform3D` values through explicit bindings; generated `DesktopRuntime3DPackage` scaffolds now apply a cooked scalar clip to `RuntimeSceneRenderInstance` data through `sample_and_apply_runtime_scene_render_animation_float_clip`; tool-side morph targets plus LINEAR mesh weights animation import via `import_gltf_morph_mesh_cpu_primitive` / `import_gltf_animation_morph_weights_for_mesh_primitive` (see `gltf-morph-mesh-and-weights-animation-import-v1`), with first-party `morph_mesh_cpu` source/cook/runtime package rows; `import_gltf_morph_weights_animation_float_clip` bridges the same LINEAR morph weights into `GameEngine.AnimationFloatClipSource.v1` rows for cooking and `MK_animation` byte-row sampling; glTF materials, broader authored scene-node binding asset workflows, GPU morph execution, and broader GPU skinning execution remain follow-up work
- use `import_gltf_node_transform_animation_quaternion_clip` when full 3D node TRS quaternion animation should be cooked into `GameEngine.AnimationQuaternionClipSource.v1` / `AssetKind::animation_quaternion_clip` payloads for runtime `AnimationPose3d` sampling through `runtime_animation_quaternion_clip_payload` and `make_animation_joint_tracks_3d_from_f32_bytes`; generated `DesktopRuntime3DPackage` games now include a deterministic cooked quaternion scene-transform smoke via `--require-quaternion-animation`, `sample_and_apply_runtime_scene_render_animation_pose_3d`, `quaternion_animation_scene_applied`, and `quaternion_animation_scene_rotation_z`, while animation graph authoring remains follow-up work
- continue from the implemented cooked package index, content hashes, deterministic recook decisions, explicit material binding metadata, GUI-independent editor/core import progress/dependency/thumbnail/material-preview/diagnostic models, and retained editor-core import/material-preview diagnostic models into texture sampling and production GPU material binding
- keep game projects consuming cooked assets through public engine APIs, not parsing external formats directly

### Wave 6: Runtime Audio, Physics, and Animation Production Pass

- use the implemented implemented Windows WASAPI audio output adapter as the first production desktop path while continuing from the device-independent gameplay cue/bus planner, spatial voice planning, decoded streaming queue contracts, streaming buffer scheduling, format conversion intent, sample-frame scheduling, underrun diagnostics, selectable nearest/linear resampling, interleaved float PCM generation, and first-party `AudioDeviceStreamPlan` bounded queue-fill/render pump baseline into streaming decode integration, mobile backend QA, HRTF/spatial quality, DSP graphs, device hotplug/selection, and mixer authoring UI
- treat the validated first-party Physics 1.0 ready surface as closed after package-authored collision rows, exact 3D AABB/sphere/vertical-capsule shape sweeps, deterministic contact manifolds, opt-in deterministic 3D CCD rows, conservative capsule character movement, generated-game character/dynamic policy rows, explicit distance-joint solving, replay signatures, count-based determinism gates, and explicit Jolt/native adapter exclusion; keep opt-in distance/fixed/linear-axis positional constraints, value-only kinematic motion planning, and simple vehicle policy rows as post-1.0 expansions, and future optional middleware remains dependency/legal/notice/benchmark/fail-closed gated outside the 1.0 ready surface
- use `review_physics_production_breadth` and `review_navigation_production_breadth` as the clean-break Phase 4 promotion gates before broad physics/navigation claims: evidence rows must carry official-source URLs, package counters, host validation or host gates, deterministic replay/path proof, budgets, and optional adapter legal records; optional native/Jolt/Recast/Detour-class rows must also carry `adapter_boundary_id`, `host_validation_recipe_id`, and `adapter_lifecycle_reviewed`, while runtime importer/bake execution, native handle exposure, arbitrary runtime baking, and broad middleware parity remain denied until implemented in later slices
- continue animation from the implemented skeleton/rest-pose/local-pose, skin payload/palette, CPU skinned-position plus optional skinned-normal/skinned-tangent/skinned-bitangent foundations, CPU POSITION/NORMAL/TANGENT morph targets, first-party `morph_mesh_cpu` package rows, morph weight time tracks plus tool-side glTF morph/weights LINEAR import, LINEAR glTF morph weights and node TRS -> scalar float clip bridges plus `make_float_animation_tracks_from_f32_bytes` byte-row sampling and `apply_float_animation_samples_to_transform3d` transform application, generated 3D transform/morph/quaternion scene-transform package smoke wiring, generated D3D12 POSITION/NORMAL/TANGENT morph draw/payload smoke, generated D3D12 POSITION/NORMAL/TANGENT compute morph package smoke, direct D3D12 NORMAL/TANGENT GPU morph readback proof, planar two-bone IK with explicit bend-side plus pole-vector branch selection, analytic two-bone IK 3D orientation basis output, 3D FABRIK chain IK position solving, 3D FABRIK solution -> unit-quaternion local pose write-back, unit-quaternion 3D local-pose/model-pose matrix construction, quaternion keyframe sampling, selected 3D joint-track local-pose application, and named runtime-scene pose application through `apply_runtime_scene_animation_pose_3d`, XY FABRIK chain IK with optional bend-side plus pole-vector branch selection, XY FABRIK pose application, local IK rotation limits, solver-integrated XY FABRIK segment local Z limits, signed-pi IK local angle normalization, 3D local rotation limits/twist controls, root-motion translation plus Z-rotation delta sampling and explicit-duration forward loop accumulation, blend tree samples, 1D blend graph samples, layered animation samples, curve binding samples, state machines, authored timeline event tracks, timeline events, retargeting policy, and editor timeline controls into tangent-space material/render integration, broader cooked animation gameplay workflows, authored binding assets, and authored animation asset workflows

### Wave 7: Hot Reload and Live Iteration

- use the implemented file watcher backend selection, deterministic polling fallback, native backend family reporting, and Windows `ReadDirectoryChangesW` / Linux `inotify` / macOS `FSEvents` native backends as the platform contract baseline
- keep the implemented debounced recook scheduler, failed-reload rollback state, editor/core/GUI diagnostic state, editor/runtime recook execution path, safe runtime replacement staging, shader hot reload plan, batch shader pipeline cache-index reconciliation, and renderer pipeline replacement boundary as the live-iteration baseline

### Wave 8: Editor and Runtime UI Architecture

- continue from the completed `first-party-editor-shell-v1` clean-break milestone; keep Dear ImGui only as historical completed-shell evidence after inactive implementation files and active dependency records were deleted
- follow `docs/ui.md`: own the `MK_ui`/`MK_editor_ui` contracts, but do not hand-roll mature low-level font, text shaping, IME, accessibility, image decoding, or platform integration systems without an accepted architecture decision
- continue the first-party retained-mode UI runtime, `MK_ui`, and its first `MK_ui_renderer` adapter from the implemented headless document/focus/style/text/image/layout/animation/binding/renderer-submission/text-image-accessibility-payload/monospace-text-layout/IRenderer-adapter contracts plus host-independent text shaping/font/image/accessibility/IME/text-input/text-edit command/clipboard text gates, Runtime UI Workbench Production v1 dense menu/inventory/equipment/shop/simulation-dashboard intent rows, the narrow Win32 text edit key-command and clipboard text bridges, `UiRendererGlyphAtlasPalette`, `UiRendererImagePalette`, `UiAtlasMetadataGlyph`, `RuntimeUiAtlasGlyph`, and the decoded-image plus rasterized-glyph atlas package bridges into concrete text shaping/font loading/rasterization adapters, accessibility, real image upload adapters, richer layout models, broader localization integration, accessibility metadata, and future editor surfaces
- keep runtime UI independent from `editor`, Dear ImGui, Qt, NoesisGUI, Slint, RmlUi, or platform middleware APIs; evaluate those and lower-level text/font/accessibility dependencies only as optional adapters after `license-audit` and dependency policy updates
- expand the implemented `MK_editor_ui` model bridge so high-complexity editor panels move into tested retained models and the current visible shell no longer depends on Dear ImGui, Qt, Slint, RmlUi, or other UI middleware
- add UI-heavy sample coverage after the core retained-mode contracts exist

### Wave 9: Mobile Packaging

- continue from the implemented SDK-independent mobile lifecycle/touch/orientation/safe-area/storage/permission contracts
- Android Release Device Matrix v1: maintain the implemented Android Gradle/NDK/GameActivity CMake/Prefab template, current `sample_headless` Windows-host Debug/Release APK evidence, native Vulkan surface creation bridge, generated resource packaging, private AAudio output adapter, Release signing verification with a user-local non-repository validation key, upload certificate export, AVD readiness diagnostics, deterministic `ANDROID_AVD_HOME` normalization, and API 36 emulator install/launch smoke coverage
- Apple Metal iOS Host Evidence v1: keep `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-apple-host-evidence.ps1` in validation so Apple/iOS/Metal readiness is reported as ready only on macOS/full-Xcode hosts with `xcrun`, iOS Simulator SDK/runtime access, and Xcode-resolved `metal`/`metallib`; Windows and Linux hosts must keep reporting `apple-host-evidence-check: host-gated`
- continue from the implemented iOS CMake/Xcode UIKit/Metal bundle template, Metal-backed root view, sandbox storage roots, game manifest/resource bundling, bundle identifier inputs, simulator/device SDK selection, simulator signing suppression, Xcode signing inputs, Simulator smoke script, and macOS GitHub Actions lane into host-validated simulator/device builds, backend-neutral Metal presentation, and device signing validation
- keep `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-mobile-packaging.ps1` as the diagnostic gate for missing SDK/toolchain blockers

### Wave 10: SDK, Samples, and Release Packaging

- maintain the installable SDK layout for headers, libraries, CMake config, tools, editor contracts, samples, schemas, docs, and AI manifests
- Production 1.0 Readiness Audit v1: keep `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-production-readiness-audit.ps1` in default validation so malformed `unsupportedProductionGaps` rows, duplicate gap ids, or stale statuses fail before any 1.0 closeout claim and `production-readiness-audit-check` remains the required gate status token
- keep `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/validate-installed-sdk.ps1` as the installed SDK payload gate: it now checks installed CMake package config/version files, parseable AI manifest and schemas, non-empty workflow tools, installed consumer source, sample manifests, docs README, third-party notices, and proprietary license payload before building the clean installed consumer
- keep `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/package.ps1` as the desktop SDK release artifact gate: it now validates the current CPack ZIP, SHA-256 sidecar, and required archived SDK metadata/tools/docs/notices/license entries after CPack generation without adding signing, upload, notarization, desktop-runtime selected-game package, Android, or Apple readiness claims
- expand package metadata, installed-example validation, CI package/log artifacts, and sample games as new runtime systems land

### Wave 11: AI-Driven Game Creation Excellence

- use the implemented engine/game schema fields for backend readiness, importer capabilities, packaging targets, and validation recipes so AI-generated games avoid planned or dependency-gated systems by default
- use `docs/specs/generated-game-validation-scenarios.md` and `docs/specs/game-prompt-pack.md` for common game types and prompt templates
- ensure Codex/GPT/Claude can inspect the manifest, scaffold a C++23 game, use only public `mirakana::` APIs, and run validation

## Current Next Steps

1. Keep Wave 0 governance synchronized whenever docs, manifests, skills, rules, subagents, or public APIs change. Update `docs/superpowers/plans/README.md` when a plan is created, completed, or intentionally made historical. `editor-productization` Windows scope closeout is recorded in `Editor Productization 1 0 Scope Closeout v1`; `asset-identity-v2` placement resolution is complete in `Asset Identity v2 Placement Resolution v1`, command-owned apply-surface evidence is complete in `Asset Identity v2 Command Apply Surface Evidence v1`, Scene v2 runtime package migration placement evidence is complete in `Asset Identity v2 Scene Runtime Reference Placement Evidence v1`, read-only Content Browser identity rows are complete in `Editor Content Browser Asset Identity v2 Rows v1`, source-registry-backed Content Browser population is complete in `Editor Content Browser Source Registry Population v1`, retained source registry loading rows are complete in `Editor Source Registry Visible Content Browser v1`, and runtime scene/game/template reference cleanup is complete in `Asset Identity v2 Reference Cleanup Milestone v1`. `runtime-resource-v2` resident package mount set is complete in `Runtime Resource v2 Resident Package Mount Set v1` through explicit `RuntimeResidentPackageMountSetV2` mount ids, deterministic mount diagnostics, mount-set generations, and catalog rebuild from mounted package order; resident catalog cache is complete in `Runtime Resource v2 Resident Catalog Cache v1` through `RuntimeResidentCatalogCacheV2` cache-hit reuse and budget-failure catalog preservation; streaming resident mount commit is complete in `Runtime Resource v2 Streaming Resident Mount Commit v1` through the resident-mount `execute_selected_runtime_package_streaming_safe_point` overload, projected resident budget validation, raw loaded-package catalog preflight, and catalog-cache refresh; resident unmount/cache refresh is complete in `Runtime Resource v2 Resident Unmount Cache Refresh v1` through `commit_runtime_resident_package_unmount_v2` copy-then-commit live-state preservation; resident package replacement commit is complete in `Runtime Resource v2 Resident Package Replacement Commit v1` through `commit_runtime_resident_package_replace_v2` slot-preserving copy-then-commit cache refresh; runtime package streaming resident replace is complete in `Runtime Package Streaming Resident Replace v1` through `execute_selected_runtime_package_streaming_resident_replace_safe_point` and `resident_replace` result rows over the same copy-then-commit replacement path. `runtime-resource-v2` is closed for the Engine 1.0 runtime-resource ready surface by `Runtime Resource v2 1 0 Scope Closeout v1`; `renderer-rhi-resource-foundation` is closed for the Windows-default renderer/RHI foundation surface by `Renderer RHI Resource Foundation 1 0 Scope Closeout v1`; `frame-graph-v1` is closed for the Windows-default ready surface by `Frame Graph v1 1 0 Scope Closeout v1`; `upload-staging-v1` is closed by `Upload Staging v1 Async Ready Resource Updates v1`; Scene/Prefab Schema v2, 2D playable vertical slice, 3D playable vertical slice, Editor Productization, Production UI Importer Platform Adapters, and Full Repository Quality Gate are closed for their Windows-default 1.0 surfaces; no active unsupported production gap remains.
   Latest `runtime-resource-v2` evidence includes hot-reload recook replacement through `commit_runtime_package_hot_reload_recook_replacement_v2` plus registered asset watch-tick orchestration through `execute_asset_runtime_package_hot_reload_registered_asset_watch_tick_safe_point`. The closeout keeps file watching, automatic target inference, background work, renderer/RHI ownership, upload/staging, allocator/GPU enforcement, inferred content roots, native handles, package scripts, and broad hot reload productization outside the runtime-resource ready claim.
2. Continue the desktop visible game runtime shell from the first `MK_runtime_host`/`MK_runtime_host_win32`/`MK_runtime_host_win32_presentation` slices into shippable native RHI-backed game-window presentation. The focused `desktop-game-runtime` validation lane, reusable `mirakana::Win32DesktopGameHost` contract, backend-neutral `RhiFrameRenderer` swapchain resize foundation, Win32 D3D12 surface probing, host-supplied D3D12 bytecode renderer creation, DXC-available build-output packaged DXIL sample path, Win32 Vulkan surface probing, host-supplied Vulkan SPIR-V renderer creation, real surface format/present-mode swapchain gating, backend-neutral presentation report rows, target-driven desktop-runtime package selection, manifest-linked registered package metadata, selected-target package file install/validation, reviewed PowerShell `runtimePackageFiles` append tooling, editor GUI package registration apply for reviewed draft rows, non-shell package config plus cooked scene package load/renderer-submit proof, backend-neutral runtime scene GPU binding palette bridge, scene request vertex input validation against cooked mesh layouts, D3D12 and Vulkan visible cooked-scene GPU palette indexed draw/readback proofs, target-specific `sample_desktop_runtime_game` scene/postprocess/shadow/UI-overlay shader artifacts, installed D3D12 and Vulkan scene GPU binding package proofs, Lit Material v0 position+normal+uv/base-color/direct-light sample proof, Frame Graph/Postprocess v0 package proof with `postprocess_status=ready`, depth-aware postprocess package proof with `postprocess_depth_input_ready=1`, fixed 3x3 PCF directional shadow smoke with `framegraph_passes=3`, native GPU runtime UI overlay package proof with `ui_overlay_status=ready`, cooked texture/atlas-backed UI image sprite package proof with `ui_texture_overlay_status=ready`, authored `GameEngine.Scene.v1` cook/package workflow, automatic cooked package assembly from successful import results, explicit `GameEngine.MaterialGraph.v1` -> runtime `GameEngine.Material.v1` package binding, GUI-independent editor scene/prefab package authoring models, play-in-editor session isolation, caller-supplied gameplay driver callbacks, visible Play-In-Editor Run/Viewport wiring, explicit editor game-module driver load for the reviewed in-process handoff, prefab variant/override authoring model, visible editor scene/prefab GUI wiring with package candidate rows and package registration draft/apply rows, visible editor prefab variant GUI wiring, generated config-only `DesktopRuntimePackage` scaffolding with manifest-derived package files, generated `DesktopRuntimeCookedScenePackage` scaffolding with manifest-derived config, `.geindex`, cooked texture/mesh/material/scene files, package load, and renderer-neutral scene submission, generated `DesktopRuntimeMaterialShaderPackage` scaffolding with source material/HLSL authoring inputs and selected-target shader artifact validation, installed shader-artifact smoke, real-window D3D12 smoke, real-window Vulkan smoke on a ready Windows host, and `NullRenderer` fallback diagnostics are implemented; the remaining general roadmap work is Metal surface presentation, full shader graph/live shader generation work, production source image decoding/atlas packing, and more general material/shader pipeline authoring without public native handles. For the Engine 1.0 Windows-default ready surface, editor-productization is closed through reviewed editor authoring/playtest/AI command/resource/input/prefab/material-preview evidence; Vulkan/Metal material-preview display parity beyond D3D12 host-owned execution evidence, active-session hot reload execution, broader in-process runtime embedding beyond reviewed handoffs, automatic nested prefab merge/rebase UX, resource management/capture execution beyond host-owned evidence rows, automatic capture/ETW/debug-layer execution, and vendor-stable third-party editor DLL ABI are explicit future or host-gated exclusions.
   Generated `DesktopRuntimeMaterialShaderPackage` scaffolds now also have a DXC-available source-tree shader-artifact CTest plus selected installed package validation for manifest-derived cooked scene files, target-specific D3D12 scene/postprocess DXIL, scene GPU bindings, color-postprocess status, and modern material package counters (`modern_material_variants=1`, `modern_material_ready=1`, `modern_material_diagnostics=0`, `modern_material_texture_dependencies=1`, `modern_material_shader_evidence_ready=1`); this remains a generated scaffold proof, not production material/shader graph or live shader generation.
   `Desktop Runtime Shippable RHI Window v1` completed the packaged D3D12/Vulkan real-window smoke. `2D Native RHI Sprite Package Proof v1` then completed the installed D3D12 `sample_2d_desktop_runtime_package` native 2D sprite/HUD proof with `--require-native-2d-sprites`, cooked sprite texture/material package upload, and ready native overlay counters. `Generated 2D Native RHI Sprite Package Scaffold v1` extends generated `DesktopRuntime2DPackage` games with the same host-gated native sprite smoke metadata and generated shader source. `2D Sprite Batch Planning Contract v1` adds a renderer-neutral planning contract, and `Sprite Batching Renderer v1` adds selected world/UI/effects budget profile counters for the committed 2D package sample. `3D Scene Mesh Package Telemetry v1` adds package-visible `scene_mesh_plan_*` telemetry for the committed 3D package smoke, `Generated 3D Mesh Telemetry Scaffold v1` carries the same telemetry counters into generated `DesktopRuntime3DPackage` scaffolds, Generated 3D Committed Package Sample v1 adds `games/sample_generated_desktop_runtime_3d_package` as a committed generated package proof with cooked-only files, manifest descriptors, source-tree camera/controller smoke, and selected D3D12/Vulkan compute morph package metadata including Vulkan skin+compute, and Generated 3D Package Streaming Safe Point Smoke v1 adds selected safe-point package streaming counters through `--require-package-streaming-safe-point`. Remaining desktop-runtime work still excludes broad production sprite batching readiness beyond selected plan/budget/native-execution evidence, broad generated 3D production readiness, public native/RHI handles, Metal, broad package cooking/background streaming, and broad renderer quality.
3. Treat the remaining Apple roadmap item as host-gated: macOS/Xcode validation for iOS simulator/device packaging, device signing, and backend-neutral Metal visible presentation. Do not mark it complete without an Apple host or CI result.
4. Frame Graph/Postprocess v0 is implemented and validated as the current two-pass renderer foundation; Postprocess Depth Input Readback Foundation v0 adds an opt-in renderer-owned `depth24_stencil8` sampled scene-depth input to `RhiPostprocessFrameRenderer` with stable bindings `2/3`, NullRHI descriptor/state coverage, and D3D12 plus env-gated Vulkan RHI readback proof. Package-Visible Postprocess Depth Effect v0 wires that input into `sample_desktop_runtime_game` D3D12/Vulkan selected package smokes with `postprocess_depth_input_ready=1`. Runtime UI adapter, monospace text layout, image sprite, font image, accessibility, IME, platform text-input, text-edit command, clipboard, text shaping/font request/image decode, PNG decode, atlas package bridge, glyph atlas package bridge, and native UI textured sprite atlas foundations are implemented through first-party `MK_ui`, `MK_ui_renderer`, `MK_renderer`, `MK_platform_win32`, and `MK_tools` contracts. These rows close the Engine 1.0 Windows-default production UI/importer/platform adapter surface without claiming production text shaping, production font loading/rasterization, bidirectional reordering, production line breaking, full text editing widgets, rich clipboard formats, OS accessibility bridge publication, arbitrary importer adapters, UI middleware, or native-handle exposure.

  Runtime UI Platform Text Input Proof Rows v1 keeps candidate/composition/committed-text/clipboard evidence behind selected platform adapter rows; broad native IME/text services, UI-side candidate display, and cross-platform platform adapters remain follow-up work.

  Runtime UI Decoded Image Atlas Package Bridge v1 and Runtime UI Glyph Atlas Package Bridge v1 remain recorded through `author_packed_ui_atlas_from_decoded_images`, `GameEngine.CookedTexture.v1`, `UiAtlasMetadataGlyph`, `RuntimeUiAtlasGlyph`, and `author_packed_ui_glyph_atlas_from_rasterized_glyphs`. Runtime Input Rebinding Capture Contract v1, Runtime Input Rebinding Focus Consumption v1, and Runtime Input Rebinding Presentation Rows v1 remain recorded through `capture_runtime_input_rebinding_action`, `RuntimeInputRebindingFocusCaptureRequest`, `capture_runtime_input_rebinding_action_with_focus`, `gameplay_input_consumed`, `RuntimeInputRebindingPresentationModel`, `make_runtime_input_rebinding_presentation`, symbolic glyph lookup keys, and host-owned platform input glyph generation follow-up rows.

D3D12 Native Depth Proof v0, Vulkan Native Depth Proof v0, Depth Sampling Contract v0, Postprocess Depth Input Readback Foundation v0, Package-Visible Postprocess Depth Effect v0, and Package-Visible Directional Shadow Filtering v0 are implemented on top of RHI Depth Attachment Contract v0. `MK_rhi`/`MK_renderer` provide backend-neutral optional depth attachments, depth-test/write/compare state, `NullRhiDevice` compatibility validation, `RhiFrameRenderer` optional host-owned depth texture forwarding plus frame-between host-owned depth texture replacement after resize/recreation, and `RhiPostprocessFrameRenderer` opt-in renderer-owned sampled scene-depth input. `MK_rhi_d3d12` creates private `Format::depth24_stencil8` DSV-backed textures, allows sampled depth textures through private typeless resources plus D24 DSV/R24 SRV views, binds RTV+DSV render passes, clears depth while validating clear values and tracked `depth_write` state, maps `DepthStencilStateDesc` into native PSO depth-stencil state, validates active color/depth formats at pipeline bind, proves depth testing through overlapping-triangle CPU readback, proves shader sampling of a previously written depth value through CPU readback, proves a depth-aware postprocess pass through CPU readback, proves a directional shadow receiver can be darkened from sampled shadow depth through CPU readback, and proves fixed 3x3 PCF produces a filtered shadow boundary through CPU readback. `MK_rhi_vulkan` now creates private depth-aspect image views for sampled descriptors, uses depth|stencil image-barrier aspect masks for D24S8 layout transitions, requires and enables Vulkan 1.3 `synchronization2` before requesting `vkCmdPipelineBarrier2`/`vkQueueSubmit2`, maps depth+sampled texture create plans to depth-stencil attachment plus sampled image usage, updates sampled-depth descriptors through private descriptor writes when a Vulkan runtime is available, maps `depth_write` into synchronization2 depth attachment barriers and `depth_write` to `shader_read` into shader-read barriers, binds dynamic-rendering depth attachments, maps `DepthStencilStateDesc` into native pipeline depth-stencil state, validates active color/depth formats at pipeline bind, includes an environment-gated depth-ordered draw/readback proof via `MK_VULKAN_TEST_DEPTH_VERTEX_SPV` / `MK_VULKAN_TEST_DEPTH_FRAGMENT_SPV`, includes an environment-gated visible sampled-depth readback proof via those depth-write artifacts plus `MK_VULKAN_TEST_DEPTH_SAMPLE_VERTEX_SPV` / `MK_VULKAN_TEST_DEPTH_SAMPLE_FRAGMENT_SPV`, includes an environment-gated postprocess-depth readback proof via those depth-write artifacts plus `MK_VULKAN_TEST_POSTPROCESS_DEPTH_VERTEX_SPV` / `MK_VULKAN_TEST_POSTPROCESS_DEPTH_FRAGMENT_SPV`, includes an environment-gated height-fog runtime readback proof via `MK_VULKAN_TEST_HEIGHT_FOG_DEPTH_VERTEX_SPV` / `MK_VULKAN_TEST_HEIGHT_FOG_DEPTH_FRAGMENT_SPV` plus `MK_VULKAN_TEST_HEIGHT_FOG_VERTEX_SPV` / `MK_VULKAN_TEST_HEIGHT_FOG_FRAGMENT_SPV` and `VK_LAYER_KHRONOS_validation` instance-layer readiness, and includes an environment-gated directional shadow receiver readback proof via those depth-write artifacts plus `MK_VULKAN_TEST_SHADOW_RECEIVER_VERTEX_SPV` / `MK_VULKAN_TEST_SHADOW_RECEIVER_FRAGMENT_SPV`, using the shared fixed 3x3 PCF receiver shader when artifacts are configured. `sample_desktop_runtime_game` selected D3D12/Vulkan package smokes now require and report `postprocess_depth_input_ready=1`, `directional_shadow_filter_mode=fixed_pcf_3x3`, `directional_shadow_filter_taps=9`, and `directional_shadow_filter_radius_texels=1` while keeping all native resources host-owned. Metal remains Apple-host-gated. This is not SSAO, depth of field, height-fog Vulkan package readiness, volumetric fog package/Vulkan/Metal execution, temporal history, Metal depth recording/sampling, stencil features, hardware comparison samplers, or production shadow maps beyond the directional cascade atlas smoke slice.

Shadow Map Foundation v0 and Stable Directional Light-Space Policy v0 are implemented as renderer-owned planning/pass-declaration layers. `MK_renderer` validates a directional shadow-casting light, non-zero extent, `Format::depth24_stencil8`, `TextureUsage::depth_stencil | TextureUsage::shader_resource`, caster/receiver counts, deterministic diagnostics, stable shadow-depth-before-receiver ordering, deterministic texel-snapped orthographic directional light-space matrices through `DirectionalShadowLightSpaceDesc` / `DirectionalShadowLightSpacePlan`, multi-cascade atlased `ShadowMapPlan` depth sizing, and a receiver-side sampled-depth plus sampler descriptor layout with deterministic depth-bias, lit/shadow intensity, filter mode, radius, and tap-count diagnostics. `MK_scene_renderer` adapts `SceneRenderPacket` by choosing the first directional shadow-casting light and counting visible meshes as v0 casters/receivers, then derives mesh-center bounds and selected light direction for `build_scene_directional_shadow_light_space_plan` while keeping `MK_scene` renderer-neutral. D3D12 and env-gated Vulkan RHI tests prove sampled shadow depth can darken a receiver pass through CPU readback; D3D12 also proves fixed 3x3 PCF creates a filtered boundary pixel. This is not hardware comparison samplers, Metal presentation, editor controls, or production shadow rendering beyond the directional CSM atlas smoke path.

Package-Visible Directional Shadow Filtering v0 is implemented for `sample_desktop_runtime_game` selected D3D12/Vulkan package validation. `RhiDirectionalShadowSmokeFrameRenderer` runs its host-owned three-pass shadow-depth (per-cascade viewport over an atlased depth target), fixed 3x3 PCF shadow-receiver scene, and postprocess sequence through the Frame Graph RHI texture executor for scheduled inter-pass transitions, writer-access-backed target-state preparation, executor-owned render pass envelopes around pass-body callbacks, and reusable depth final-state restoration, and the sample can require `directional_shadow_status=ready`, `directional_shadow_ready=1`, `directional_shadow_filter_mode=fixed_pcf_3x3`, `directional_shadow_filter_taps=9`, `directional_shadow_filter_radius_texels=1`, and `framegraph_passes=3` without exposing native handles. This remains a smoke path, not multi-queue scheduling, hardware comparison samplers, Metal shadow presentation, editor shadow controls, or production shadow authoring beyond the directional CSM atlas slice.

Lighting Shadow Policy v1 has package-visible value-only evidence through `sample_desktop_runtime_game --require-lighting-shadow-policy`, which adapts the loaded `SceneRenderPacket` into `plan_scene_lighting_shadow_policy` and validates `lighting_shadow_policy_status=ready`, `lighting_shadow_policy_ready=1`, `lighting_shadow_policy_diagnostics=0`, one directional shadowed light, a 1024x1024 atlas, one light row, and two ready frames. This is policy/counter evidence, not additional rendered-light execution, native/RHI handle exposure, Vulkan/Metal visual parity, or production shadow quality.

Renderer Package Quality Gates v1 is implemented for the same selected package lane. `evaluate_win32_desktop_presentation_quality_gate` summarizes existing `Win32DesktopPresentationReport` scene GPU, postprocess, depth-input, directional-shadow filtering, framegraph pass-count, render-pass-envelope count, and first-party renderer counter fields into `renderer_quality_status`, `renderer_quality_ready`, `renderer_quality_diagnostics`, `renderer_quality_expected_framegraph_passes`, `renderer_quality_expected_framegraph_render_passes`, `renderer_quality_framegraph_render_passes_ok`, and `renderer_quality_framegraph_execution_budget_ok`; `sample_desktop_runtime_game --require-renderer-quality-gates` makes those fields package-visible. This remains a deterministic smoke gate over existing counters, not GPU timestamps, backend-native stats, cross-backend performance parity, Metal readiness, or general production renderer quality.

Crash Telemetry Trace Ops v1 is implemented in `MK_core` as a dependency-free `DiagnosticsOpsPlan` handoff. `build_diagnostics_ops_plan` turns a `DiagnosticCapture` into ready summary and Chrome Trace Event JSON rows, a `crash_dump_review` row that is `host_gated` until Debugging Tools for Windows are available, and a `telemetry_upload` row that is `unsupported` without a caller-provided telemetry backend. This is not native dump writing, crash-report backend integration, telemetry SDK/upload execution, symbol publication, trace import tooling, flame graph UI, or OS/native handle exposure.

Editor Profiler Trace Export v1, Editor Profiler Trace File Save v1, Editor Profiler Native Trace Save Dialog v1, Editor Profiler Telemetry Handoff v1, Editor Profiler Trace Import Review v1, Editor Profiler Trace File Import Review v1, Editor Profiler Native Trace Open Dialog v1, and Editor Profiler Trace Import Reconstruction v1 are implemented for the developer editor. `EditorProfilerTraceExportModel`, `EditorProfilerTraceFileSaveRequest`, `EditorProfilerTraceSaveDialogModel`, `EditorProfilerTelemetryHandoffModel`, `EditorProfilerTraceImportReviewModel`, `EditorProfilerTraceFileImportRequest`, `EditorProfilerTraceFileImportResult`, `EditorProfilerTraceOpenDialogModel`, `make_editor_profiler_trace_export_model`, `save_editor_profiler_trace_json`, `make_editor_profiler_trace_save_dialog_request`, `make_editor_profiler_trace_save_dialog_model`, `make_editor_profiler_telemetry_handoff_model`, `make_editor_profiler_trace_import_review_model`, `import_editor_profiler_trace_json`, `make_editor_profiler_trace_open_dialog_request`, and `make_editor_profiler_trace_open_dialog_model` provide retained `profiler.trace_export`, `profiler.trace_file_save`, `profiler.trace_save_dialog`, `profiler.telemetry`, `profiler.trace_import`, `profiler.trace_file_import`, `profiler.trace_open_dialog`, and `profiler.trace_import.reconstructed_*` rows. `MK_core` also exposes `DiagnosticsTraceImportResult` and `import_diagnostics_trace_json` for first-party exported Trace Event JSON reconstruction. The visible Profiler panel exposes `Copy Trace JSON`, `Save Trace JSON`, `Browse Save Trace JSON`, `Profiler Telemetry Handoff`, `Review Trace JSON`, `Import Trace JSON`, and `Browse Trace JSON` for in-project Trace Event JSON save/review/reconstruction without adding arbitrary JSON conversion beyond the first-party exported subset, telemetry SDK/upload execution, crash-report backends, GPU/backend timestamps, allocator diagnostics, native handles, production flame graphs, or broader native file dialogs outside this Profiler path and the Prefab Variant `.prefabvariant` browse path.

   Editor Material Authoring Model v0 remains the `GameEngine.Material.v1` GUI-independent factor/texture authoring model in `MK_editor_core`. Material Graph Authoring v1 adds `GameEngine.MaterialGraph.v1` plus `MaterialGraphAuthoringDocument` text IO, validation, and lowering to `MaterialDefinition`; Material Graph Package Binding v1 adds the reviewed `MK_tools` dry-run/apply path that writes the lowered runtime `.material` plus `.geindex` material/texture dependency rows. Shader Hot Reload And Pipeline Cache v1 separately adds selected-request batch planning and first-party cache-index reconciliation through `build_shader_pipeline_cache_plan` / `reconcile_shader_pipeline_cache_index`. These paths still do not add shader graph codegen, live shader generation, shader compiler execution from graph binding or cache-index planning, native PSO/Vulkan/Metal cache blob APIs, or a first-party live GPU material-graph preview. Editor Scene/Prefab Package Authoring v0, Play-In-Editor Session Isolation v1, Play-In-Editor Gameplay Driver v1, Play-In-Editor Visible Viewport Wiring v1, Prefab Variant / Override Authoring v0, visible editor scene/prefab GUI wiring including the narrow `.scene` native open/save dialog path, visible prefab variant GUI wiring including the narrow `.prefabvariant` native open/save dialog path, editor package registration draft/apply rows, and editor playtest package review models remain developer tooling over first-party authoring data, not game-runtime dependencies.

   Editor Scene Prefab Instance Refresh Review v1 adds `ScenePrefabInstanceRefreshPlan`, `ScenePrefabInstanceRefreshRow`, `ScenePrefabInstanceRefreshResult`, `plan_scene_prefab_instance_refresh`, `apply_scene_prefab_instance_refresh`, `make_scene_prefab_instance_refresh_action`, and retained `scene_prefab_instance_refresh` rows for explicit refresh of a selected linked prefab root from its source path. Editor Prefab Instance Local Child Refresh Resolution v1 adds `ScenePrefabInstanceRefreshPolicy`, `keep_local_child` rows, and `Keep Local Children` visible editor control so local child subtrees can be preserved only through an explicit reviewed policy while the default policy still blocks unsupported local-child mappings. Editor Prefab Instance Stale Node Refresh Resolution v1 adds `keep_stale_source_node_as_local` rows, `keep_stale_source_node_count`, `kept_stale_source_node_count`, `unsupported_stale_source_subtree`, and the visible `Keep Stale Source Nodes` control so safe stale source-node subtrees can be kept as local author-owned nodes only through an explicit reviewed policy while the default policy still removes reviewed stale source nodes. Editor Nested Prefab Refresh Resolution v1 adds `keep_nested_prefab_instance` rows, `unsupported_nested_prefab_instance` blockers, `keep_nested_prefab_instance_count`, `kept_nested_prefab_instance_count`, and the visible `Keep Nested Prefab Instances` control so nested linked prefab subtrees can be preserved only through an explicit reviewed policy while nested source links stay intact. It preserves retained scene node state, adds new refreshed source nodes, removes or explicitly keeps reviewed stale nodes, and blocks ambiguous mappings, unsafe stale subtrees, and unsupported nested prefab instances; automatic nested prefab refresh, full nested prefab propagation, fuzzy matching, automatic merge/rebase/resolution UX, runtime prefab instance semantics, package scripts, validation recipes, renderer/RHI uploads, native handles, and package streaming remain outside the ready claim.

   Animation Skeleton Pose Foundation v0, Animation Skin Payload Foundation v0, Animation CPU Skinning Foundation v0, Animation CPU Skinned Normal Stream v1, Animation CPU Skinned Tangent Stream v1, Animation CPU Skinned Bitangent Handedness v1, root motion, morph, quaternion clip, 3D pose, IK, and compute morph slices are implemented only to the boundaries named by their focused plans. Physics Trigger/Sweep Foundation v0, Audio Spatial/Streaming Foundation v0, Audio Device Streaming Baseline v1, Audio Gameplay Mix Planner v1, Path Following / Steering Foundation v0, Navigation Agent Controller Foundation v0, Grid Dynamic Obstacle Replan v0, Navigation Agent Local Avoidance v0, Navigation Grid Path Smoothing v0, Navigation Production Path v1, Navigation Navmesh Production Path v1, Navigation Crowd Local Avoidance Readiness v1, Behavior Tree Foundation v0, Behavior Tree Blackboard Conditions v0, AI Behavior Authoring Foundation v1, and AI Perception Services v1 remain dependency-free first-party contracts, not middleware, navmesh asset import/baking, persistent/full crowd simulation, codec integration, HRTF, DSP graph execution, device hotplug/selection, mixer authoring UI, crowd, scene/physics perception integration, async task, persistent blackboard services, or editor graph claims.

   Runtime Scene Gameplay Instance v0, Authored Scene Cook / Package Workflow v0, Automatic Cooked Package Assembly v0, Manifest-Driven Desktop Runtime Package Authoring v0, Desktop Runtime Package Registration Apply Tool v0, Generated Desktop Runtime Package Scaffold v0, Generated Desktop Runtime Cooked Scene Package Scaffold v0, and Generated Desktop Runtime Material/Shader Package Scaffold v0 remain narrow package and generated-game evidence paths. Good follow-up candidates are richer postprocess execution beyond the current tone-mapping evidence gate, broader renderer/RHI skinning integration, cooked animation asset workflows, navmesh asset import and persistent/full crowd simulation, broader material/shader authoring models, crash/telemetry backend adapters, trace import workflows, Vulkan editor material-preview display promotion, imported glTF mesh editor UX, and Metal native RHI desktop game-window presentation behind Apple hosts.

AI Generated Game Playtest Loop v1 adds fail-closed `game.agent.json.aiWorkflow.generatedGamePlaytestLoop` rows so generated/sample 2D/3D package games select reviewed validation recipes, evidence roots, package-smoke/gameplay-counter evidence kinds, failure classifications, and mutation-ledger remediation policy without validation weakening, evidence deletion, host-gate bypass, arbitrary shell, raw manifest command evaluation, cooked-package mutation, engine-internal edits, native handles, or broad quality claims.

AI Validation Remediation Recipes v1 adds fail-closed `game.agent.json.aiWorkflow.validationRemediationRecipes` rows so generated/sample 2D/3D package games map common validation failures to reviewed mutation-ledger actions, required evidence, stop conditions, and rerun-selected-validation-recipe policy without validation weakening, evidence deletion, host-gate bypass, arbitrary shell, raw manifest command evaluation, cooked-package mutation, engine-internal edits, native handles, or broad quality claims.

AI Generated Game Quality Rubric v1 adds fail-closed `game.agent.json.aiWorkflow.generatedGameQualityRubric` rows so generated/sample 2D/3D package games connect objective, controls, feedback, fail/restart, deterministic package smoke, and budget evidence gates to same-manifest design, playtest, remediation, validation recipe, and report ids without subjective fun, commercial quality, platform parity, unbounded performance, native handles, validation weakening, or broad production readiness claims.

AI Codex/Claude/Cursor Agent Surface v1 adds fail-closed `engine/agent/manifest.json.aiSurfaces.crossToolAlignment` rows so Codex, Claude Code, and Cursor share official docs anchors, game-owned write scopes, reviewed command surfaces, validation guards, required read-only roles, forbidden broad grants, and unsupported claims without direct default-branch push, arbitrary shell, raw manifest command evaluation, native handle public API leakage, validation weakening, or broad production readiness claims.

5. Keep dependency-gated work explicit. Broader importer depth, production UI text/font/accessibility/image adapters, and optional middleware must start with `license-audit`, dependency records, `vcpkg.json`, and `THIRD_PARTY_NOTICES.md` updates.
6. Keep Wave 3 Vulkan and Wave 4 Metal visible backend proofs maintained behind runtime/toolchain gates while shared asset/material paths evolve.
7. Keep Wave 11 updated whenever public APIs, manifests, generated-game scaffolding, skills, rules, or subagents change; `agent-context` exposes module ownership, sample game readiness, asset/importer capabilities, platform targets, and validation recipes.
